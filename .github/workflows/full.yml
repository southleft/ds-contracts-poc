# FULL LANE — the comprehensive sweep. Runs on every pull request and every push
# to main. Slower than `fast`, and the one that has to be green before a merge.
#
# WHAT IT COVERS, and what it deliberately does NOT — read this before assuming
# a green run means "everything was checked".
#
# THE INVENTORY IS EXECUTABLE, not copied into this comment. `npm run ci:lanes`
# enumerates the root and every configured workspace manifest, prints each
# package-qualified gate and its lane(s), and refuses uncovered or stale
# exclusions. That derived receipt is the authoritative arithmetic.
#
# RUNS HERE include:
#     eval, plugin:check, plugin:ui-check, paste:check, mint:check,
#     emitters:check, dagger:census, closure:check, site:build, figma:fresh,
#     core:browser-check, extract:computed:ceiling:check, verify:package,
#     and the extract:figma:*:check family
#     (base, cbds, cbds:batch, cbds:bridge, canvas, dialog, composite, cross,
#      partstate, tooltip, overlap, wrap, constraints, repeat, theme).
#
# THE FAST LANE also owns package closure: CLI + Worker tests, measured CLI
#   source coverage, schema/emitter package checks, Worker/app typechecks, and
#   scoped lint/format gates. Those checks are not duplicated here.
#
# The fast lane runs `npm run ci:lanes`, which re-derives this split from the
# manifests and workflow files on every run and refuses when a gate is absent.
#
# EXCLUDED BY NAME — no gate in this repository is left to silently not-run:
#
#   · deploy:check — asserts LOCAL build artifacts against the LIVE Cloudflare
#     Pages bytes (ds-contracts-playground.pages.dev, ds-contracts-spec.pages.dev).
#     Both projects are direct-upload: nothing deploys unless a human runs
#     `npm run deploy`. So this gate is RED for every commit that has not yet
#     been deployed — which is every PR, by construction. Putting it in a merge
#     lane would train people to ignore a red check. It lives in
#     deploy-check.yml as a scheduled + manually-triggered job instead, which is
#     the question it actually answers: "is what the world sees still what main
#     says?"  See that file's header for the schedule and the reasoning.
#
#   · parity — `npm run parity` is RED at 8a5c455 and not because of any code:
#     parity/figma-components.json and parity/figma-tokens.json are 27.0 days
#     old and the differ refuses snapshots older than 14 days
#     (MAX_SNAPSHOT_AGE_DAYS). Refreshing them requires a human running
#     parity/extract-figma.plugin.js inside the live Figma file — not
#     automatable from a runner. It also REWRITES parity/report.json on every
#     run, so it would dirty the checkout. Covered instead by: the three-way
#     differ's logic is exercised by the C3-detection eval family, which runs
#     above. If someone refreshes the snapshots and wants this gated, the honest
#     home is the scheduled workflow, not the PR lane.
#
#   · npm run build + `git diff --exit-code` (generated output is
#     committed-fresh) — an earlier cut of this comment called it "measured
#     byte-inert on macOS/arm64". It was not: on 2026-08-22 both committed
#     contract.schema.json copies were eleven days behind the Zod document.
#     The freshness claim now has a gate of its own — `schema:fresh`
#     (scripts/schema-fresh-check.ts, fast lane) emits the JSON Schema in
#     memory and refuses when either committed copy differs. Determinism is
#     separately covered by the C1-determinism eval family.
#
#   · conformance / conformance:capture, extract:figma:gauntlet:live*,
#     extract:figma:visual, extract:figma:rest, extract:figma:mcp,
#     roundtrip:code, adherence:aggregate — not gate-shaped scripts; they
#     are capture/replay instruments that need either a live Figma file, a
#     Figma token, or a banked artifact directory that is gitignored. Named
#     here so their absence is a decision on the record rather than an
#     oversight.
#
# BROWSERS. Three gates need one, and they need two DIFFERENT browsers:
#   · eval (4 launches) and extract:computed:ceiling:check resolve through
#     chromiumExecutable() in extract/figma/visual-parity/render.ts —
#     PLAYWRIGHT_CHROMIUM_PATH, else the ms-playwright cache, else system
#     Chrome. This repo depends on playwright-core, which ships no browser, so
#     the cache must be populated: `npx playwright-core install chromium`.
#   · plugin:ui-check calls chromium.launch({ channel: 'chrome' })
#     (scripts/plugin-ui-check.mjs:77) — a real Google Chrome, which GitHub's
#     ubuntu runner images preinstall at /opt/google/chrome/chrome. Installing
#     playwright's chromium does NOT satisfy a channel.
#   .github/scripts/resolve-browsers.ts proves BOTH by launching them before
#   any gate runs, and fails naming which gates a missing browser takes down.
#   It imports the real chromiumExecutable() rather than reimplementing the
#   search order.
#
# HOW A FAILURE REPORTS. Every gate is its own step guarded by
# `if: !cancelled() && steps.setup.outcome == 'success'`, so one red gate never
# hides the later gates behind it: all of them run, each reports its own
# conclusion in the checks list, and the job fails at the end.
# `continue-on-error` is deliberately NOT used — it would paint a failing gate
# green.
#
# HISTORICAL TIMING measured locally with `npm run ci:lane full` (node
# v20.19.4, macOS arm64) at 8a5c455:
#   npm run eval                              883.1s  (188 of 188 cases)
#   npm run plugin:check                       11.7s
#   npm run dagger:census                      10.5s
#   npm run plugin:ui-check                     9.0s
#   npm run figma:fresh                         3.8s
#   npm run extract:computed:ceiling:check      1.9s
#   npm run site:build                          1.4s
#   npm run paste:check                         1.2s
#   npm run core:browser-check                  0.7s
#   the 15 extract:figma:*:check gates          0.2-0.4s each (3.7s total)
#   mint:check / emitters:check / closure:check / verify:package  ≤0.3s each
#                                            --------
#                                             926.7s wall (~15.5 min), of which
# eval is 95%. The 60-minute job timeout is ~4x that; a GitHub runner is slower
# than this machine, and the headroom is deliberate.
name: full

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  # v1:readiness --trust-lanes cites the fast / catalog-visual runs for the
  # same commit through `gh run list` instead of re-running their steps.
  actions: read

concurrency:
  group: full-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  full:
    name: full gate sweep
    runs-on: ubuntu-latest
    # 2026-08-23: 60 → 120. The lane now carries the eval suite (~35 min) AND
    # extract:computed:drift:remeasure (~37 min of Chromium re-fusing, the
    # V1-CLASS-01 evidence); at 60 the job was CANCELLED mid-remeasure on
    # 537022b0 and a merge went through on a cancelled lane — the watcher
    # counted failures, not non-successes. Both are fixed: the budget here,
    # the watcher in the session that merges.
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        # Full history: eval:record:check proves evals/results.json was
        # measured on an ANCESTOR of the commit under test (git merge-base
        # --is-ancestor). A depth-1 checkout of a PR merge commit has no
        # ancestors at all, so the stamped record read as foreign
        # (2026-08-22, run 32600919494). .git is ~280 MB; the cost is seconds.
        with:
          fetch-depth: 0

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20.19.4"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: npm ci
        run: npm ci

      # THE ONLY HARD STOP IN THIS JOB. `npm ci` is the one step without which
      # nothing below can even be attempted, so `setup` sits directly after it:
      # if the install fails, `setup` is skipped, every guarded step below
      # evaluates its condition to false, and the job stops honestly.
      #
      # Everything AFTER this point — browsers, artifact builds, and
      # every later gate — carries the same guard, so a failing preparation step
      # takes down only the gates that genuinely depend on it. An earlier cut of
      # this file put `setup` after the builds; a broken `plugin:zip` would then
      # have blanked the sweep and reported nothing about the unrelated gates that
      # never touch the plugin.
      - name: install complete
        id: setup
        run: echo "Every step below runs even if an earlier one fails; the job fails at the end."

      # ----------------------------------------------------------- browsers
      # Cache the downloaded Chromium, keyed on the lockfile (playwright-core's
      # pinned version decides the revision). The install command runs
      # UNCONDITIONALLY below — a cache hit makes it a no-op, a partial or
      # corrupt cache gets repaired, and resolve-browsers.ts then proves the
      # binary actually launches. A cache that hides a broken install is worse
      # than a slow job.
      - name: cache playwright browsers
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-ms-playwright-${{ hashFiles('package-lock.json') }}
          restore-keys: ${{ runner.os }}-ms-playwright-

      - name: install chromium (playwright-core ships no browser)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        env: { CI_LANE_STEP: prep }
        run: npx playwright-core install --with-deps chromium

      - name: resolve + prove both browsers
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        env: { CI_LANE_STEP: prep }
        run: npx tsx .github/scripts/resolve-browsers.ts

      # -------------------------------------------------- artifact builds
      # These targets are gitignored, so a fresh checkout does not have
      # them and the gate that consumes each one would refuse BY NAME. That
      # refusal is correct behaviour, which is exactly why these builds are
      # guarded rather than fail-fast: if one breaks, its consumer goes red
      # naming the missing file and the rest of the sweep still reports.

      # Root consumers resolve @ds-contracts/schema through package exports
      # that point at gitignored dist/. Build it explicitly on clean checkout;
      # `npm run ci:lane full` prints this prerequisite for local cold trees.
      - name: build @ds-contracts/schema (clean-checkout resolution)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        env: { CI_LANE_STEP: prep }
        run: npm --prefix packages/schema run build
      - name: build @ds-contracts/core (published engine surface)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        env: { CI_LANE_STEP: prep }
        run: npm --prefix packages/core run build
      - name: verify:published (pack schema/core/wc/cli tarballs into a temp project; a Vue emitter that depends only on the published packages generates the Flowbite eight)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run verify:published

      # packages/cli/dist/ (.gitignore:3) — dagger:census and paste:check both
      # EXECUTE the built cli.js. dagger-census.ts:72 refuses by name
      # ("REFUSED: packages/cli/dist/cli.js is not built") and paste-door-check
      # spawns it directly.
      #
      # NOT for the eval suite: the cli-smoke case builds its OWN copy inside
      # evals/.scratch (evals/run.ts:4403 runs packages/cli/build.mjs there),
      # so it is independent of this step. Stated because an earlier draft of
      # this comment credited the step to the wrong gates.
      #
      # Equivalent spellings: `npm run build -w @ds-contracts/cli`, or
      # `cd packages/cli && node build.mjs` (what publish-check.mjs prints).
      - name: build @ds-contracts/cli
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        env: { CI_LANE_STEP: prep }
        run: npm --prefix packages/cli run build

      # dist/index.js (.gitignore:3) — verify:package imports the BUILT package.
      - name: build the library bundle (verify:package prerequisite)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        env: { CI_LANE_STEP: prep }
        run: npm run build:lib

      # figma-sync/plugin-dist/ (.gitignore:45) — plugin:ui-check drives the
      # REAL packaged ui.html from that directory. Without this step the gate
      # cannot find the file it is supposed to be checking.
      #
      # NOT run with --update-engine-receipt: the default REFUSES when the
      # committed figma-sync/plugin/engine.receipt.json disagrees with a fresh
      # engine bundle, and never rewrites it. The refusal path is not
      # theoretical — it is the same comparison plugin:check flow 1 makes.
      - name: package the Figma plugin (plugin:ui-check prerequisite)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        env: { CI_LANE_STEP: prep }
        run: npm run plugin:zip

      # ----------------------------------------------------- the eval suite
      # Longest gate by a wide margin. It runs the REAL pipeline in a scratch
      # copy of the repo (evals/.scratch) per case.
      # The suite MEASURES into a scratch record (evals/.ci/ is gitignored) and
      # the next step compares it row-by-row against the committed
      # evals/results.json — CI's number and the committed number must be the
      # same number, named per case when they are not. The step runs the suite
      # through eval:carried:check, so a red case fails it UNLESS
      # parity/receipts/v1/eval-reds.json already names that case with a
      # measured cause and a stated closing condition — and an unnamed red, or
      # a named row whose case has gone green, still fails the step.
      - name: eval (the full deterministic suite, recorded to a scratch path, reds carried by name)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: mkdir -p evals/.ci && npm run eval:carried:check -- --record evals/.ci/results.json
      - name: eval record compare (CI's measurement vs the committed evals/results.json, row by row)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: node scripts/eval-record-check.mjs evals/.ci/results.json

      # ------------------------------------------------- engine + plugin
      - name: plugin:check (engine bundle + every plugin flow vs the mocked canvas)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run plugin:check

      - name: plugin:ui-check (the packaged ui.html driven in real Chrome)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run plugin:ui-check
      - name: functional:flowbite (maintain — authored Flowbite contracts render, hydrate, and flip state in real Chromium)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run functional:flowbite
      - name: parity:flowbite (maintain — authored vs code-captured Flowbite contracts; receipt date only moves when the body does)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run parity:flowbite
      - name: generated:fresh (every committed examples/<lib>/storybook generated tree is byte-identical to a re-emission)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run generated:fresh

      - name: paste:check (contract → CLI bundle → paste door, both kits)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run paste:check

      - name: mint:check
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run mint:check

      - name: emitters:check
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run emitters:check

      - name: core:browser-check (the core barrel stays browser-safe)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run core:browser-check

      - name: verify:package (the built package imports and SSRs)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run verify:package

      # ------------------------------------------------- receipts + census
      - name: dagger:census (every corpus reports the dropped facts it last reported)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run dagger:census

      - name: closure:check (every channel the canvas can draw is read back or named lost)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run closure:check

      # BUNDLES:FRESH — the same discipline as figma:fresh, one artifact class
      # over. `examples/<lib>/figma/*.bundle.json` is the ONE JSON a designer
      # pastes, and until 2026-08-24 only four of the eleven were rebuilt and
      # byte-compared anywhere. Two were provably stale — polaris and
      # astryx-docs were missing ~4.2 KB of codeOnlyFacts receipts the engine
      # had learned to write — and nothing noticed, because EXECUTING a stale
      # bundle succeeds. ~27s.
      - name: bundles:fresh (every committed example bundle byte-identical to a rebuild)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run bundles:fresh

      - name: figma:fresh (committed *.figma.js byte-identical to a re-emission)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run figma:fresh

      # THE ONE JSON A TEAM PASTES, and the token layer the census code half
      # renders every example-library row through
      # (extract/figma/census/corpus.ts:290). figma:fresh holds the *.figma.js
      # scripts still and generated:fresh holds the storybook trees still;
      # neither touches <lib>.bundle.json, so a round that re-derived two
      # contracts left both bundles stale with every lane green and was only
      # caught because the census render then refused BY NAME on an
      # unresolvable token. Eight libraries rebuild from a conventional
      # recipe; polaris is reported as a NAMED ungated hole.
      - name: bundle:fresh (committed <lib>.bundle.json byte-identical to a rebuild)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run bundle:fresh

      - name: site:build (the spec site + schema-coverage + journey commands)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run site:build

      - name: extract:computed:ceiling:check (an unreadable stylesheet is a counted ceiling)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:ceiling:check

      # V1-CLASS-01's drift instrument, RE-MEASURE half (docs/23 §D.32): every
      # committed captured truth replayed through the CURRENT fusion + emitters
      # in headless Chromium and scored against the recorded baseline. Every
      # artifact lands in extract/computed/.drift-remeasure/ (gitignored) via
      # regate's --scorecard-out; the tracked out/** paths are read only, so a
      # green run leaves `git status` clean. Cost, measured on the recording
      # machine (macOS arm64): 7–55s per component (polaris Button, 84,480 cells, several minutes), 104 components, ~37 min —
      # the gate page is rendered per variant × interaction and scored cell by
      # cell (carbon Button alone is 21,056 cells). The fast lane runs the
      # browser-free VERIFY half of the same instrument.
      - name: extract:computed:drift:remeasure (the committed captures re-fused through the current engine, scored against the baseline)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:drift:remeasure

      # Full lane, not fast: it launches Chromium and one of its four cases is
      # a real 10s Playwright screenshot timeout (the un-shootable root), so it
      # costs ~20s — the same reason the ceiling check above lives here.
      - name: extract:computed:portal:check (the portal root pick — wrapper unwrap + un-shootable root)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:portal:check

      # Full lane: launches Chromium. Pins that click-mutated React state
      # (a repeating-cell selected class) is remounted away before the next
      # plane, so double-run byte-identity does not thrash on calendars.
      - name: extract:computed:remount:check (click-mutated React state does not leak across sweeps)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:remount:check

      # Full lane because it re-fuses the COMMITTED captured-truth files under
      # extract/computed/out/** and conformance/ — no browser, ~2s, but it
      # needs the corpus the fast lane does not touch. It pins that a
      # measurement of the capture WINDOW (a 900px root width, an 852px
      # popover offset) is refused by name while an in-stage measurement is
      # carried unchanged.
      - name: extract:computed:viewport:check (the capture window is not a design token)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:viewport:check

      # THE FUSION SURFACE'S OWN FRESHNESS GATE. figma:fresh re-emits from
      # committed CONTRACTS and conformance/run.ts reads committed ARTIFACTS,
      # so neither can see an engine edit that changed what FUSION carries —
      # an adversarial review found a door change that moved 18 components
      # with every lane green. This re-fuses all 104 committed captured-truth
      # corpora offline (~7s, no browser) and names every component whose
      # carried geometry or geometry receipts moved.
      - name: extract:computed:geometry:census (a changed fusion door cannot go unseen)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:geometry:census

      # Full lane for the same reason as viewport above: both re-check the
      # committed pseudo-decor promotions (Carbon Toggle rotate receipts, the
      # sm/md/lg shape-size ladder) against the live engine over the committed
      # corpus — no browser, seconds each.
      - name: extract:computed:pseudo-decor-rotate:check (promoted decor keeps its rotation receipts)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:pseudo-decor-rotate:check

      - name: extract:computed:pseudo-decor-size:check (promoted decor keeps the size ladder)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:pseudo-decor-size:check

      # FC-FONT-SLANT-NOT-CARRIED. Same lane and same reason as the two above:
      # it re-fuses committed captured truth offline (no browser) and then
      # emits one synthetic contract through the plugin's Figma mock. It pins
      # BOTH halves, because either alone is a silent loss — the slant must
      # reach `Part.declared`, and the canvas must draw it as the italic FACE
      # rather than accepting the fact and rendering upright text.
      - name: extract:computed:font-slant:check (an authored italic is carried AND drawn)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:computed:font-slant:check

      # Full lane, not fast: it re-fuses all 54 committed captured-truth files
      # through the live engine (~40s, no browser). It pins that every "no
      # schema channel today" refusal states whether its baseline was the right
      # tag's control or the <span> fallback — 147 of 403 captured parts sit on
      # a tag the harness renders no control for, so 138 of those refusals were
      # silently presenting a USER-AGENT default as a fact the library authored.
      - name: ua-baseline:check (a refusal may only claim what its baseline supports)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run ua-baseline:check

      # THE BASELINE THE CONTROL DELTA SUBTRACTS (door capture.control-baseline-mint).
      # The harness mounts its control elements INSIDE mount.wrapperOpen, in the
      # same document as the component, so every page-global rule a library
      # ships styles the control too and fuse.control-element-delta cancelled
      # library-authored facts as if they were user-agent defaults (the
      # invisible shadcn Input, the inkless Polaris/Fluent text). A capture
      # taken before the fix carries no `uaControls`; fusion falls back and
      # SAYS SO per part, but the honest state is a re-measured baseline.
      # No browser: it reads the committed captures.
      - name: ua-baseline:present:check (every committed capture carries a library-free baseline)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run ua-baseline:present:check

      # Pure functions over CapturedNode, no browser. A portaled overlay root
      # is not the component's box: capture the modal LAYER and every
      # downstream measurement is of the browser window (task #20 measured a
      # Modal shipping root.width = 900px where the dialog is 448). Two
      # libraries present two shapes — MUI paints the scrim on a separate
      # Backdrop, Flowbite paints it on the fixed layer itself — and the
      # discriminator is the PAINT, not the child count.
      - name: scrim-demotion:check (both overlay shapes demote to the dialog)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run scrim-demotion:check

      # ------------------------------------------- the extract:figma family
      # Fifteen separate steps on purpose. They cost ~0.3s each, and a single
      # `npm run a && npm run b && …` chain would stop at the first refusal and
      # hide which of the other fourteen also broke.
      - name: "extract:figma:base:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:base:check

      # The carriage-gap TAXONOMY itself. Its stems are what a human reads to
      # see what the engine could not carry; over-merging two distinct failure
      # modes under one stem would be a false receipt, so the check pins both
      # directions (same mode collapses, different modes stay apart).
      - name: "extract:figma:gauntlet:noteclass:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:gauntlet:noteclass:check

      - name: "extract:figma:cbds:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:cbds:check

      - name: "extract:figma:cbds:batch:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:cbds:batch:check

      - name: "extract:figma:cbds:bridge:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:cbds:bridge:check

      - name: "extract:figma:canvas:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:canvas:check

      - name: "extract:figma:dialog:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:dialog:check

      - name: "extract:figma:composite:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:composite:check

      - name: "extract:figma:cross:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:cross:check

      - name: "extract:figma:partstate:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:partstate:check

      - name: "extract:figma:tooltip:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:tooltip:check

      - name: "extract:figma:overlap:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:overlap:check

      - name: "extract:figma:wrap:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:wrap:check

      - name: "extract:figma:gradient:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:gradient:check

      - name: "extract:figma:constraints:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:constraints:check

      - name: "extract:figma:repeat:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:repeat:check

      - name: "extract:figma:theme:check"
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        run: npm run extract:figma:theme:check

      # THE DEFINITION OF V1, END TO END. Every row of docs/26 runs its
      # acceptance chain on this commit. --trust-lanes: a command this lane ran
      # above is cited as this run; a command the fast / catalog-visual lanes
      # run is cited from their completed run for the same commit (gh, bounded
      # wait) and runs here only when no such run exists. --pre-release: the
      # post-publish chain (deploy:check) is DEFERRED by name and the audit
      # ledger's open human rows are NAMED rather than red. The release owner's
      # run on the release commit passes neither flag.
      - name: v1:readiness (every docs/26 row on this commit; lane steps cited, the rest run)
        if: ${{ !cancelled() && steps.setup.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: npm run v1:readiness -- --trust-lanes --pre-release
