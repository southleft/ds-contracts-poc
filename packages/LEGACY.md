# LEGACY — published universal-contract envelope

`@ds-contracts/cli`, `@ds-contracts/schema`, `@ds-contracts/core`, and
`@ds-contracts/emitter-web-components` still export the **pre-pivot
universal-contract** envelope (extract / generate / bundle / onboard).
They are not the v1 proof. Recipe-IR is not published from these
packages. npm publish of a recipe surface is deferred.

The v1 **proof** path is `recipe/` plus
[docs/32](../docs/32-recipe-ir-pivot.md) through
[docs/35](../docs/35-two-journey-v1-plan.md). Product v1 is incomplete.

This folder was labeled, not moved. No new semver in the commit that
added this file.
