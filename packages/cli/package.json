{
  "name": "@ds-contracts/cli",
  "version": "0.5.0-rc.2",
  "publishConfig": {
    "access": "public"
  },
  "description": "ds-contracts — contracts as the deterministic bridge between design and code. onboard / init / extract / generate / promote / figma / diff / propose-pr over the same engine the reference repo ships.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ds-contracts": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/draft-capture-config.test.ts test/propose-bool-axis.test.ts test/accept-candidates.test.ts test/init-detect.test.ts test/library-scorecard.test.ts test/onboard.test.ts test/figma-receive.test.ts test/token-routing.test.ts test/promotion-integrity.test.ts",
    "coverage": "c8 --all --include='src/**/*.ts' --reporter=text --reporter=json --reports-dir=../../coverage/cli --check-coverage --statements=55 --branches=60 --functions=55 --lines=55 npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "optionalDependencies": {
    "playwright-core": "^1.61.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/southleft/ds-contracts-poc.git",
    "directory": "packages/cli"
  },
  "homepage": "https://ds-contracts-spec.pages.dev",
  "bugs": "https://github.com/southleft/ds-contracts-poc/issues"
}
