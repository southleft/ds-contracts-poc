# Is the minted set usable — reflow, variant switching, token binding, no faked layout

GENERATED by `npm run canvas:usable:check` (scripts/canvas-usable-check.ts) — do not edit. Byte-stable: rows in manifest order, no dates.

**Phase: `probed`.** Rows with no committed observation read PENDING; every row that HAS one is fully held.

## The bar

Owner, 2026-08-25, asked whether a minted Figma set must be *usable* as a design-system component or merely *look right*, and chose usable: *"a screenshot-perfect set of frozen rectangles is useless to a DS team."* The census already measures recognisability (does the canvas look like the component) and round-trip (does the set propose back to its contract). Both can be green on a set a designer cannot work with. This is the third column, and nothing in this repo had ever tested it.

## The four assertions

| # | assertion | how it is measured | what fails |
|---|---|---|---|
| 1 | **reflow** | the variant `COMPONENT` is resized +40×+40 through the Plugin API and every child's box is re-measured | 0 children move or resize (`FROZEN-CHILDREN`); `layoutMode: NONE` over ≥2 children (`NO-AUTOLAYOUT`); a delta larger than the growth (`REFLOW-INCONSISTENT`); the original size not restored (`REFLOW-NOT-RESTORED`) |
| 2 | **variant switching** | one instance is created off-canvas and driven across every value of every axis in `variantGroupProperties`; each render is fingerprinted on geometry, fills and text separately | the set cannot report its axes (`SET-ERRORS`); it cannot be instantiated (`NOT-INSTANTIABLE`); a contract axis was never minted (`AXIS-NOT-MINTED`); two values render identically (`DEAD-AXIS-VALUE`, or `DEAD-AXIS` when every value collapses) |
| 3 | **token binding** | every fill, stroke, spacing, sizing and corner-radius channel that actually carries a value is classified from `boundVariables` / `inferredVariables` — Plugin API, because `file_variables:read` is Enterprise-gated and unavailable here | any literal (`LITERAL-CHANNELS`, sites named); nothing bound at all (`ALL-LITERAL`) |
| 4 | **no fake layout** | every `layoutPositioning: ABSOLUTE` node and every `layoutMode: NONE` container of ≥2 children is cross-examined against the CONTRACT's `anatomy` (`declared`/`literals`/`tokens` `position`) | out of flow on the canvas, in flow in the source (`FAKE-ABSOLUTE`, `FAKE-STACK`); no part of that name in the anatomy, so legitimacy cannot be proven (`UNRESOLVED-PART`). A genuine `position:absolute` PASSES |

## How the measurement is taken, and why the gate cannot take it

docs/31 §6: the figma-console bridge speaks MCP over stdio to its own client and WebSocket to plugin clients, and a Node process is neither — so this gate can never drive the canvas itself. An agent holding the MCP tools runs `extract/figma/census/usable-probe.plugin.js` through `figma_execute` against the scratch project `byMp6lt0Ij9b2QbkDGFwBh` (the only writable file; the probe refuses to run anywhere else), and commits each returned observation as `parity/receipts/v1/usable/<library>/<id>.json` via `npx tsx extract/figma/census/usable-record.ts`. The gate is a pure READER of those observations. A row with no observation is `PENDING` and at `--phase full` that is a refusal by name; an observation from any other file, or at the wrong probe version, is void and refused by name. The gate never passes for want of a bridge.

**The canvas is left byte-identical.** The probe makes exactly two writes and unmakes both: one resize (restored to the original width, height and sizing modes) and one throwaway instance (removed). It hashes every node on the page — id, name, type, x, y, width, height, layoutMode, itemSpacing, characters — before and after the whole batch, and the `restored` column carries that proof.

## Tally

| assertion | pass | FAIL | n/a | measured | pending |
|---|---|---|---|---|---|
| reflow | 74 | 91 | 5 | 170 | 0 |
| variant switching | 90 | 44 | 36 | 170 | 0 |
| token binding | 126 | 44 | 0 | 170 | 0 |
| no fake layout | 160 | 10 | 0 | 170 | 0 |

**39/170 measured sets are usable on all four assertions** (0 of 170 census rows not yet probed).

Gate: **GREEN** at phase `probed`.

## The baseline — the burn-down queue, not a list of acceptable defects

`parity/receipts/v1/usable-baseline.json` pins every named failure that reproduces on the sets probed today, keyed on library / id / assertion / code / subject / **detail**. The gate runs in the fast lane against that pin: a failure the file does not pin is a **NEW RED**; a pinned failure that stops reproducing is **FIXED** and refuses until it is re-recorded with its cause named (a fix nobody records silently un-freezes when the next one regresses); a pin whose set has left the census, or is no longer probed, is **STALE**. `detail` carries the discriminating payload — which children froze, which axis values collapsed, which channels are literal — so a pinned defect that **changes shape** fails as a new red rather than hiding inside an old pin. Re-record deliberately with `npm run canvas:usable:check -- --write-baseline` and say what moved.

| library | id | assertion | code | subject | detail | reproduces | why it is pinned |
|---|---|---|---|---|---|---|---|
| first-party | `ds.accordion-item` | binding | `LITERAL-CHANNELS` | State=Closed | 3/20; /trigger[0]/chevron[0]/Vector[0]·height=6.3, /trigger[0]/chevron[0]/Vector[0]·strokeWeight=1.0499999523162842, /trigger[0]/chevron[0]/Vector[0]·width=3.15 | yes | 3 of 20 carrying channels on this set are raw numbers with no variable behind them (13 bound + 4 inferred hold the rest). 3 are on a reified SVG `Vector` (/trigger[0]/chevron[0]/Vector[0]·strokeWeight=1.0499999523162842, /trigger[0]/chevron[0]/Vector[0]·width=3.15, /trigger[0]/chevron[0]/Vector[0]·height=6.3): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.badge` | variants | `DEAD-AXIS-VALUE` | Badge (ds.badge)/Variant | Danger≡Error | yes | Switching `Variant` renders Danger≡Error identically — geometry, fills and text all hash equal — leaving 4 of 5 values (Info, Success, Warning, Danger) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| first-party | `ds.banner` | reflow | `FROZEN-CHILDREN` | Status=Info, Container=Card | 1/3 frozen; statusIcon | yes | Growing the variant +40×+40 re-laid 2/3 (body, endArea) and left 1 behind. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems MIN. `statusIcon` (FRAME 20×20) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| first-party | `ds.banner` | binding | `LITERAL-CHANNELS` | Status=Info, Container=Card | 3/38; /statusIcon[0]/Vector[1]·height=4.5, /statusIcon[0]/Vector[2]·height=1.8, /statusIcon[0]/Vector[2]·width=1.8 | yes | 3 of 38 carrying channels on this set are raw numbers with no variable behind them (19 bound + 16 inferred hold the rest). 3 are on a reified SVG `Vector` (/statusIcon[0]/Vector[1]·height=4.5, /statusIcon[0]/Vector[2]·width=1.8, /statusIcon[0]/Vector[2]·height=1.8): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.banner` | fakeLayout | `FAKE-STACK` | Status=Info, Container=Card/statusIcon[0] | layoutMode NONE over 3 in-flow children | yes | `/statusIcon[0]` (FRAME) is minted `layoutMode: NONE` over 3 children whose contract parts are in flow; the row is held together by x/y coordinates, so nothing inside it re-lays when anything changes. Fix is in the forward emitter: this container's source display (flex/block) has to become an auto-layout mode. |
| first-party | `ds.banner` | fakeLayout | `FAKE-STACK` | Status=Info, Container=Card/endArea[2]/close[1] | layoutMode NONE over 2 in-flow children | yes | `/endArea[2]/close[1]` (FRAME) is minted `layoutMode: NONE` over 2 children whose contract parts are in flow; the row is held together by x/y coordinates, so nothing inside it re-lays when anything changes. Fix is in the forward emitter: this container's source display (flex/block) has to become an auto-layout mode. |
| first-party | `ds.bento-grid` | binding | `LITERAL-CHANNELS` | BentoGrid | 1/3; /BentoGrid·itemSpacing=0 | yes | 1 of 3 carrying channels on this set is a raw number with no variable behind it (0 bound + 2 inferred hold the rest). 1 is the grid gap (/BentoGrid·itemSpacing=0) — a GRID root whose `itemSpacing` is an unbound 0 while the real gutters sit in the track geometry. Fix is the grid lowering: `gap` has to be bound on the grid container. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.blockquote` | reflow | `FROZEN-CHILDREN` | Blockquote | 0/2 responded; frozen Children, Cite | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `Children` (SLOT 1×1), `Cite` (SLOT 1×1) are empty SLOTs minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.breadcrumb-item` | binding | `LITERAL-CHANNELS` | BreadcrumbItem | 3/8; /separatorIcon[0]/Vector[0]·height=5.4, /separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869, /separatorIcon[0]/Vector[0]·width=2.7 | yes | 3 of 8 carrying channels on this set are raw numbers with no variable behind them (3 bound + 2 inferred hold the rest). 3 are on a reified SVG `Vector` (/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869, /separatorIcon[0]/Vector[0]·width=2.7, /separatorIcon[0]/Vector[0]·height=5.4): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.breadcrumbs` | binding | `LITERAL-CHANNELS` | Breadcrumbs (ds.breadcrumbs) | 9/26; /Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·height=5.4, /Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869, /Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·width=2.7, /Children[0]/BreadcrumbItem[1]/separatorIcon[0]/Vector[0]·height=5.4, /Children[0]/BreadcrumbItem[1]/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869, /Children[0]/BreadcrumbItem[1]/separatorIcon[0]/Vector[0]·width=2.7, /Children[0]/BreadcrumbItem[2]/separatorIcon[0]/Vector[0]·height=5.4, /Children[0]/BreadcrumbItem[2]/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869, /Children[0]/BreadcrumbItem[2]/separatorIcon[0]/Vector[0]·width=2.7 | yes | 9 of 26 carrying channels on this set are raw numbers with no variable behind them (11 bound + 6 inferred hold the rest). 9 are on a reified SVG `Vector` (/Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869, /Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·width=2.7, /Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·height=5.4, …): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.button` | reflow | `FROZEN-CHILDREN` | Variant=Primary, Size=Medium, State=Default | 1/2 frozen; loadingSpinner | yes | Growing the variant +40×+40 re-laid 1/2 (label) and left 1 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems CENTER. `loadingSpinner` (FRAME 14×14) is minted visible:false; Figma keeps an invisible child out of auto-layout, so it neither draws nor re-lays. Fix is in the forward emitter: an optional part that is off in this cell should be a component property (or omitted), not an invisible node parked in the stack. |
| first-party | `ds.button` | variants | `DEAD-AXIS-VALUE` | Button (ds.button)/State | Default≡Disabled | yes | Switching `State` renders Default≡Disabled identically — geometry, fills and text all hash equal — leaving 3 of 4 values (Default, Hover, Focus Visible) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| first-party | `ds.button` | binding | `LITERAL-CHANNELS` | Variant=Primary, Size=Medium, State=Default | 2/17; /loadingSpinner[0]/Vector[0]·height=5.25, /loadingSpinner[0]/Vector[0]·width=5.25 | yes | 2 of 17 carrying channels on this set are raw numbers with no variable behind them (12 bound + 3 inferred hold the rest). 2 are on a reified SVG `Vector` (/loadingSpinner[0]/Vector[0]·width=5.25, /loadingSpinner[0]/Vector[0]·height=5.25): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.card` | reflow | `FROZEN-CHILDREN` | Card (ds.card) | 1/3 frozen; Actions | yes | Growing the variant +40×+40 re-laid 2/3 (header, Body) and left 1 behind. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `Actions` (SLOT 318×16) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.card` | binding | `LITERAL-CHANNELS` | Card (ds.card) | 1/34; /Actions[2]·width=318 | yes | 1 of 34 carrying channels on this set is a raw number with no variable behind it (28 bound + 5 inferred hold the rest). 1 is a fixed box dimension (/Actions[2]·width=318) minted from the captured geometry rather than from a sizing token. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.chat-message-metadata` | reflow | `FROZEN-CHILDREN` | Status=Sent | 1/3 frozen; Footer | yes | Growing the variant +40×+40 re-laid 2/3 (statusIcon, timestampText) and left 1 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `Footer` (SLOT 1×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.chat-message-metadata` | variants | `DEAD-AXIS-VALUE` | ChatMessageMetadata/Status | Delivered≡Read | yes | Switching `Status` renders Delivered≡Read identically — geometry, fills and text all hash equal — leaving 4 of 5 values (Sent, Sending, Delivered, Error) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| first-party | `ds.chat-message-metadata` | binding | `LITERAL-CHANNELS` | Status=Sent | 3/9; /statusIcon[0]/Vector[0]·height=4.56, /statusIcon[0]/Vector[0]·strokeWeight=0.9000000357627869, /statusIcon[0]/Vector[0]·width=6.48 | yes | 3 of 9 carrying channels on this set are raw numbers with no variable behind them (4 bound + 2 inferred hold the rest). 3 are on a reified SVG `Vector` (/statusIcon[0]/Vector[0]·strokeWeight=0.9000000357627869, /statusIcon[0]/Vector[0]·width=6.48, /statusIcon[0]/Vector[0]·height=4.56): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.chat-message` | reflow | `FROZEN-CHILDREN` | Sender=Assistant | 1/2 frozen; Avatar | yes | Growing the variant +40×+40 re-laid 1/2 (body) and left 1 behind. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems MIN. `Avatar` (SLOT 1×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.chat-system-message` | reflow | `FROZEN-CHILDREN` | Variant=Default | 1/2 frozen; Icon | yes | Growing the variant +40×+40 re-laid 1/2 (messageText) and left 1 behind. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems CENTER. `Icon` (SLOT 1×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.checkbox` | reflow | `FROZEN-CHILDREN` | Value=Unchecked, Size=Medium | 1/2 frozen; box | yes | Growing the variant +40×+40 re-laid 1/2 (textCol) and left 1 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `box` (FRAME 20×20) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| first-party | `ds.empty-state` | reflow | `FROZEN-CHILDREN` | EmptyState | 2/4 frozen; Actions, Icon | yes | Growing the variant +40×+40 re-laid 2/4 (title, descriptionText) and left 2 behind. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `Icon` (SLOT 1×1), `Actions` (SLOT 1×8) are empty SLOTs minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.field` | reflow | `FROZEN-CHILDREN` | Field | 1/3 frozen; descriptionText | yes | Growing the variant +40×+40 re-laid 2/3 (labelRow, Children) and left 1 behind. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `descriptionText` (TEXT 240×17) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| first-party | `ds.field` | binding | `LITERAL-CHANNELS` | Field | 8/19; /labelRow[0]/requiredMark[1]/Vector[0]·height=5.5, /labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[1]·height=2.8, /labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[1]·width=4.8, /labelRow[0]/requiredMark[1]/Vector[2]·height=2.8, /labelRow[0]/requiredMark[1]/Vector[2]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[2]·width=4.8 | yes | 8 of 19 carrying channels on this set are raw numbers with no variable behind them (7 bound + 4 inferred hold the rest). 8 are on a reified SVG `Vector` (/labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[0]·height=5.5, /labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75, …): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.field` | fakeLayout | `FAKE-STACK` | Field/labelRow[0]/requiredMark[1] | layoutMode NONE over 3 in-flow children | yes | `/labelRow[0]/requiredMark[1]` (FRAME) is minted `layoutMode: NONE` over 3 children whose contract parts are in flow; the row is held together by x/y coordinates, so nothing inside it re-lays when anything changes. Fix is in the forward emitter: this container's source display (flex/block) has to become an auto-layout mode. |
| first-party | `ds.grid-gallery` | binding | `LITERAL-CHANNELS` | GridGallery | 1/2; /GridGallery·itemSpacing=0 | yes | 1 of 2 carrying channels on this set is a raw number with no variable behind it (0 bound + 1 inferred hold the rest). 1 is the grid gap (/GridGallery·itemSpacing=0) — a GRID root whose `itemSpacing` is an unbound 0 while the real gutters sit in the track geometry. Fix is the grid lowering: `gap` has to be bound on the grid container. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.heading` | variants | `DEAD-AXIS` | Heading (ds.heading)/Level | H2≡H1, H2≡H3, H2≡H4, H2≡H5, H2≡H6 | yes | Every value of `Level` (H2, H1, H3, H4, H5, H6) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| first-party | `ds.list-item` | reflow | `FROZEN-CHILDREN` | ListItem | 2/3 frozen; EndContent, StartContent | yes | Growing the variant +40×+40 re-laid 1/3 (textCol) and left 2 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `StartContent` (SLOT 1×1), `EndContent` (SLOT 1×1) are empty SLOTs minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.metadata-list-item` | reflow | `FROZEN-CHILDREN` | MetadataListItem | 0/3 responded; frozen Icon, labelText, valueText | yes | Growing the variant +40×+40 moved and resized NOTHING: all 3 children kept their exact boxes. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `Icon` (SLOT 1×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. `labelText` (FRAME 120×17), `valueText` (TEXT 38×17) are exactly as wide/tall as the container was and were fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside them — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| first-party | `ds.metadata-list` | reflow | `FROZEN-CHILDREN` | MetadataList | 1/2 frozen; titleText | yes | Growing the variant +40×+40 re-laid 1/2 (Children) and left 1 behind. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `titleText` (TEXT 47×17) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| first-party | `ds.page-shell` | binding | `LITERAL-CHANNELS` | PageShell | 1/3; /PageShell·itemSpacing=0 | yes | 1 of 3 carrying channels on this set is a raw number with no variable behind it (0 bound + 2 inferred hold the rest). 1 is the grid gap (/PageShell·itemSpacing=0) — a GRID root whose `itemSpacing` is an unbound 0 while the real gutters sit in the track geometry. Fix is the grid lowering: `gap` has to be bound on the grid container. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.pagination` | binding | `LITERAL-CHANNELS` | Variant=Pages | 6/52; /nextButton[2]/Vector[0]·height=6.3, /nextButton[2]/Vector[0]·strokeWeight=1.0499999523162842, /nextButton[2]/Vector[0]·width=3.15, /prevButton[0]/Vector[0]·height=6.3, /prevButton[0]/Vector[0]·strokeWeight=1.0499999523162842, /prevButton[0]/Vector[0]·width=3.15 | yes | 6 of 52 carrying channels on this set are raw numbers with no variable behind them (42 bound + 4 inferred hold the rest). 6 are on a reified SVG `Vector` (/prevButton[0]/Vector[0]·strokeWeight=1.0499999523162842, /prevButton[0]/Vector[0]·width=3.15, /prevButton[0]/Vector[0]·height=6.3, …): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.progress-bar` | reflow | `FROZEN-CHILDREN` | Variant=Accent | 0/1 responded; frozen track | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `track` (FRAME 320×8) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| first-party | `ds.progress-bar` | binding | `LITERAL-CHANNELS` | Variant=Accent | 1/15; /track[0]/fill[0]·width=192 | yes | 1 of 15 carrying channels on this set is a raw number with no variable behind it (13 bound + 1 inferred hold the rest). 1 is a value-carrying bar width (/track[0]/fill[0]·width=192): the component's current value is baked in as a pixel width, so whatever the source expressed it with (a percentage of the track, a transform) did not survive lowering and no variable can move it. Fix is the percentage/value lowering rule. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.side-nav-item` | reflow | `FROZEN-CHILDREN` | State=Default | 2/3 frozen; EndContent, Icon | yes | Growing the variant +40×+40 re-laid 1/3 (labelText) and left 2 behind. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems MIN. `Icon` (SLOT 1×1), `EndContent` (SLOT 1×1) are empty SLOTs minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.sidebar-layout` | reflow | `FROZEN-CHILDREN` | SidebarLayout | 1/2 frozen; Sidebar | yes | Growing the variant +40×+40 re-laid 1/2 (Main) and left 1 behind. The container is GRID FIXED×HUG, primaryAxisAlignItems MIN. `Sidebar` (SLOT 240×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.sidebar-layout` | binding | `LITERAL-CHANNELS` | SidebarLayout | 1/2; /SidebarLayout·itemSpacing=0 | yes | 1 of 2 carrying channels on this set is a raw number with no variable behind it (0 bound + 1 inferred hold the rest). 1 is the grid gap (/SidebarLayout·itemSpacing=0) — a GRID root whose `itemSpacing` is an unbound 0 while the real gutters sit in the track geometry. Fix is the grid lowering: `gap` has to be bound on the grid container. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.skeleton` | reflow | `FROZEN-CHILDREN` | Variant=Text | 0/1 responded; frozen block | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `block` (FRAME 120×12) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| first-party | `ds.slider` | reflow | `FROZEN-CHILDREN` | Slider (ds.slider) | 0/2 responded; frozen labelText, trackRow | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `trackRow` (FRAME 320×16) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. `labelText` (TEXT 50×17) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| first-party | `ds.switch` | reflow | `FROZEN-CHILDREN` | Value=Off | 1/2 frozen; track | yes | Growing the variant +40×+40 re-laid 1/2 (textCol) and left 1 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `track` (FRAME 32×20) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| first-party | `ds.tab` | reflow | `FROZEN-CHILDREN` | State=Default | 2/3 frozen; EndContent, Icon | yes | Growing the variant +40×+40 re-laid 1/3 (labelText) and left 2 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `Icon` (SLOT 1×1), `EndContent` (SLOT 1×1) are empty SLOTs minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.text-area` | reflow | `FROZEN-CHILDREN` | Size=Medium | 1/3 frozen; descriptionText | yes | Growing the variant +40×+40 re-laid 2/3 (labelRow, input) and left 1 behind. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `descriptionText` (TEXT 316×17) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| first-party | `ds.text-area` | binding | `LITERAL-CHANNELS` | Size=Medium | 8/31; /labelRow[0]/requiredMark[1]/Vector[0]·height=5.5, /labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[1]·height=2.8, /labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[1]·width=4.8, /labelRow[0]/requiredMark[1]/Vector[2]·height=2.8, /labelRow[0]/requiredMark[1]/Vector[2]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[2]·width=4.8 | yes | 8 of 31 carrying channels on this set are raw numbers with no variable behind them (18 bound + 5 inferred hold the rest). 8 are on a reified SVG `Vector` (/labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[0]·height=5.5, /labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75, …): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.text-area` | fakeLayout | `FAKE-STACK` | Size=Medium/labelRow[0]/requiredMark[1] | layoutMode NONE over 3 in-flow children | yes | `/labelRow[0]/requiredMark[1]` (FRAME) is minted `layoutMode: NONE` over 3 children whose contract parts are in flow; the row is held together by x/y coordinates, so nothing inside it re-lays when anything changes. Fix is in the forward emitter: this container's source display (flex/block) has to become an auto-layout mode. |
| first-party | `ds.text-field` | reflow | `FROZEN-CHILDREN` | Size=Medium | 1/3 frozen; descriptionText | yes | Growing the variant +40×+40 re-laid 2/3 (labelRow, input) and left 1 behind. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `descriptionText` (TEXT 296×17) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| first-party | `ds.text-field` | binding | `LITERAL-CHANNELS` | Size=Medium | 8/31; /labelRow[0]/requiredMark[1]/Vector[0]·height=5.5, /labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[1]·height=2.8, /labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[1]·width=4.8, /labelRow[0]/requiredMark[1]/Vector[2]·height=2.8, /labelRow[0]/requiredMark[1]/Vector[2]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[2]·width=4.8 | yes | 8 of 31 carrying channels on this set are raw numbers with no variable behind them (18 bound + 5 inferred hold the rest). 8 are on a reified SVG `Vector` (/labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75, /labelRow[0]/requiredMark[1]/Vector[0]·height=5.5, /labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75, …): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.text-field` | fakeLayout | `FAKE-STACK` | Size=Medium/labelRow[0]/requiredMark[1] | layoutMode NONE over 3 in-flow children | yes | `/labelRow[0]/requiredMark[1]` (FRAME) is minted `layoutMode: NONE` over 3 children whose contract parts are in flow; the row is held together by x/y coordinates, so nothing inside it re-lays when anything changes. Fix is in the forward emitter: this container's source display (flex/block) has to become an auto-layout mode. |
| first-party | `ds.toast` | reflow | `FROZEN-CHILDREN` | Type=Info | 1/2 frozen; EndContent | yes | Growing the variant +40×+40 re-laid 1/2 (Children) and left 1 behind. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems MIN. `EndContent` (SLOT 1×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.token` | reflow | `FROZEN-CHILDREN` | Color=Default, Size=Medium | 2/3 frozen; EndContent, Icon | yes | Growing the variant +40×+40 re-laid 1/3 (labelText) and left 2 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `Icon` (SLOT 1×1), `EndContent` (SLOT 1×1) are empty SLOTs minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.toolbar` | reflow | `FROZEN-CHILDREN` | Size=Medium | 0/3 responded; frozen CenterContent, EndContent, StartContent | yes | Growing the variant +40×+40 moved and resized NOTHING: all 3 children kept their exact boxes. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems SPACE_BETWEEN. `StartContent` (SLOT 1×1), `CenterContent` (SLOT 1×1), `EndContent` (SLOT 1×1) are empty SLOTs minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.top-nav-item` | reflow | `FROZEN-CHILDREN` | State=Default | 1/2 frozen; Icon | yes | Growing the variant +40×+40 re-laid 1/2 (labelText) and left 1 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `Icon` (SLOT 1×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.top-nav` | reflow | `FROZEN-CHILDREN` | TopNav | 0/3 responded; frozen EndContent, Heading, StartContent | yes | Growing the variant +40×+40 moved and resized NOTHING: all 3 children kept their exact boxes. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems SPACE_BETWEEN. `Heading` (SLOT 1×1), `StartContent` (SLOT 605×1), `EndContent` (SLOT 1×1) are empty SLOTs minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| first-party | `ds.top-nav` | binding | `LITERAL-CHANNELS` | TopNav | 1/13; /StartContent[1]·width=605 | yes | 1 of 13 carrying channels on this set is a raw number with no variable behind it (10 bound + 2 inferred hold the rest). 1 is a fixed box dimension (/StartContent[1]·width=605) minted from the captured geometry rather than from a sizing token. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.two-column` | binding | `LITERAL-CHANNELS` | TwoColumn | 1/2; /TwoColumn·itemSpacing=0 | yes | 1 of 2 carrying channels on this set is a raw number with no variable behind it (0 bound + 1 inferred hold the rest). 1 is the grid gap (/TwoColumn·itemSpacing=0) — a GRID root whose `itemSpacing` is an unbound 0 while the real gutters sit in the track geometry. Fix is the grid lowering: `gap` has to be bound on the grid container. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| first-party | `ds.typeahead-item` | reflow | `FROZEN-CHILDREN` | TypeaheadItem | 1/2 frozen; Icon | yes | Growing the variant +40×+40 re-laid 1/2 (textCol) and left 1 behind. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems MIN. `Icon` (SLOT 1×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| altitude | `altitude.avatar` | reflow | `FROZEN-CHILDREN` | Avatar (altitude.avatar) | 1/2 frozen; avatar__badge | yes | `avatar__badge` is minted as a 0x0 FRAME with visible:false, so it neither draws nor participates in the parent's HORIZONTAL auto-layout: growing the component 40x40 moves `label` and leaves the badge exactly where it was. Separately named as "Avatar's avatar__badge child does not draw". Fixing the draw fixes this pin — do not re-record it until the badge has a non-zero box. |
| altitude | `altitude.chip` | variants | `SET-ERRORS` | Chip | in get_variantGroupProperties: Component set has existing errors (15 children) | yes | The minted set carries 15 children on TWO different variant schemas — ten rows of `Variant=*, Type=*` beside five of `Variant=*, Type=Squared, State=*` — so Figma marks the component set as having errors and `variantGroupProperties` throws. The set cannot be instantiated against an axis at all, which makes it unusable however it looks. The census manifest declares 2 axes x 10 variants; the emitter put 15 children on the canvas. Fix is in the forward emitter's state-plane placement, not in the canvas. |
| altitude | `altitude.heading` | reflow | `FROZEN-CHILDREN` | Variant=Display Lg, Weight=Regular | 0/1 responded; frozen label | yes | The variant is VERTICAL with counterAxisSizingMode FIXED and primaryAxisAlignItems MIN, and `label` is a 179-wide TEXT inside a 328-wide frame that is still 179 wide at 368. Nothing stretches, nothing re-aligns: the heading is a fixed box with a fixed word in it. Fixing this means the text part carries a fill/stretch sizing decision (the layout-family lowering rules, plan step 2), not a nudge to this baseline. |
| altitude | `altitude.iconclose` | binding | `LITERAL-CHANNELS` | Size=Xs | 2/5; /root-xs[0]/Vector[0]·height=3, /root-xs[0]/Vector[0]·width=3 | yes | The reified SVG's `Vector` child carries literal width=3 and height=3 rather than variable-bound sizing. Every other carrying channel on this set is bound or inferred (3/5). This is the exact number the re-promote wave should move: diff the SITES in the receipt's literal-site table, not the percentage, or a round that merely relocated the literals will read as an improvement. |
| altitude | `altitude.link` | variants | `DEAD-AXIS-VALUE` | Link/State | Default≡Focus Visible | yes | `State=Focus Visible` fingerprints identically to `State=Default` on geometry, fills AND text — the focus ring is not drawn, so the axis value exists but renders nothing. A designer switching to Focus Visible sees no change, which is the definition of a dead axis value. Separately named as "Link's Focus Visible column shows no focus ring". |
| antd | `antd.alert` | reflow | `FROZEN-CHILDREN` | Type=Info, Show Icon=NoIcon | 1/2 frozen; alert-close-icon | yes | Growing the variant +40×+40 re-laid 1/2 (alert-content) and left 1 behind. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems MIN. `alert-close-icon` (FRAME 12×12) is minted visible:false; Figma keeps an invisible child out of auto-layout, so it neither draws nor re-lays. Fix is in the forward emitter: an optional part that is off in this cell should be a component property (or omitted), not an invisible node parked in the stack. |
| antd | `antd.alert` | binding | `LITERAL-CHANNELS` | Type=Info, Show Icon=NoIcon | 2/22; /alert-close-icon[1]/anticon[0]/Vector[0]·height=9.67, /alert-close-icon[1]/anticon[0]/Vector[0]·width=9.67 | yes | 2 of 22 carrying channels on this set are raw numbers with no variable behind them (15 bound + 5 inferred hold the rest). 2 are on a reified SVG `Vector` (/alert-close-icon[1]/anticon[0]/Vector[0]·width=9.67, /alert-close-icon[1]/anticon[0]/Vector[0]·height=9.67): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| antd | `antd.badge` | reflow | `FROZEN-CHILDREN` | Mode=Count, Color=Blue | 0/2 responded; frozen avatar, badge-count | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `badge-count` (FRAME 20×20) is layoutPositioning ABSOLUTE and keeps its offset when the parent grows, because the minted node carries no constraint that follows the parent. Fix is in the lowering rule for out-of-flow parts: a source `inset`/`right`/`bottom` has to become a Figma constraint (STRETCH/MAX), not a frozen x/y. `avatar` (FRAME 32×32) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| antd | `antd.button` | reflow | `FROZEN-CHILDREN` | Type=Default, Size=Middle, Danger=Safe | 2/3 frozen; btn-icon, btn-icon-2 | yes | Growing the variant +40×+40 re-laid 1/3 (label) and left 2 behind. The container is HORIZONTAL HUG×FIXED, primaryAxisAlignItems CENTER. `btn-icon` (FRAME 14×14), `btn-icon-2` (FRAME 14×14) are minted visible:false; Figma keeps an invisible child out of auto-layout, so it neither draws nor re-lays. Fix is in the forward emitter: an optional part that is off in this cell should be a component property (or omitted), not an invisible node parked in the stack. |
| antd | `antd.button` | variants | `DEAD-AXIS-VALUE` | Button/Type | Default≡Dashed | yes | Switching `Type` renders Default≡Dashed identically — geometry, fills and text all hash equal — leaving 4 of 5 values (Default, Primary, Link, Text) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| antd | `antd.button` | binding | `LITERAL-CHANNELS` | Type=Default, Size=Middle, Danger=Safe | 4/22; /btn-icon-2[1]/anticon-2[0]/Vector[0]·height=7.49, /btn-icon-2[1]/anticon-2[0]/Vector[0]·width=7.49, /btn-icon[0]/anticon[0]/Vector[0]·height=12.31, /btn-icon[0]/anticon[0]/Vector[0]·width=12.31 | yes | 4 of 22 carrying channels on this set are raw numbers with no variable behind them (12 bound + 6 inferred hold the rest). 4 are on a reified SVG `Vector` (/btn-icon[0]/anticon[0]/Vector[0]·width=12.31, /btn-icon[0]/anticon[0]/Vector[0]·height=12.31, /btn-icon-2[1]/anticon-2[0]/Vector[0]·width=7.49, …): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| antd | `antd.checkbox` | reflow | `FROZEN-CHILDREN` | Checked=Unchecked | 0/2 responded; frozen checkbox, label | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `label` (FRAME 78×22) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. `checkbox` (FRAME 16×16) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| antd | `antd.input` | variants | `DEAD-AXIS-VALUE` | Input/State | Focus Visible≡Hover, Focus Visible≡Active | yes | Switching `State` renders Focus Visible≡Hover, Focus Visible≡Active identically — geometry, fills and text all hash equal — leaving 3 of 5 values (Default, Disabled, Focus Visible) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| antd | `antd.progress` | variants | `DEAD-AXIS-VALUE` | Progress/Status | Unset≡Active | yes | Switching `Status` renders Unset≡Active identically — geometry, fills and text all hash equal — leaving 3 of 4 values (Unset, Exception, Success) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| antd | `antd.progress` | binding | `LITERAL-CHANNELS` | Status=Unset | 1/16; /progress-outer[0]/progress-inner[0]/progress-bg[0]·width=101 | yes | 1 of 16 carrying channels on this set is a raw number with no variable behind it (12 bound + 3 inferred hold the rest). 1 is a value-carrying bar width (/progress-outer[0]/progress-inner[0]/progress-bg[0]·width=101): the component's current value is baked in as a pixel width, so whatever the source expressed it with (a percentage of the track, a transform) did not survive lowering and no variable can move it. Fix is the percentage/value lowering rule. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| antd | `antd.radio` | reflow | `FROZEN-CHILDREN` | Checked=Unchecked | 0/2 responded; frozen label, radio | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `label` (FRAME 52×22) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. `radio` (FRAME 16×16) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| antd | `antd.switch` | reflow | `FROZEN-CHILDREN` | Size=Default, Checked=Unchecked, State=Default | 0/2 responded; frozen switch-handle, switch-inner | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `switch-handle` (FRAME 18×18) is layoutPositioning ABSOLUTE and keeps its offset when the parent grows, because the minted node carries no constraint that follows the parent. Fix is in the lowering rule for out-of-flow parts: a source `inset`/`right`/`bottom` has to become a Figma constraint (STRETCH/MAX), not a frozen x/y. `switch-inner` (FRAME 44×22) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| antd | `antd.switch` | variants | `DEAD-AXIS-VALUE` | Switch/State | Default≡Disabled, Active≡Hover | yes | Switching `State` renders Default≡Disabled, Active≡Hover identically — geometry, fills and text all hash equal — leaving 3 of 5 values (Default, Focus Visible, Active) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| antd | `antd.switch` | binding | `LITERAL-CHANNELS` | Size=Default, Checked=Unchecked, State=Default | 1/31; /switch-inner[0]/switch-inner-unchecked (margin box)[1]·height=0.01 | yes | 1 of 31 carrying channels on this set is a raw number with no variable behind it (20 bound + 10 inferred hold the rest). 1 is on a synthetic `(margin box)` wrapper (/switch-inner[0]/switch-inner-unchecked (margin box)[1]·height=0.01), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| antd | `antd.tag` | reflow | `FROZEN-CHILDREN` | Color=Blue, Bordered=Bordered | 0/2 responded; frozen anticon, label | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `anticon` (FRAME 19.77×10) is minted visible:false; Figma keeps an invisible child out of auto-layout, so it neither draws nor re-lays. Fix is in the forward emitter: an optional part that is off in this cell should be a component property (or omitted), not an invisible node parked in the stack. `label` (TEXT 20×20) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| antd | `antd.tag` | binding | `LITERAL-CHANNELS` | Color=Blue, Bordered=Bordered | 3/17; /anticon[1]/Vector[0]·height=8.06, /anticon[1]/Vector[0]·width=15.93, /anticon[1]·width=19.77 | yes | 3 of 17 carrying channels on this set are raw numbers with no variable behind them (10 bound + 4 inferred hold the rest). 2 are on a reified SVG `Vector` (/anticon[1]/Vector[0]·width=15.93, /anticon[1]/Vector[0]·height=8.06): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. 1 is a fixed box dimension (/anticon[1]·width=19.77) minted from the captured geometry rather than from a sizing token. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| antd | `antd.tooltip` | reflow | `FROZEN-CHILDREN` | Tooltip | 0/2 responded; frozen tooltip-arrow, tooltip-content | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL HUG×FIXED, primaryAxisAlignItems MIN. `tooltip-arrow` (FRAME 16×16) is layoutPositioning ABSOLUTE and keeps its offset when the parent grows, because the minted node carries no constraint that follows the parent. Fix is in the lowering rule for out-of-flow parts: a source `inset`/`right`/`bottom` has to become a Figma constraint (STRETCH/MAX), not a frozen x/y. `tooltip-content` (FRAME 85.91×34) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| astryx | `astryx.banner` | reflow | `FROZEN-CHILDREN` | Status=Info, Container=Card | 0/1 responded; frozen header | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `header` (FRAME 299×64) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| astryx | `astryx.card` | reflow | `FROZEN-CHILDREN` | Variant=Default | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `label` (TEXT 99×24) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| astryx | `astryx.checkbox-input` | variants | `DEAD-AXIS` | CheckboxInput/Size | Md≡Sm | yes | Every value of `Size` (Md, Sm) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are size steps, so the size token they resolve to is the same number by the time it is minted (or the size only changes a channel the fingerprint cannot see, such as a font metric that did not re-measure). Fix belongs in the size-axis lowering: the axis is minted, its effect is not. |
| astryx | `astryx.dropdown-menu` | reflow | `FROZEN-CHILDREN` | DropdownMenu | 0/2 responded; frozen menu, trigger | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `menu` (FRAME 240×111) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. `trigger` (INSTANCE 99.81×32) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| astryx | `astryx.progress-bar` | reflow | `FROZEN-CHILDREN` | Variant=Accent | 0/2 responded; frozen label, track | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `track` (FRAME 240×4) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. `label` (TEXT 47×12) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| astryx | `astryx.slider` | reflow | `FROZEN-CHILDREN` | Orientation=Horizontal, Value Display=Tooltip | 1/2 frozen; label | yes | Growing the variant +40×+40 re-laid 1/2 (slider) and left 1 behind. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `label` (FRAME 50×20) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| astryx | `astryx.switch` | reflow | `FROZEN-CHILDREN` | Value=Off, Label Position=End | 0/1 responded; frozen part-0 | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `part-0` (FRAME 178.02×24) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| astryx | `astryx.text-input` | reflow | `FROZEN-CHILDREN` | Type=Text, Size=Md | 1/2 frozen; label | yes | Growing the variant +40×+40 re-laid 1/2 (field) and left 1 behind. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `label` (TEXT 25×12) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| astryx | `astryx.text-input` | binding | `LITERAL-CHANNELS` | Type=Text, Size=Md | 1/22; /field[1]/start-icon[0]/icon-text[0]/Vector[0]·width=10.67 | yes | 1 of 22 carrying channels on this set is a raw number with no variable behind it (15 bound + 6 inferred hold the rest). 1 is on a reified SVG `Vector` (/field[1]/start-icon[0]/icon-text[0]/Vector[0]·width=10.67): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| astryx | `astryx.toast` | reflow | `FROZEN-CHILDREN` | Toast | 1/3 frozen; EndContent | yes | Growing the variant +40×+40 re-laid 2/3 (content, dismiss) and left 1 behind. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems MIN. `EndContent` (SLOT 1×1) is an empty SLOT minted at 1×1: a slot with no content has no box to stretch, so it can never respond to a resize. Fix is in the forward emitter's slot minting — an empty slot should either fill its axis or not be minted into the flow at all. |
| carbon | `carbon.accordion` | binding | `LITERAL-CHANNELS` | Align=End, Size=Sm, State=Default | 1/19; /accordion__item[0]/accordion__heading[0]/icon[0]/Vector[0]·width=5.7 | yes | 1 of 19 carrying channels on this set is a raw number with no variable behind it (12 bound + 6 inferred hold the rest). 1 is on a reified SVG `Vector` (/accordion__item[0]/accordion__heading[0]/icon[0]/Vector[0]·width=5.7): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| carbon | `carbon.button` | reflow | `FROZEN-CHILDREN` | Kind=Primary, Size=Xs, State=Default | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems SPACE_BETWEEN. `label` (TEXT 45×18) keeps its intrinsic box in a SPACE_BETWEEN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| carbon | `carbon.button` | variants | `DEAD-AXIS-VALUE` | Button (carbon.button)/State | Default≡Focus Visible | yes | Switching `State` renders Default≡Focus Visible identically — geometry, fills and text all hash equal — leaving 4 of 5 values (Default, Disabled, Active, Hover) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| carbon | `carbon.checkbox` | reflow | `FROZEN-CHILDREN` | Checked=Unchecked | 0/1 responded; frozen checkbox-label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `checkbox-label` (FRAME 92.92×20) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| carbon | `carbon.iconbutton` | reflow | `FROZEN-CHILDREN` | Kind=Primary, Size=Xs | 0/1 responded; frozen tooltip-trigger__wrapper | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `tooltip-trigger__wrapper` (FRAME 24×24) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| carbon | `carbon.iconbutton` | binding | `LITERAL-CHANNELS` | Kind=Primary, Size=Xs | 2/14; /tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·height=10.67, /tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·width=10.67 | yes | 2 of 14 carrying channels on this set are raw numbers with no variable behind them (7 bound + 5 inferred hold the rest). 2 are on a reified SVG `Vector` (/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·width=10.67, /tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·height=10.67): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| carbon | `carbon.inlinenotification` | binding | `LITERAL-CHANNELS` | Kind=Error, Contrast=High | 2/28; /inline-notification__close-button[1]/inline-notification__close-button-icon[0]/Vector[0]·height=10.67, /inline-notification__close-button[1]/inline-notification__close-button-icon[0]/Vector[0]·width=10.67 | yes | 2 of 28 carrying channels on this set are raw numbers with no variable behind them (18 bound + 8 inferred hold the rest). 2 are on a reified SVG `Vector` (/inline-notification__close-button[1]/inline-notification__close-button-icon[0]/Vector[0]·width=10.67, /inline-notification__close-button[1]/inline-notification__close-button-icon[0]/Vector[0]·height=10.67): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| carbon | `carbon.inlinenotification` | fakeLayout | `FAKE-STACK` | Kind=Error, Contrast=High/inline-notification__details[0]/icon[0]/icon-error[0] | layoutMode NONE over 2 in-flow children | yes | `/inline-notification__details[0]/icon[0]/icon-error[0]` (FRAME) is minted `layoutMode: NONE` over 2 children whose contract parts are in flow; the row is held together by x/y coordinates, so nothing inside it re-lays when anything changes. Fix is in the forward emitter: this container's source display (flex/block) has to become an auto-layout mode. |
| carbon | `carbon.modal` | binding | `LITERAL-CHANNELS` | Size=Xs | 3/62; /modal-container[0]/modal-header (margin box)[0]/modal-header[0]/modal-close-button[2]/icon-tooltip[0]/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·height=13.33, /modal-container[0]/modal-header (margin box)[0]/modal-header[0]/modal-close-button[2]/icon-tooltip[0]/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·width=13.33, /modal-container[0]/modal-header (margin box)[0]·height=71.98 | yes | 3 of 62 carrying channels on this set are raw numbers with no variable behind them (49 bound + 10 inferred hold the rest). 2 are on a reified SVG `Vector` (/modal-container[0]/modal-header (margin box)[0]/modal-header[0]/modal-close-button[2]/icon-tooltip[0]/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·width=13.33, /modal-container[0]/modal-header (margin box)[0]/modal-header[0]/modal-close-button[2]/icon-tooltip[0]/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·height=13.33): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. 1 is on a synthetic `(margin box)` wrapper (/modal-container[0]/modal-header (margin box)[0]·height=71.98), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| carbon | `carbon.tabs` | reflow | `FROZEN-CHILDREN` | State=Default | 0/1 responded; frozen part-1 | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `part-1` (FRAME 261.78×40) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| carbon | `carbon.tabs` | variants | `DEAD-AXIS-VALUE` | Tabs/State | Active≡Hover | yes | Switching `State` renders Active≡Hover identically — geometry, fills and text all hash equal — leaving 2 of 3 values (Default, Active) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| carbon | `carbon.textinput` | reflow | `FROZEN-CHILDREN` | Size=Xs, State=Default | 0/2 responded; frozen text-input__field-outer-wrapper, text-input__label-wrapper | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `text-input__label-wrapper` (FRAME 288×24), `text-input__field-outer-wrapper` (FRAME 288×24) are exactly as wide/tall as the container was and were fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside them — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| fluent | `fluent.avatar` | variants | `DEAD-AXIS` | Avatar (fluent.avatar)/Active | None≡Active, None≡Inactive | yes | Every value of `Active` (None, Active, Inactive) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.badge` | variants | `DEAD-AXIS-VALUE` | Badge (fluent.badge)/Appearance | Ghost≡Outline | yes | Switching `Appearance` renders Ghost≡Outline identically — geometry, fills and text all hash equal — leaving 3 of 4 values (Filled, Ghost, Tint) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.button` | variants | `DEAD-AXIS-VALUE` | Button (fluent.button)/Appearance | Subtle≡Transparent | yes | Switching `Appearance` renders Subtle≡Transparent identically — geometry, fills and text all hash equal — leaving 4 of 5 values (Secondary, Primary, Outline, Subtle) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.card` | reflow | `FROZEN-CHILDREN` | Appearance=Filled, Size=Medium, Orientation=Vertical, State=Default | 0/2 responded; frozen cardheader, label (margin box) | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `label (margin box)` (FRAME 29×8), `cardheader` (FRAME 404×40) keep their intrinsic boxes in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| fluent | `fluent.card` | variants | `DEAD-AXIS-VALUE` | Card (fluent.card)/Appearance | Outline≡Subtle | yes | Switching `Appearance` renders Outline≡Subtle identically — geometry, fills and text all hash equal — leaving 3 of 4 values (Filled, Filled Alternative, Outline) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.card` | variants | `DEAD-AXIS-VALUE` | Card (fluent.card)/State | Default≡Focus Visible | yes | Switching `State` renders Default≡Focus Visible identically — geometry, fills and text all hash equal — leaving 4 of 5 values (Default, Disabled, Active, Hover) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| fluent | `fluent.checkbox` | reflow | `FROZEN-CHILDREN` | Checked=Unchecked, Size=Medium, Shape=Square, State=Default | 0/2 responded; frozen indicator (margin box), label (margin box) | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `indicator (margin box)` (FRAME 24×32), `label (margin box)` (FRAME 79×32) are exactly as wide/tall as the container was and were fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside them — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| fluent | `fluent.checkbox` | variants | `DEAD-AXIS` | Checkbox (fluent.checkbox)/State | Default≡Disabled, Default≡Focus Visible, Default≡Active, Default≡Hover | yes | Every value of `State` (Default, Disabled, Focus Visible, Active, Hover) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| fluent | `fluent.checkbox` | binding | `LITERAL-CHANNELS` | Checked=Unchecked, Size=Medium, Shape=Square, State=Default | 1/21; /label (margin box)[1]·width=79 | yes | 1 of 21 carrying channels on this set is a raw number with no variable behind it (14 bound + 6 inferred hold the rest). 1 is on a synthetic `(margin box)` wrapper (/label (margin box)[1]·width=79), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| fluent | `fluent.dialog` | reflow | `FROZEN-CHILDREN` | Modal Type=Modal | 0/2 responded; frozen dialogsurface (margin box), dialogsurface__backdrop | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `dialogsurface__backdrop` (FRAME 1×1) is minted with a degenerate dimension (≤1px), so there is nothing for auto-layout to move. Fix is in the forward emitter: the part's box came out of the capture with a collapsed axis and is minted as measured. `dialogsurface (margin box)` (FRAME 1038×705) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| fluent | `fluent.dialog` | variants | `DEAD-AXIS-VALUE` | Dialog/Modal Type | Modal≡Alert | yes | Switching `Modal Type` renders Modal≡Alert identically — geometry, fills and text all hash equal — leaving 2 of 3 values (Modal, Non Modal) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.dialog` | binding | `LITERAL-CHANNELS` | Modal Type=Modal | 3/43; /dialogsurface (margin box)[1]/dialogsurface[0]/dialogbody[0]/label-2 (margin box)[1]·width=272, /dialogsurface (margin box)[1]·height=705, /dialogsurface (margin box)[1]·width=1038 | yes | 3 of 43 carrying channels on this set are raw numbers with no variable behind them (34 bound + 6 inferred hold the rest). 3 are on a synthetic `(margin box)` wrapper (/dialogsurface (margin box)[1]·width=1038, /dialogsurface (margin box)[1]·height=705, /dialogsurface (margin box)[1]/dialogsurface[0]/dialogbody[0]/label-2 (margin box)[1]·width=272), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| fluent | `fluent.input` | variants | `DEAD-AXIS-VALUE` | Input (fluent.input)/Appearance | Filled Darker≡Filled Darker Shadow, Filled Lighter≡Filled Lighter Shadow | yes | Switching `Appearance` renders Filled Darker≡Filled Darker Shadow, Filled Lighter≡Filled Lighter Shadow identically — geometry, fills and text all hash equal — leaving 4 of 6 values (Outline, Underline, Filled Darker, Filled Lighter) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.input` | variants | `DEAD-AXIS-VALUE` | Input (fluent.input)/State | Active≡Focus Visible, Default≡Hover | yes | Switching `State` renders Active≡Focus Visible, Default≡Hover identically — geometry, fills and text all hash equal — leaving 3 of 5 values (Default, Active, Disabled) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| fluent | `fluent.spinner` | variants | `DEAD-AXIS-VALUE` | Spinner/Label Position | Above≡Below, After≡Before | yes | Switching `Label Position` renders Above≡Below, After≡Before identically — geometry, fills and text all hash equal — leaving 2 of 4 values (After, Above) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.switch` | reflow | `FROZEN-CHILDREN` | Checked=Unchecked, Label Position=After, State=Default | 0/2 responded; frozen indicator (margin box), label | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `indicator (margin box)` (FRAME 48×36), `label` (FRAME 57×36) are exactly as wide/tall as the container was and were fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside them — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| fluent | `fluent.switch` | variants | `DEAD-AXIS-VALUE` | Switch (fluent.switch)/State | Default≡Focus Visible | yes | Switching `State` renders Default≡Focus Visible identically — geometry, fills and text all hash equal — leaving 2 of 3 values (Default, Disabled) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| fluent | `fluent.tab-list` | reflow | `FROZEN-CHILDREN` | Appearance=Transparent, Size=Medium, Orientation=Horizontal | 0/2 responded; frozen tab, tab-2 | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `tab` (FRAME 89×44), `tab-2` (FRAME 74×44) are exactly as wide/tall as the container was and were fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside them — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| fluent | `fluent.tab-list` | variants | `DEAD-AXIS-VALUE` | TabList/Appearance | Transparent≡Subtle | yes | Switching `Appearance` renders Transparent≡Subtle identically — geometry, fills and text all hash equal — leaving 3 of 4 values (Transparent, Subtle Circular, Filled Circular) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.tab-list` | variants | `DEAD-AXIS` | TabList/Orientation | Horizontal≡Vertical | yes | Every value of `Orientation` (Horizontal, Vertical) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| fluent | `fluent.tooltip` | reflow | `FROZEN-CHILDREN` | Appearance=Normal | 0/2 responded; frozen label, part-0 | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL HUG×FIXED, primaryAxisAlignItems MIN. `part-0` (FRAME 8.47×8.47) is minted visible:false; Figma keeps an invisible child out of auto-layout, so it neither draws nor re-lays. Fix is in the forward emitter: an optional part that is off in this cell should be a component property (or omitted), not an invisible node parked in the stack. `label` (TEXT 189×16) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| mui | `mui.accordion` | reflow | `FROZEN-CHILDREN` | Variant=Elevation, Expanded=Collapsed | 1/2 frozen; collapse-root | yes | Growing the variant +40×+40 re-laid 1/2 (accordion-heading) and left 1 behind. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `collapse-root` (FRAME 288×0) is minted with a degenerate dimension (≤1px), so there is nothing for auto-layout to move. Fix is in the forward emitter: the part's box came out of the capture with a collapsed axis and is minted as measured. |
| mui | `mui.alert` | reflow | `FROZEN-CHILDREN` | Severity=Info, Variant=Standard | 0/2 responded; frozen alert-icon, label | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems MIN. `alert-icon` (FRAME 22×36), `label` (FRAME 190×37) keep their intrinsic boxes in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| mui | `mui.autocomplete` | reflow | `FROZEN-CHILDREN` | Size=Medium | 0/1 responded; frozen formcontrol-fullwidth | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `formcontrol-fullwidth` (FRAME 288×56) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| mui | `mui.autocomplete` | binding | `LITERAL-CHANNELS` | Size=Medium | 6/92; /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]/autocomplete-tag[0]/icon[1]/Vector[0]·height=14.55, /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]/autocomplete-tag[0]/icon[1]/Vector[0]·width=14.55, /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]·width=83.3, /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag-2 (margin box)[1]/autocomplete-tag-2[0]/icon-2[1]/Vector[0]·height=14.55, /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag-2 (margin box)[1]/autocomplete-tag-2[0]/icon-2[1]/Vector[0]·width=14.55, /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag-2 (margin box)[1]·width=76.97 | yes | 6 of 92 carrying channels on this set are raw numbers with no variable behind them (68 bound + 18 inferred hold the rest). 4 are on a reified SVG `Vector` (/formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]/autocomplete-tag[0]/icon[1]/Vector[0]·width=14.55, /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]/autocomplete-tag[0]/icon[1]/Vector[0]·height=14.55, /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag-2 (margin box)[1]/autocomplete-tag-2[0]/icon-2[1]/Vector[0]·width=14.55, …): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. 2 are on a synthetic `(margin box)` wrapper (/formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]·width=83.3, /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag-2 (margin box)[1]·width=76.97), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| mui | `mui.badge` | reflow | `FROZEN-CHILDREN` | Color=Default, Variant=Standard | 1/2 frozen; label | yes | Growing the variant +40×+40 re-laid 1/2 (label-2) and left 1 behind. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `label` (FRAME 14×20) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| mui | `mui.button` | variants | `DEAD-AXIS-VALUE` | Button (mui.button)/State | Default≡Active, Default≡Focus Visible, Default≡Hover | yes | Switching `State` renders Default≡Active, Default≡Focus Visible, Default≡Hover identically — geometry, fills and text all hash equal — leaving 2 of 5 values (Default, Disabled) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| mui | `mui.card` | variants | `DEAD-AXIS` | Card (mui.card)/Elevation | 1≡0, 1≡3, 1≡8 | yes | Every value of `Elevation` (1, 0, 3, 8) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are size steps, so the size token they resolve to is the same number by the time it is minted (or the size only changes a channel the fingerprint cannot see, such as a font metric that did not re-measure). Fix belongs in the size-axis lowering: the axis is minted, its effect is not. |
| mui | `mui.circular-progress` | binding | `LITERAL-CHANNELS` | Variant=Indeterminate | 3/6; /root-indeterminate[0]/Vector[0]·height=36.73, /root-indeterminate[0]/Vector[0]·strokeWeight=3.5999999046325684, /root-indeterminate[0]/Vector[0]·width=36.73 | yes | 3 of 6 carrying channels on this set are raw numbers with no variable behind them (1 bound + 2 inferred hold the rest). 3 are on a reified SVG `Vector` (/root-indeterminate[0]/Vector[0]·strokeWeight=3.5999999046325684, /root-indeterminate[0]/Vector[0]·width=36.73, /root-indeterminate[0]/Vector[0]·height=36.73): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| mui | `mui.dialog` | reflow | `FROZEN-CHILDREN` | Max width=Sm | 1/2 frozen; dialog-container | yes | Growing the variant +40×+40 re-laid 1/2 (backdrop-root) and left 1 behind. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `dialog-container` (FRAME 424×123) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| mui | `mui.dialog` | variants | `DEAD-AXIS` | Dialog (mui.dialog)/Max width | Sm≡Xs, Sm≡Md, Sm≡Lg, Sm≡Xl | yes | Every value of `Max width` (Sm, Xs, Md, Lg, Xl) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are size steps, so the size token they resolve to is the same number by the time it is minted (or the size only changes a channel the fingerprint cannot see, such as a font metric that did not re-measure). Fix belongs in the size-axis lowering: the axis is minted, its effect is not. |
| mui | `mui.dialog` | binding | `LITERAL-CHANNELS` | Max width=Sm | 2/15; /dialog-container[1]/dialog-paper (margin box)[0]·height=123, /dialog-container[1]/dialog-paper (margin box)[0]·width=424 | yes | 2 of 15 carrying channels on this set are raw numbers with no variable behind them (12 bound + 1 inferred hold the rest). 2 are on a synthetic `(margin box)` wrapper (/dialog-container[1]/dialog-paper (margin box)[0]·width=424, /dialog-container[1]/dialog-paper (margin box)[0]·height=123), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| mui | `mui.dialog` | fakeLayout | `FAKE-ABSOLUTE` | Max width=Sm/backdrop-root[0] | source part `backdrop-root` is in flow | yes | `/backdrop-root[0]` (FRAME 424×123) is minted `layoutPositioning: ABSOLUTE`, but the contract's anatomy part `backdrop-root` is in flow (no out-of-flow position declared). The canvas is holding this child in place with coordinates that the source never asked for, which is exactly the frozen-rectangle failure this assertion exists to catch. Fix is in the forward emitter's placement of this part: it should sit in its parent's auto-layout. |
| mui | `mui.input-adornment` | variants | `DEAD-AXIS` | InputAdornment/Position | Start≡End | yes | Every value of `Position` (Start, End) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| mui | `mui.linear-progress` | reflow | `FROZEN-CHILDREN` | Variant=Indeterminate | 0/2 responded; frozen linearprogress-bar, linearprogress-bar-2 | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `linearprogress-bar` (FRAME 100.8×4), `linearprogress-bar-2` (FRAME 1×4) are layoutPositioning ABSOLUTE and keep their offsets when the parent grows, because the minted nodes carry no constraint that follows the parent. Fix is in the lowering rule for out-of-flow parts: a source `inset`/`right`/`bottom` has to become a Figma constraint (STRETCH/MAX), not a frozen x/y. |
| mui | `mui.link` | reflow | `FROZEN-CHILDREN` | Color=Primary, Underline=Hover, State=Default | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `label` (TEXT 31×19) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| mui | `mui.link` | variants | `DEAD-AXIS` | Link (mui.link)/Underline | Hover≡None, Hover≡Always | yes | Every value of `Underline` (Hover, None, Always) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| mui | `mui.link` | variants | `DEAD-AXIS-VALUE` | Link (mui.link)/State | Default≡Active, Default≡Hover | yes | Switching `State` renders Default≡Active, Default≡Hover identically — geometry, fills and text all hash equal — leaving 2 of 4 values (Default, Focus Visible) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| mui | `mui.menu` | reflow | `FROZEN-CHILDREN` | Menu | 0/1 responded; frozen list-padding | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `list-padding` (FRAME 114.77×124) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| mui | `mui.paper` | reflow | `FROZEN-CHILDREN` | Elevation=1, Variant=Elevation | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `label` (TEXT 107×19) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| mui | `mui.paper` | variants | `DEAD-AXIS` | Paper/Elevation | 1≡0, 1≡3, 1≡8 | yes | Every value of `Elevation` (1, 0, 3, 8) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are size steps, so the size token they resolve to is the same number by the time it is minted (or the size only changes a channel the fingerprint cannot see, such as a font metric that did not re-measure). Fix belongs in the size-axis lowering: the axis is minted, its effect is not. |
| mui | `mui.radio` | variants | `DEAD-AXIS` | Radio (mui.radio)/Color | Primary≡Secondary, Primary≡Error, Primary≡Info, Primary≡Success, Primary≡Warning, Primary≡Default | yes | Every value of `Color` (Primary, Secondary, Error, Info, Success, Warning, Default) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| mui | `mui.select` | reflow | `FROZEN-CHILDREN` | Size=Medium | 1/3 frozen; outlinedinput-notchedoutline | yes | Growing the variant +40×+40 re-laid 2/3 (label, icon) and left 1 behind. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `outlinedinput-notchedoutline` (FRAME 73.94×61) is layoutPositioning ABSOLUTE and keeps its offset when the parent grows, because the minted node carries no constraint that follows the parent. Fix is in the lowering rule for out-of-flow parts: a source `inset`/`right`/`bottom` has to become a Figma constraint (STRETCH/MAX), not a frozen x/y. |
| mui | `mui.slider` | reflow | `FROZEN-CHILDREN` | Color=Primary, Size=Medium | 0/3 responded; frozen slider-rail, slider-thumb, slider-track | yes | Growing the variant +40×+40 moved and resized NOTHING: all 3 children kept their exact boxes. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `slider-rail` (FRAME 288×4), `slider-track` (FRAME 117.19×6), `slider-thumb` (FRAME 20×20) are layoutPositioning ABSOLUTE and keep their offsets when the parent grows, because the minted nodes carry no constraint that follows the parent. Fix is in the lowering rule for out-of-flow parts: a source `inset`/`right`/`bottom` has to become a Figma constraint (STRETCH/MAX), not a frozen x/y. |
| mui | `mui.snackbar` | variants | `DEAD-AXIS` | Snackbar/Position | BottomLeft≡BottomCenter, BottomLeft≡TopRight | yes | Every value of `Position` (BottomLeft, BottomCenter, TopRight) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| mui | `mui.switch` | reflow | `FROZEN-CHILDREN` | Color=Primary, Size=Medium, Checked=Unchecked | 0/2 responded; frozen buttonbase-root, switch-track | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `buttonbase-root` (FRAME 38×38) is layoutPositioning ABSOLUTE and keeps its offset when the parent grows, because the minted node carries no constraint that follows the parent. Fix is in the lowering rule for out-of-flow parts: a source `inset`/`right`/`bottom` has to become a Figma constraint (STRETCH/MAX), not a frozen x/y. `switch-track` (FRAME 34×14) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| mui | `mui.switch` | variants | `DEAD-AXIS` | Switch (mui.switch)/Color | Primary≡Secondary, Primary≡Error, Primary≡Warning, Primary≡Info, Primary≡Success, Primary≡Default | yes | Every value of `Color` (Primary, Secondary, Error, Warning, Info, Success, Default) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| mui | `mui.table-pagination` | reflow | `FROZEN-CHILDREN` | TablePagination | 0/1 responded; frozen tablepagination-toolbar | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `tablepagination-toolbar` (FRAME 428×52) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| mui | `mui.table-pagination` | binding | `LITERAL-CHANNELS` | TablePagination | 6/73; /tablepagination-toolbar[0]/label (margin box)[1]/label[0]·width=98, /tablepagination-toolbar[0]/label (margin box)[1]·width=98, /tablepagination-toolbar[0]/label-3 (margin box)[3]/label-3[0]·width=54, /tablepagination-toolbar[0]/label-3 (margin box)[3]·width=54, /tablepagination-toolbar[0]/tablepagination-actions (margin box)[4]/tablepagination-actions[0]/buttonbase-root-2[1]/buttonbase-root-2-icon[0]/Vector[0]·width=7.41, /tablepagination-toolbar[0]/tablepagination-actions (margin box)[4]/tablepagination-actions[0]/buttonbase-root[0]/buttonbase-root-icon[0]/Vector[0]·width=7.41 | yes | 6 of 73 carrying channels on this set are raw numbers with no variable behind them (44 bound + 23 inferred hold the rest). 2 are on a reified SVG `Vector` (/tablepagination-toolbar[0]/tablepagination-actions (margin box)[4]/tablepagination-actions[0]/buttonbase-root[0]/buttonbase-root-icon[0]/Vector[0]·width=7.41, /tablepagination-toolbar[0]/tablepagination-actions (margin box)[4]/tablepagination-actions[0]/buttonbase-root-2[1]/buttonbase-root-2-icon[0]/Vector[0]·width=7.41): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. 4 are on a synthetic `(margin box)` wrapper (/tablepagination-toolbar[0]/label (margin box)[1]·width=98, /tablepagination-toolbar[0]/label (margin box)[1]/label[0]·width=98, /tablepagination-toolbar[0]/label-3 (margin box)[3]·width=54, …), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| mui | `mui.table` | reflow | `FROZEN-CHILDREN` | Size=Medium | 0/2 responded; frozen tablebody-root, tablehead-root | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `tablehead-root` (FRAME 688×57.8), `tablebody-root` (FRAME 688×130) are exactly as wide/tall as the container was and were fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside them — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| mui | `mui.table` | binding | `LITERAL-CHANNELS` | Size=Medium | 2/172; /tablehead-root[0]/tablerow-head[0]/tablecell-head-2[1]/label[0]/icon-2[1]/Vector[0]·height=14.4, /tablehead-root[0]/tablerow-head[0]/tablecell-head-2[1]/label[0]/icon-2[1]/Vector[0]·width=14.4 | yes | 2 of 172 carrying channels on this set are raw numbers with no variable behind them (153 bound + 17 inferred hold the rest). 2 are on a reified SVG `Vector` (/tablehead-root[0]/tablerow-head[0]/tablecell-head-2[1]/label[0]/icon-2[1]/Vector[0]·width=14.4, /tablehead-root[0]/tablerow-head[0]/tablecell-head-2[1]/label[0]/icon-2[1]/Vector[0]·height=14.4): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| mui | `mui.tabs` | reflow | `FROZEN-CHILDREN` | Text color=Primary, Indicator color=Primary | 0/1 responded; frozen tabs-fixed | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `tabs-fixed` (FRAME 288×48) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| mui | `mui.text-field` | reflow | `FROZEN-CHILDREN` | Variant=Outlined, Size=Medium | 0/3 responded; frozen inputbase-adornedend, label, label-5 (margin box) | yes | Growing the variant +40×+40 moved and resized NOTHING: all 3 children kept their exact boxes. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `inputbase-adornedend` (FRAME 252.97×56) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. `label-5 (margin box)` (FRAME 138×17), `label` (FRAME 56.19×23) keep their intrinsic boxes in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| mui | `mui.text-field` | variants | `DEAD-AXIS` | TextField/Variant | Outlined≡Filled, Outlined≡Standard | yes | Every value of `Variant` (Outlined, Filled, Standard) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| mui | `mui.text-field` | variants | `DEAD-AXIS` | TextField/Size | Medium≡Small | yes | Every value of `Size` (Medium, Small) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are size steps, so the size token they resolve to is the same number by the time it is minted (or the size only changes a channel the fingerprint cannot see, such as a font metric that did not re-measure). Fix belongs in the size-axis lowering: the axis is minted, its effect is not. |
| mui | `mui.text-field` | binding | `LITERAL-CHANNELS` | Variant=Outlined, Size=Medium | 2/37; /label-5 (margin box)[1]/label-5[0]·width=110, /label-5 (margin box)[1]·width=138 | yes | 2 of 37 carrying channels on this set are raw numbers with no variable behind them (28 bound + 7 inferred hold the rest). 2 are on a synthetic `(margin box)` wrapper (/label-5 (margin box)[1]·width=138, /label-5 (margin box)[1]/label-5[0]·width=110), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| polaris | `polaris.avatar` | reflow | `FROZEN-CHILDREN` | Size=Md | 0/1 responded; frozen initials | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `initials` (FRAME 28×28) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| polaris | `polaris.banner` | reflow | `FROZEN-CHILDREN` | Tone=Info, State=Default | 0/1 responded; frozen box | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is HORIZONTAL FIXED×HUG, primaryAxisAlignItems MIN. `box` (FRAME 319.33×88) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| polaris | `polaris.banner` | binding | `LITERAL-CHANNELS` | Tone=Info, State=Default | 3/91; /box[0]/blockstack[0]/box-2[0]/inlinestack[0]/button (margin box)[1]/button[0]/button__icon (margin box)[0]/button__icon[0]/part-0-0-0-0-1-0-0[0]/icon-3[0]/Vector[0]·height=10.5, /box[0]/blockstack[0]/box-2[0]/inlinestack[0]/button (margin box)[1]/button[0]/button__icon (margin box)[0]/button__icon[0]/part-0-0-0-0-1-0-0[0]/icon-3[0]/Vector[0]·width=10.54, /box[0]/blockstack[0]/box-3[1]/blockstack-2[0]/buttongroup (margin box)[1]/buttongroup[0]/buttongroup__item (margin box)[0]·width=90.08 | yes | 3 of 91 carrying channels on this set are raw numbers with no variable behind them (67 bound + 21 inferred hold the rest). 2 are on a reified SVG `Vector` (/box[0]/blockstack[0]/box-2[0]/inlinestack[0]/button (margin box)[1]/button[0]/button__icon (margin box)[0]/button__icon[0]/part-0-0-0-0-1-0-0[0]/icon-3[0]/Vector[0]·width=10.54, /box[0]/blockstack[0]/box-2[0]/inlinestack[0]/button (margin box)[1]/button[0]/button__icon (margin box)[0]/button__icon[0]/part-0-0-0-0-1-0-0[0]/icon-3[0]/Vector[0]·height=10.5): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. 1 is on a synthetic `(margin box)` wrapper (/box[0]/blockstack[0]/box-3[1]/blockstack-2[0]/buttongroup (margin box)[1]/buttongroup[0]/buttongroup__item (margin box)[0]·width=90.08), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| polaris | `polaris.banner` | fakeLayout | `FAKE-STACK` | Tone=Info, State=Default/box[0]/blockstack[0]/box-2[0]/inlinestack[0]/inlinestack-2[0]/part-0-0-0-0-0-0[0]/icon[0]/icon-info[0] | layoutMode NONE over 3 in-flow children | yes | `/box[0]/blockstack[0]/box-2[0]/inlinestack[0]/inlinestack-2[0]/part-0-0-0-0-0-0[0]/icon[0]/icon-info[0]` (FRAME) is minted `layoutMode: NONE` over 3 children whose contract parts are in flow; the row is held together by x/y coordinates, so nothing inside it re-lays when anything changes. Fix is in the forward emitter: this container's source display (flex/block) has to become an auto-layout mode. |
| polaris | `polaris.button` | reflow | `FROZEN-CHILDREN` | Size=Medium, Text Align=Center, Tone=Default, Variant=Secondary, State=Default | 1/2 frozen; icon (margin box) | yes | Growing the variant +40×+40 re-laid 1/2 (label) and left 1 behind. The container is HORIZONTAL HUG×HUG, primaryAxisAlignItems CENTER. `icon (margin box)` (FRAME 20×16) is minted visible:false; Figma keeps an invisible child out of auto-layout, so it neither draws nor re-lays. Fix is in the forward emitter: an optional part that is off in this cell should be a component property (or omitted), not an invisible node parked in the stack. |
| polaris | `polaris.button` | variants | `DEAD-AXIS-VALUE` | Button (polaris.button)/Size | Medium≡Slim | yes | Switching `Size` renders Medium≡Slim identically — geometry, fills and text all hash equal — leaving 3 of 4 values (Medium, Micro, Large) that actually draw differently. The collapsed values are size steps, so the size token they resolve to is the same number by the time it is minted (or the size only changes a channel the fingerprint cannot see, such as a font metric that did not re-measure). Fix belongs in the size-axis lowering: the axis is minted, its effect is not. |
| polaris | `polaris.button` | variants | `DEAD-AXIS` | Button (polaris.button)/Text Align | Center≡Left, Center≡Right, Center≡Start, Center≡End | yes | Every value of `Text Align` (Center, Left, Right, Start, End) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| polaris | `polaris.button` | variants | `DEAD-AXIS-VALUE` | Button (polaris.button)/Variant | Tertiary≡Monochrome Plain | yes | Switching `Variant` renders Tertiary≡Monochrome Plain identically — geometry, fills and text all hash equal — leaving 4 of 5 values (Secondary, Plain, Primary, Tertiary) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| polaris | `polaris.button` | fakeLayout | `FAKE-STACK` | Size=Medium, Text Align=Center, Tone=Default, Variant=Secondary, State=Default/icon (margin box)[0]/icon[0]/icon-2[0] | layoutMode NONE over 2 in-flow children | yes | `/icon (margin box)[0]/icon[0]/icon-2[0]` (FRAME) is minted `layoutMode: NONE` over 2 children whose contract parts are in flow; the row is held together by x/y coordinates, so nothing inside it re-lays when anything changes. Fix is in the forward emitter: this container's source display (flex/block) has to become an auto-layout mode. |
| polaris | `polaris.checkbox` | fakeLayout | `FAKE-ABSOLUTE` | Checked=unchecked/choice__control[0]/checkbox (margin box)[0]/checkbox[0]/backdrop[0] | source part `backdrop` is in flow | yes | `/choice__control[0]/checkbox (margin box)[0]/checkbox[0]/backdrop[0]` (RECTANGLE 18×18) is minted `layoutPositioning: ABSOLUTE`, but the contract's anatomy part `backdrop` is in flow (`position: relative`). The canvas is holding this child in place with coordinates that the source never asked for, which is exactly the frozen-rectangle failure this assertion exists to catch. Fix is in the forward emitter's placement of this part: it should sit in its parent's auto-layout. |
| polaris | `polaris.progress-bar` | reflow | `FROZEN-CHILDREN` | Size=Medium, Tone=Highlight | 0/1 responded; frozen indicator | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `indicator` (FRAME 115×16) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| polaris | `polaris.progress-bar` | binding | `LITERAL-CHANNELS` | Size=Medium, Tone=Highlight | 1/10; /indicator[0]·width=115 | yes | 1 of 10 carrying channels on this set is a raw number with no variable behind it (7 bound + 2 inferred hold the rest). 1 is a value-carrying bar width (/indicator[0]·width=115): the component's current value is baked in as a pixel width, so whatever the source expressed it with (a percentage of the track, a transform) did not survive lowering and no variable can move it. Fix is the percentage/value lowering rule. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| polaris | `polaris.radio-button` | fakeLayout | `FAKE-ABSOLUTE` | Checked=unchecked/choice__control[0]/radiobutton (margin box)[0]/radiobutton[0]/backdrop[0] | source part `backdrop` is in flow | yes | `/choice__control[0]/radiobutton (margin box)[0]/radiobutton[0]/backdrop[0]` (RECTANGLE 18×18) is minted `layoutPositioning: ABSOLUTE`, but the contract's anatomy part `backdrop` is in flow (`position: relative`). The canvas is holding this child in place with coordinates that the source never asked for, which is exactly the frozen-rectangle failure this assertion exists to catch. Fix is in the forward emitter's placement of this part: it should sit in its parent's auto-layout. |
| polaris | `polaris.spinner` | binding | `LITERAL-CHANNELS` | Size=Large | 2/6; /root-large[0]/Vector[0]·height=42.08, /root-large[0]/Vector[0]·width=42.36 | yes | 2 of 6 carrying channels on this set are raw numbers with no variable behind them (1 bound + 3 inferred hold the rest). 2 are on a reified SVG `Vector` (/root-large[0]/Vector[0]·width=42.36, /root-large[0]/Vector[0]·height=42.08): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| polaris | `polaris.tag` | reflow | `FROZEN-CHILDREN` | Size=none, State=Default | 2/3 frozen; button-2 (margin box), link | yes | Growing the variant +40×+40 re-laid 1/3 (text) and left 2 behind. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `link` (FRAME 59.92×20), `button-2 (margin box)` (FRAME 21×20) are minted visible:false; Figma keeps an invisible child out of auto-layout, so it neither draws nor re-lays. Fix is in the forward emitter: an optional part that is off in this cell should be a component property (or omitted), not an invisible node parked in the stack. |
| polaris | `polaris.tag` | variants | `DEAD-AXIS-VALUE` | Tag (polaris.tag)/State | Active≡Focus Visible | yes | Switching `State` renders Active≡Focus Visible identically — geometry, fills and text all hash equal — leaving 3 of 4 values (Default, Disabled, Active) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| polaris | `polaris.tag` | binding | `LITERAL-CHANNELS` | Size=none, State=Default | 2/33; /Size=none, State=Default·width=71.92, /button-2 (margin box)[2]/button-2[0]/icon-3[0]/Vector[0]·width=8.04 | yes | 2 of 33 carrying channels on this set are raw numbers with no variable behind them (22 bound + 9 inferred hold the rest). 1 is on a reified SVG `Vector` (/button-2 (margin box)[2]/button-2[0]/icon-3[0]/Vector[0]·width=8.04): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. 1 is a fixed box dimension (/Size=none, State=Default·width=71.92) minted from the captured geometry rather than from a sizing token. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| polaris | `polaris.text-field` | reflow | `FROZEN-CHILDREN` | Type=Text, Input Mode=Text, Align=Left, Variant=Inherit, Size=Medium, State=Default | 1/2 frozen; connected | yes | Growing the variant +40×+40 re-laid 1/2 (labelled__labelwrapper) and left 1 behind. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `connected` (FRAME 211×36) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| polaris | `polaris.text-field` | variants | `DEAD-AXIS` | TextField (polaris.text-field)/Variant | Inherit≡Borderless | yes | Every value of `Variant` (Inherit, Borderless) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| polaris | `polaris.text-field` | variants | `DEAD-AXIS-VALUE` | TextField (polaris.text-field)/State | Active≡Focus Visible | yes | Switching `State` renders Active≡Focus Visible identically — geometry, fills and text all hash equal — leaving 4 of 5 values (Default, Disabled, Active, Hover) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| polaris | `polaris.text-field` | binding | `LITERAL-CHANNELS` | Type=Text, Input Mode=Text, Align=Left, Variant=Inherit, Size=Medium, State=Default | 3/34; /connected[1]/connected__item[0]/textfield[0]/backdrop[0]·width=210.81, /connected[1]/connected__item[0]/textfield[0]/prefix (margin box)[1]·width=26.41, /connected[1]/connected__item[0]/textfield[0]/suffix (margin box)[3]·width=43.41 | yes | 3 of 34 carrying channels on this set are raw numbers with no variable behind them (24 bound + 7 inferred hold the rest). 2 are on a synthetic `(margin box)` wrapper (/connected[1]/connected__item[0]/textfield[0]/prefix (margin box)[1]·width=26.41, /connected[1]/connected__item[0]/textfield[0]/suffix (margin box)[3]·width=43.41), which is sized to the child's captured box. Fix is in the margin-box lowering: the wrapper carries no token and does not round-trip, so its size can only ever be a literal. 1 is a value-carrying bar width (/connected[1]/connected__item[0]/textfield[0]/backdrop[0]·width=210.81): the component's current value is baked in as a pixel width, so whatever the source expressed it with (a percentage of the track, a transform) did not survive lowering and no variable can move it. Fix is the percentage/value lowering rule. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| polaris | `polaris.text` | reflow | `FROZEN-CHILDREN` | Alignment=Start, As=P, Tone=Base, Font Weight=Regular, Variant=Heading Xs | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `label` (TEXT 134×16) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| polaris | `polaris.text` | variants | `DEAD-AXIS-VALUE` | Text/Variant | Heading Lg≡Heading Xl, Heading Xs≡Body Sm, Heading Sm≡Body Md, Heading Md≡Body Lg | yes | Switching `Variant` renders Heading Lg≡Heading Xl, Heading Xs≡Body Sm, Heading Sm≡Body Md, Heading Md≡Body Lg identically — geometry, fills and text all hash equal — leaving 7 of 11 values (Heading Xs, Heading Sm, Heading Md, Heading Lg, Heading2xl, Heading3xl, Body Xs) that actually draw differently. The axis exists on the canvas but the declarations that distinguish these values never became a drawn difference — the emitter minted the cells from the same resolved style. Fix belongs in the forward emitter's per-cell style resolution for this axis. |
| polaris | `polaris.thumbnail` | reflow | `FROZEN-CHILDREN` | Size=Medium | 0/1 responded; frozen part-0 | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `part-0` (FRAME 60×60) is layoutPositioning ABSOLUTE and keeps its offset when the parent grows, because the minted node carries no constraint that follows the parent. Fix is in the lowering rule for out-of-flow parts: a source `inset`/`right`/`bottom` has to become a Figma constraint (STRETCH/MAX), not a frozen x/y. |
| shadcn | `shadcn.alert` | reflow | `FROZEN-CHILDREN` | Variant=Default | 0/2 responded; frozen label, label-2 | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `label` (TEXT 63×20), `label-2` (TEXT 292×20) keep their intrinsic boxes in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| shadcn | `shadcn.avatar` | reflow | `FROZEN-CHILDREN` | Size=Default | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is HORIZONTAL FIXED×FIXED, primaryAxisAlignItems MIN. `label` (FRAME 21×20) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| shadcn | `shadcn.badge` | variants | `DEAD-AXIS` | Badge (shadcn.badge)/State | Default≡Active, Default≡Hover | yes | Every value of `State` (Default, Active, Hover) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| shadcn | `shadcn.button` | variants | `DEAD-AXIS-VALUE` | Button (shadcn.button)/Size | Default≡Sm, Default≡Lg, Icon≡Icon Xs, Icon≡Icon Sm, Icon≡Icon Lg | yes | Switching `Size` renders Default≡Sm, Default≡Lg, Icon≡Icon Xs, Icon≡Icon Sm, Icon≡Icon Lg identically — geometry, fills and text all hash equal — leaving 3 of 8 values (Default, Xs, Icon) that actually draw differently. The collapsed values are size steps, so the size token they resolve to is the same number by the time it is minted (or the size only changes a channel the fingerprint cannot see, such as a font metric that did not re-measure). Fix belongs in the size-axis lowering: the axis is minted, its effect is not. |
| shadcn | `shadcn.button` | variants | `DEAD-AXIS-VALUE` | Button (shadcn.button)/State | Default≡Disabled, Default≡Focus Visible, Active≡Hover | yes | Switching `State` renders Default≡Disabled, Default≡Focus Visible, Active≡Hover identically — geometry, fills and text all hash equal — leaving 2 of 5 values (Default, Active) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| shadcn | `shadcn.card` | reflow | `FROZEN-CHILDREN` | Size=Default | 0/2 responded; frozen label-3, part-0 | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `label-3` (FRAME 317×20) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. `part-0` (FRAME 158×44) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| shadcn | `shadcn.checkbox` | variants | `DEAD-AXIS` | Checkbox (shadcn.checkbox)/State | Default≡Disabled, Default≡Focus Visible | yes | Every value of `State` (Default, Disabled, Focus Visible) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| shadcn | `shadcn.input` | reflow | `FROZEN-CHILDREN` | State=Default | 0/1 responded; frozen placeholder | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `placeholder` (TEXT 0×20) is minted with a degenerate dimension (≤1px), so there is nothing for auto-layout to move. Fix is in the forward emitter: the part's box came out of the capture with a collapsed axis and is minted as measured. |
| shadcn | `shadcn.input` | variants | `DEAD-AXIS` | Input (shadcn.input)/State | Default≡Disabled, Default≡Active, Default≡Focus Visible | yes | Every value of `State` (Default, Disabled, Active, Focus Visible) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| shadcn | `shadcn.select` | binding | `LITERAL-CHANNELS` | Select | 2/18; /icon[1]/Vector[0]·height=5.33, /icon[1]/Vector[0]·width=10.67 | yes | 2 of 18 carrying channels on this set are raw numbers with no variable behind them (13 bound + 3 inferred hold the rest). 2 are on a reified SVG `Vector` (/icon[1]/Vector[0]·width=10.67, /icon[1]/Vector[0]·height=5.33): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| shadcn | `shadcn.switch` | variants | `DEAD-AXIS` | Switch (shadcn.switch)/State | Default≡Disabled, Default≡Focus Visible | yes | Every value of `State` (Default, Disabled, Focus Visible) renders the same instance — geometry, fills AND text all hash equal — so the axis is a row of identical children a designer can switch between and see nothing. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| shadcn | `shadcn.tabs` | reflow | `FROZEN-CHILDREN` | Tabs (shadcn.tabs) | 0/2 responded; frozen label-4, part-0 | yes | Growing the variant +40×+40 moved and resized NOTHING: all 2 children kept their exact boxes. The container is VERTICAL FIXED×FIXED, primaryAxisAlignItems MIN. `label-4` (TEXT 285×20) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. `part-0` (FRAME 229.3×36) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| tailwind | `flowbite.alert` | reflow | `FROZEN-CHILDREN` | Color=Info | 0/1 responded; frozen part-0 | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `part-0` (FRAME 96×20) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| tailwind | `flowbite.alert` | binding | `LITERAL-CHANNELS` | Color=Info | 1/34; /part-0[0]/dismiss[2]/dismiss-icon[0]/Vector[0]·width=12.01 | yes | 1 of 34 carrying channels on this set is a raw number with no variable behind it (18 bound + 15 inferred hold the rest). 1 is on a reified SVG `Vector` (/part-0[0]/dismiss[2]/dismiss-icon[0]/Vector[0]·width=12.01): the icon path's own box and stroke weight are emitted as measured pixels, so the icon cannot follow an icon-size or stroke token. Fix is in the SVG reification path of the forward emitter — the same class as the pinned `altitude.iconclose`. Diff the SITES, not the percentage: a round that only relocated the literals must not read as an improvement. |
| tailwind | `flowbite.badge` | variants | `DEAD-AXIS-VALUE` | Badge (flowbite.badge)/State | Active≡Hover | yes | Switching `State` renders Active≡Hover identically — geometry, fills and text all hash equal — leaving 2 of 3 values (Default, Active) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| tailwind | `flowbite.button` | variants | `DEAD-AXIS-VALUE` | Button (flowbite.button)/State | Default≡Disabled, Active≡Hover | yes | Switching `State` renders Default≡Disabled, Active≡Hover identically — geometry, fills and text all hash equal — leaving 3 of 5 values (Default, Active, Focus Visible) that actually draw differently. The collapsed values are interaction states, and the state plane carries no drawn difference here: whatever the source expresses on `:hover`/`:focus-visible`/`:active`/`[disabled]` (a ring, a shadow, an opacity) is not reaching the canvas. Fix belongs in the state-preview emitter — the state cell is minted from the same declarations as the default cell. |
| tailwind | `flowbite.card` | reflow | `FROZEN-CHILDREN` | Card (flowbite.card) | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL HUG×HUG, primaryAxisAlignItems MIN. `label` (FRAME 147×72) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| tailwind | `flowbite.helpertext` | reflow | `FROZEN-CHILDREN` | Color=Gray | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `label` (TEXT 74×20) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |
| tailwind | `flowbite.kbd` | reflow | `FROZEN-CHILDREN` | Kbd | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `label` (TEXT 22×16) keeps its intrinsic box in a MIN stack: the container simply gains slack and nothing stretches or re-aligns. Fix is the layout-family lowering rule (fill/stretch sizing and alignment from the source CSS), not a nudge to this baseline. |
| tailwind | `flowbite.label` | reflow | `FROZEN-CHILDREN` | Color=Default | 0/1 responded; frozen label | yes | Growing the variant +40×+40 moved and resized NOTHING: the single child kept its exact box. The container is VERTICAL FIXED×HUG, primaryAxisAlignItems MIN. `label` (TEXT 67×20) is exactly as wide/tall as the container was and was fixed at that measured size rather than filling the axis, so the extra 40px becomes slack beside it — the part was lowered to the pixel size it happened to have when captured. Fix is the layout-family lowering rule: a part the source stretches (`width:100%`, `flex:1`, a block child) has to carry a fill/grow sizing decision. |

## Literal channel sites — the number the re-promote wave must move

The bound ratio alone cannot tell a round that IMPROVED the binding from one that merely changed which channels are literal. Every literal site is therefore named here, per set, path and channel and value — so the next round can diff sites, not just percentages. A set with no row here has every carrying channel bound or inferred.

| library | id | bound / total | literal | sites |
|---|---|---|---|---|
| first-party | `ds.accordion-item` | 17/20 (85%) | 3 | `/trigger[0]/chevron[0]/Vector[0]·height=6.3`, `/trigger[0]/chevron[0]/Vector[0]·strokeWeight=1.0499999523162842`, `/trigger[0]/chevron[0]/Vector[0]·width=3.15` |
| first-party | `ds.banner` | 35/38 (92%) | 3 | `/statusIcon[0]/Vector[1]·height=4.5`, `/statusIcon[0]/Vector[2]·height=1.8`, `/statusIcon[0]/Vector[2]·width=1.8` |
| first-party | `ds.bento-grid` | 2/3 (67%) | 1 | `/BentoGrid·itemSpacing=0` |
| first-party | `ds.breadcrumb-item` | 5/8 (63%) | 3 | `/separatorIcon[0]/Vector[0]·height=5.4`, `/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869`, `/separatorIcon[0]/Vector[0]·width=2.7` |
| first-party | `ds.breadcrumbs` | 17/26 (65%) | 9 | `/Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·height=5.4`, `/Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869`, `/Children[0]/BreadcrumbItem[0]/separatorIcon[0]/Vector[0]·width=2.7`, `/Children[0]/BreadcrumbItem[1]/separatorIcon[0]/Vector[0]·height=5.4`, `/Children[0]/BreadcrumbItem[1]/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869`, `/Children[0]/BreadcrumbItem[1]/separatorIcon[0]/Vector[0]·width=2.7`, `/Children[0]/BreadcrumbItem[2]/separatorIcon[0]/Vector[0]·height=5.4`, `/Children[0]/BreadcrumbItem[2]/separatorIcon[0]/Vector[0]·strokeWeight=0.9000000357627869`, `/Children[0]/BreadcrumbItem[2]/separatorIcon[0]/Vector[0]·width=2.7` |
| first-party | `ds.button` | 15/17 (88%) | 2 | `/loadingSpinner[0]/Vector[0]·height=5.25`, `/loadingSpinner[0]/Vector[0]·width=5.25` |
| first-party | `ds.card` | 33/34 (97%) | 1 | `/Actions[2]·width=318` |
| first-party | `ds.chat-message-metadata` | 6/9 (67%) | 3 | `/statusIcon[0]/Vector[0]·height=4.56`, `/statusIcon[0]/Vector[0]·strokeWeight=0.9000000357627869`, `/statusIcon[0]/Vector[0]·width=6.48` |
| first-party | `ds.field` | 11/19 (58%) | 8 | `/labelRow[0]/requiredMark[1]/Vector[0]·height=5.5`, `/labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[1]·height=2.8`, `/labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[1]·width=4.8`, `/labelRow[0]/requiredMark[1]/Vector[2]·height=2.8`, `/labelRow[0]/requiredMark[1]/Vector[2]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[2]·width=4.8` |
| first-party | `ds.grid-gallery` | 1/2 (50%) | 1 | `/GridGallery·itemSpacing=0` |
| first-party | `ds.page-shell` | 2/3 (67%) | 1 | `/PageShell·itemSpacing=0` |
| first-party | `ds.pagination` | 46/52 (88%) | 6 | `/nextButton[2]/Vector[0]·height=6.3`, `/nextButton[2]/Vector[0]·strokeWeight=1.0499999523162842`, `/nextButton[2]/Vector[0]·width=3.15`, `/prevButton[0]/Vector[0]·height=6.3`, `/prevButton[0]/Vector[0]·strokeWeight=1.0499999523162842`, `/prevButton[0]/Vector[0]·width=3.15` |
| first-party | `ds.progress-bar` | 14/15 (93%) | 1 | `/track[0]/fill[0]·width=192` |
| first-party | `ds.sidebar-layout` | 1/2 (50%) | 1 | `/SidebarLayout·itemSpacing=0` |
| first-party | `ds.text-area` | 23/31 (74%) | 8 | `/labelRow[0]/requiredMark[1]/Vector[0]·height=5.5`, `/labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[1]·height=2.8`, `/labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[1]·width=4.8`, `/labelRow[0]/requiredMark[1]/Vector[2]·height=2.8`, `/labelRow[0]/requiredMark[1]/Vector[2]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[2]·width=4.8` |
| first-party | `ds.text-field` | 23/31 (74%) | 8 | `/labelRow[0]/requiredMark[1]/Vector[0]·height=5.5`, `/labelRow[0]/requiredMark[1]/Vector[0]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[1]·height=2.8`, `/labelRow[0]/requiredMark[1]/Vector[1]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[1]·width=4.8`, `/labelRow[0]/requiredMark[1]/Vector[2]·height=2.8`, `/labelRow[0]/requiredMark[1]/Vector[2]·strokeWeight=0.75`, `/labelRow[0]/requiredMark[1]/Vector[2]·width=4.8` |
| first-party | `ds.top-nav` | 12/13 (92%) | 1 | `/StartContent[1]·width=605` |
| first-party | `ds.two-column` | 1/2 (50%) | 1 | `/TwoColumn·itemSpacing=0` |
| altitude | `altitude.iconclose` | 3/5 (60%) | 2 | `/root-xs[0]/Vector[0]·height=3`, `/root-xs[0]/Vector[0]·width=3` |
| antd | `antd.alert` | 20/22 (91%) | 2 | `/alert-close-icon[1]/anticon[0]/Vector[0]·height=9.67`, `/alert-close-icon[1]/anticon[0]/Vector[0]·width=9.67` |
| antd | `antd.button` | 18/22 (82%) | 4 | `/btn-icon-2[1]/anticon-2[0]/Vector[0]·height=7.49`, `/btn-icon-2[1]/anticon-2[0]/Vector[0]·width=7.49`, `/btn-icon[0]/anticon[0]/Vector[0]·height=12.31`, `/btn-icon[0]/anticon[0]/Vector[0]·width=12.31` |
| antd | `antd.progress` | 15/16 (94%) | 1 | `/progress-outer[0]/progress-inner[0]/progress-bg[0]·width=101` |
| antd | `antd.switch` | 30/31 (97%) | 1 | `/switch-inner[0]/switch-inner-unchecked (margin box)[1]·height=0.01` |
| antd | `antd.tag` | 14/17 (82%) | 3 | `/anticon[1]/Vector[0]·height=8.06`, `/anticon[1]/Vector[0]·width=15.93`, `/anticon[1]·width=19.77` |
| astryx | `astryx.text-input` | 21/22 (95%) | 1 | `/field[1]/start-icon[0]/icon-text[0]/Vector[0]·width=10.67` |
| carbon | `carbon.accordion` | 18/19 (95%) | 1 | `/accordion__item[0]/accordion__heading[0]/icon[0]/Vector[0]·width=5.7` |
| carbon | `carbon.iconbutton` | 12/14 (86%) | 2 | `/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·height=10.67`, `/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·width=10.67` |
| carbon | `carbon.inlinenotification` | 26/28 (93%) | 2 | `/inline-notification__close-button[1]/inline-notification__close-button-icon[0]/Vector[0]·height=10.67`, `/inline-notification__close-button[1]/inline-notification__close-button-icon[0]/Vector[0]·width=10.67` |
| carbon | `carbon.modal` | 59/62 (95%) | 3 | `/modal-container[0]/modal-header (margin box)[0]/modal-header[0]/modal-close-button[2]/icon-tooltip[0]/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·height=13.33`, `/modal-container[0]/modal-header (margin box)[0]/modal-header[0]/modal-close-button[2]/icon-tooltip[0]/tooltip-trigger__wrapper[0]/btn[0]/btn-icon[0]/Vector[0]·width=13.33`, `/modal-container[0]/modal-header (margin box)[0]·height=71.98` |
| fluent | `fluent.checkbox` | 20/21 (95%) | 1 | `/label (margin box)[1]·width=79` |
| fluent | `fluent.dialog` | 40/43 (93%) | 3 | `/dialogsurface (margin box)[1]/dialogsurface[0]/dialogbody[0]/label-2 (margin box)[1]·width=272`, `/dialogsurface (margin box)[1]·height=705`, `/dialogsurface (margin box)[1]·width=1038` |
| mui | `mui.autocomplete` | 86/92 (93%) | 6 | `/formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]/autocomplete-tag[0]/icon[1]/Vector[0]·height=14.55`, `/formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]/autocomplete-tag[0]/icon[1]/Vector[0]·width=14.55`, `/formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag (margin box)[0]·width=83.3`, `/formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag-2 (margin box)[1]/autocomplete-tag-2[0]/icon-2[1]/Vector[0]·height=14.55`, `/formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag-2 (margin box)[1]/autocomplete-tag-2[0]/icon-2[1]/Vector[0]·width=14.55`, `/formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-tag-2 (margin box)[1]·width=76.97` |
| mui | `mui.circular-progress` | 3/6 (50%) | 3 | `/root-indeterminate[0]/Vector[0]·height=36.73`, `/root-indeterminate[0]/Vector[0]·strokeWeight=3.5999999046325684`, `/root-indeterminate[0]/Vector[0]·width=36.73` |
| mui | `mui.dialog` | 13/15 (87%) | 2 | `/dialog-container[1]/dialog-paper (margin box)[0]·height=123`, `/dialog-container[1]/dialog-paper (margin box)[0]·width=424` |
| mui | `mui.table-pagination` | 67/73 (92%) | 6 | `/tablepagination-toolbar[0]/label (margin box)[1]/label[0]·width=98`, `/tablepagination-toolbar[0]/label (margin box)[1]·width=98`, `/tablepagination-toolbar[0]/label-3 (margin box)[3]/label-3[0]·width=54`, `/tablepagination-toolbar[0]/label-3 (margin box)[3]·width=54`, `/tablepagination-toolbar[0]/tablepagination-actions (margin box)[4]/tablepagination-actions[0]/buttonbase-root-2[1]/buttonbase-root-2-icon[0]/Vector[0]·width=7.41`, `/tablepagination-toolbar[0]/tablepagination-actions (margin box)[4]/tablepagination-actions[0]/buttonbase-root[0]/buttonbase-root-icon[0]/Vector[0]·width=7.41` |
| mui | `mui.table` | 170/172 (99%) | 2 | `/tablehead-root[0]/tablerow-head[0]/tablecell-head-2[1]/label[0]/icon-2[1]/Vector[0]·height=14.4`, `/tablehead-root[0]/tablerow-head[0]/tablecell-head-2[1]/label[0]/icon-2[1]/Vector[0]·width=14.4` |
| mui | `mui.text-field` | 35/37 (95%) | 2 | `/label-5 (margin box)[1]/label-5[0]·width=110`, `/label-5 (margin box)[1]·width=138` |
| polaris | `polaris.banner` | 88/91 (97%) | 3 | `/box[0]/blockstack[0]/box-2[0]/inlinestack[0]/button (margin box)[1]/button[0]/button__icon (margin box)[0]/button__icon[0]/part-0-0-0-0-1-0-0[0]/icon-3[0]/Vector[0]·height=10.5`, `/box[0]/blockstack[0]/box-2[0]/inlinestack[0]/button (margin box)[1]/button[0]/button__icon (margin box)[0]/button__icon[0]/part-0-0-0-0-1-0-0[0]/icon-3[0]/Vector[0]·width=10.54`, `/box[0]/blockstack[0]/box-3[1]/blockstack-2[0]/buttongroup (margin box)[1]/buttongroup[0]/buttongroup__item (margin box)[0]·width=90.08` |
| polaris | `polaris.progress-bar` | 9/10 (90%) | 1 | `/indicator[0]·width=115` |
| polaris | `polaris.spinner` | 4/6 (67%) | 2 | `/root-large[0]/Vector[0]·height=42.08`, `/root-large[0]/Vector[0]·width=42.36` |
| polaris | `polaris.tag` | 31/33 (94%) | 2 | `/Size=none, State=Default·width=71.92`, `/button-2 (margin box)[2]/button-2[0]/icon-3[0]/Vector[0]·width=8.04` |
| polaris | `polaris.text-field` | 31/34 (91%) | 3 | `/connected[1]/connected__item[0]/textfield[0]/backdrop[0]·width=210.81`, `/connected[1]/connected__item[0]/textfield[0]/prefix (margin box)[1]·width=26.41`, `/connected[1]/connected__item[0]/textfield[0]/suffix (margin box)[3]·width=43.41` |
| shadcn | `shadcn.select` | 16/18 (89%) | 2 | `/icon[1]/Vector[0]·height=5.33`, `/icon[1]/Vector[0]·width=10.67` |
| tailwind | `flowbite.alert` | 33/34 (97%) | 1 | `/part-0[0]/dismiss[2]/dismiss-icon[0]/Vector[0]·width=12.01` |

## Rows

| library | id | set | reflow | variant switching | token binding | bound ratio | no fake layout | canvas restored |
|---|---|---|---|---|---|---|---|---|
| first-party | `ds.accordion-item` | AccordionItem | pass — 1/1 re-laid (trigger ⤢40,0) | pass — State 2/2 distinct | **FAIL** — 17/20 bound — 3 literal | 17/20 (85%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.avatar-group` | AvatarGroup | pass — 2/2 re-laid (Children +0,20; overflowText +0,20) | n/a — no axes on canvas (single-variant component) | pass — 34/34 bound (34 bound, 0 inferred) | 34/34 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.avatar` | Avatar (ds.avatar) | pass — 1/1 re-laid (label +20,20) | pass — Size 2/2 distinct | pass — 8/8 bound (6 bound, 2 inferred) | 8/8 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.badge` | Badge (ds.badge) | pass — 1/1 re-laid (label +20,20) | **FAIL** — Variant 4/5 distinct — DEAD Danger≡Error | pass — 10/10 bound (10 bound, 0 inferred) | 10/10 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.banner` | Banner (ds.banner) | **FAIL** — 2/3 re-laid (body ⤢40,0; endArea +40,0); frozen: statusIcon | pass — Status 4/4 distinct; Container 2/2 distinct | **FAIL** — 35/38 bound — 3 literal | 35/38 (92%) | **FAIL** — 2 offender(s) | yes (061165feb6984ac4) |
| first-party | `ds.bento-grid` | BentoGrid | pass — 5/5 re-laid (Header ⤢40,0; Sidebar ⤢0,40; Main ⤢40,13.33; Rail +40,0 ⤢0,40; Footer +0,13.33 ⤢40,26.67) | n/a — no axes on canvas (single-variant component) | **FAIL** — 2/3 bound — 1 literal | 2/3 (67%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.blockquote` | Blockquote | **FAIL** — FROZEN 0/2 (Children, Cite) | n/a — no axes on canvas (single-variant component) | pass — 6/6 bound (6 bound, 0 inferred) | 6/6 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.breadcrumb-item` | BreadcrumbItem | pass — 2/2 re-laid (separatorIcon +0,20; link +0,20) | n/a — no axes on canvas (single-variant component) | **FAIL** — 5/8 bound — 3 literal | 5/8 (63%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.breadcrumbs` | Breadcrumbs (ds.breadcrumbs) | pass — 1/1 re-laid (Children +0,20) | n/a — no axes on canvas (single-variant component) | **FAIL** — 17/26 bound — 9 literal | 17/26 (65%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.button` | Button (ds.button) | **FAIL** — 1/2 re-laid (label +20,20); frozen: loadingSpinner | **FAIL** — Variant 4/4 distinct; Size 3/3 distinct; State 3/4 distinct — DEAD Default≡Disabled | **FAIL** — 15/17 bound — 2 literal | 15/17 (88%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.card` | Card (ds.card) | **FAIL** — 2/3 re-laid (header ⤢40,0; Body ⤢40,0); frozen: Actions | n/a — no axes on canvas (single-variant component) | **FAIL** — 33/34 bound — 1 literal | 33/34 (97%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.chat-message-metadata` | ChatMessageMetadata | **FAIL** — 2/3 re-laid (statusIcon +0,20; timestampText +0,20); frozen: Footer | **FAIL** — Status 4/5 distinct — DEAD Delivered≡Read | **FAIL** — 6/9 bound — 3 literal | 6/9 (67%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.chat-message` | ChatMessage | **FAIL** — 1/2 re-laid (body ⤢40,0); frozen: Avatar | pass — Sender 3/3 distinct | pass — 15/15 bound (13 bound, 2 inferred) | 15/15 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.chat-system-message` | ChatSystemMessage | **FAIL** — 1/2 re-laid (messageText +20,20); frozen: Icon | pass — Variant 2/2 distinct | pass — 3/3 bound (2 bound, 1 inferred) | 3/3 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.checkbox` | Checkbox (ds.checkbox) | **FAIL** — 1/2 re-laid (textCol ⤢40,0); frozen: box | pass — Value 3/3 distinct; Size 2/2 distinct | pass — 13/13 bound (12 bound, 1 inferred) | 13/13 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.citation` | Citation | pass — 1/1 re-laid (labelText +0,20) | pass — Variant 2/2 distinct | pass — 10/10 bound (10 bound, 0 inferred) | 10/10 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.code` | Code | pass — 1/1 re-laid (label +20,20) | n/a — no axes on canvas (single-variant component) | pass — 10/10 bound (10 bound, 0 inferred) | 10/10 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.divider` | Divider (ds.divider) | n/a — HORIZONTAL, no children to re-lay | pass — Variant 2/2 distinct | pass — 3/3 bound (1 bound, 2 inferred) | 3/3 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.empty-state` | EmptyState | **FAIL** — 2/4 re-laid (title +20,0; descriptionText +20,0); frozen: Actions, Icon | n/a — no axes on canvas (single-variant component) | pass — 11/11 bound (10 bound, 1 inferred) | 11/11 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.field` | Field | **FAIL** — 2/3 re-laid (labelRow ⤢40,0; Children ⤢40,0); frozen: descriptionText | n/a — no axes on canvas (single-variant component) | **FAIL** — 11/19 bound — 8 literal | 11/19 (58%) | **FAIL** — 1 offender(s) | yes (061165feb6984ac4) |
| first-party | `ds.grid-gallery` | GridGallery | pass — 6/6 re-laid (Item 1 ⤢13.33,0; Item 2 +13.33,0 ⤢13.33,0; Item 3 +26.67,0 ⤢13.33,0; Item 4 +0,20 ⤢13.33,0; Item 5 +13.33,20 ⤢13.33,0; Item 6 +26.67,20 ⤢13.33,0) | n/a — no axes on canvas (single-variant component) | **FAIL** — 1/2 bound — 1 literal | 1/2 (50%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.heading` | Heading (ds.heading) | pass — 1/1 re-laid (label +0,20) | **FAIL** — Level 1/6 distinct — DEAD H2≡H1, H2≡H3, H2≡H4, H2≡H5, H2≡H6; Size 3/3 distinct | pass — 1/1 bound (1 bound, 0 inferred) | 1/1 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.icon-button` | IconButton (ds.icon-button) | pass — 1/1 re-laid (Icon +20,20) | pass — Variant 4/4 distinct; Size 3/3 distinct | pass — 7/7 bound (5 bound, 2 inferred) | 7/7 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.kbd` | Kbd (ds.kbd) | pass — 1/1 re-laid (keysText +20,20) | n/a — no axes on canvas (single-variant component) | pass — 12/12 bound (11 bound, 1 inferred) | 12/12 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.list-item` | ListItem | **FAIL** — 1/3 re-laid (textCol +0,20); frozen: EndContent, StartContent | n/a — no axes on canvas (single-variant component) | pass — 8/8 bound (8 bound, 0 inferred) | 8/8 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.list` | List | pass — 1/1 re-laid (Children ⤢40,0) | pass — Density 3/3 distinct | pass — 38/38 bound (36 bound, 2 inferred) | 38/38 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.metadata-list-item` | MetadataListItem | **FAIL** — FROZEN 0/3 (Icon, labelText, valueText) | n/a — no axes on canvas (single-variant component) | pass — 4/4 bound (4 bound, 0 inferred) | 4/4 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.metadata-list` | MetadataList | **FAIL** — 1/2 re-laid (Children ⤢40,0); frozen: titleText | n/a — no axes on canvas (single-variant component) | pass — 16/16 bound (15 bound, 1 inferred) | 16/16 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.page-shell` | PageShell | pass — 4/4 re-laid (Header ⤢40,0; Aside ⤢0,40; Main ⤢40,40; Footer +0,40 ⤢40,0) | n/a — no axes on canvas (single-variant component) | **FAIL** — 2/3 bound — 1 literal | 2/3 (67%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.pagination` | Pagination | pass — 3/3 re-laid (prevButton +0,20; pagesGroup +0,20; nextButton +0,20) | pass — Variant 3/3 distinct | **FAIL** — 46/52 bound — 6 literal | 46/52 (88%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.progress-bar` | ProgressBar (ds.progress-bar) | **FAIL** — FROZEN 0/1 (track) | pass — Variant 5/5 distinct | **FAIL** — 14/15 bound — 1 literal | 14/15 (93%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.section` | Section | pass — 1/1 re-laid (Children ⤢40,0) | pass — Variant 2/2 distinct | pass — 14/14 bound (12 bound, 2 inferred) | 14/14 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.side-nav-item` | SideNavItem | **FAIL** — 1/3 re-laid (labelText +0,20); frozen: EndContent, Icon | pass — State 2/2 distinct | pass — 12/12 bound (11 bound, 1 inferred) | 12/12 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.sidebar-layout` | SidebarLayout | **FAIL** — 1/2 re-laid (Main ⤢40,0); frozen: Sidebar | n/a — no axes on canvas (single-variant component) | **FAIL** — 1/2 bound — 1 literal | 1/2 (50%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.skeleton` | Skeleton | **FAIL** — FROZEN 0/1 (block) | pass — Variant 3/3 distinct | pass — 7/7 bound (7 bound, 0 inferred) | 7/7 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.slider` | Slider (ds.slider) | **FAIL** — FROZEN 0/2 (labelText, trackRow) | n/a — no axes on canvas (single-variant component) | pass — 27/27 bound (23 bound, 4 inferred) | 27/27 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.spinner` | Spinner (ds.spinner) | pass — 1/1 re-laid (arc +0,20) | n/a — no axes on canvas (single-variant component) | pass — 6/6 bound (1 bound, 5 inferred) | 6/6 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.status-dot` | StatusDot | n/a — HORIZONTAL, no children to re-lay | pass — Variant 5/5 distinct | pass — 7/7 bound (5 bound, 2 inferred) | 7/7 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.switch` | Switch (ds.switch) | **FAIL** — 1/2 re-laid (textCol ⤢40,0); frozen: track | pass — Value 2/2 distinct | pass — 23/23 bound (22 bound, 1 inferred) | 23/23 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.tab-list` | TabList (ds.tab-list) | pass — 1/1 re-laid (Children +0,20) | n/a — no axes on canvas (single-variant component) | pass — 39/39 bound (38 bound, 1 inferred) | 39/39 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.tab` | Tab | **FAIL** — 1/3 re-laid (labelText +0,20); frozen: EndContent, Icon | pass — State 2/2 distinct | pass — 11/11 bound (11 bound, 0 inferred) | 11/11 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.table-cell` | TableCell | pass — 1/1 re-laid (label +0,20) | pass — Density 2/2 distinct | pass — 6/6 bound (6 bound, 0 inferred) | 6/6 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.table-header-cell` | TableHeaderCell | pass — 1/1 re-laid (label +0,20) | pass — Density 2/2 distinct | pass — 6/6 bound (6 bound, 0 inferred) | 6/6 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.table-row` | TableRow | pass — 1/1 re-laid (Cells +0,20) | pass — State 2/2 distinct | pass — 20/20 bound (19 bound, 1 inferred) | 20/20 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.table` | Table (ds.table) | pass — 2/2 re-laid (header ⤢40,0; Rows ⤢40,0) | pass — Density 2/2 distinct | pass — 90/90 bound (82 bound, 8 inferred) | 90/90 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.text-area` | TextArea | **FAIL** — 2/3 re-laid (labelRow ⤢40,0; input ⤢40,0); frozen: descriptionText | pass — Size 3/3 distinct | **FAIL** — 23/31 bound — 8 literal | 23/31 (74%) | **FAIL** — 1 offender(s) | yes (061165feb6984ac4) |
| first-party | `ds.text-field` | TextField (ds.text-field) | **FAIL** — 2/3 re-laid (labelRow ⤢40,0; input ⤢40,0); frozen: descriptionText | pass — Size 3/3 distinct | **FAIL** — 23/31 bound — 8 literal | 23/31 (74%) | **FAIL** — 1 offender(s) | yes (061165feb6984ac4) |
| first-party | `ds.toast` | Toast (ds.toast) | **FAIL** — 1/2 re-laid (Children +0,20 ⤢40,0); frozen: EndContent | pass — Type 2/2 distinct | pass — 11/11 bound (10 bound, 1 inferred) | 11/11 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.token` | Token (ds.token) | **FAIL** — 1/3 re-laid (labelText +0,20); frozen: EndContent, Icon | pass — Color 11/11 distinct; Size 3/3 distinct | pass — 11/11 bound (11 bound, 0 inferred) | 11/11 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.toolbar` | Toolbar | **FAIL** — FROZEN 0/3 (CenterContent, EndContent, StartContent) | pass — Size 3/3 distinct | pass — 17/17 bound (15 bound, 2 inferred) | 17/17 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.top-nav-item` | TopNavItem | **FAIL** — 1/2 re-laid (labelText +0,20); frozen: Icon | pass — State 2/2 distinct | pass — 11/11 bound (11 bound, 0 inferred) | 11/11 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.top-nav` | TopNav | **FAIL** — FROZEN 0/3 (EndContent, Heading, StartContent) | n/a — no axes on canvas (single-variant component) | **FAIL** — 12/13 bound — 1 literal | 12/13 (92%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.two-column` | TwoColumn | pass — 2/2 re-laid (Start ⤢20,0; End +20,0 ⤢20,0) | n/a — no axes on canvas (single-variant component) | **FAIL** — 1/2 bound — 1 literal | 1/2 (50%) | pass — every node in flow | yes (061165feb6984ac4) |
| first-party | `ds.typeahead-item` | TypeaheadItem | **FAIL** — 1/2 re-laid (textCol +0,20 ⤢40,0); frozen: Icon | n/a — no axes on canvas (single-variant component) | pass — 14/14 bound (13 bound, 1 inferred) | 14/14 (100%) | pass — every node in flow | yes (061165feb6984ac4) |
| altitude | `altitude.avatar` | Avatar (altitude.avatar) | **FAIL** — 1/2 re-laid (label +20,20); frozen: avatar__badge | n/a — no axes on canvas — SINGLE-VALUE-AXIS-NOT-MINTED:Variant=sm | pass — 25/25 bound (17 bound, 8 inferred) | 25/25 (100%) | pass — 2 genuine out-of-flow: /avatar__badge[1]/badge[0] (source position:absolute), /avatar__badge[1]/badge[0]/badge-after[0] (source position:absolute) | yes (a5bbd7223f0db150) |
| altitude | `altitude.badge` | Badge (altitude.badge) | pass — 1/1 re-laid (label +20,20) | pass — Variant 4/4 distinct; Dot 2/2 distinct | pass — 11/11 bound (11 bound, 0 inferred) | 11/11 (100%) | pass — every node in flow | yes (a5bbd7223f0db150) |
| altitude | `altitude.button` | Button (altitude.button) | pass — 1/1 re-laid (label +20,20) | pass — Variant 4/4 distinct; State 3/3 distinct | pass — 13/13 bound (12 bound, 1 inferred) | 13/13 (100%) | pass — every node in flow | yes (a5bbd7223f0db150) |
| altitude | `altitude.chip` | Chip | pass — 1/1 re-laid (label +20,20) | **FAIL** — SET HAS ERRORS — no axes readable | pass — 13/13 bound (12 bound, 1 inferred) | 13/13 (100%) | pass — every node in flow | yes (a5bbd7223f0db150) |
| altitude | `altitude.divider` | Divider | n/a — VERTICAL, no children to re-lay | pass — Variant 2/2 distinct | pass — 3/3 bound (1 bound, 2 inferred) | 3/3 (100%) | pass — every node in flow | yes (a5bbd7223f0db150) |
| altitude | `altitude.heading` | Heading | **FAIL** — FROZEN 0/1 (label) | pass — Variant 6/6 distinct; Weight 2/2 distinct | pass — 2/2 bound (0 bound, 2 inferred) | 2/2 (100%) | pass — every node in flow | yes (a5bbd7223f0db150) |
| altitude | `altitude.iconclose` | IconClose | pass — 1/1 re-laid (root-xs ⤢40,0) | pass — Size 7/7 distinct | **FAIL** — 3/5 bound — 2 literal | 3/5 (60%) | pass — every node in flow | yes (a5bbd7223f0db150) |
| altitude | `altitude.link` | Link | pass — 1/1 re-laid (label +0,20) | **FAIL** — Variant 3/3 distinct; State 2/3 distinct — DEAD Default≡Focus Visible | pass — 2/2 bound (2 bound, 0 inferred) | 2/2 (100%) | pass — every node in flow | yes (a5bbd7223f0db150) |
| antd | `antd.alert` | Alert | **FAIL** — 1/2 re-laid (alert-content ⤢40,0); frozen: alert-close-icon | pass — Type 4/4 distinct; Show Icon 2/2 distinct | **FAIL** — 20/22 bound — 2 literal | 20/22 (91%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.avatar` | Avatar | pass — 1/1 re-laid (label +20,20) | pass — Size 3/3 distinct; Shape 2/2 distinct | pass — 10/10 bound (7 bound, 3 inferred) | 10/10 (100%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.badge` | Badge | **FAIL** — FROZEN 0/2 (avatar, badge-count) | pass — Mode 2/2 distinct; Color 3/3 distinct | pass — 26/26 bound (22 bound, 4 inferred) | 26/26 (100%) | pass — 1 genuine out-of-flow: /badge-count[1] (source position:absolute) | yes (0be3843bf548afa0) |
| antd | `antd.button` | Button | **FAIL** — 1/3 re-laid (label +20,20); frozen: btn-icon, btn-icon-2 | **FAIL** — Type 4/5 distinct — DEAD Default≡Dashed; Size 3/3 distinct; Danger 2/2 distinct | **FAIL** — 18/22 bound — 4 literal | 18/22 (82%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.card` | Card | pass — 2/2 re-laid (card-head ⤢40,0; label-2 ⤢40,0) | pass — Size 2/2 distinct; Variant 2/2 distinct | pass — 23/23 bound (20 bound, 3 inferred) | 23/23 (100%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.checkbox` | Checkbox | **FAIL** — FROZEN 0/2 (checkbox, label) | pass — Checked 3/3 distinct | pass — 21/21 bound (17 bound, 4 inferred) | 21/21 (100%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.input` | Input | pass — 1/1 re-laid (label +0,20) | **FAIL** — Size 3/3 distinct; Status 2/2 distinct; Variant 4/4 distinct; State 3/5 distinct — DEAD Focus Visible≡Hover, Focus Visible≡Active | pass — 13/13 bound (11 bound, 2 inferred) | 13/13 (100%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.progress` | Progress | pass — 1/1 re-laid (progress-outer +0,20 ⤢40,0) | **FAIL** — Status 3/4 distinct — DEAD Unset≡Active | **FAIL** — 15/16 bound — 1 literal | 15/16 (94%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.radio` | Radio | **FAIL** — FROZEN 0/2 (label, radio) | pass — Checked 2/2 distinct | pass — 21/21 bound (17 bound, 4 inferred) | 21/21 (100%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.switch` | Switch | **FAIL** — FROZEN 0/2 (switch-handle, switch-inner) | **FAIL** — Size 2/2 distinct; Checked 2/2 distinct; State 3/5 distinct — DEAD Default≡Disabled, Active≡Hover | **FAIL** — 30/31 bound — 1 literal | 30/31 (97%) | pass — 2 genuine out-of-flow: /switch-handle[1] (source position:absolute), /switch-handle[1]/switch-handle-before[0] (source position:absolute) | yes (0be3843bf548afa0) |
| antd | `antd.tag` | Tag | **FAIL** — FROZEN 0/2 (anticon, label) | pass — Color 7/7 distinct; Bordered 2/2 distinct | **FAIL** — 14/17 bound — 3 literal | 14/17 (82%) | pass — every node in flow | yes (0be3843bf548afa0) |
| antd | `antd.tooltip` | Tooltip | **FAIL** — FROZEN 0/2 (tooltip-arrow, tooltip-content) | n/a — no axes on canvas (single-variant component) | pass — 22/22 bound (17 bound, 5 inferred) | 22/22 (100%) | pass — 2 genuine out-of-flow: /tooltip-arrow[1] (source position:absolute), /tooltip-arrow[1]/tooltip-arrow-before[0] (source position:absolute) | yes (0be3843bf548afa0) |
| astryx | `astryx.badge` | Badge (astryx.badge) | pass — 1/1 re-laid (label +20,20) | pass — Variant 14/14 distinct | pass — 9/9 bound (9 bound, 0 inferred) | 9/9 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.banner` | Banner | **FAIL** — FROZEN 0/1 (header) | pass — Status 4/4 distinct; Container 2/2 distinct | pass — 18/18 bound (9 bound, 9 inferred) | 18/18 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.button` | Button (astryx.button) | pass — 1/1 re-laid (label +20,20) | pass — Variant 4/4 distinct; Size 3/3 distinct | pass — 13/13 bound (11 bound, 2 inferred) | 13/13 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.card` | Card (astryx.card) | **FAIL** — FROZEN 0/1 (label) | pass — Variant 13/13 distinct | pass — 13/13 bound (11 bound, 2 inferred) | 13/13 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.checkbox-input` | CheckboxInput | pass — 2/2 re-laid (control +0,20; label +0,20) | **FAIL** — Size 1/2 distinct — DEAD Md≡Sm | pass — 11/11 bound (10 bound, 1 inferred) | 11/11 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.dropdown-menu-item` | DropdownMenuItem | pass — 1/1 re-laid (label +0,20) | n/a — no axes on canvas (single-variant component) | pass — 9/9 bound (9 bound, 0 inferred) | 9/9 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.dropdown-menu` | DropdownMenu | **FAIL** — FROZEN 0/2 (menu, trigger) | n/a — no axes on canvas (single-variant component) | pass — 52/52 bound (48 bound, 4 inferred) | 52/52 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.progress-bar` | ProgressBar | **FAIL** — FROZEN 0/2 (label, track) | pass — Variant 5/5 distinct | pass — 17/17 bound (14 bound, 3 inferred) | 17/17 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.slider` | Slider | **FAIL** — 1/2 re-laid (slider ⤢40,0); frozen: label | pass — Orientation 2/2 distinct; Value Display 3/3 distinct | pass — 41/41 bound (31 bound, 10 inferred) | 41/41 (100%) | pass — 4 genuine out-of-flow: /slider[1]/part-1-0[0]/slider-track[0] (source position:absolute), /slider[1]/part-1-0[0]/part-1-0-1[1] (source position:absolute), /slider[1]/part-1-0[0]/slider-thumb-2[2] (source position:absolute), /slider[1]/part-1-0[0]/tooltip[3] (source position:absolute) | yes (8f17151526c142bc) |
| astryx | `astryx.switch` | Switch (astryx.switch) | **FAIL** — FROZEN 0/1 (part-0) | pass — Value 2/2 distinct; Label Position 2/2 distinct | pass — 25/25 bound (20 bound, 5 inferred) | 25/25 (100%) | pass — 1 genuine out-of-flow: /part-0[0]/part-0-0[0]/switch[0]/switch-thumb[0] (source position:absolute) | yes (8f17151526c142bc) |
| astryx | `astryx.text-input` | TextInput (astryx.text-input) | **FAIL** — 1/2 re-laid (field ⤢40,0); frozen: label | pass — Type 3/3 distinct; Size 3/3 distinct | **FAIL** — 21/22 bound — 1 literal | 21/22 (95%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.toast` | Toast | **FAIL** — 2/3 re-laid (content ⤢40,0; dismiss +40,0); frozen: EndContent | n/a — no axes on canvas (single-variant component) | pass — 25/25 bound (23 bound, 2 inferred) | 25/25 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| astryx | `astryx.token` | Token | pass — 1/1 re-laid (label +20,20) | pass — Size 3/3 distinct; Color 11/11 distinct | pass — 11/11 bound (11 bound, 0 inferred) | 11/11 (100%) | pass — every node in flow | yes (8f17151526c142bc) |
| carbon | `carbon.accordion` | Accordion | pass — 1/1 re-laid (accordion__item ⤢40,0) | pass — Align 2/2 distinct; Size 3/3 distinct; State 2/2 distinct | **FAIL** — 18/19 bound — 1 literal | 18/19 (95%) | pass — every node in flow | yes (7d6ced592fbfa72c) |
| carbon | `carbon.button` | Button (carbon.button) | **FAIL** — FROZEN 0/1 (label) | **FAIL** — Kind 8/8 distinct; Size 6/6 distinct; State 4/5 distinct — DEAD Default≡Focus Visible | pass — 10/10 bound (9 bound, 1 inferred) | 10/10 (100%) | pass — every node in flow | yes (7d6ced592fbfa72c) |
| carbon | `carbon.checkbox` | Checkbox (carbon.checkbox) | **FAIL** — FROZEN 0/1 (checkbox-label) | pass — Checked 3/3 distinct | pass — 17/17 bound (5 bound, 12 inferred) | 17/17 (100%) | pass — 1 genuine out-of-flow: /checkbox-label[0]/checkbox-label-before[1] (source position:absolute) | yes (7d6ced592fbfa72c) |
| carbon | `carbon.iconbutton` | IconButton | **FAIL** — FROZEN 0/1 (tooltip-trigger__wrapper) | pass — Kind 4/4 distinct; Size 4/4 distinct | **FAIL** — 12/14 bound — 2 literal | 12/14 (86%) | pass — every node in flow | yes (7d6ced592fbfa72c) |
| carbon | `carbon.inlinenotification` | InlineNotification | pass — 2/2 re-laid (inline-notification__details +0,20; inline-notification__close-button +0,20) | pass — Kind 6/6 distinct; Contrast 2/2 distinct | **FAIL** — 26/28 bound — 2 literal | 26/28 (93%) | **FAIL** — 1 offender(s) | yes (7d6ced592fbfa72c) |
| carbon | `carbon.modal` | Modal | pass — 1/1 re-laid (modal-container +20,20) | pass — Size 4/4 distinct | **FAIL** — 59/62 bound — 3 literal | 59/62 (95%) | pass — 1 genuine out-of-flow: /modal-container[0]/modal-header (margin box)[0]/modal-header[0]/modal-close-button[2] (source position:absolute) | yes (7d6ced592fbfa72c) |
| carbon | `carbon.tabs` | Tabs | **FAIL** — FROZEN 0/1 (part-1) | **FAIL** — State 2/3 distinct — DEAD Active≡Hover | pass — 26/26 bound (23 bound, 3 inferred) | 26/26 (100%) | pass — every node in flow | yes (7d6ced592fbfa72c) |
| carbon | `carbon.tag` | Tag (carbon.tag) | pass — 1/1 re-laid (label +20,20) | pass — Type 12/12 distinct; Size 3/3 distinct | pass — 11/11 bound (11 bound, 0 inferred) | 11/11 (100%) | pass — every node in flow | yes (7d6ced592fbfa72c) |
| carbon | `carbon.textinput` | TextInput | **FAIL** — FROZEN 0/2 (text-input__field-outer-wrapper, text-input__label-wrapper) | pass — Size 4/4 distinct; State 2/2 distinct | pass — 21/21 bound (13 bound, 8 inferred) | 21/21 (100%) | pass — every node in flow | yes (7d6ced592fbfa72c) |
| carbon | `carbon.toggle` | Toggle | pass — 1/1 re-laid (toggle__label ⤢40,0) | pass — Toggled 2/2 distinct; State 2/2 distinct | pass — 18/18 bound (12 bound, 6 inferred) | 18/18 (100%) | pass — 1 genuine out-of-flow: /toggle__label[0]/toggle__appearance[1]/toggle__switch[0]/toggle__switch-before[0] (source position:absolute) | yes (7d6ced592fbfa72c) |
| fluent | `fluent.avatar` | Avatar (fluent.avatar) | pass — 1/1 re-laid (label ⤢40,40) | **FAIL** — Shape 2/2 distinct; Active 1/3 distinct — DEAD None≡Active, None≡Inactive; Size 3/3 distinct | pass — 16/16 bound (11 bound, 5 inferred) | 16/16 (100%) | pass — 1 genuine out-of-flow: /label[0] (source position:absolute) | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.badge` | Badge (fluent.badge) | pass — 1/1 re-laid (label +20,20) | **FAIL** — Appearance 3/4 distinct — DEAD Ghost≡Outline; Color 8/8 distinct; Size 6/6 distinct | pass — 10/10 bound (8 bound, 2 inferred) | 10/10 (100%) | pass — every node in flow | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.button` | Button (fluent.button) | pass — 1/1 re-laid (label +20,20) | **FAIL** — Appearance 4/5 distinct — DEAD Subtle≡Transparent; Size 3/3 distinct; Shape 3/3 distinct; State 5/5 distinct | pass — 13/13 bound (12 bound, 1 inferred) | 13/13 (100%) | pass — every node in flow | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.card` | Card (fluent.card) | **FAIL** — FROZEN 0/2 (cardheader, label (margin box)) | **FAIL** — Appearance 3/4 distinct — DEAD Outline≡Subtle; Size 3/3 distinct; Orientation 2/2 distinct; State 4/5 distinct — DEAD Default≡Focus Visible | pass — 22/22 bound (12 bound, 10 inferred) | 22/22 (100%) | pass — every node in flow | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.checkbox` | Checkbox (fluent.checkbox) | **FAIL** — FROZEN 0/2 (indicator (margin box), label (margin box)) | **FAIL** — Checked 3/3 distinct; Size 2/2 distinct; Shape 2/2 distinct; State 1/5 distinct — DEAD Default≡Disabled, Default≡Focus Visible, Default≡Active, Default≡Hover | **FAIL** — 20/21 bound — 1 literal | 20/21 (95%) | pass — every node in flow | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.dialog` | Dialog | **FAIL** — FROZEN 0/2 (dialogsurface (margin box), dialogsurface__backdrop) | **FAIL** — Modal Type 2/3 distinct — DEAD Modal≡Alert | **FAIL** — 40/43 bound — 3 literal | 40/43 (93%) | pass — every node in flow | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.input` | Input (fluent.input) | pass — 1/1 re-laid (input +0,20) | **FAIL** — Appearance 4/6 distinct — DEAD Filled Darker≡Filled Darker Shadow, Filled Lighter≡Filled Lighter Shadow; Size 3/3 distinct; State 3/5 distinct — DEAD Active≡Focus Visible, Default≡Hover | pass — 15/15 bound (11 bound, 4 inferred) | 15/15 (100%) | pass — every node in flow | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.spinner` | Spinner | pass — 2/2 re-laid (spinner +20,20; label +20,20) | **FAIL** — Size 8/8 distinct; Appearance 2/2 distinct; Label Position 2/4 distinct — DEAD Above≡Below, After≡Before | pass — 9/9 bound (7 bound, 2 inferred) | 9/9 (100%) | pass — 1 genuine out-of-flow: /spinner[0]/spinnertail[0] (source position:absolute) | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.switch` | Switch (fluent.switch) | **FAIL** — FROZEN 0/2 (indicator (margin box), label) | **FAIL** — Checked 2/2 distinct; Label Position 3/3 distinct; State 2/3 distinct — DEAD Default≡Focus Visible | pass — 23/23 bound (13 bound, 10 inferred) | 23/23 (100%) | pass — every node in flow | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.tab-list` | TabList | **FAIL** — FROZEN 0/2 (tab, tab-2) | **FAIL** — Appearance 3/4 distinct — DEAD Transparent≡Subtle; Size 3/3 distinct; Orientation 1/2 distinct — DEAD Horizontal≡Vertical | pass — 31/31 bound (24 bound, 7 inferred) | 31/31 (100%) | pass — every node in flow | yes (0ac044e74ac6a8ac) |
| fluent | `fluent.tooltip` | Tooltip (fluent.tooltip) | **FAIL** — FROZEN 0/2 (label, part-0) | pass — Appearance 2/2 distinct | pass — 19/19 bound (16 bound, 3 inferred) | 19/19 (100%) | pass — 1 genuine out-of-flow: /part-0[1] (source position:absolute) | yes (0ac044e74ac6a8ac) |
| mui | `mui.accordion` | Accordion (mui.accordion) | **FAIL** — 1/2 re-laid (accordion-heading ⤢40,0); frozen: collapse-root | pass — Variant 2/2 distinct; Expanded 2/2 distinct | pass — 29/29 bound (25 bound, 4 inferred) | 29/29 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.alert` | Alert (mui.alert) | **FAIL** — FROZEN 0/2 (alert-icon, label) | pass — Severity 4/4 distinct; Variant 3/3 distinct | pass — 22/22 bound (17 bound, 5 inferred) | 22/22 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.autocomplete` | Autocomplete | **FAIL** — FROZEN 0/1 (formcontrol-fullwidth) | pass — Size 2/2 distinct | **FAIL** — 86/92 bound — 6 literal | 86/92 (93%) | pass — 2 genuine out-of-flow: /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/autocomplete-endadornment[3] (source position:absolute), /formcontrol-fullwidth[0]/autocomplete-inputroot[0]/outlinedinput-notchedoutline[4] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.avatar` | Avatar (mui.avatar) | pass — 1/1 re-laid (label +20,20) | pass — Variant 3/3 distinct | pass — 8/8 bound (6 bound, 2 inferred) | 8/8 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.badge` | Badge (mui.badge) | **FAIL** — 1/2 re-laid (label-2 ⤢40,40); frozen: label | pass — Color 7/7 distinct; Variant 2/2 distinct | pass — 16/16 bound (11 bound, 5 inferred) | 16/16 (100%) | pass — 1 genuine out-of-flow: /label-2[1] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.breadcrumbs` | Breadcrumbs | pass — 1/1 re-laid (breadcrumbs-ol ⤢40,0) | n/a — no axes on canvas (single-variant component) | pass — 7/7 bound (6 bound, 1 inferred) | 7/7 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.button` | Button (mui.button) | pass — 1/1 re-laid (label +20,20) | **FAIL** — Variant 3/3 distinct; Color 7/7 distinct; Size 3/3 distinct; State 2/5 distinct — DEAD Default≡Active, Default≡Focus Visible, Default≡Hover | pass — 13/13 bound (12 bound, 1 inferred) | 13/13 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.card` | Card (mui.card) | pass — 1/1 re-laid (label ⤢40,0) | **FAIL** — Elevation 1/4 distinct — DEAD 1≡0, 1≡3, 1≡8 | pass — 11/11 bound (10 bound, 1 inferred) | 11/11 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.checkbox` | Checkbox (mui.checkbox) | pass — 1/1 re-laid (icon +20,20) | pass — Checked 3/3 distinct | pass — 16/16 bound (11 bound, 5 inferred) | 16/16 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.chip` | Chip (mui.chip) | pass — 1/1 re-laid (label +20,20) | pass — Variant 2/2 distinct; Color 7/7 distinct; Size 2/2 distinct | pass — 10/10 bound (9 bound, 1 inferred) | 10/10 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.circular-progress` | CircularProgress | pass — 1/1 re-laid (root-indeterminate ⤢40,0) | pass — Variant 2/2 distinct | **FAIL** — 3/6 bound — 3 literal | 3/6 (50%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.dialog` | Dialog (mui.dialog) | **FAIL** — 1/2 re-laid (backdrop-root ⤢40,40); frozen: dialog-container | **FAIL** — Max width 1/5 distinct — DEAD Sm≡Xs, Sm≡Md, Sm≡Lg, Sm≡Xl | **FAIL** — 13/15 bound — 2 literal | 13/15 (87%) | **FAIL** — 1 offender(s) | yes (f8c74bd2594ab098) |
| mui | `mui.divider` | Divider (mui.divider) | n/a — VERTICAL, no children to re-lay | pass — Variant 3/3 distinct | pass — 2/2 bound (1 bound, 1 inferred) | 2/2 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.drawer` | Drawer | pass — 2/2 re-laid (backdrop-root ⤢40,21; label +0,21) | pass — Variant 2/2 distinct | pass — 6/6 bound (4 bound, 2 inferred) | 6/6 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.fab` | Fab | pass — 1/1 re-laid (label +20,20) | pass — Size 3/3 distinct; Color 3/3 distinct | pass — 8/8 bound (6 bound, 2 inferred) | 8/8 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.icon-button` | IconButton (mui.icon-button) | pass — 1/1 re-laid (label +20,20) | pass — Size 3/3 distinct; Color 3/3 distinct | pass — 10/10 bound (10 bound, 0 inferred) | 10/10 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.input-adornment` | InputAdornment | pass — 1/1 re-laid (label +0,20) | **FAIL** — Position 1/2 distinct — DEAD Start≡End | pass — 1/1 bound (1 bound, 0 inferred) | 1/1 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.linear-progress` | LinearProgress | **FAIL** — FROZEN 0/2 (linearprogress-bar, linearprogress-bar-2) | pass — Variant 2/2 distinct | pass — 10/10 bound (6 bound, 4 inferred) | 10/10 (100%) | pass — 2 genuine out-of-flow: /linearprogress-bar[0] (source position:absolute), /linearprogress-bar-2[1] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.link` | Link (mui.link) | **FAIL** — FROZEN 0/1 (label) | **FAIL** — Color 7/7 distinct; Underline 1/3 distinct — DEAD Hover≡None, Hover≡Always; State 2/4 distinct — DEAD Default≡Active, Default≡Hover | pass — 1/1 bound (1 bound, 0 inferred) | 1/1 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.menu` | Menu | **FAIL** — FROZEN 0/1 (list-padding) | n/a — no axes on canvas (single-variant component) | pass — 28/28 bound (25 bound, 3 inferred) | 28/28 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.paper` | Paper | **FAIL** — FROZEN 0/1 (label) | **FAIL** — Elevation 1/4 distinct — DEAD 1≡0, 1≡3, 1≡8; Variant 2/2 distinct | pass — 9/9 bound (7 bound, 2 inferred) | 9/9 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.radio` | Radio (mui.radio) | pass — 1/1 re-laid (part-1 +20,20) | **FAIL** — Color 1/7 distinct — DEAD Primary≡Secondary, Primary≡Error, Primary≡Info, Primary≡Success, Primary≡Warning, Primary≡Default; Checked 2/2 distinct | pass — 22/22 bound (12 bound, 10 inferred) | 22/22 (100%) | pass — 1 genuine out-of-flow: /part-1[0]/icon-2[1] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.select` | Select (mui.select) | **FAIL** — 2/3 re-laid (label +0,20; icon ⤢40,40); frozen: outlinedinput-notchedoutline | pass — Size 2/2 distinct | pass — 35/35 bound (27 bound, 8 inferred) | 35/35 (100%) | pass — 2 genuine out-of-flow: /icon[1] (source position:absolute), /outlinedinput-notchedoutline[2] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.slider` | Slider (mui.slider) | **FAIL** — FROZEN 0/3 (slider-rail, slider-thumb, slider-track) | pass — Color 6/6 distinct; Size 2/2 distinct | pass — 30/30 bound (27 bound, 3 inferred) | 30/30 (100%) | pass — 3 genuine out-of-flow: /slider-rail[0] (source position:absolute), /slider-track[1] (source position:absolute), /slider-thumb[2] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.snackbar` | Snackbar | pass — 1/1 re-laid (paper-root +0,20) | **FAIL** — Position 1/3 distinct — DEAD BottomLeft≡BottomCenter, BottomLeft≡TopRight | pass — 13/13 bound (13 bound, 0 inferred) | 13/13 (100%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.switch` | Switch (mui.switch) | **FAIL** — FROZEN 0/2 (buttonbase-root, switch-track) | **FAIL** — Color 1/7 distinct — DEAD Primary≡Secondary, Primary≡Error, Primary≡Warning, Primary≡Info, Primary≡Success, Primary≡Default; Size 2/2 distinct; Checked 2/2 distinct | pass — 33/33 bound (29 bound, 4 inferred) | 33/33 (100%) | pass — 1 genuine out-of-flow: /buttonbase-root[1] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.table-pagination` | TablePagination | **FAIL** — FROZEN 0/1 (tablepagination-toolbar) | n/a — no axes on canvas (single-variant component) | **FAIL** — 67/73 bound — 6 literal | 67/73 (92%) | pass — 1 genuine out-of-flow: /tablepagination-toolbar[0]/inputbase-root (margin box)[2]/inputbase-root[0]/icon[1] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.table` | Table | **FAIL** — FROZEN 0/2 (tablebody-root, tablehead-root) | pass — Size 2/2 distinct | **FAIL** — 170/172 bound — 2 literal | 170/172 (99%) | pass — every node in flow | yes (f8c74bd2594ab098) |
| mui | `mui.tabs` | Tabs (mui.tabs) | **FAIL** — FROZEN 0/1 (tabs-fixed) | pass — Text color 3/3 distinct; Indicator color 2/2 distinct | pass — 38/38 bound (34 bound, 4 inferred) | 38/38 (100%) | pass — 1 genuine out-of-flow: /tabs-fixed[0]/tabs-indicator[1] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.text-field` | TextField | **FAIL** — FROZEN 0/3 (inputbase-adornedend, label, label-5 (margin box)) | **FAIL** — Variant 1/3 distinct — DEAD Outlined≡Filled, Outlined≡Standard; Size 1/2 distinct — DEAD Medium≡Small | **FAIL** — 35/37 bound — 2 literal | 35/37 (95%) | pass — 1 genuine out-of-flow: /inputbase-adornedend[0]/outlinedinput-notchedoutline[3] (source position:absolute) | yes (f8c74bd2594ab098) |
| mui | `mui.tooltip` | Tooltip (mui.tooltip) | pass — 1/1 re-laid (label ⤢40,0) | n/a — no axes on canvas (single-variant component) | pass — 15/15 bound (13 bound, 2 inferred) | 15/15 (100%) | pass — 1 genuine out-of-flow: /label[0]/tooltip-arrow[1] (source position:absolute) | yes (f8c74bd2594ab098) |
| polaris | `polaris.avatar` | Avatar (polaris.avatar) | **FAIL** — FROZEN 0/1 (initials) | pass — Size 5/5 distinct | pass — 11/11 bound (8 bound, 3 inferred) | 11/11 (100%) | pass — every node in flow | yes (c09e90afe241d48c) |
| polaris | `polaris.badge` | Badge (polaris.badge) | pass — 1/1 re-laid (label +0,20) | pass — Tone 14/14 distinct; Progress 4/4 distinct | pass — 10/10 bound (10 bound, 0 inferred) | 10/10 (100%) | pass — every node in flow | yes (c09e90afe241d48c) |
| polaris | `polaris.banner` | Banner (polaris.banner) | **FAIL** — FROZEN 0/1 (box) | pass — Tone 4/4 distinct; State 2/2 distinct | **FAIL** — 88/91 bound — 3 literal | 88/91 (97%) | **FAIL** — 1 offender(s) | yes (c09e90afe241d48c) |
| polaris | `polaris.button` | Button (polaris.button) | **FAIL** — 1/2 re-laid (label +20,20); frozen: icon (margin box) | **FAIL** — Size 3/4 distinct — DEAD Medium≡Slim; Text Align 1/5 distinct — DEAD Center≡Left, Center≡Right, Center≡Start, Center≡End; Tone 3/3 distinct; Variant 4/5 distinct — DEAD Tertiary≡Monochrome Plain; State 5/5 distinct | pass — 23/23 bound (15 bound, 8 inferred) | 23/23 (100%) | **FAIL** — 1 offender(s) | yes (c09e90afe241d48c) |
| polaris | `polaris.checkbox` | Checkbox (polaris.checkbox) | pass — 2/2 re-laid (choice__control +0,20; choice__label +0,20) | pass — Checked 3/3 distinct | pass — 19/19 bound (10 bound, 9 inferred) | 19/19 (100%) | **FAIL** — 1 offender(s) | yes (c09e90afe241d48c) |
| polaris | `polaris.progress-bar` | ProgressBar (polaris.progress-bar) | **FAIL** — FROZEN 0/1 (indicator) | pass — Size 3/3 distinct; Tone 4/4 distinct | **FAIL** — 9/10 bound — 1 literal | 9/10 (90%) | pass — every node in flow | yes (c09e90afe241d48c) |
| polaris | `polaris.radio-button` | RadioButton | pass — 2/2 re-laid (choice__control +0,20; choice__label +0,20) | pass — Checked 2/2 distinct | pass — 21/21 bound (11 bound, 10 inferred) | 21/21 (100%) | **FAIL** — 1 offender(s) | yes (c09e90afe241d48c) |
| polaris | `polaris.spinner` | Spinner (polaris.spinner) | pass — 1/1 re-laid (root-large +20,20) | pass — Size 2/2 distinct | **FAIL** — 4/6 bound — 2 literal | 4/6 (67%) | pass — every node in flow | yes (c09e90afe241d48c) |
| polaris | `polaris.tag` | Tag (polaris.tag) | **FAIL** — 1/3 re-laid (text +0,20); frozen: button-2 (margin box), link | **FAIL** — Size 2/2 distinct; State 3/4 distinct — DEAD Active≡Focus Visible | **FAIL** — 31/33 bound — 2 literal | 31/33 (94%) | pass — every node in flow | yes (c09e90afe241d48c) |
| polaris | `polaris.text-field` | TextField (polaris.text-field) | **FAIL** — 1/2 re-laid (labelled__labelwrapper ⤢40,0); frozen: connected | **FAIL** — Type 1/1 distinct; Input Mode 1/1 distinct; Align 1/1 distinct; Variant 1/2 distinct — DEAD Inherit≡Borderless; Size 2/2 distinct; State 4/5 distinct — DEAD Active≡Focus Visible | **FAIL** — 31/34 bound — 3 literal | 31/34 (91%) | pass — 1 genuine out-of-flow: /connected[1]/connected__item[0]/textfield[0]/backdrop[0] (source position:absolute) | yes (c09e90afe241d48c) |
| polaris | `polaris.text` | Text | **FAIL** — FROZEN 0/1 (label) | **FAIL** — Alignment 1/1 distinct; As 1/1 distinct; Tone 5/5 distinct; Font Weight 1/1 distinct; Variant 7/11 distinct — DEAD Heading Lg≡Heading Xl, Heading Xs≡Body Sm, Heading Sm≡Body Md, Heading Md≡Body Lg | pass — 1/1 bound (1 bound, 0 inferred) | 1/1 (100%) | pass — every node in flow | yes (c09e90afe241d48c) |
| polaris | `polaris.thumbnail` | Thumbnail | **FAIL** — FROZEN 0/1 (part-0) | pass — Size 4/4 distinct | pass — 15/15 bound (11 bound, 4 inferred) | 15/15 (100%) | pass — 1 genuine out-of-flow: /part-0[0] (source position:absolute) | yes (c09e90afe241d48c) |
| shadcn | `shadcn.alert` | Alert (shadcn.alert) | **FAIL** — FROZEN 0/2 (label, label-2) | pass — Variant 2/2 distinct | pass — 12/12 bound (12 bound, 0 inferred) | 12/12 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.avatar` | Avatar (shadcn.avatar) | **FAIL** — FROZEN 0/1 (label) | pass — Size 3/3 distinct | pass — 12/12 bound (10 bound, 2 inferred) | 12/12 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.badge` | Badge (shadcn.badge) | pass — 1/1 re-laid (label +20,20) | **FAIL** — Variant 6/6 distinct; State 1/3 distinct — DEAD Default≡Active, Default≡Hover | pass — 13/13 bound (12 bound, 1 inferred) | 13/13 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.button` | Button (shadcn.button) | pass — 1/1 re-laid (label +20,20) | **FAIL** — Variant 6/6 distinct; Size 3/8 distinct — DEAD Default≡Sm, Default≡Lg, Icon≡Icon Xs, Icon≡Icon Sm, Icon≡Icon Lg; State 2/5 distinct — DEAD Default≡Disabled, Default≡Focus Visible, Active≡Hover | pass — 10/10 bound (9 bound, 1 inferred) | 10/10 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.card` | Card (shadcn.card) | **FAIL** — FROZEN 0/2 (label-3, part-0) | pass — Size 2/2 distinct | pass — 19/19 bound (17 bound, 2 inferred) | 19/19 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.checkbox` | Checkbox (shadcn.checkbox) | n/a — HORIZONTAL, no children to re-lay | **FAIL** — Checked 3/3 distinct; State 1/3 distinct — DEAD Default≡Disabled, Default≡Focus Visible | pass — 9/9 bound (6 bound, 3 inferred) | 9/9 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.input` | Input (shadcn.input) | **FAIL** — FROZEN 0/1 (placeholder) | **FAIL** — State 1/4 distinct — DEAD Default≡Disabled, Default≡Active, Default≡Focus Visible | pass — 13/13 bound (9 bound, 4 inferred) | 13/13 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.select` | Select | pass — 2/2 re-laid (label +0,20; icon +40,20) | n/a — no axes on canvas (single-variant component) | **FAIL** — 16/18 bound — 2 literal | 16/18 (89%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.switch` | Switch (shadcn.switch) | pass — 1/1 re-laid (part-0 +0,20) | **FAIL** — Size 2/2 distinct; Checked 2/2 distinct; State 1/3 distinct — DEAD Default≡Disabled, Default≡Focus Visible | pass — 16/16 bound (13 bound, 3 inferred) | 16/16 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.tabs` | Tabs (shadcn.tabs) | **FAIL** — FROZEN 0/2 (label-4, part-0) | n/a — no axes on canvas (single-variant component) | pass — 59/59 bound (52 bound, 7 inferred) | 59/59 (100%) | pass — every node in flow | yes (2323b4b6ec9f2790) |
| shadcn | `shadcn.tooltip` | Tooltip (shadcn.tooltip) | pass — 1/1 re-laid (label ⤢40,0) | n/a — no axes on canvas (single-variant component) | pass — 23/23 bound (21 bound, 2 inferred) | 23/23 (100%) | pass — 1 genuine out-of-flow: /label[0]/part-0-0[1] (source position:absolute) | yes (2323b4b6ec9f2790) |
| tailwind | `flowbite.alert` | Alert (flowbite.alert) | **FAIL** — FROZEN 0/1 (part-0) | pass — Color 4/4 distinct | **FAIL** — 33/34 bound — 1 literal | 33/34 (97%) | pass — every node in flow | yes (e17d8ad6cd30fd1c) |
| tailwind | `flowbite.badge` | Badge (flowbite.badge) | pass — 1/1 re-laid (label +0,20) | **FAIL** — Color 6/6 distinct; Size 2/2 distinct; State 2/3 distinct — DEAD Active≡Hover | pass — 11/11 bound (11 bound, 0 inferred) | 11/11 (100%) | pass — every node in flow | yes (e17d8ad6cd30fd1c) |
| tailwind | `flowbite.button` | Button (flowbite.button) | pass — 1/1 re-laid (label +20,20) | **FAIL** — Color 5/5 distinct; Size 5/5 distinct; State 3/5 distinct — DEAD Default≡Disabled, Active≡Hover | pass — 11/11 bound (9 bound, 2 inferred) | 11/11 (100%) | pass — every node in flow | yes (e17d8ad6cd30fd1c) |
| tailwind | `flowbite.card` | Card (flowbite.card) | **FAIL** — FROZEN 0/1 (label) | n/a — no axes on canvas (single-variant component) | pass — 13/13 bound (11 bound, 2 inferred) | 13/13 (100%) | pass — every node in flow | yes (e17d8ad6cd30fd1c) |
| tailwind | `flowbite.helpertext` | HelperText | **FAIL** — FROZEN 0/1 (label) | pass — Color 5/5 distinct | pass — 2/2 bound (1 bound, 1 inferred) | 2/2 (100%) | pass — every node in flow | yes (e17d8ad6cd30fd1c) |
| tailwind | `flowbite.kbd` | Kbd | **FAIL** — FROZEN 0/1 (label) | n/a — no axes on canvas (single-variant component) | pass — 13/13 bound (11 bound, 2 inferred) | 13/13 (100%) | pass — every node in flow | yes (e17d8ad6cd30fd1c) |
| tailwind | `flowbite.label` | Label | **FAIL** — FROZEN 0/1 (label) | pass — Color 5/5 distinct | pass — 2/2 bound (1 bound, 1 inferred) | 2/2 (100%) | pass — every node in flow | yes (e17d8ad6cd30fd1c) |
| tailwind | `flowbite.toggleswitch` | ToggleSwitch | pass — 2/2 re-laid (part-0 +0,20; label (margin box) +0,20) | pass — Sizing 3/3 distinct; Checked 2/2 distinct | pass — 25/25 bound (14 bound, 11 inferred) | 25/25 (100%) | pass — 1 genuine out-of-flow: /part-0[0]/part-0-after[0] (source position:absolute) | yes (e17d8ad6cd30fd1c) |
