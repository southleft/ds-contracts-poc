{
  "_marker": "ds-contracts channel table v1",
  "generatedFrom": {
    "enumeration": "getComputedStyle full enumeration, Chromium 151.0.7922.34 (the same enumeration the capture layer records per element via window.__ALL_PROPS — extract/computed/capture.ts; the committed captured-truth artifacts carry the identical 475-property list in _provenance.channels)",
    "doors": [
      "packages/schema/src/contract-schema.ts (TOKEN_CHANNELS / DECLARED_CHANNELS / LITERAL_CHANNELS)",
      "extract/computed/fuse.ts (styledChannels, buildBaseObs, enrichLayout)",
      "extract/computed/lib.ts (isFusable, GEOMETRY_CHANNELS, LOGICAL_ALIASES, SYNTHETIC_CHANNELS, kindOf)",
      "core/emit-figma-script.ts (applyTokens / applyLiterals / applyDeclared / layoutSpec)",
      "conformance/MANIFEST.json (112 css-dom cases)",
      "extract/figma/conformance/MANIFEST.json (157 canvas cases)",
      "extract/figma/channel-closure.json (draw-read closure)"
    ]
  },
  "classes": {
    "CARRIED": "drawn on the canvas — the row names the Figma projection and the engine symbol that performs it",
    "LEDGERED": "carried as data, not drawn — the row names the receipt channel the value rides",
    "REFUSED": "a named wall — the row names the code (conformance expectName, FC-*, or the code-only fallback receipt)",
    "INERT": "provably no independent visual effect at computed level — the row carries the justification"
  },
  "propertyCount": 487,
  "totals": {
    "CARRIED": 82,
    "LEDGERED": 80,
    "REFUSED": 271,
    "INERT": 54
  },
  "unclassifiedBeforeThisTable": 272,
  "properties": [
    {
      "property": "-webkit-border-horizontal-spacing",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-border-image",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-border-vertical-spacing",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-box-align",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-box-decoration-break",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-box-direction",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-box-flex",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-box-ordinal-group",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-box-orient",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-box-pack",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-box-reflect",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-font-smoothing",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-line-break",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-line-clamp",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "-webkit-line-clamp",
      "note": "line clamping has no canvas truncation-by-count projection; refused by name",
      "conformance": [
        "webkit-line-clamp"
      ]
    },
    {
      "property": "-webkit-locale",
      "class": "INERT",
      "prior": "none",
      "note": "content-language metadata; affects locale-dependent glyph selection only through the carried font identity"
    },
    {
      "property": "-webkit-mask-box-image",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-mask-box-image-outset",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-mask-box-image-repeat",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-mask-box-image-slice",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-mask-box-image-source",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-mask-box-image-width",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-mask-position-x",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-mask-position-y",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-rtl-ordering",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-ruby-position",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-tap-highlight-color",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-text-combine",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-text-decorations-in-effect",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-text-fill-color",
      "class": "CARRIED",
      "prior": "capture fold",
      "projection": "folded into the color channel at the read boundary (the painted ink is the fill); drawn as the text node fill",
      "engine": [
        {
          "file": "extract/computed/capture.ts",
          "symbol": "textFillFolds"
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTextIdentity("
        }
      ],
      "valueNotes": "fold receipt: \"text-fill-is-the-ink\" — quoted before→after per element",
      "evidence": "measured",
      "observedAs": "color",
      "conformance": [
        "webkit-text-fill-color"
      ]
    },
    {
      "property": "-webkit-text-orientation",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-text-security",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-text-stroke-color",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-text-stroke-width",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-user-drag",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-user-modify",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "-webkit-writing-mode",
      "class": "LEDGERED",
      "prior": "webkit census (R4)",
      "receipt": "the -webkit census (extract/computed/fuse.ts styledChannels: `webkitStyled` → styledChannelReceipts) — recorded per part when styled, never fused"
    },
    {
      "property": "accent-color",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "accent-color",
      "note": "the only paint a native form control has; no Figma projection — named in the run LEDGER",
      "conformance": [
        "accent-color"
      ]
    },
    {
      "property": "align-content",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-line flex/grid content alignment; the single-line auto-layout lowering never consults it. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "align-items",
      "class": "CARRIED",
      "prior": "layout vocabulary",
      "projection": "auto-layout counterAxisAlignItems",
      "engine": [
        {
          "file": "extract/computed/fuse.ts",
          "symbol": "function enrichLayout("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function layoutSpec("
        }
      ],
      "valueNotes": "flex-start | center | flex-end | stretch carry; baseline carries via the canvas layout-align-baseline path; other keywords fall to the code-only ledger",
      "evidence": "measured",
      "conformance": [
        "flex-align-items-center"
      ]
    },
    {
      "property": "align-self",
      "class": "CARRIED",
      "prior": "layout vocabulary (G7)",
      "projection": "grid child cross alignment inside its cell",
      "engine": [
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "'align-self'"
        }
      ],
      "evidence": "measured",
      "conformance": [
        "grid-child-align-self-start"
      ]
    },
    {
      "property": "alignment-baseline",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "anchor-name",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "anchor-scope",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-composition",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-delay",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-direction",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-duration",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-fill-mode",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-iteration-count",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-name",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-play-state",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-range-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-range-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-timeline",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-timing-function",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "animation-trigger",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "app-region",
      "class": "INERT",
      "prior": "none",
      "note": "window-drag hit-testing hint; paints nothing (also excluded from replay application — REPLAY_APPLY_EXCLUDE)"
    },
    {
      "property": "appearance",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Native form-control appearance is reset only in the coded component."
    },
    {
      "property": "aspect-ratio",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "a height BAKED from the ratio (fixedHeight) — never targetAspectRatio",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        }
      ],
      "valueNotes": "draw-read closure: STRUCTURAL — the ratio returns as the captured height (extract/figma/channel-closure.json)",
      "conformance": [
        "aspect-ratio"
      ],
      "evidence": "measured"
    },
    {
      "property": "backdrop-filter",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "backdrop-filter",
      "note": "Figma background blur exists but no deterministic projection is claimed; refused by name",
      "conformance": [
        "backdrop-filter"
      ]
    },
    {
      "property": "backface-visibility",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "background",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "fills[0] (the CSS shorthand colour layer).",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "background-attachment",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Background attachment exists only in code."
    },
    {
      "property": "background-blend-mode",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Background blend modes exist only in code (per-paint blendMode upgrade deferred by name)."
    },
    {
      "property": "background-clip",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Background clipping exists only in code."
    },
    {
      "property": "background-color",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "fills[0].",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "conformance": [
        "antd-forwarded-root-attrs",
        "color-rgb-solid",
        "color-rgba-alpha",
        "combobox-option-selected",
        "container-query",
        "media-non-matching",
        "oklch-color",
        "pseudo-backdrop",
        "pseudo-selection",
        "repeat-grid-month",
        "repeat-siblings-seven",
        "repeat-siblings-thirtyone",
        "repeat-siblings-three",
        "shadow-root-closed",
        "table-zebra-nth-child",
        "var-longhand"
      ],
      "evidence": "measured"
    },
    {
      "property": "background-image",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "gradient stacks mint as whole-value gradient kind; image fills carry as imageFill hashes",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "extract/computed/lib.ts",
          "symbol": "export function kindOf("
        }
      ],
      "valueNotes": "linear/radial/conic gradient stacks and none carry; a gradient painted only on a pseudo part REFUSES by name (pseudo-gradient-only → pseudo-decor); arbitrary url() images ride the fill-image path",
      "conformance": [
        "pseudo-gradient-only"
      ],
      "evidence": "measured"
    },
    {
      "property": "background-origin",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Background origin exists only in code."
    },
    {
      "property": "background-position",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Background positioning exists only in code."
    },
    {
      "property": "background-repeat",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Background repetition exists only in code (uniform TILE approximation deferred by name)."
    },
    {
      "property": "background-size",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Background sizing exists only in code."
    },
    {
      "property": "baseline-shift",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "baseline-source",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "block-size",
      "class": "INERT",
      "prior": "geometry exclusion",
      "note": "byte-equal alias of height under the pinned writing mode (see inline-size)"
    },
    {
      "property": "border-block-end-color",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-bottom-color under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-block-end-style",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-bottom-style under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-block-end-width",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-bottom-width under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-block-start-color",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-top-color under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-block-start-style",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-top-style under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-block-start-width",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-top-width under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-bottom-color",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "one strokes paint serves all four sides — lowered only when every carried side agrees.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-bottom-left-radius",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "bottomLeftRadius.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-bottom-right-radius",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "bottomRightRadius.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-bottom-style",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "This part's borders use different styles per side in code; Figma strokes share one style."
    },
    {
      "property": "border-bottom-width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "strokeBottomWeight.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-collapse",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "table formatting; tables lower to flex stacks (extract/computed/anatomy.ts table lowering) and these channels fall to the named refusal — on fallback-control tags the receipt itself warns the baseline may be the UA's. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "border-color",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the strokes paint.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-end-end-radius",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-bottom-right-radius under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-end-start-radius",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-bottom-left-radius under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-image-outset",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "border-image-repeat",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "border-image-slice",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "border-image-source",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "border-image-width",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "border-inline-end-color",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-right-color under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-inline-end-style",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-right-style under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-inline-end-width",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-right-width under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-inline-start-color",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-left-color under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-inline-start-style",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-left-style under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-inline-start-width",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-left-width under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-left-color",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "one strokes paint serves all four sides — lowered only when every carried side agrees.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "measured",
      "conformance": [
        "combobox-option-highlighted"
      ]
    },
    {
      "property": "border-left-style",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "This part's borders use different styles per side in code; Figma strokes share one style."
    },
    {
      "property": "border-left-width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "strokeLeftWeight.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-radius",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "all four corner radius fields.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-right-color",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "one strokes paint serves all four sides — lowered only when every carried side agrees.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-right-style",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "This part's borders use different styles per side in code; Figma strokes share one style."
    },
    {
      "property": "border-right-width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "strokeRightWeight.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "border-start-end-radius",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-top-right-radius under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-start-start-radius",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of border-top-left-radius under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "border-style",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Non-solid border styles exist only in code; the canvas shows a solid stroke of the same width."
    },
    {
      "property": "border-top-color",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "one strokes paint serves all four sides — lowered only when every carried side agrees.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "measured",
      "conformance": [
        "page-global-star-rule"
      ]
    },
    {
      "property": "border-top-left-radius",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "topLeftRadius.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "conformance": [
        "border-radius-px"
      ],
      "evidence": "measured"
    },
    {
      "property": "border-top-right-radius",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "topRightRadius.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-top-style",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "This part's borders use different styles per side in code; Figma strokes share one style."
    },
    {
      "property": "border-top-width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "strokeTopWeight.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "border-width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "strokeWeight.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "bottom",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "absolutePartPlacement / insetOverlayOffsets / boundFullBleedScrimRoot.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "box-decoration-break",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fragmentation; no canvas concept. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "box-shadow",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "DROP_SHADOW effect (single) or the full multi-layer/inset effect stack (v17)",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function parseShadowStack("
        }
      ],
      "valueNotes": "none clears effects; a stack the parser cannot hold records `shadowMiss`; canvas-side INNER_SHADOW/LAYER_BLUR/text-shadow effects REFUSE by name in the canvas kit (effect-inner-shadow, effect-layer-blur, effect-text-shadow)",
      "conformance": [
        "box-shadow-single"
      ],
      "evidence": "measured"
    },
    {
      "property": "box-sizing",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "strokeAlign carries the inside/outside distinction; content-box geometry gets padding added when lowered",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "break-after",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fragmentation; no canvas concept. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "break-before",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fragmentation; no canvas concept. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "break-inside",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fragmentation; no canvas concept. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "buffered-rendering",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "caption-side",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "table formatting; tables lower to flex stacks (extract/computed/anatomy.ts table lowering) and these channels fall to the named refusal — on fallback-control tags the receipt itself warns the baseline may be the UA's. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "caret-animation",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "caret-color",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "the text caret is runtime UI, not canvas ink."
    },
    {
      "property": "caret-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "clear",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "see float. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "clip",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "clip-path",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "clip-path",
      "note": "polygon/inset clipping has no bounded projection; refused by name",
      "conformance": [
        "clip-path-polygon"
      ]
    },
    {
      "property": "clip-rule",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "color",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the text node fill.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "conformance": [
        "antd-presence-times-axis-glyph",
        "color-hex",
        "custom-prop-two-hop",
        "page-inherited-ink",
        "pseudo-marker",
        "pseudo-placeholder",
        "shadow-part",
        "var-fallback-chain",
        "webkit-text-fill-color"
      ],
      "evidence": "measured"
    },
    {
      "property": "color-interpolation",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "color-interpolation-filters",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "color-rendering",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "color-scheme",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "UA theming; the capture pins one theme, so cross-theme facts are out of scope (FC-THEME-ISO names the theme-isolation rule). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-count",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-fill",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-gap",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "itemSpacing on a HORIZONTAL stack; on a VERTICAL stack it is the CROSS axis and only matters under wrap — CSS-side there.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "conformance": [
        "flex-gap"
      ],
      "evidence": "measured"
    },
    {
      "property": "column-height",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-rule-break",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-rule-color",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "a column-rule longhand the computed sweep enumerates; the canvas has no column rules."
    },
    {
      "property": "column-rule-inset-cap-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-rule-inset-cap-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-rule-inset-junction-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-rule-inset-junction-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-rule-style",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-rule-visibility-items",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-rule-width",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-span",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-width",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "column-wrap",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "contain",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "containment/container-query machinery; a non-matching container query is already a named conformance wall (@container). Size-containment layout effects are baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "contain-intrinsic-block-size",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of contain-intrinsic-height under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "contain-intrinsic-height",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "containment/container-query machinery; a non-matching container query is already a named conformance wall (@container). Size-containment layout effects are baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "contain-intrinsic-inline-size",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of contain-intrinsic-width under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "contain-intrinsic-size",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "containment/container-query machinery; a non-matching container query is already a named conformance wall (@container). Size-containment layout effects are baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "contain-intrinsic-width",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "containment/container-query machinery; a non-matching container query is already a named conformance wall (@container). Size-containment layout effects are baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "container-name",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "containment/container-query machinery; a non-matching container query is already a named conformance wall (@container). Size-containment layout effects are baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "container-type",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "containment/container-query machinery; a non-matching container query is already a named conformance wall (@container). Size-containment layout effects are baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "content",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "pseudo-content-not-canvas-ink",
      "note": "generated ::before/::after character content is not canvas ink; refused by name",
      "conformance": [
        "pseudo-content-glyph"
      ]
    },
    {
      "property": "content-visibility",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "content-visibility",
      "conformance": [
        "content-visibility-auto"
      ]
    },
    {
      "property": "corner-bottom-left-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "corner-bottom-right-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "corner-end-end-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "corner-end-start-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "corner-start-end-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "corner-start-start-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "corner-top-left-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "corner-top-right-shape",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "corner-shaping (squircle) family, Chromium 139+; no projection — per-corner radii carry through the radius channels. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "counter-increment",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "generated-content counters/quotes; see the pseudo-content named wall. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "counter-reset",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "generated-content counters/quotes; see the pseudo-content named wall. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "counter-set",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "generated-content counters/quotes; see the pseudo-content named wall. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "cursor",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Cursor changes (pointer on hover) exist only in the coded component."
    },
    {
      "property": "cx",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "cy",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "d",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "direction",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "direction",
      "note": "RTL flow is outside the pinned writing mode (horizontal-tb + ltr, recorded in provenance)",
      "conformance": [
        "direction-rtl"
      ]
    },
    {
      "property": "display",
      "class": "CARRIED",
      "prior": "schema-declared + layout vocabulary",
      "projection": "auto-layout: layoutMode (flex/inline-flex, grid via lowering; the declared registry annotates the keyword itself)",
      "engine": [
        {
          "file": "extract/computed/fuse.ts",
          "symbol": "function enrichLayout("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function layoutSpec("
        }
      ],
      "valueNotes": "flex | inline-flex | block | grid | inline-grid and the lowered table subset carry; any other computed display is named residue (\"display-outside-vocabulary\", extract/computed/anatomy.ts); display varying across combos is named residue; conformance holds display-out-of-vocab REFUSED by table-lowering",
      "conformance": [
        "display-block",
        "display-flex",
        "display-inline-flex",
        "display-out-of-vocab",
        "grid-2d",
        "grid-in-flex-fill",
        "table-semantic-row-group"
      ],
      "evidence": "measured"
    },
    {
      "property": "dominant-baseline",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "dynamic-range-limit",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "UA theming; the capture pins one theme, so cross-theme facts are out of scope (FC-THEME-ISO names the theme-isolation rule). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "empty-cells",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "table formatting; tables lower to flex stacks (extract/computed/anatomy.ts table lowering) and these channels fall to the named refusal — on fallback-control tags the receipt itself warns the baseline may be the UA's. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "field-sizing",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "fill",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "baked into the promoted glyph markup (iconSvg), like currentColor bakes the text colour.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "fill-opacity",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "fill-rule",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "filter",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "filter",
      "note": "blur/drop-shadow filter functions have no bounded projection; refused by name",
      "conformance": [
        "filter-blur"
      ]
    },
    {
      "property": "flex-basis",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")"
    },
    {
      "property": "flex-direction",
      "class": "CARRIED",
      "prior": "layout vocabulary",
      "projection": "auto-layout layoutMode HORIZONTAL/VERTICAL",
      "engine": [
        {
          "file": "extract/computed/fuse.ts",
          "symbol": "function enrichLayout("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function layoutSpec("
        }
      ],
      "valueNotes": "row | column carry (LAYOUT_CHANNEL_TO_FIELD); row-reverse/column-reverse are outside the map and fall to the code-only ledger",
      "evidence": "measured",
      "conformance": [
        "flex-direction-column"
      ]
    },
    {
      "property": "flex-grow",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "fill-vs-hug is lowered structurally (annotateFillW); the raw grow factor rides the contract token; FC-RT-FILL-SIZING-REFUSED names the round-trip wall",
      "conformance": [
        "grid-child-grow-invalid"
      ]
    },
    {
      "property": "flex-line-count",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "flex-shrink",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")"
    },
    {
      "property": "flex-wrap",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "wrapping flex containers are outside the single-line auto-layout lowering (Figma auto-layout wrap exists; no projection is claimed today). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "float",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "float layout is outside the flex/grid layout grammar. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "flood-color",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "flood-opacity",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-family",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "fontName family (substrate-checked: FC-FONT-SUBSTRATE when the face is not available)",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTextIdentity("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "font-feature-settings",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Tabular figures / ligature settings apply only in code — Figma's plugin API cannot set OpenType features."
    },
    {
      "property": "font-kerning",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-language-override",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-optical-sizing",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-palette",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-size",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "fontSize.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "font-size-adjust",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-stretch",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-style",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "italic face selection via fontName",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "const figmaFaceStyle = ("
        }
      ],
      "valueNotes": "oblique angles refuse via FC-FONT-SLANT-NOT-CARRIED; italic dropped on propose is FC-DUMP-PROPOSE-ITALIC-DROPPED",
      "evidence": "code-cited"
    },
    {
      "property": "font-synthesis-small-caps",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-synthesis-style",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-synthesis-weight",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variant",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variant-alternates",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variant-caps",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variant-east-asian",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variant-emoji",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variant-ligatures",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variant-numeric",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variant-position",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-variation-settings",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "OpenType/typographic detail outside the carried font identity (family/size/weight/style/line-height/letter-spacing). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "font-weight",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the font STYLE name (Regular/Medium/Semi Bold/Bold)",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "const figmaFaceStyle = ("
        }
      ],
      "valueNotes": "numeric weights map to style names; a face the family does not ship refuses by FC-WEIGHT-IDENTITY / FC-FONT-STYLE-UNRESOLVED",
      "evidence": "measured",
      "conformance": [
        "table-header-row-distinct"
      ]
    },
    {
      "property": "forced-color-adjust",
      "class": "INERT",
      "prior": "none",
      "note": "effective only under forced-colors mode, which the pinned capture environment never enables"
    },
    {
      "property": "gap",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "itemSpacing.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "grid-auto-columns",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "grid-auto-flow",
      "class": "CARRIED",
      "prior": "conformance (G7)",
      "projection": "row flow lowers to the grid emulation",
      "engine": [
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "parseGridAutoFlow"
        }
      ],
      "valueNotes": "row carries (grid-auto-flow-row); column and dense REFUSE by name (grid-flow-column, grid-flow-dense)",
      "conformance": [
        "grid-auto-flow-row",
        "grid-flow-column",
        "grid-flow-dense"
      ],
      "evidence": "measured"
    },
    {
      "property": "grid-auto-rows",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "grid-column-end",
      "class": "CARRIED",
      "prior": "conformance (G7)",
      "projection": "grid child span end on the lowered grid",
      "engine": [
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "'grid-column-end'"
        }
      ],
      "valueNotes": "same walls as grid-column-start; non-rectangular area ends LOWER by name",
      "conformance": [
        "grid-area-nonrectangular",
        "grid-col-span"
      ],
      "evidence": "measured"
    },
    {
      "property": "grid-column-start",
      "class": "CARRIED",
      "prior": "schema-token (G7)",
      "projection": "grid child placement/span on the lowered grid",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "'grid-column-start'"
        }
      ],
      "valueNotes": "explicit anchors and spans carry (grid-col-span, grid-explicit-anchor); implicit tracks REFUSE by name (grid-implicit-tracks)",
      "conformance": [
        "grid-instance-child"
      ],
      "evidence": "measured"
    },
    {
      "property": "grid-row-end",
      "class": "CARRIED",
      "prior": "schema-token (G7)",
      "projection": "grid child row span end",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "'grid-row-end'"
        }
      ],
      "valueNotes": "same walls as grid-column-start",
      "conformance": [
        "grid-row-span"
      ],
      "evidence": "measured"
    },
    {
      "property": "grid-row-start",
      "class": "CARRIED",
      "prior": "schema-token (G7)",
      "projection": "grid child row placement on the lowered grid",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "'grid-row-start'"
        }
      ],
      "valueNotes": "same walls as grid-column-start",
      "conformance": [
        "grid-child-fill-cell",
        "grid-explicit-anchor",
        "grid-implicit-tracks"
      ],
      "evidence": "measured"
    },
    {
      "property": "grid-template-areas",
      "class": "CARRIED",
      "prior": "layout vocabulary (G7)",
      "projection": "named-area slots on the lowered grid",
      "engine": [
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "grid-template-areas"
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function layoutSpec("
        }
      ],
      "valueNotes": "rectangular named areas carry as slots (grid-named-area-slots); non-rectangular areas LOWER by name (grid-area-nonrectangular); empty slots carry (grid-area-empty-slot)",
      "conformance": [
        "grid-area-empty-slot",
        "grid-named-area-slots"
      ],
      "evidence": "measured"
    },
    {
      "property": "grid-template-columns",
      "class": "CARRIED",
      "prior": "schema-token + declared-track read (G7)",
      "projection": "lowered grid: per-track frames/auto-layout emulation; declared track lists read from the stylesheet (computed truth holds only used sizes)",
      "engine": [
        {
          "file": "extract/computed/capture.ts",
          "symbol": "GDECL_PROPS"
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function layoutSpec("
        }
      ],
      "valueNotes": "px / fr / fit-content / mixed-fractional tracks carry (grid-two-column, grid-sidebar-px-fr, grid-track-fit-content, grid-tracks-mixed-fractional); minmax(), percent tracks, auto-fit/minmax, zero-valued tracks and subgrid REFUSE by their G7 names (grid-track-minmax, grid-track-percent, grid-auto-fit-minmax, grid-track-zero, grid-subgrid)",
      "conformance": [
        "grid-auto-fit-minmax",
        "grid-bento-span-matrix",
        "grid-sidebar-px-fr",
        "grid-subgrid",
        "grid-track-fit-content",
        "grid-track-minmax",
        "grid-track-percent",
        "grid-track-zero-value",
        "grid-tracks-mixed-fractional",
        "grid-two-column"
      ],
      "evidence": "measured"
    },
    {
      "property": "grid-template-rows",
      "class": "CARRIED",
      "prior": "schema-token + declared-track read (G7)",
      "projection": "lowered grid rows (same path as columns)",
      "engine": [
        {
          "file": "extract/computed/capture.ts",
          "symbol": "GDECL_PROPS"
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function layoutSpec("
        }
      ],
      "valueNotes": "same value walls as grid-template-columns",
      "conformance": [
        "grid-on-component-variant"
      ],
      "evidence": "measured"
    },
    {
      "property": "height",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "a fixed height (HUG compiles to no fixed size)",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "valueNotes": "same admission doors as width; table cells read their row height; aspect-ratio bakes a height (see aspect-ratio)",
      "evidence": "code-cited"
    },
    {
      "property": "hyphenate-character",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "hyphenate-limit-chars",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "hyphens",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "image-orientation",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "replaced-content fitting; image fills carry by hash but fit/position mapping to scaleMode is not claimed. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "image-rendering",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "replaced-content fitting; image fills carry by hash but fit/position mapping to scaleMode is not claimed. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "initial-letter",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "inline-size",
      "class": "INERT",
      "prior": "geometry exclusion",
      "note": "byte-equal alias of width under the pinned writing mode; in GEOMETRY_CHANNELS so a styled value lands in the geometry-excluded ledger — the width row carries the fact"
    },
    {
      "property": "inset-block-end",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of bottom under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "inset-block-start",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of top under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "inset-inline-end",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of right under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "inset-inline-start",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of left under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "interactivity",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "interest-delay-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "interest-delay-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "interpolate-size",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "isolation",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Stacking-context isolation exists only in code."
    },
    {
      "property": "justify-content",
      "class": "CARRIED",
      "prior": "layout vocabulary",
      "projection": "auto-layout primaryAxisAlignItems (space-between included)",
      "engine": [
        {
          "file": "extract/computed/fuse.ts",
          "symbol": "function enrichLayout("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function layoutSpec("
        }
      ],
      "valueNotes": "flex-start | center | flex-end | space-between carry; space-around/evenly fall to the code-only ledger",
      "evidence": "measured",
      "conformance": [
        "combobox-closed-trigger"
      ]
    },
    {
      "property": "justify-items",
      "class": "CARRIED",
      "prior": "layout vocabulary (G7)",
      "projection": "cross-axis alignment of the lowered single-track grid",
      "engine": [
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "'justify-items'"
        }
      ],
      "evidence": "unobservable"
    },
    {
      "property": "justify-self",
      "class": "CARRIED",
      "prior": "layout vocabulary (G7)",
      "projection": "grid child alignment inside its cell",
      "engine": [
        {
          "file": "extract/computed/anatomy.ts",
          "symbol": "'justify-self'"
        }
      ],
      "conformance": [
        "grid-child-align"
      ],
      "evidence": "measured"
    },
    {
      "property": "left",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "absolutePartPlacement / insetOverlayOffsets / boundFullBleedScrimRoot.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "letter-spacing",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "pixel letterSpacing on text nodes.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "lighting-color",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "line-break",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fragmentation; no canvas concept. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "line-height",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "pixel lineHeight on text nodes.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "list-style-image",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "list marker family; the ::marker plane is read but marker generation has no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "list-style-position",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "list marker family; the ::marker plane is read but marker generation has no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "list-style-type",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "list marker family; the ::marker plane is read but marker generation has no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "margin-block-end",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of margin-bottom under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "margin-block-start",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of margin-top under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "margin-bottom",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the margin-box wrapper / sibling-gap itemSpacing lowering.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "margin-inline-end",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of margin-right under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "margin-inline-start",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of margin-left under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "margin-left",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the margin-box wrapper / sibling-gap itemSpacing lowering.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "conformance": [
        "antd-empty-margin-only-parts"
      ],
      "evidence": "measured"
    },
    {
      "property": "margin-right",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the margin-box wrapper / sibling-gap itemSpacing lowering.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "margin-top",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the margin-box wrapper / sibling-gap itemSpacing lowering.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "evidence": "measured",
      "conformance": [
        "margin-top-in-flow"
      ]
    },
    {
      "property": "marker-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "marker-mid",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "marker-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "mask-clip",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "mask family; see the mask-image named wall (conformance) — the longhands fall to the same refusal. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "mask-composite",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "mask family; see the mask-image named wall (conformance) — the longhands fall to the same refusal. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "mask-image",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "mask-image",
      "conformance": [
        "mask-image"
      ]
    },
    {
      "property": "mask-mode",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "mask family; see the mask-image named wall (conformance) — the longhands fall to the same refusal. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "mask-origin",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "mask family; see the mask-image named wall (conformance) — the longhands fall to the same refusal. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "mask-position",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "mask family; see the mask-image named wall (conformance) — the longhands fall to the same refusal. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "mask-repeat",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "mask family; see the mask-image named wall (conformance) — the longhands fall to the same refusal. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "mask-size",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "mask family; see the mask-image named wall (conformance) — the longhands fall to the same refusal. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "mask-type",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "mask family; see the mask-image named wall (conformance) — the longhands fall to the same refusal. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "math-depth",
      "class": "INERT",
      "prior": "none",
      "note": "MathML-only scaling input; inert outside MathML content"
    },
    {
      "property": "math-shift",
      "class": "INERT",
      "prior": "none",
      "note": "MathML-only; inert outside MathML content"
    },
    {
      "property": "math-style",
      "class": "INERT",
      "prior": "none",
      "note": "MathML-only; inert outside MathML content"
    },
    {
      "property": "max-block-size",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of max-height under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "max-height",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Fluid max-height constraints live in code; the canvas draws the component at its real size (standing choice)."
    },
    {
      "property": "max-inline-size",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of max-width under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "max-width",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Fluid max-width constraints live in code; the canvas draws the component at its real size (standing choice)."
    },
    {
      "property": "min-block-size",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of min-height under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "min-height",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "minHeight (suppressed when the same part carries height — a fixed height is the drawn design truth).",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "min-inline-size",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of min-width under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "min-width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "minWidth.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "conformance": [
        "min-max-width"
      ],
      "evidence": "measured"
    },
    {
      "property": "mix-blend-mode",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "mix-blend-mode",
      "note": "canvas-side blend modes LEDGER on read (blend-mode-multiply); the CSS-side channel is refused by name",
      "conformance": [
        "mix-blend-mode"
      ]
    },
    {
      "property": "object-fit",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "replaced-content fitting; image fills carry by hash but fit/position mapping to scaleMode is not claimed. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "object-position",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "replaced-content fitting; image fills carry by hash but fit/position mapping to scaleMode is not claimed. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "object-view-box",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "replaced-content fitting; image fills carry by hash but fit/position mapping to scaleMode is not claimed. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "offset-anchor",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "CSS motion path; no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "offset-distance",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "CSS motion path; no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "offset-path",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "CSS motion path; no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "offset-position",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "CSS motion path; no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "offset-rotate",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "CSS motion path; no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "opacity",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the node opacity (literal — Figma opacity is percent-scaled, so the 0-1 token is not bound).",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "order",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "visual reordering of flex children has no anatomy spelling (parts carry DOM order). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "orphans",
      "class": "INERT",
      "prior": "none",
      "note": "print pagination only; no effect in the captured screen medium"
    },
    {
      "property": "outline-color",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the strokes paint of an OUTSIDE-aligned stroke — drawn only when outline-width is carried too (a resting outline with no width paints nothing in CSS, and the toned base cells must not gain a ring).",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "outline-offset",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "Figma strokes have no offset field — an outside-aligned stroke sits flush against the box."
    },
    {
      "property": "outline-style",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Focus outlines render as a bound stroke approximation on canvas state previews (standing C5 approximation)."
    },
    {
      "property": "outline-width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "strokeWeight of an OUTSIDE-aligned stroke — drawn only when outline-color is carried too. emit-react supplies the outline-style:solid the pair needs; without a width no outline-style is written at all.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "conformance": [
        "antd-focus-outline-ring",
        "antd-focus-outline-ring-child-part",
        "antd-focus-outline-ring-ua-width"
      ],
      "evidence": "measured"
    },
    {
      "property": "overflow-anchor",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "overflow-block",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of overflow-y under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "overflow-clip-margin",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "overflow-inline",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of overflow-x under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "overflow-wrap",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "overflow-x",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "clipsContent on the frame",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        }
      ],
      "valueNotes": "draw-read closure: READ back as clipsContent; overflow:clip vs hidden nuances ledger via FC-OVERFLOW-CLIP-LOST / FC-FIGMA-CLIP-DEFAULT",
      "evidence": "code-cited"
    },
    {
      "property": "overflow-y",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "clipsContent on the frame",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        }
      ],
      "valueNotes": "same as overflow-x",
      "evidence": "code-cited"
    },
    {
      "property": "overlay",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "overscroll-behavior-block",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of overscroll-behavior-y under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "overscroll-behavior-inline",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of overscroll-behavior-x under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "overscroll-behavior-x",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "overscroll-behavior-y",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "padding-block",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "paddingTop + paddingBottom.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "padding-block-end",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of padding-bottom under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "padding-block-start",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of padding-top under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "padding-bottom",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "paddingBottom.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "padding-inline",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "paddingLeft + paddingRight.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "padding-inline-end",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of padding-right under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "padding-inline-start",
      "class": "INERT",
      "prior": "logical-alias (extract/computed/lib.ts LOGICAL_ALIASES)",
      "note": "byte-equal alias of padding-left under the pinned writing mode (horizontal-tb + ltr); excluded from fusion so the physical row carries the one fact — real only if writing-mode ever stops being refused by name"
    },
    {
      "property": "padding-left",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "paddingLeft.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "conformance": [
        "antd-component-scoped-custom-property",
        "calc-var",
        "em-relative-padding",
        "percentage-padding"
      ],
      "evidence": "measured"
    },
    {
      "property": "padding-right",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "paddingRight.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "padding-top",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "paddingTop.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "measured",
      "conformance": [
        "padding-asymmetric-block"
      ]
    },
    {
      "property": "paint-order",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "perspective",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "perspective-origin",
      "class": "LEDGERED",
      "prior": "geometry exclusion",
      "receipt": "the geometry-excluded ledger (extract/computed/fuse.ts `geometryExcluded`, surfaced as FC-GEOMETRY-EXCLUDED receipts) — recorded when styled, never minted"
    },
    {
      "property": "pointer-events",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Pointer-event gating exists only in the coded component."
    },
    {
      "property": "position",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "absolute parts leave auto-layout: layoutPositioning ABSOLUTE with folded x/y offsets (the declared registry annotates the keyword)",
      "engine": [
        {
          "file": "extract/computed/fuse.ts",
          "symbol": "absClusterParts"
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function layoutSpec("
        }
      ],
      "valueNotes": "static | relative | absolute in the bounded grammar; fixed and sticky REFUSED by name (conformance position-fixed, position-sticky — code \"position\")",
      "conformance": [
        "grid-absolute-overlay",
        "position-fixed",
        "position-sticky"
      ],
      "evidence": "measured"
    },
    {
      "property": "position-anchor",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "position-area",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "position-try-fallbacks",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "position-try-order",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "position-visibility",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "anchor-positioning / interest-invoker machinery (new platform features); no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "print-color-adjust",
      "class": "INERT",
      "prior": "none",
      "note": "print rendering hint; no effect in the captured screen medium"
    },
    {
      "property": "quotes",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "generated-content counters/quotes; see the pseudo-content named wall. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "r",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent",
      "conformance": [
        "svg-outside-grammar"
      ]
    },
    {
      "property": "reading-flow",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "reading-order",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "resize",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "right",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "absolutePartPlacement / insetOverlayOffsets / boundFullBleedScrimRoot.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "evidence": "code-cited"
    },
    {
      "property": "rotate",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "rotate",
      "note": "the independent rotate longhand is refused by name (transform's bounded rotate() grammar is the carried spelling)",
      "conformance": [
        "rotate-independent-property"
      ]
    },
    {
      "property": "row-gap",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "itemSpacing on a VERTICAL stack; on a HORIZONTAL stack it is the CROSS axis — CSS-side there.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "conformance": [
        "grid-gap-row-column",
        "grid-gap-shorthand"
      ],
      "evidence": "measured"
    },
    {
      "property": "row-rule-break",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "row-rule-color",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "a Chromium GAP-DECORATION longhand (CSS gap decorations) that the computed sweep enumerates for every element — nobody authored it, and neither surface paints it. Registered so it is CARRIED and NAMED rather than silently believed."
    },
    {
      "property": "row-rule-inset-cap-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "row-rule-inset-cap-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "row-rule-inset-junction-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "row-rule-inset-junction-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "row-rule-style",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "row-rule-visibility-items",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "row-rule-width",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "ruby-align",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "ruby annotation layout; no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "ruby-overhang",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "ruby annotation layout; no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "ruby-position",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "ruby annotation layout; no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "rule-overlap",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "multi-column / gap-decoration family; no projection (column-rule-color and row-rule-color are the exceptions — schema token channels). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "rx",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "ry",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scale",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "Figma has no scale transform — a node's size IS its box, so a scale factor cannot be lowered without silently rewriting the box; the third independent-transform longhand alongside translate (folded to absolute placement) and rotate (carried by the transform grammar)."
    },
    {
      "property": "scroll-behavior",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-initial-target",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-margin-block-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-margin-block-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-margin-bottom",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-margin-inline-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-margin-inline-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-margin-left",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-margin-right",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-margin-top",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-marker-group",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-padding-block-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-padding-block-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-padding-bottom",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-padding-inline-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-padding-inline-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-padding-left",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-padding-right",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-padding-top",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-snap-align",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-snap-stop",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-snap-type",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-target-group",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-timeline-axis",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scroll-timeline-name",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scrollbar-color",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scrollbar-gutter",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "scrollbar-width",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scrolling behaviour; the canvas has no scroll model. scrollbar-gutter/width layout effects are already baked into captured boxes. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "shape-image-threshold",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "shape-margin",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "shape-outside",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "shape-rendering",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "speak",
      "class": "INERT",
      "prior": "none",
      "note": "speech synthesis hint; no visual effect"
    },
    {
      "property": "stop-color",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "stop-opacity",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "stroke",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the glyph's stroke paint — baked into the promoted glyph markup (iconSvg) via the currentColor pass; the stroke-drawn (lucide-class) counterpart of `fill`.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "evidence": "code-cited",
      "valueNotes": "MEASURED 2026-08-26 and NOT closed (the engine file is owned by another branch this round). The carriage this row claims happens at CAPTURE time, not through the cited switch: extract/computed/anatomy.ts:1387-1389 writes the value into the promoted glyph markup string, and iconSvg stamps that markup. core/emit-figma-script.ts has NO `case 'stroke'` in applyTokens / applyLiterals / applyDeclared. Because TOKEN_CHANNELS marks this channel `drawn(...)`, the default arm's guard `if (reg && reg.canvas !== 'draw') miss(...)` (emit-figma-script.ts:2348) is FALSE, so a contract that BINDS this channel as a token is neither drawn nor recorded as a miss — it is dropped silently. That is exactly the class the default arm's own comment was written to close, and this channel is a member the comment does not list."
    },
    {
      "property": "stroke-dasharray",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "stroke-dashoffset",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "stroke-linecap",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "stroke-linejoin",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "stroke-miterlimit",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "stroke-opacity",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "stroke-width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "the glyph's stroke weight — carried verbatim in the promoted glyph markup (stroke-width attribute); CSS surfaces render the computed value.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "evidence": "code-cited",
      "valueNotes": "MEASURED 2026-08-26 and NOT closed (the engine file is owned by another branch this round). The carriage this row claims happens at CAPTURE time, not through the cited switch: extract/computed/anatomy.ts:1387-1389 writes the value into the promoted glyph markup string, and iconSvg stamps that markup. core/emit-figma-script.ts has NO `case 'stroke-width'` in applyTokens / applyLiterals / applyDeclared. Because TOKEN_CHANNELS marks this channel `drawn(...)`, the default arm's guard `if (reg && reg.canvas !== 'draw') miss(...)` (emit-figma-script.ts:2348) is FALSE, so a contract that BINDS this channel as a token is neither drawn nor recorded as a miss — it is dropped silently. That is exactly the class the default arm's own comment was written to close, and this channel is a member the comment does not list."
    },
    {
      "property": "tab-size",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "table-layout",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "table formatting; tables lower to flex stacks (extract/computed/anatomy.ts table lowering) and these channels fall to the named refusal — on fallback-control tags the receipt itself warns the baseline may be the UA's. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-align",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "textAlignHorizontal on text nodes",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTextIdentity("
        }
      ],
      "valueNotes": "canvas→code read-back is NAMED (text-channel-unsupported) in the draw-read closure; the css→canvas write carries",
      "evidence": "measured",
      "conformance": [
        "table-column-numeric-alignment"
      ]
    },
    {
      "property": "text-align-last",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-anchor",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-autospace",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-box-edge",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-box-trim",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-combine-upright",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-decoration",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-decoration-color",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "Figma textDecoration is an enum with no independent colour."
    },
    {
      "property": "text-decoration-line",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "textDecoration UNDERLINE/STRIKETHROUGH",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTextIdentity("
        }
      ],
      "valueNotes": "read-back NAMED (text-channel-unsupported) in the draw-read closure",
      "evidence": "code-cited"
    },
    {
      "property": "text-decoration-skip-ink",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-decoration-style",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "This decoration style variant exists only in code (canvas draws a solid decoration)."
    },
    {
      "property": "text-decoration-thickness",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Decoration thickness exists only in code (canvas draws the default thickness)."
    },
    {
      "property": "text-emphasis-color",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "Figma has no text-emphasis marks."
    },
    {
      "property": "text-emphasis-position",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-emphasis-style",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-fit",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-indent",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\") — plus, where MEASURED off-box, the withheld label (Part.textOutOfBox; extract/computed/fuse.ts textOutOfBoxEvidence, core/emit-figma-script.ts textOutOfBoxOn)",
      "note": "Figma text nodes have no first-line indent. Annotating the lost offset is honest only while the canvas still draws what the browser draws: where the measured indent lays the first line entirely outside the content box (altitude Badge's 8px dot pip at text-indent: 9999px, and the -9999px visually-hidden idiom) the browser paints NO text in the box, so the canvas draws no text child there rather than a label at indent 0."
    },
    {
      "property": "text-justify",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-orientation",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-overflow",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "textTruncation ellipsis",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        }
      ],
      "valueNotes": "read-back NAMED (text-channel-unsupported); FC-RT-TRUNCATION-REFUSED names the round-trip wall",
      "conformance": [
        "text-overflow-ellipsis"
      ],
      "evidence": "measured"
    },
    {
      "property": "text-rendering",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Text rasterization hints (text-rendering) apply only in code."
    },
    {
      "property": "text-shadow",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-size-adjust",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-spacing-trim",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-transform",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "textCase upper/lower/title",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        }
      ],
      "valueNotes": "read-back NAMED (text-channel-unsupported)",
      "evidence": "code-cited"
    },
    {
      "property": "text-underline-offset",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-underline-position",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "text-wrap-mode",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Line-breaking rules differ: Figma wraps by box width only."
    },
    {
      "property": "text-wrap-style",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "timeline-scope",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "timeline-trigger-activation-range-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "timeline-trigger-activation-range-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "timeline-trigger-active-range-end",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "timeline-trigger-active-range-start",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "timeline-trigger-name",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "timeline-trigger-source",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "top",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "absolutePartPlacement / insetOverlayOffsets / boundFullBleedScrimRoot — absolute offsets, not a switch case.",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "conformance": [
        "combobox-popover-overlay-inset",
        "position-absolute-insets"
      ],
      "evidence": "measured"
    },
    {
      "property": "touch-action",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Touch gesture handling (touch-action) exists only in the coded component."
    },
    {
      "property": "transform",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "identity-translate folds into absolute x/y (synthetic translate-x/y); rotate(<n>deg) carries for icon/shape orientation (FC-SVG-ROTATION)",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        },
        {
          "file": "extract/computed/lib.ts",
          "symbol": "SYNTHETIC_CHANNELS"
        }
      ],
      "valueNotes": "anything outside the bounded grammar (scale, skew, 3d, combined transforms) is named residue — conformance transform-scale-rotate REFUSED, code \"transform\"",
      "conformance": [
        "transform-scale-rotate"
      ],
      "evidence": "measured"
    },
    {
      "property": "transform-box",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "transform-origin",
      "class": "LEDGERED",
      "prior": "geometry exclusion",
      "receipt": "the geometry-excluded ledger (extract/computed/fuse.ts `geometryExcluded`, surfaced as FC-GEOMETRY-EXCLUDED receipts) — recorded when styled, never minted",
      "note": "environment-coupled geometry; recorded when styled, never minted — transforms outside the identity-translate grammar are refused anyway"
    },
    {
      "property": "transform-style",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "transition",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Motion (spin, pulse, easing) runs only in the coded component; the canvas shows one still frame."
    },
    {
      "property": "transition-behavior",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "motion machinery; captures poll to steady state (pinned animations receipt in extract/computed/capture.ts) and the property falls to the named code-only refusal when styled. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "transition-delay",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Motion (spin, pulse, easing) runs only in the coded component; the canvas shows one still frame."
    },
    {
      "property": "transition-duration",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Motion (spin, pulse, easing) runs only in the coded component; the canvas shows one still frame.",
      "conformance": [
        "antd-part-transition-channel",
        "transition-channel"
      ]
    },
    {
      "property": "transition-property",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Motion (spin, pulse, easing) runs only in the coded component; the canvas shows one still frame."
    },
    {
      "property": "transition-timing-function",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Motion (spin, pulse, easing) runs only in the coded component; the canvas shows one still frame."
    },
    {
      "property": "translate",
      "class": "CARRIED",
      "prior": "schema-declared",
      "projection": "independent translate longhand folded into absolute offsets via translate-x/y",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyDeclared("
        },
        {
          "file": "extract/computed/lib.ts",
          "symbol": "SYNTHETIC_CHANNELS"
        }
      ],
      "valueNotes": "one or two <len|pct> components or none; transform and translate both set on one plane is named residue",
      "evidence": "code-cited"
    },
    {
      "property": "translate-x",
      "class": "CARRIED",
      "prior": "synthetic channel (pseudo-decor v2)",
      "synthetic": true,
      "projection": "absolute x offset on the overlay part (identity-translate fold)",
      "engine": [
        {
          "file": "extract/computed/lib.ts",
          "symbol": "SYNTHETIC_CHANNELS"
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "valueNotes": "derived from transform matrix(1,0,0,1,tx,ty) or the independent translate longhand; absent ≡ 0px on admitted parts",
      "evidence": "code-cited"
    },
    {
      "property": "translate-y",
      "class": "CARRIED",
      "prior": "synthetic channel (pseudo-decor v2)",
      "synthetic": true,
      "projection": "absolute y offset on the overlay part",
      "engine": [
        {
          "file": "extract/computed/lib.ts",
          "symbol": "SYNTHETIC_CHANNELS"
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        }
      ],
      "valueNotes": "same grammar as translate-x",
      "evidence": "code-cited"
    },
    {
      "property": "trigger-scope",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "unicode-bidi",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "user-select",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Text-selection behavior (user-select) exists only in the coded component."
    },
    {
      "property": "vector-effect",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "vertical-align",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "Figma has no inline baseline alignment; auto-layout counterAxisAlignItems is the nearest, coarser, fact."
    },
    {
      "property": "view-timeline-axis",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "view-timeline-inset",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "view-timeline-name",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scroll-driven animation / transition machinery; no steady-state projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "view-transition-class",
      "class": "INERT",
      "prior": "none",
      "note": "participates only during a view transition; no steady-state paint"
    },
    {
      "property": "view-transition-group",
      "class": "INERT",
      "prior": "none",
      "note": "view-transition machinery; no steady-state paint"
    },
    {
      "property": "view-transition-name",
      "class": "INERT",
      "prior": "none",
      "note": "view-transition machinery; no steady-state paint"
    },
    {
      "property": "view-transition-scope",
      "class": "INERT",
      "prior": "none",
      "note": "view-transition machinery; no steady-state paint"
    },
    {
      "property": "visibility",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "visibility",
      "note": "a visibility:hidden part still occupies its box — no canvas spelling distinguishes it from painted; refused by name (nonpainting-text-visibility-hidden)"
    },
    {
      "property": "white-space",
      "class": "LEDGERED",
      "prior": "schema-declared",
      "receipt": "Part.declared / Part.declaredStates in the contract; surfaces as code-only facts (bundle `codeOnlyFacts`, plugin data + plugin report — gated by core/code-only-facts-check.ts)",
      "note": "Line-breaking rules differ: Figma wraps by box width only."
    },
    {
      "property": "white-space-collapse",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "the collapse half of white-space; the schema declared channel `white-space` (legacy spelling) and `text-wrap-mode` carry the wrap facts — collapse behaviour itself has no projection. falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "widows",
      "class": "INERT",
      "prior": "none",
      "note": "print pagination only; no effect in the captured screen medium"
    },
    {
      "property": "width",
      "class": "CARRIED",
      "prior": "schema-token",
      "projection": "a fixed width / minWidth-bound frame size",
      "engine": [
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyTokens("
        },
        {
          "file": "core/emit-figma-script.ts",
          "symbol": "function applyLiterals("
        }
      ],
      "valueNotes": "admitted through named doors only: absolute-cluster, table cell, block-root and token-named geometry (extract/computed/fuse.ts styledChannels); viewport-derived boxes refuse (FC-GEOMETRY-EXCLUDED); a plain flow width stays excluded as environment geometry — the receipt names it",
      "conformance": [
        "stage-box-equal"
      ],
      "evidence": "measured"
    },
    {
      "property": "will-change",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "word-break",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "word-spacing",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "fine text layout outside the carried text identity; word-break/overflow-wrap are the most common observed members (present in the committed code-only census). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "writing-mode",
      "class": "REFUSED",
      "prior": "conformance",
      "code": "writing-mode",
      "note": "vertical writing modes are outside the pinned writing mode",
      "conformance": [
        "writing-mode-vertical"
      ]
    },
    {
      "property": "x",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "y",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "SVG presentation channel: inside icon parts the rendered SVG subtree is carried VERBATIM (core/emit-figma-script.ts iconSvg), so the drawn ink survives; as a computed channel on a non-icon part it has no projection (conformance svg-outside-grammar names the refusal shape). falls to the named per-channel refusal in extract/computed/fuse.ts buildBaseObs (\"value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today\"); the observed value rides enriched.extension.json `codeOnlyChannels`, so the drop is named, not silent"
    },
    {
      "property": "z-index",
      "class": "LEDGERED",
      "prior": "schema-token",
      "receipt": "contract token binding, emitted verbatim in generated CSS; on canvas an annotation only (TOKEN_CHANNELS canvas:\"annotate\")",
      "note": "canvas stacking is child order; reversed z ledgered on read (layout-item-reverse-z-index)"
    },
    {
      "property": "zoom",
      "class": "REFUSED",
      "prior": "none",
      "code": "code-only:no-schema-channel",
      "note": "scales descendant layout; the scaled geometry is already baked into every captured box, so the keyword itself carries no additional fact — still named through the code-only ledger when styled"
    }
  ],
  "customProperties": {
    "pattern": "--*",
    "class": "CARRIED",
    "projection": "custom properties are the token SUBSTRATE, not styled channels: var() references resolve through the CSS-vars reader (capture.ts vrefsOf, one indirection hop) into contract token bindings; a custom property observed directly on an element is refused by name as token plumbing (fuse.ts: \"CSS custom property, not a styled channel\")",
    "engine": [
      {
        "file": "extract/computed/capture.ts",
        "symbol": "vrefsOf"
      }
    ]
  },
  "planes": {
    "pseudoElements": {
      "read": [
        "::before",
        "::after",
        "::marker",
        "::placeholder"
      ],
      "readAnchor": {
        "file": "extract/computed/lib.ts",
        "symbol": "READ_PSEUDOS"
      },
      "named": {
        "::selection": "pseudo-selection (conformance, UNSUPPORTED)",
        "::backdrop": "pseudo-backdrop (conformance, UNSUPPORTED)"
      },
      "unread": {
        "class": "REFUSED",
        "code": "FC-PSEUDO-PLANE-UNREAD",
        "note": "MINTED BY THIS TABLE: these pseudo-element planes are never read by the capture layer and no degradation names them today — a rule styling one is invisible with no receipt. Found-silent.",
        "items": [
          "::first-line",
          "::first-letter",
          "::file-selector-button",
          "::spelling-error",
          "::grammar-error",
          "::highlight",
          "::target-text",
          "::details-content",
          "::cue"
        ]
      }
    },
    "states": {
      "driven": [
        "default",
        "hover (mouse)",
        "active (mouse down)",
        "focus-visible (sentinel+Tab keyboard modality)",
        "disabled (prop combos)",
        "checked (variant axis reclassification)"
      ],
      "drivenAnchor": {
        "file": "extract/computed/capture.ts",
        "symbol": "'focus-visible'"
      },
      "named": {
        "state deltas outside the vocabulary": "stateCodeOnly named residue (fuse.ts); FC-STATE-PLANE-UNDRAWN; FC-STATE-PREVIEW-NOISE"
      },
      "undriven": {
        "class": "REFUSED",
        "code": "FC-STATE-PLANE-UNDRIVEN",
        "note": "MINTED BY THIS TABLE: these interaction/UA state planes are never driven by the capture sweep and nothing names the gap — a library styling one loses the plane with no receipt. Found-silent.",
        "items": [
          ":visited",
          ":target",
          ":focus-within (as a distinct plane)",
          ":placeholder-shown",
          ":autofill",
          ":valid",
          ":invalid",
          ":user-valid",
          ":user-invalid",
          ":indeterminate (where no variant axis carries it)"
        ]
      }
    }
  },
  "foundSilent": [
    {
      "surface": "pseudo-element planes",
      "items": [
        "::first-line",
        "::first-letter",
        "::file-selector-button",
        "::spelling-error",
        "::grammar-error",
        "::highlight",
        "::target-text",
        "::details-content",
        "::cue"
      ],
      "mintedCode": "FC-PSEUDO-PLANE-UNREAD",
      "why": "the capture reads only READ_PSEUDOS (::before/::after/::marker/::placeholder); ::selection and ::backdrop are named by conformance cases, but a rule on any plane listed here is dropped with no receipt anywhere in the pipeline"
    },
    {
      "surface": "state planes",
      "items": [
        ":visited",
        ":target",
        ":focus-within (as a distinct plane)",
        ":placeholder-shown",
        ":autofill",
        ":valid",
        ":invalid",
        ":user-valid",
        ":user-invalid",
        ":indeterminate (where no variant axis carries it)"
      ],
      "mintedCode": "FC-STATE-PLANE-UNDRIVEN",
      "why": "the sweep drives default/hover/active/focus-visible (plus disabled and checked through the prop space); a library styling any plane listed here loses the whole plane silently"
    },
    {
      "surface": "logical-alias longhands",
      "items": [
        "border-block-end-color",
        "border-block-end-style",
        "border-block-end-width",
        "border-block-start-color",
        "border-block-start-style",
        "border-block-start-width",
        "border-end-end-radius",
        "border-end-start-radius",
        "border-inline-end-color",
        "border-inline-end-style",
        "border-inline-end-width",
        "border-inline-start-color",
        "border-inline-start-style",
        "border-inline-start-width",
        "border-start-end-radius",
        "border-start-start-radius",
        "contain-intrinsic-block-size",
        "contain-intrinsic-inline-size",
        "inset-block-end",
        "inset-block-start",
        "inset-inline-end",
        "inset-inline-start",
        "margin-block-end",
        "margin-block-start",
        "margin-inline-end",
        "margin-inline-start",
        "max-block-size",
        "max-inline-size",
        "min-block-size",
        "min-inline-size",
        "overflow-block",
        "overflow-inline",
        "overscroll-behavior-block",
        "overscroll-behavior-inline",
        "padding-block-end",
        "padding-block-start",
        "padding-inline-end",
        "padding-inline-start"
      ],
      "mintedCode": "(none — INERT)",
      "why": "excluded from fusion by isFusable with no receipt; classified INERT because each is a byte-equal alias of its physical longhand under the pinned writing mode, so no independent fact is lost while writing-mode stays refused by name — the silence is justified, and this row is the written justification"
    }
  ]
}
