# Proposed contracts — design-side extraction report

1 component set(s) extracted from the canvas dump. Every proposal parses against the contract schema. A proposal is a STARTING POINT: unbound values are NAMED below (never silently tokenized), and each note is a review line item.

## Badge

- proposed: 3 props
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label — paddingLeft: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label — paddingRight: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label — topLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label — topRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label — bottomLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label — bottomRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label — fill: variable id VariableID:3590:5442 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Label/Badge — fill: variable id VariableID:386:753 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label — paddingLeft: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label — paddingRight: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label — topLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label — topRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label — bottomLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label — bottomRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label — fill: variable id VariableID:386:727 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Label/Badge — fill: variable id VariableID:386:741 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label — paddingLeft: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label — paddingRight: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label — topLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label — topRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label — bottomLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label — bottomRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label — fill: variable id VariableID:3590:5439 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Label/Badge — fill: variable id VariableID:386:747 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label — paddingLeft: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label — paddingRight: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label — topLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label — topRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label — bottomLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label — bottomRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label — fill: variable id VariableID:3590:5440 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Label/Badge — fill: variable id VariableID:386:749 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label — paddingLeft: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label — paddingRight: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label — topLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label — topRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label — bottomLeftRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label — bottomRightRadius: variable id VariableID:3653:29320 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label — fill: variable id VariableID:3590:5441 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Label/Badge — fill: variable id VariableID:386:751 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Dot — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Dot — width: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Dot — height: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Dot — topLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Dot — topRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Dot — bottomLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Dot — bottomRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Danger, Shape=Dot — fill: variable id VariableID:3590:5442 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Dot — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Dot — width: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Dot — height: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Dot — topLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Dot — topRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Dot — bottomLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Dot — bottomRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Neutral, Shape=Dot — fill: variable id VariableID:386:727 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Dot — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Dot — width: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Dot — height: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Dot — topLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Dot — topRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Dot — bottomLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Dot — bottomRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Info, Shape=Dot — fill: variable id VariableID:3590:5439 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Dot — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Dot — width: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Dot — height: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Dot — topLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Dot — topRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Dot — bottomLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Dot — bottomRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Success, Shape=Dot — fill: variable id VariableID:3590:5440 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Dot — itemSpacing: variable id VariableID:3653:29281 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Dot — width: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Dot — height: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Dot — topLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Dot — topRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Dot — bottomLeftRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Dot — bottomRightRadius: variable id VariableID:3653:29282 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- dump variable-unresolved: Badge:Variant=Warning, Shape=Dot — fill: variable id VariableID:3590:5441 unresolvable — the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local; NOT a plan limit); regenerate the token with file_variables:read and re-run; resolved value used meanwhile
- this dump's reader could not see: variable names and multi-mode values (dump v1.4/v1.6): NOT captured because the token lacks the file_variables:read scope (Figma 403 on /v1/files/:key/variables/local — a token scope, not a plan limit): every bound fact on this dump degrades to its resolved literal (the collection’s DEFAULT mode — Light/Default — with no mode recorded), no variable NAME survives into the proposal, and the other modes’ values are absent; FIX: regenerate the token with file_variables:read and re-run; absolute placement on non-shape nodes (dump v1.7): not captured on this route — an out-of-flow FRAME/TEXT (e.g. a corner-pinned badge) re-enters the flow and renders in-line; image fills (dump v1.7 imageFill / v1.9 imageHash): not captured on this route — an IMAGE paint (e.g. an avatar photo) is read as no fill and renders as an empty box; fixed sizes on plain rectangles (dump v1.8 fixedSize): not captured on this route — a drawn width/height is lost and the node sizes to content; strokeAlign (dump v1.11): not captured on this route — an OUTSIDE stroke (focus ring) will be read as an inward border; layout wrap + row spacing (dump v1.12 layout.wrap/rowSpacing): not captured on this route — a wrapping row is read as a single non-wrapping line; the full constraints map (dump v1.13): not captured on this route — MIN/MAX/CENTER/STRETCH/SCALE pinning carries only on shape decor, not on other node types — absence of these facts is a READ limit of the import route, not evidence about the design; re-import via the plugin dump (extract/figma/dump.plugin.js, dump v1.13) to capture them
- semantics.element defaulted to "div" — element/role/ARIA are not drawn on the canvas and the name/axis inference table matched nothing; set the real host element
- prop `variant`: the set's DECLARED default for "Variant" is "Neutral" (propertyDefinitions.defaultValue, dump v1.5), not the first variant in tree order ("Danger") — the proposal's default follows the declared default, and the base plane every per-value carrier deviates from (tokens / tokensByProp / layoutByProp / textByProp) is the Neutral plane (Phase 2 exam: Badge Size, Chip Dismissible, Heading Tag/Variant defaulted to the first-drawn value with no receipt)
- Badge:root/Badge: rides text style "typography/body/xs/semibold" which is not a token-derived style (the kit binds no variable to its typography) — minted under the STYLE's own name as `imported.text.typography-body-xs-semibold`, shared by every part riding it; rename against your real type tokens (provisional)
- Badge:root/Badge: font family "Public Sans" drawn in every variant (dump v1.31) — carried as declared font-family: Public Sans (a canvas-drawable channel; the return leg sets fontName.family — the face must be available to the renderer, else it falls back to Inter — FC-FONT-SUBSTRATE, named limit)
- Badge:root/Badge: letter-spacing 1px observed in every variant — carried as a pixel literal, not a token identity
- Badge:root/Badge: the text box sizes itself to its text in every variant (textAutoResize WIDTH_AND_HEIGHT, dump v1.36) — carried as textAutoResize: WIDTH_AND_HEIGHT; a Figma auto-width text box is a whole number of pixels wide (the advance rounded up), so the code emitters round the element's fit-content inline size up to the pixel where calc-size() is supported (a label that does not fit still wraps), and the writer sets the field back on the node
- Badge:root: root width is DRAWN FIXED in 5/10 variants and HUG in the rest — minted as root width with the fixed planes' observed bbox and `fit-content` on the hug planes (the sizing MODE each plane actually draws; a pinned hug measure would freeze a box sized for the DEFAULT content and clip or float whatever a host puts in it)
- Badge:root: root height is DRAWN FIXED in 5/10 variants and HUG in the rest — minted as root height with the fixed planes' observed bbox and `fit-content` on the hug planes (the sizing MODE each plane actually draws; a pinned hug measure would freeze a box sized for the DEFAULT content and clip or float whatever a host puts in it)
- prop `shape`: two-value axis [Label, Dot] kept as an ENUM (both states render truthfully on both surfaces); a code boolean is a compatible code-side binding — see extract/reconcile.ts bool⇄axis treatment
- prop `text`: the single text prop carries the component's main content — repo contracts bind main content to code prop "children" (ds.button convention); adopt by setting bindings.code.prop to "children" when this is the label (note-only, nothing renamed mechanically)
- description: carried VERBATIM from the set's own Figma description (295 chars, dump v1.32) — the proposal-provenance sentence rides this note instead of overwriting the designer's words: "PROPOSED contract extracted from the design canvas (extract/figma dump v1) — API, anatomy, and token bindings inverted from the drawn structure. Semantics beyond the name/axis inference table, a11y, events, and slot accepts are not canvas-recoverable; review before adoption."
- documentationLinks: 1 Figma documentation link(s) carried onto the contract (https://altitude.pages.dev/docs/components/badge/) — code emitters surface them as JSDoc @see / Storybook docs links
- Badge:root padding: observed 0 8 0 8 carried as a PROVISIONAL minted token (rename against your real system) — nearest real tokens by value: {radius.banner.section}, {radius.none}; the proposal binds the provisional name, never a real token the canvas did not use
- Badge:root cornerRadius: observed 999 carried as a PROVISIONAL minted token (rename against your real system) — nearest real tokens by value: {radius.badge}, {radius.avatar}, {radius.skeleton.circle}, {radius.pill}; the proposal binds the provisional name, never a real token the canvas did not use
- Badge:root/Badge fontWeight: observed 600 carried as a PROVISIONAL minted token (rename against your real system) — nearest real tokens by value: {font.title.weight}, {font.tab.selected.weight}, {font.weight.semibold}; the proposal binds the provisional name, never a real token the canvas did not use
- Badge:root/Badge lineHeight: observed 20 carried as a PROVISIONAL minted token (rename against your real system) — nearest real tokens by value: {size.checkbox.md}, {size.switch.height}, {space.250}; the proposal binds the provisional name, never a real token the canvas did not use
- MINTED {imported.badge.root.background-color.danger} = #38191c — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root background-color (variant=danger)
- MINTED {imported.badge.root.background-color.neutral} = #797469 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root background-color (variant=neutral)
- MINTED {imported.badge.root.background-color.info} = #2b3a63 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root background-color (variant=info)
- MINTED {imported.badge.root.background-color.success} = #14301f — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root background-color (variant=success)
- MINTED {imported.badge.root.background-color.warning} = #2f2610 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root background-color (variant=warning)
- MINTED {imported.badge.root.padding-inline.label} = 8px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root padding-inline (shape=label)
- MINTED {imported.badge.root.padding-inline.dot} = 0px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root padding-inline (shape=dot)
- MINTED {imported.badge.root.border-radius.label} = 999px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root border-radius (shape=label)
- MINTED {imported.badge.root.border-radius.dot} = 8px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root border-radius (shape=dot)
- MINTED {imported.badge.badge.color.danger} = #ff6d76 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root/Badge color (variant=danger)
- MINTED {imported.badge.badge.color.neutral} = #f1f0ea — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root/Badge color (variant=neutral)
- MINTED {imported.badge.badge.color.info} = #a3b5ff — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root/Badge color (variant=info)
- MINTED {imported.badge.badge.color.success} = #4cd98a — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root/Badge color (variant=success)
- MINTED {imported.badge.badge.color.warning} = #f5b942 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root/Badge color (variant=warning)
- MINTED {imported.text.typography-body-xs-semibold.font-size} = 12px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root/Badge font-size
- MINTED {imported.text.typography-body-xs-semibold.font-weight} = 600 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root/Badge font-weight
- MINTED {imported.text.typography-body-xs-semibold.line-height} = 20px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root/Badge line-height
- MINTED {imported.badge.root.width.label} = fit-content — a SIZING MODE, not a measure: this plane HUGS its content on the canvas, so the leaf states the mode instead of freezing a measurement of the default content. Nothing to rename (no design token names HUG); the canvas leg reads it back as Figma HUG sizing and upserts no variable for it; bound at: Badge:root width (shape=label)
- MINTED {imported.badge.root.width.dot} = 8px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root width (shape=dot)
- MINTED {imported.badge.root.height.label} = fit-content — a SIZING MODE, not a measure: this plane HUGS its content on the canvas, so the leaf states the mode instead of freezing a measurement of the default content. Nothing to rename (no design token names HUG); the canvas leg reads it back as Figma HUG sizing and upserts no variable for it; bound at: Badge:root height (shape=label)
- MINTED {imported.badge.root.height.dot} = 8px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Badge:root height (shape=dot)

## Export envelope (v2) — everything this run wrote

- contract: ./badge.contract.proposed.json
- minted token tree: ./minted.dtcg.json (21 token(s); machine-derived provisional names)
- no captured variables: the REST variables endpoint refused — the token lacks the `file_variables:read` scope (HTTP 403; a token scope, NOT a plan limit). FIX: regenerate the token with file_variables:read and re-run extract:figma:rest; until then every binding is a resolved literal and captured.dtcg.json is NOT written

## Batch notes (receipts no single set owns)

- dump variables-unavailable: file:y83n4o9LOGs74oAoguFcGS — /v1/files/:key/variables/local refused with HTTP 403: the token lacks the file_variables:read scope (NOT a plan limit — the same token reads the file). FIX: regenerate the token with file_variables:read and re-run. Until then every variable binding on this dump is a resolved literal (see the variable-unresolved rows) and no variable name survives

## Dependency closure (the REST import followed its instances)

- requested: Badge (3538:35772)
- followed into this dump: none
- ⚠ corpus fallback: no token corpus was supplied, so the repo-demo reference tokens (tokens/primitives.tokens.json, tokens/semantic.tokens.json, tokens/modes/semantic.light.tokens.json, tokens/modes/brand.default.tokens.json) matched the canvas values — for a foreign kit this binds their values to this repo's token names, wrong by construction.

Next — generate the code:

```
npx ds-contracts generate ./badge.contract.proposed.json --out ./generated --stories --tokens tokens/primitives.tokens.json,tokens/semantic.tokens.json,tokens/modes/semantic.light.tokens.json,tokens/modes/brand.default.tokens.json,./minted.dtcg.json
```
