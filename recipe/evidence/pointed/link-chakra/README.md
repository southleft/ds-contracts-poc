# link@1 pointed at chakra

- 1. capture   extract/computed/out/chakra/link/captured-truth.json (4 captures)
- 2. roles     DRAFTED from the ledger — box:high (review recipe/evidence/pointed/link-chakra/roles.draft.json)
- 3. propose   14 leaves read from the ledger · 0 reviewed (named) · 2 archetype spellings · 0 invented → recipe/fixtures/generated/link.chakra.ts
- 4. compile   fixed point ✔ · 1 variants · 25 carried · 3 receipts · recipe 3c37b1a8
- 5. emit      writer.plugin.js (25 KB, page "Recipe Pivot / Link / 3c37b1a8-link-v7") and writer.scratch.js

## What a person did
- reviewed the role map (drafted with evidence, see roles.draft.json)
- (switch@1 has no glyph)


## What to do next
1. In Figma desktop, open the file the set should live in, open the development plugin, choose **Paste a script**, paste `writer.plugin.js`, run. It creates its own page named "Recipe Pivot / Link / 3c37b1a8-link-v7" and never touches an existing page.
2. To score it: export the unchecked variant's control and run `npx tsx recipe/fidelity-score.ts --canvas <png> --reference extract/computed/out/chakra/link/orig-shots/<off-state>__default.png --label link/chakra --out <json> --reference-control-only`.
3. To keep it: add the generated module to the link live proof's sources and to the reader's subjects (see checkbox v6 for the pattern).
