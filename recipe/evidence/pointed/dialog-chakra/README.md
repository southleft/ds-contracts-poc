# dialog@1 pointed at chakra

- 1. capture   extract/computed/out/chakra/dialog/captured-truth.json (1 captures)
- 2. roles     DRAFTED from the ledger — title:high body:high paper:high titleBlock:high bodyBlock:high (review recipe/evidence/pointed/dialog-chakra/roles.draft.json)
- 3. propose   17 leaves read from the ledger · 0 reviewed (named) · 0 archetype spellings · 0 invented → recipe/fixtures/generated/dialog.chakra.ts
- 4. compile   fixed point ✔ · 2 variants · 35 carried · 1 receipts · recipe 22e17299
- 5. emit      writer.plugin.js (27 KB, page "Recipe Pivot / Dialog / 22e17299-dialog-v6") and writer.scratch.js

## What a person did
- reviewed the role map (drafted with evidence, see roles.draft.json)
- (switch@1 has no glyph)


## What to do next
1. In Figma desktop, open the file the set should live in, open the development plugin, choose **Paste a script**, paste `writer.plugin.js`, run. It creates its own page named "Recipe Pivot / Dialog / 22e17299-dialog-v6" and never touches an existing page.
2. To score it: export the unchecked variant's control and run `npx tsx recipe/fidelity-score.ts --canvas <png> --reference extract/computed/out/chakra/dialog/orig-shots/<off-state>__default.png --label dialog/chakra --out <json> --reference-control-only`.
3. To keep it: add the generated module to the dialog live proof's sources and to the reader's subjects (see checkbox v6 for the pattern).
