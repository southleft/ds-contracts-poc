# menu@1 pointed at mui

- 1. capture   extract/computed/out/mui/menu/captured-truth.json (1 captures)
- 2. roles     from recipe/evidence/pointed/menu-mui/roles.json (reviewed)
- 3. propose   15 leaves read from the ledger · 0 reviewed (named) · 0 archetype spellings · 0 invented → recipe/fixtures/generated/menu.mui.ts
- 4. compile   fixed point ✔ · 2 variants · 25 carried · 1 receipts · recipe 25b7db99
- 5. emit      writer.plugin.js (28 KB, page "Recipe Pivot / Menu / 25b7db99-menu-v7") and writer.scratch.js

## What a person did
- reviewed the role map (supplied)
- (switch@1 has no glyph)


## What to do next
1. In Figma desktop, open the file the set should live in, open the development plugin, choose **Paste a script**, paste `writer.plugin.js`, run. It creates its own page named "Recipe Pivot / Menu / 25b7db99-menu-v7" and never touches an existing page.
2. To score it: export the unchecked variant's control and run `npx tsx recipe/fidelity-score.ts --canvas <png> --reference extract/computed/out/mui/menu/orig-shots/<off-state>__default.png --label menu/mui --out <json> --reference-control-only`.
3. To keep it: add the generated module to the menu live proof's sources and to the reader's subjects (see checkbox v6 for the pattern).
