# radio@1 pointed at antd

- 1. capture   extract/computed/out/antd/radio/captured-truth.json (16 captures)
- 2. roles     DRAFTED from the ledger — combos:high circle:high dot:high label:high row:high hit:medium opacityOn:high (review recipe/evidence/pointed/radio-antd/roles.draft.json)
- 3. propose   32 leaves read from the ledger · 0 reviewed (named) · 4 archetype spellings · 0 invented → recipe/fixtures/generated/radio.antd.ts
- 4. compile   fixed point ✔ · 4 variants · 45 carried · 4 receipts · recipe a645c0bf
- 5. emit      writer.plugin.js (64 KB, page "Recipe Pivot / Radio / a645c0bf-radio-v8") and writer.scratch.js

## What a person did
- reviewed the role map (drafted with evidence, see roles.draft.json)
- (switch@1 has no glyph)


## What to do next
1. In Figma desktop, open the file the set should live in, open the development plugin, choose **Paste a script**, paste `writer.plugin.js`, run. It creates its own page named "Recipe Pivot / Radio / a645c0bf-radio-v8" and never touches an existing page.
2. To score it: export the unchecked variant's control and run `npx tsx recipe/fidelity-score.ts --canvas <png> --reference extract/computed/out/antd/radio/orig-shots/<off-state>__default.png --label radio/antd --out <json> --reference-control-only`.
3. To keep it: add the generated module to the radio live proof's sources and to the reader's subjects (see checkbox v6 for the pattern).
