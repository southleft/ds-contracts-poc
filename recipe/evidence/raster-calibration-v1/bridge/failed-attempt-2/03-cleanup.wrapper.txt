return await (async()=>{const envelope={"protocol":"ds-contracts/figma-writer-utf8-base64/v1","encoding":"utf8+base64","payload":"","payloadBytes":523,"payloadSha256":"8019937844a7752a9ffcae1776f055d8c329068cf964fabbbc6a03e47ad12da5"};
const pureSha256=(bytes)=>{
  const K=[1116352408,1899447441,3049323471,3921009573,961987163,1508970993,2453635748,2870763221,3624381080,310598401,607225278,1426881987,1925078388,2162078206,2614888103,3248222580,3835390401,4022224774,264347078,604807628,770255983,1249150122,1555081692,1996064986,2554220882,2821834349,2952996808,3210313671,3336571891,3584528711,113926993,338241895,666307205,773529912,1294757372,1396182291,1695183700,1986661051,2177026350,2456956037,2730485921,2820302411,3259730800,3345764771,3516065817,3600352804,4094571909,275423344,430227734,506948616,659060556,883997877,958139571,1322822218,1537002063,1747873779,1955562222,2024104815,2227730452,2361852424,2428436474,2756734187,3204031479,3329325298];
  const h=[1779033703,3144134277,1013904242,2773480762,1359893119,2600822924,528734635,1541459225];
  const length=bytes.length;
  const padded=new Uint8Array(Math.ceil((length+9)/64)*64);
  padded.set(bytes);
  padded[length]=128;
  const bitLength=length*8;
  const view=new DataView(padded.buffer);
  view.setUint32(padded.length-8,Math.floor(bitLength/4294967296));
  view.setUint32(padded.length-4,bitLength>>>0);
  const rotr=(x,n)=>(x>>>n)|(x<<(32-n));
  for(let offset=0;offset<padded.length;offset+=64){
    const w=new Uint32Array(64);
    for(let i=0;i<16;i++)w[i]=view.getUint32(offset+i*4);
    for(let i=16;i<64;i++){
      const a=w[i-15],b=w[i-2];
      const s0=rotr(a,7)^rotr(a,18)^(a>>>3);
      const s1=rotr(b,17)^rotr(b,19)^(b>>>10);
      w[i]=(w[i-16]+s0+w[i-7]+s1)>>>0;
    }
    let [a,b,c,d,e,f,g,q]=h;
    for(let i=0;i<64;i++){
      const s1=rotr(e,6)^rotr(e,11)^rotr(e,25);
      const ch=(e&f)^((~e)&g);
      const t1=(q+s1+ch+K[i]+w[i])>>>0;
      const s0=rotr(a,2)^rotr(a,13)^rotr(a,22);
      const maj=(a&b)^(a&c)^(b&c);
      const t2=(s0+maj)>>>0;
      q=g;g=f;f=e;e=(d+t1)>>>0;d=c;c=b;b=a;a=(t1+t2)>>>0;
    }
    h[0]=(h[0]+a)>>>0;h[1]=(h[1]+b)>>>0;h[2]=(h[2]+c)>>>0;h[3]=(h[3]+d)>>>0;
    h[4]=(h[4]+e)>>>0;h[5]=(h[5]+f)>>>0;h[6]=(h[6]+g)>>>0;h[7]=(h[7]+q)>>>0;
  }
  return h.map(value=>value.toString(16).padStart(8,"0")).join("");
};
const pureUtf8=(bytes)=>{
  let result="";
  const continuation=(value)=>value>=128&&value<=191;
  for(let index=0;index<bytes.length;){
    const first=bytes[index++];
    let point;
    if(first<=127){point=first;}
    else if(first>=194&&first<=223){
      const second=bytes[index++];
      if(!continuation(second))throw new Error("WRITER-TRANSPORT-UTF8");
      point=((first&31)<<6)|(second&63);
    }else if(first>=224&&first<=239){
      const second=bytes[index++],third=bytes[index++];
      if(!continuation(second)||!continuation(third)||(first===224&&second<160)||(first===237&&second>159))throw new Error("WRITER-TRANSPORT-UTF8");
      point=((first&15)<<12)|((second&63)<<6)|(third&63);
    }else if(first>=240&&first<=244){
      const second=bytes[index++],third=bytes[index++],fourth=bytes[index++];
      if(!continuation(second)||!continuation(third)||!continuation(fourth)||(first===240&&second<144)||(first===244&&second>143))throw new Error("WRITER-TRANSPORT-UTF8");
      point=((first&7)<<18)|((second&63)<<12)|((third&63)<<6)|(fourth&63);
    }else{throw new Error("WRITER-TRANSPORT-UTF8");}
    if(point<=65535)result+=String.fromCharCode(point);
    else{point-=65536;result+=String.fromCharCode(55296+(point>>10),56320+(point&1023));}
  }
  return result;
};
const state={protocol:envelope.protocol,payloadBytes:envelope.payloadBytes,expectedSha256:envelope.payloadSha256,decodedBytes:null,decodedSha256:null,hashImplementation:null,evalBegan:false};
globalThis.__recipeTransportV3=state;
const fail=(code)=>{state.failure=code;throw new Error(code);};
if(figma.fileKey!=="byMp6lt0Ij9b2QbkDGFwBh"||figma.root.name!=="Scratch Project"||figma.editorType!=="figma")fail("WRITER-TRANSPORT-WRONG-TARGET");
if(envelope.protocol!=="ds-contracts/figma-writer-utf8-base64/v1")fail("WRITER-TRANSPORT-PROTOCOL");
if(envelope.encoding!=="utf8+base64")fail("WRITER-TRANSPORT-ENCODING");
if(!/^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/.test(envelope.payload))fail("WRITER-TRANSPORT-BASE64");
let binary;try{binary=atob(envelope.payload)}catch{fail("WRITER-TRANSPORT-BASE64")}
const bytes=Uint8Array.from(binary,character=>character.charCodeAt(0));
state.decodedBytes=bytes.byteLength;
if(bytes.byteLength!==envelope.payloadBytes)fail("WRITER-TRANSPORT-LENGTH");
let decodedSha256;
if(typeof crypto!=="undefined"&&crypto.subtle){
  const digest=await crypto.subtle.digest("SHA-256",bytes);
  decodedSha256=Array.from(new Uint8Array(digest),value=>value.toString(16).padStart(2,"0")).join("");
  state.hashImplementation="webcrypto";
}else{
  decodedSha256=pureSha256(bytes);
  state.hashImplementation="verified-js-fallback";
}
state.decodedSha256=decodedSha256;
if(decodedSha256!==envelope.payloadSha256)fail("WRITER-TRANSPORT-SHA256");
let source;try{
  if(typeof TextDecoder!=="undefined"){source=new TextDecoder("utf-8",{fatal:true}).decode(bytes);state.utf8Implementation="text-decoder";}
  else{source=pureUtf8(bytes);state.utf8Implementation="verified-js-fallback";}
}catch{fail("WRITER-TRANSPORT-UTF8")}
state.evalBegan=true;
const result=await eval("(async()=>{"+source+"\n})()");
state.evalCompleted=true;
return {transport:state,result};
})();
