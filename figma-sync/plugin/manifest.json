{
  "name": "DS Contracts Sync Runner",
  "id": "ds-contracts-sync-runner",
  "api": "1.0.0",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": [
    "figma"
  ],
  "networkAccess": {
    "allowedDomains": [
      "https://ds-contracts-assist.southleft-llc.workers.dev"
    ],
    "devAllowedDomains": [
      "http://localhost:8765",
      "http://localhost:8787"
    ],
    "reasoning": "Send to Playground POSTs the read-only component dump to the playground's pairing bridge (the workers.dev origin). Dev-only: the local runner fetches generated figma-sync scripts from npm run figma:serve (port 8765), and 8787 is wrangler dev for testing the bridge locally."
  }
}
