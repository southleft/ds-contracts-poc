/**
 * Re-export shim — contract provenance SOURCE moved to
 * packages/core/src/contract-provenance.ts (@ds-contracts/core). Every
 * existing import path (`../core/contract-provenance.js`) keeps working
 * through this shim; there is still exactly ONE implementation.
 */
export * from '../packages/core/src/contract-provenance.js';
