{
  "$schema": "./contract.schema.json",
  "id": "antd.checkbox",
  "name": "Checkbox",
  "version": "0.1.0",
  "status": "draft",
  "description": "Subject: antd@5.29.3 (pinned, examples/antd/PROVENANCE.md). CURATED seed for the antd P2 exam (code→canvas, held out) — props/axes only; anatomy is promoted from captured DOM truth. Every axis value is the library's own declared enum (antd spells them as `readonly [...]` tuples; seed-gen reads them once its declaration lookup ignores directory case — W1, examples/antd/RECON.md §4). THE PSEUDO-GLYPH CASE (FC-PSEUDO-STROKE-GLYPH): the tick is `.ant-checkbox-inner::after`, a rotate(45deg)+scale+translate L-stroke. The tri-state rides ONE enum axis (unchecked|checked|indeterminate) expanded by the config through $props into antd's two booleans (`checked` + `indeterminate`) — one axis so per-value glyph carriage stays a single-axis function (the MUI Checkbox precedent). ROOT = label.ant-checkbox-wrapper: antd forwards className/data-* to the hidden INPUT, not the label (RECON S2) — the harness stamps the stage child, so the captured root is the label.",
  "semantics": {
    "element": "label"
  },
  "props": [
    {
      "name": "checked",
      "type": {
        "enum": [
          "unchecked",
          "checked",
          "indeterminate"
        ]
      },
      "default": "unchecked",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Checked",
          "values": {
            "unchecked": "Unchecked",
            "checked": "Checked",
            "indeterminate": "Indeterminate"
          }
        },
        "code": {
          "prop": "checked"
        }
      }
    },
    {
      "name": "disabled",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Disabled"
        },
        "code": {
          "prop": "disabled"
        }
      }
    },
    {
      "name": "children",
      "type": "text",
      "default": "Checkbox",
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Label"
        },
        "code": {
          "prop": "children"
        }
      }
    }
  ],
  "states": [
    "disabled",
    "focus-visible",
    "active",
    "hover"
  ],
  "anatomy": {
    "root": {}
  },
  "bindings": {
    "figma": {
      "anchors": {
        "fileKey": null,
        "componentSetKey": null
      }
    },
    "code": {
      "anchors": {
        "importPath": "antd",
        "export": "Checkbox"
      }
    }
  }
}
