{
  "_marker": "NON-SCHEMA EXTENSION BLOCK — computed-capture overflow. Nothing here is contract vocabulary; every entry names why it does not fit (DESIGN §5.4).",
  "generatedBy": "extract/computed/regate.ts --write-enriched (OFFLINE RE-FUSE of the committed captured truth; no library recapture). Differences vs the harness path, NAMED: portal receipts are a capture-time artifact and are absent here; the frontier receipts are capture-time too and are CARRIED FORWARD unchanged from the extension this re-fuse overwrote (5 receipt(s)); every other receipt is recomputed from the same truth.",
  "library": "@shadcn-sandbox/ui@0.0.1",
  "browser": "151.0.7922.34",
  "mintedTokens": {
    "imported": {
      "shared": {
        "color-e5e5e5": {
          "$value": "#e5e5e5",
          "$type": "color"
        },
        "size-8": {
          "$value": "8px",
          "$type": "dimension"
        },
        "size-1": {
          "$value": "1px",
          "$type": "dimension"
        },
        "color-a1a1a1": {
          "$value": "#a1a1a1",
          "$type": "color"
        }
      },
      "input": {
        "root": {
          "box-shadow": {
            "$value": "rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0.05) 0px 1px 2px 0px",
            "$type": "shadow"
          },
          "font-size": {
            "$value": "14px",
            "$type": "dimension"
          },
          "height": {
            "$value": "36px",
            "$type": "dimension"
          },
          "line-height": {
            "$value": "20px",
            "$type": "dimension"
          },
          "min-width": {
            "$value": "0px",
            "$type": "dimension"
          },
          "padding-bottom": {
            "$value": "4px",
            "$type": "dimension"
          },
          "padding-left": {
            "$value": "10px",
            "$type": "dimension"
          },
          "padding-right": {
            "$value": "10px",
            "$type": "dimension"
          },
          "padding-top": {
            "$value": "4px",
            "$type": "dimension"
          },
          "width": {
            "$value": "288px",
            "$type": "dimension"
          },
          "box-shadow-state-active": {
            "$value": "rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, oklab(0.708 0 0 / 0.5) 0px 0px 0px 3px, rgba(0, 0, 0, 0.05) 0px 1px 2px 0px",
            "$type": "shadow"
          },
          "outline-width-state-active": {
            "$value": "1px",
            "$type": "dimension"
          },
          "opacity-state-disabled": {
            "$value": "0.5",
            "$type": "number"
          },
          "box-shadow-state-focus-visible": {
            "$value": "rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, oklab(0.708 0 0 / 0.5) 0px 0px 0px 3px, rgba(0, 0, 0, 0.05) 0px 1px 2px 0px",
            "$type": "shadow"
          },
          "outline-width-state-focus-visible": {
            "$value": "1px",
            "$type": "dimension"
          }
        }
      }
    }
  },
  "folds": [],
  "foldedStateSkips": [],
  "layout": {
    "enriched": [],
    "contradictions": [],
    "receipts": [],
    "_note": "computed flex keywords carried via Part.layout (the schema's own vocabulary); carried-slot contradictions are receipts, never silent overrides"
  },
  "declaredFacts": {
    "_note": "v15 (S4): uniform registry-channel facts carried as Part.declared / Part.declaredStates — first-class contract vocabulary, listed here as the enrichment receipt",
    "base": [
      {
        "part": "root",
        "channel": "appearance",
        "value": "auto"
      },
      {
        "part": "root",
        "channel": "border-bottom-style",
        "value": "solid"
      },
      {
        "part": "root",
        "channel": "border-left-style",
        "value": "solid"
      },
      {
        "part": "root",
        "channel": "border-right-style",
        "value": "solid"
      },
      {
        "part": "root",
        "channel": "border-top-style",
        "value": "solid"
      },
      {
        "part": "root",
        "channel": "cursor",
        "value": "text"
      },
      {
        "part": "root",
        "channel": "overflow-x",
        "value": "clip"
      },
      {
        "part": "root",
        "channel": "overflow-y",
        "value": "clip"
      },
      {
        "part": "root",
        "channel": "transition-duration",
        "value": "0.15s"
      },
      {
        "part": "root",
        "channel": "transition-property",
        "value": "color, box-shadow"
      },
      {
        "part": "root",
        "channel": "transition-timing-function",
        "value": "cubic-bezier(0.4, 0, 0.2, 1)"
      }
    ],
    "state": [
      {
        "state": "disabled",
        "part": "root",
        "channel": "cursor",
        "value": "not-allowed"
      },
      {
        "state": "disabled",
        "part": "root",
        "channel": "pointer-events",
        "value": "none"
      }
    ]
  },
  "codeOnlyChannels": [
    {
      "part": "root",
      "channel": "--tw-outline-style",
      "reason": "CSS custom property, not a styled channel — `--tw-outline-style` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `none`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for \"declares a custom property\" and should not invent one.",
      "sample": "none",
      "distinctValues": 1
    },
    {
      "part": "root",
      "channel": "--tw-shadow",
      "reason": "CSS custom property, not a styled channel — `--tw-shadow` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `0 1px 2px 0 rgb(0 0 0 / 0.05)`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for \"declares a custom property\" and should not invent one.",
      "sample": "0 1px 2px 0 rgb(0 0 0 / 0.05)",
      "distinctValues": 1
    }
  ],
  "stateOverflow": [
    {
      "state": "active",
      "part": "root",
      "channel": "--tw-ring-shadow",
      "sample": "0 0 0 calc(3px + 0px) color-mix(in oklab, oklch(0.708 0 0) 50%, transparent)",
      "reason": "CSS custom property, not a styled channel — the same door the base plane applies: a custom property is a token DECLARATION the library sets on this element (its plumbing), never a rendered fact; the rendered state delta rides the consuming channel"
    },
    {
      "state": "focus-visible",
      "part": "root",
      "channel": "--tw-ring-shadow",
      "sample": "0 0 0 calc(3px + 0px) color-mix(in oklab, oklch(0.708 0 0) 50%, transparent)",
      "reason": "CSS custom property, not a styled channel — the same door the base plane applies: a custom property is a token DECLARATION the library sets on this element (its plumbing), never a rendered fact; the rendered state delta rides the consuming channel"
    }
  ],
  "overflowBindings": [],
  "pairwiseRefusals": [],
  "pseudoParts": {
    "_reason": "S5 (DESIGN §5.4): pseudo-element decor has no anatomy spelling — captured, receipted, not carried",
    "findings": [],
    "totalFindings": 0
  },
  "bindingContradictions": [],
  "interactionOnDisabled": [],
  "interactionOnlyPlaneDrops": [],
  "interactionOnlyPlaneDropCount": 0,
  "structureReceipts": [],
  "styledChannelReceipts": [
    "block-root-width-admitted: root — display:block root fills its container in CSS; the captured stage width joins fusion (stage-dependent, receipted — the canvas card draws at the captured block width instead of hugging its text)",
    "token-named-geometry-admitted: root.height — the library's own stylesheet binds it to --spacing (0.25rem) at `.h-9`; a dimension named by a token is a design value, not an environment measurement, so it joins fusion with its name (the absolute-cluster / table / block-root doors are unchanged)",
    "control-fallback: no control for <input> — span control used (part root)",
    "reset-supplied-border-color-admitted: root.border-bottom-color = oklch(0.922 0 0) — EQUAL to the <input> control, so the styled-channel door would normally drop it as \"not a fact of this component\". Admitted anyway because this part draws a real border (border-bottom-width = 1px, border-bottom-style = solid) and the control's own border-bottom-color (oklch(0.922 0 0)) differs from its `color` (oklch(0.145 0 0)) — the UA's border colour is currentcolor, so the control was coloured by the library's GLOBAL CSS (shadcn's `* { border-color: var(--border) }`, Tailwind preflight's equivalents). The control correctly subtracts the reset; the emitted CSS does not REPRODUCE it, so without this the width and style ship and the border paints currentcolor on the code surface and NOTHING on the canvas.",
    "reset-supplied-border-style-admitted: root.border-bottom-style = solid — EQUAL to the <input> control, so the styled-channel door would normally drop it as \"not a fact of this component\". Admitted anyway because this part draws a real border (border-bottom-width = 1px) and the style comes from the library's GLOBAL CSS (Tailwind preflight's `* { border-style: solid }` and its equivalents). The control correctly subtracts the reset; the emitted CSS does not REPRODUCE it, so without this the width and colour ship and the border paints nothing.",
    "reset-supplied-border-color-admitted: root.border-left-color = oklch(0.922 0 0) — EQUAL to the <input> control, so the styled-channel door would normally drop it as \"not a fact of this component\". Admitted anyway because this part draws a real border (border-left-width = 1px, border-left-style = solid) and the control's own border-left-color (oklch(0.922 0 0)) differs from its `color` (oklch(0.145 0 0)) — the UA's border colour is currentcolor, so the control was coloured by the library's GLOBAL CSS (shadcn's `* { border-color: var(--border) }`, Tailwind preflight's equivalents). The control correctly subtracts the reset; the emitted CSS does not REPRODUCE it, so without this the width and style ship and the border paints currentcolor on the code surface and NOTHING on the canvas.",
    "reset-supplied-border-style-admitted: root.border-left-style = solid — EQUAL to the <input> control, so the styled-channel door would normally drop it as \"not a fact of this component\". Admitted anyway because this part draws a real border (border-left-width = 1px) and the style comes from the library's GLOBAL CSS (Tailwind preflight's `* { border-style: solid }` and its equivalents). The control correctly subtracts the reset; the emitted CSS does not REPRODUCE it, so without this the width and colour ship and the border paints nothing.",
    "reset-supplied-border-color-admitted: root.border-right-color = oklch(0.922 0 0) — EQUAL to the <input> control, so the styled-channel door would normally drop it as \"not a fact of this component\". Admitted anyway because this part draws a real border (border-right-width = 1px, border-right-style = solid) and the control's own border-right-color (oklch(0.922 0 0)) differs from its `color` (oklch(0.145 0 0)) — the UA's border colour is currentcolor, so the control was coloured by the library's GLOBAL CSS (shadcn's `* { border-color: var(--border) }`, Tailwind preflight's equivalents). The control correctly subtracts the reset; the emitted CSS does not REPRODUCE it, so without this the width and style ship and the border paints currentcolor on the code surface and NOTHING on the canvas.",
    "reset-supplied-border-style-admitted: root.border-right-style = solid — EQUAL to the <input> control, so the styled-channel door would normally drop it as \"not a fact of this component\". Admitted anyway because this part draws a real border (border-right-width = 1px) and the style comes from the library's GLOBAL CSS (Tailwind preflight's `* { border-style: solid }` and its equivalents). The control correctly subtracts the reset; the emitted CSS does not REPRODUCE it, so without this the width and colour ship and the border paints nothing.",
    "reset-supplied-border-color-admitted: root.border-top-color = oklch(0.922 0 0) — EQUAL to the <input> control, so the styled-channel door would normally drop it as \"not a fact of this component\". Admitted anyway because this part draws a real border (border-top-width = 1px, border-top-style = solid) and the control's own border-top-color (oklch(0.922 0 0)) differs from its `color` (oklch(0.145 0 0)) — the UA's border colour is currentcolor, so the control was coloured by the library's GLOBAL CSS (shadcn's `* { border-color: var(--border) }`, Tailwind preflight's equivalents). The control correctly subtracts the reset; the emitted CSS does not REPRODUCE it, so without this the width and style ship and the border paints currentcolor on the code surface and NOTHING on the canvas.",
    "reset-supplied-border-style-admitted: root.border-top-style = solid — EQUAL to the <input> control, so the styled-channel door would normally drop it as \"not a fact of this component\". Admitted anyway because this part draws a real border (border-top-width = 1px) and the style comes from the library's GLOBAL CSS (Tailwind preflight's `* { border-style: solid }` and its equivalents). The control correctly subtracts the reset; the emitted CSS does not REPRODUCE it, so without this the width and colour ship and the border paints nothing.",
    "geometry-excluded: root — block-size 36px, inline-size 288px, perspective-origin 144px 18px, transform-origin 144px 18px — FC-GEOMETRY-EXCLUDED (Option B): box geometry is environment-dependent and is not fused; it is admitted only through the absolute-cluster, table-cell and block-root doors, none of which this part passed. The canvas sizes the box from its carried content, padding and min/max channels."
  ],
  "frontierReceipts": [
    "closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.",
    "pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::placeholder (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.",
    "pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight",
    "pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, \"::backdrop\") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit",
    "pseudo-elements-refused: 2 (::selection, ::backdrop)"
  ],
  "anatomyJoin": {
    "computed": [
      {
        "part": "root",
        "join": "matched"
      }
    ],
    "staticOnly": []
  },
  "anatomyPromotion": {
    "_note": "Round 4 DOM-anatomy promotion: computed-only elements carried as REAL parts (extract/computed/anatomy.ts); svg subtrees carried as reconstructed icon assets; presence facts via visibleWhen/stylesWhen; refusals named.",
    "partsCarried": [
      "root"
    ],
    "svgAssets": [],
    "receipts": [],
    "refusals": []
  },
  "enrichmentNotes": [
    "declared fact carried: root.appearance = auto (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: root.border-bottom-style = solid (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: root.border-left-style = solid (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: root.border-right-style = solid (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: root.border-top-style = solid (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: root.cursor = text (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: root.overflow-x = clip (v15 declared vocabulary — canvas-drawable)",
    "declared fact carried: root.overflow-y = clip (v15 declared vocabulary — canvas-drawable)",
    "declared fact carried: root.transition-duration = 0.15s (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: root.transition-property = color, box-shadow (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: root.transition-timing-function = cubic-bezier(0.4, 0, 0.2, 1) (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared state fact carried: [disabled] root.cursor = not-allowed (v15 declared vocabulary)",
    "declared state fact carried: [disabled] root.pointer-events = none (v15 declared vocabulary)"
  ]
}
