{
  "$schema": "./contract.schema.json",
  "id": "chakra.textarea-field",
  "name": "TextareaField",
  "version": "0.1.0",
  "status": "draft",
  "description": "SEED contract for the Chakra UI Field + Textarea capture — props/axes only; anatomy is promoted from captured DOM truth. Subject: @chakra-ui/react@3.37.0. Composition from the package's flat exports: FieldRoot ⊃ (FieldLabel, Textarea). Authored 2026-09-02 as the person's step of docs/36 for textarea@1's labelled plane: the content axis's value lives on the Textarea child, so the config entry uses the $childProps axis form; disabled is the Field root's, which the Textarea reads from the field context.",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "content",
      "type": {
        "enum": [
          "empty",
          "value"
        ]
      },
      "default": "empty",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Content",
          "values": {
            "empty": "Empty",
            "value": "Value"
          }
        },
        "code": {
          "prop": "value"
        }
      }
    },
    {
      "name": "disabled",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Disabled"
        },
        "code": {
          "prop": "disabled"
        }
      }
    }
  ],
  "states": [
    "disabled",
    "focus-visible",
    "active",
    "hover"
  ],
  "anatomy": {
    "root": {}
  },
  "bindings": {
    "figma": {
      "anchors": {
        "fileKey": null,
        "componentSetKey": null
      }
    },
    "code": {
      "anchors": {
        "importPath": "@chakra-ui/react",
        "export": "Textarea"
      }
    }
  }
}
