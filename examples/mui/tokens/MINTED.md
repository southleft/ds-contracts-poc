# MUI minted tokens — promotion receipt

Generated by `examples/mui/scripts/promote-floor.mjs`.

- **134 leaves source-aliased** to MUI's own CSS-variable-named tokens (value-verified twice: capture + promotion)
- **1702 leaves kept literal** (no verified source reference)
- **0 named alias refusals**

- **4 authored fact(s) applied** from `examples/mui/authored-facts.json` — reviewed hand-authoring the capture cannot carry; every row names its cause, and a row the capture has since learned to carry REFUSES the promotion by name:
  - accordion.collapse-root: set declared overflow-x, overflow-y — Collapsed cells leaked the body copy past the card on canvas: the 0-height collapse-root did not clip (VISUAL-REVIEW-2026-08-17, fail-closed FC-ABS-SIZE). MUI's Collapse is overflow:hidden while collapsed and overflow:visible once entered, so the computed truth varies across the expanded axis and the capture carries no declared fact (LEDGER.md: 'declared-channel value varies across combos — declared facts carry uniform values only (v15)'). The clip is authored for both planes: the collapsed cell needs it and the expanded body fits its own frame.
  - avatar.root: set tokens width, height; mint imported.avatar.root.width, imported.avatar.root.height — The canvas hugged the initial glyph (14×20) where MUI draws a 40×40 avatar (VISUAL-REVIEW-2026-08-17, fail-closed FC-GEOMETRY-EXCLUDED). The captured root box IS 40×40 (captured-truth.json base root: width 40px, height 40px, display flex) but the fusion geometry door admits width/height only for absolute clusters, table cells and block roots — this is a flex root, so nothing minted. Authored from the captured box; FC-GEOMETRY-EXCLUDED stays closed (Path B authoring, not a fuse relaxation).
  - fab.root: set tokensByProp size=large|medium|small width, height; mint imported.fab.root.width.small, imported.fab.root.width.medium, imported.fab.root.width.large, imported.fab.root.height.small, imported.fab.root.height.medium, imported.fab.root.height.large — The canvas drew 8×36 capsules hugging the '+' glyph where MUI draws 56/48/40 circles per size (VISUAL-REVIEW-2026-08-17, fail-closed FC-GEOMETRY-EXCLUDED). The captured root box is 56×56 at large (captured-truth.json base root, display flex) and MUI's sizeSmall/sizeMedium are 40/48; the fusion geometry door refuses a flex root's width/height, so nothing minted. Authored per size from the captured boxes; FC-GEOMETRY-EXCLUDED stays closed.
  - link.root: unset tokens width; prune imported.link.root.width — The minted 30.2188px root width is the capture font's measure of the sample text 'Link' (Roboto 16px, admitted through the block-root-width door as a shrink-to-fit block) — a harness fact, not the library's. Bound, it wrapped the Inter-rendered label 'Lin/k' in every cell (VISUAL-REVIEW-2026-08-17, fail-closed FC-TEXT-WRAP). Unbound so the label hugs; the leaf is pruned so no Figma variable ships that nothing binds (task #42 cause B).
