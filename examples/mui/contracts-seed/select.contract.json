{
  "$schema": "./contract.schema.json",
  "id": "mui.select",
  "name": "Select",
  "version": "0.1.0",
  "status": "draft",
  "description": "SEED contract for the MUI 50% denominator round — props/axes only; anatomy is promoted from captured DOM truth. Bounded capture: value + MenuItem children pinned; menu held CONTROLLED-CLOSED (open=false — TablePagination select precedent). Small axis: size only. Subject: @mui/material@9.2.0 (pinned, examples/mui/PROVENANCE.md).",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "size",
      "type": {
        "enum": ["small", "medium"]
      },
      "default": "medium",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Size",
          "values": {
            "small": "Small",
            "medium": "Medium"
          }
        },
        "code": { "prop": "size" }
      }
    }
  ],
  "states": [],
  "anatomy": {
    "root": {}
  },
  "anchors": {
    "figma": {
      "fileKey": null,
      "componentSetKey": null
    },
    "code": {
      "importPath": "@mui/material",
      "export": "Select"
    }
  }
}
