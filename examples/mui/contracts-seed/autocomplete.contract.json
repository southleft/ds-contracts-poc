{
  "$schema": "./contract.schema.json",
  "id": "mui.autocomplete",
  "name": "Autocomplete",
  "version": "0.1.0",
  "status": "draft",
  "description": "SEED contract for the MUI molecule round — props/axes only; anatomy is promoted from captured DOM truth. Multi-select composition: multiple + options + controlled value (two chips in the input); renderInput is the identity render-prop marker {\"$render\":\"@mui/material#TextField\"} — the only function shape the config vocabulary admits. The open listbox popper is NOT captured this round (portal listbox — named residual, PROVENANCE.md). Subject: @mui/material@9.2.0 (pinned, examples/mui/PROVENANCE.md).",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "size",
      "type": {
        "enum": [
          "small",
          "medium"
        ]
      },
      "default": "medium",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Size",
          "values": {
            "small": "Small",
            "medium": "Medium"
          }
        },
        "code": {
          "prop": "size"
        }
      }
    }
  ],
  "states": [
    "hover",
    "focus-visible",
    "active"
  ],
  "anatomy": {
    "root": {}
  },
  "bindings": {
    "figma": {
      "anchors": {
        "fileKey": null,
        "componentSetKey": null
      }
    },
    "code": {
      "anchors": {
        "importPath": "@mui/material",
        "export": "Autocomplete"
      }
    }
  }
}
