{
  "component": "Tag",
  "contractId": "polaris.tag",
  "contractVersion": "0.3.0",
  "generatedBy": "extract/figma/canvas-gate/run.ts",
  "sides": {
    "canvas": "createFigmaEngine().compileComponentData rendered by the playground canvas renderer (vendored), deviceScaleFactor 2",
    "real": "@shopify/polaris@13.9.5 in 148.0.7778.96 (playwright-core, headless), deviceScaleFactor 2"
  },
  "scoring": {
    "pctExactUnmasked": "pixelmatch threshold 0, includeAA true — every differing pixel",
    "pctAAUnmasked": "pixelmatch threshold 0.1, includeAA false — antialiased pixels excluded by the classifier",
    "pctAAMasked": "the AA point with BOTH sides' DOM text rects (+4 device px) removed from numerator and denominator",
    "sizePolicy": "content-trimmed crops center-padded onto the UNION canvas; a size delta stays in the diff (never cropped to match)"
  },
  "fonts": {
    "interAvailableInBrowser": true
  },
  "cells": [
    {
      "cell": "Size=large",
      "kind": "base",
      "pctExactUnmasked": 14.76,
      "pctAAUnmasked": 13.76,
      "pctAAMasked": 13.76,
      "maskCoveragePct": 0,
      "canvasPx": "96x96",
      "realPx": "32x48",
      "inkCanvasPct": 0,
      "inkRealPct": 14.5,
      "note": "CANVAS SIDE (NEARLY) BLANK: canvas ink 0% vs real ink 14.5% of the union canvas — the masked % understates the miss; EMITTER-DEFECT (channel table confirms): contract binds root padding-left/right = {imported.tag.root.padding-left} = 6px, but emit-figma-script applyTokens handles only the padding-inline/padding-block shorthands — longhand padding channels are silently dropped, so the canvas pill draws 12px narrower than the real Tag"
    },
    {
      "cell": "Size=large, State=Disabled",
      "kind": "state",
      "state": "disabled",
      "pctExactUnmasked": 14.76,
      "pctAAUnmasked": 0,
      "pctAAMasked": 0,
      "maskCoveragePct": 0,
      "canvasPx": "96x96",
      "realPx": "32x48",
      "inkCanvasPct": 0,
      "inkRealPct": 14.37,
      "note": "CANVAS SIDE (NEARLY) BLANK: canvas ink 0% vs real ink 14.37% of the union canvas — the masked % understates the miss; EMITTER-DEFECT (channel table confirms): contract binds root padding-left/right = {imported.tag.root.padding-left} = 6px, but emit-figma-script applyTokens handles only the padding-inline/padding-block shorthands — longhand padding channels are silently dropped, so the canvas pill draws 12px narrower than the real Tag"
    },
    {
      "cell": "Size=large, State=Active",
      "kind": "state",
      "state": "active",
      "pctExactUnmasked": 81.81,
      "pctAAUnmasked": 45.63,
      "pctAAMasked": 45.63,
      "maskCoveragePct": 0,
      "canvasPx": "12x12",
      "realPx": "60x48",
      "inkCanvasPct": 4.31,
      "inkRealPct": 78.89,
      "note": "CANVAS SIDE (NEARLY) BLANK: canvas ink 4.31% vs real ink 78.89% of the union canvas — the masked % understates the miss; EMITTER-DEFECT (channel table confirms): contract binds root padding-left/right = {imported.tag.root.padding-left} = 6px, but emit-figma-script applyTokens handles only the padding-inline/padding-block shorthands — longhand padding channels are silently dropped, so the canvas pill draws 12px narrower than the real Tag"
    },
    {
      "cell": "Size=large, State=Focus Visible",
      "kind": "state",
      "state": "focus-visible",
      "pctExactUnmasked": 88.28,
      "pctAAUnmasked": 80.73,
      "pctAAMasked": 80.73,
      "maskCoveragePct": 0,
      "canvasPx": "12x12",
      "realPx": "32x48",
      "inkCanvasPct": 8.07,
      "inkRealPct": 86.98,
      "note": "CANVAS SIDE (NEARLY) BLANK: canvas ink 8.07% vs real ink 86.98% of the union canvas — the masked % understates the miss; real side :focus-visible did not match (component root is not keyboard-focusable in this mount) — the canvas draws the contract's focus preview; the real side shows the resting paint; outline→stroke approximation (documented canvas fidelity note): the web focus ring is an outline OUTSIDE the border box; the canvas draws it as a bound stroke inside layout — geometry shifts by the ring width; EMITTER-DEFECT (channel table confirms): contract binds root padding-left/right = {imported.tag.root.padding-left} = 6px, but emit-figma-script applyTokens handles only the padding-inline/padding-block shorthands — longhand padding channels are silently dropped, so the canvas pill draws 12px narrower than the real Tag"
    }
  ],
  "summary": {
    "cells": 4,
    "meanExactUnmasked": 49.9,
    "meanAAUnmasked": 35.03,
    "meanAAMasked": 35.03,
    "maxAAMasked": 80.73,
    "worstCell": "Size=large, State=Focus Visible",
    "cellsOver10Masked": [
      {
        "cell": "Size=large",
        "pctAAMasked": 13.76,
        "note": "CANVAS SIDE (NEARLY) BLANK: canvas ink 0% vs real ink 14.5% of the union canvas — the masked % understates the miss; EMITTER-DEFECT (channel table confirms): contract binds root padding-left/right = {imported.tag.root.padding-left} = 6px, but emit-figma-script applyTokens handles only the padding-inline/padding-block shorthands — longhand padding channels are silently dropped, so the canvas pill draws 12px narrower than the real Tag"
      },
      {
        "cell": "Size=large, State=Active",
        "pctAAMasked": 45.63,
        "note": "CANVAS SIDE (NEARLY) BLANK: canvas ink 4.31% vs real ink 78.89% of the union canvas — the masked % understates the miss; EMITTER-DEFECT (channel table confirms): contract binds root padding-left/right = {imported.tag.root.padding-left} = 6px, but emit-figma-script applyTokens handles only the padding-inline/padding-block shorthands — longhand padding channels are silently dropped, so the canvas pill draws 12px narrower than the real Tag"
      },
      {
        "cell": "Size=large, State=Focus Visible",
        "pctAAMasked": 80.73,
        "note": "CANVAS SIDE (NEARLY) BLANK: canvas ink 8.07% vs real ink 86.98% of the union canvas — the masked % understates the miss; real side :focus-visible did not match (component root is not keyboard-focusable in this mount) — the canvas draws the contract's focus preview; the real side shows the resting paint; outline→stroke approximation (documented canvas fidelity note): the web focus ring is an outline OUTSIDE the border box; the canvas draws it as a bound stroke inside layout — geometry shifts by the ring width; EMITTER-DEFECT (channel table confirms): contract binds root padding-left/right = {imported.tag.root.padding-left} = 6px, but emit-figma-script applyTokens handles only the padding-inline/padding-block shorthands — longhand padding channels are silently dropped, so the canvas pill draws 12px narrower than the real Tag"
      }
    ],
    "blankCanvasCells": [
      "Size=large",
      "Size=large, State=Disabled",
      "Size=large, State=Active",
      "Size=large, State=Focus Visible"
    ]
  },
  "acceptance": {
    "maskedMeanLE5": false,
    "allCellsOver10Named": true,
    "noBlankCanvasCells": false
  }
}
