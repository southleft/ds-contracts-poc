{
  "_marker": "NON-SCHEMA EXTENSION BLOCK — computed-capture overflow. Nothing here is contract vocabulary; every entry names why it does not fit (DESIGN §5.4).",
  "generatedBy": "extract/computed/run.ts",
  "library": "@carbon/react@1.112.0",
  "browser": "149.0.7827.55",
  "mintedTokens": {
    "imported": {
      "shared": {
        "size-0": {
          "$value": "0px",
          "$type": "dimension"
        },
        "size-16": {
          "$value": "16px",
          "$type": "dimension"
        },
        "color-16161640": {
          "$value": "#16161640",
          "$type": "color"
        }
      },
      "accordion": {
        "root": {
          "width": {
            "$value": "328px",
            "$type": "dimension"
          }
        },
        "accordion-item": {
          "border-bottom-color": {
            "$value": "#e0e0e0",
            "$type": "color"
          },
          "border-bottom-width": {
            "$value": "1px",
            "$type": "dimension"
          },
          "border-top-color": {
            "$value": "#e0e0e0",
            "$type": "color"
          },
          "border-top-width": {
            "$value": "1px",
            "$type": "dimension"
          }
        },
        "accordion-heading": {
          "background-color": {
            "$value": "#00000000",
            "$type": "color"
          },
          "color": {
            "$value": "#161616",
            "$type": "color"
          },
          "min-height": {
            "unset": {
              "$value": "40px",
              "$type": "dimension"
            },
            "sm": {
              "$value": "32px",
              "$type": "dimension"
            },
            "md": {
              "$value": "40px",
              "$type": "dimension"
            },
            "lg": {
              "$value": "48px",
              "$type": "dimension"
            }
          },
          "box-shadow-state-focus-visible": {
            "$value": "rgba(15, 98, 254, 1) 0px -1px 0px 0px, rgba(15, 98, 254, 1) 0px 1px 0px 0px inset, rgba(15, 98, 254, 1) 2px 0px 0px 0px inset, rgba(15, 98, 254, 1) 0px 1px 0px 0px, rgba(15, 98, 254, 1) 0px -1px 0px 0px inset, rgba(15, 98, 254, 1) -2px 0px 0px 0px inset",
            "$type": "shadow"
          },
          "z-index-state-focus-visible": {
            "$value": "2",
            "$type": "number"
          }
        },
        "icon": {
          "fill": {
            "$value": "#161616",
            "$type": "color"
          },
          "flex-shrink": {
            "$value": "0",
            "$type": "number"
          },
          "margin-left": {
            "start": {
              "$value": "16px",
              "$type": "dimension"
            },
            "end": {
              "$value": "0px",
              "$type": "dimension"
            }
          },
          "margin-top": {
            "start": {
              "$value": "2px",
              "$type": "dimension"
            },
            "end": {
              "$value": "0px",
              "$type": "dimension"
            }
          },
          "outline-color": {
            "$value": "#00000000",
            "$type": "color"
          },
          "outline-offset": {
            "$value": "-2px",
            "$type": "dimension"
          },
          "outline-width": {
            "$value": "2px",
            "$type": "dimension"
          }
        },
        "label": {
          "font-size": {
            "$value": "14px",
            "$type": "dimension"
          },
          "font-weight": {
            "$value": "400",
            "$type": "number"
          },
          "letter-spacing": {
            "$value": "0.16px",
            "$type": "dimension"
          },
          "line-height": {
            "$value": "20px",
            "$type": "dimension"
          },
          "margin-right": {
            "start": {
              "$value": "16px",
              "$type": "dimension"
            },
            "end": {
              "$value": "0px",
              "$type": "dimension"
            }
          },
          "z-index": {
            "$value": "1",
            "$type": "number"
          }
        },
        "accordion-wrapper": {
          "padding-bottom": {
            "$value": "24px",
            "$type": "dimension"
          },
          "padding-top": {
            "$value": "8px",
            "$type": "dimension"
          }
        },
        "label-2": {
          "font-weight": {
            "$value": "400",
            "$type": "number"
          },
          "margin-left": {
            "start": {
              "$value": "32px",
              "$type": "dimension"
            },
            "end": {
              "$value": "0px",
              "$type": "dimension"
            }
          },
          "padding-right": {
            "$value": "82px",
            "$type": "dimension"
          }
        }
      }
    }
  },
  "folds": [
    {
      "part": "accordion__heading",
      "channel": "border-bottom-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "border-left-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "border-right-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "border-top-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "caret-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "column-rule-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "outline-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "row-rule-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "text-decoration-color",
      "foldedInto": "color",
      "class": "currentColor"
    },
    {
      "part": "accordion__heading",
      "channel": "text-emphasis-color",
      "foldedInto": "color",
      "class": "currentColor"
    }
  ],
  "foldedStateSkips": [
    "fold-carries-state-delta: [disabled] accordion__heading.border-bottom-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.border-left-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.border-right-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.border-top-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.caret-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.column-rule-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.outline-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.row-rule-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.text-decoration-color rides its source fact",
    "fold-carries-state-delta: [disabled] accordion__heading.text-emphasis-color rides its source fact"
  ],
  "layout": {
    "enriched": [
      {
        "part": "accordion__heading",
        "field": "align",
        "value": "center"
      },
      {
        "part": "accordion__heading",
        "field": "justify",
        "value": "start"
      }
    ],
    "contradictions": [],
    "receipts": [
      "layout-not-uniform: accordion__heading.flex-direction varies across combos — stays code-only"
    ],
    "_note": "computed flex keywords carried via Part.layout (the schema's own vocabulary); carried-slot contradictions are receipts, never silent overrides"
  },
  "declaredFacts": {
    "_note": "v15 (S4): uniform registry-channel facts carried as Part.declared / Part.declaredStates — first-class contract vocabulary, listed here as the enrichment receipt",
    "base": [
      {
        "part": "accordion__item",
        "channel": "border-bottom-style",
        "value": "solid"
      },
      {
        "part": "accordion__item",
        "channel": "border-top-style",
        "value": "solid"
      },
      {
        "part": "accordion__item",
        "channel": "transition-duration",
        "value": "0.11s"
      },
      {
        "part": "accordion__item",
        "channel": "transition-property",
        "value": "border-color"
      },
      {
        "part": "accordion__item",
        "channel": "transition-timing-function",
        "value": "cubic-bezier(0.2, 0, 0.38, 0.9)"
      },
      {
        "part": "accordion__heading",
        "channel": "appearance",
        "value": "none"
      },
      {
        "part": "accordion__heading",
        "channel": "border-bottom-style",
        "value": "none"
      },
      {
        "part": "accordion__heading",
        "channel": "border-left-style",
        "value": "none"
      },
      {
        "part": "accordion__heading",
        "channel": "border-right-style",
        "value": "none"
      },
      {
        "part": "accordion__heading",
        "channel": "border-top-style",
        "value": "none"
      },
      {
        "part": "accordion__heading",
        "channel": "cursor",
        "value": "pointer"
      },
      {
        "part": "accordion__heading",
        "channel": "position",
        "value": "relative"
      },
      {
        "part": "accordion__heading",
        "channel": "text-align",
        "value": "start"
      },
      {
        "part": "accordion__heading",
        "channel": "transition-duration",
        "value": "0.11s"
      },
      {
        "part": "accordion__heading",
        "channel": "transition-property",
        "value": "background-color"
      },
      {
        "part": "accordion__heading",
        "channel": "transition-timing-function",
        "value": "cubic-bezier(0.2, 0, 0.38, 0.9)"
      },
      {
        "part": "icon",
        "channel": "cursor",
        "value": "pointer"
      },
      {
        "part": "icon",
        "channel": "font-feature-settings",
        "value": "normal"
      },
      {
        "part": "icon",
        "channel": "outline-style",
        "value": "solid"
      },
      {
        "part": "icon",
        "channel": "overflow-x",
        "value": "hidden"
      },
      {
        "part": "icon",
        "channel": "overflow-y",
        "value": "hidden"
      },
      {
        "part": "icon",
        "channel": "text-rendering",
        "value": "auto"
      },
      {
        "part": "icon",
        "channel": "transition-duration",
        "value": "0.11s"
      },
      {
        "part": "icon",
        "channel": "transition-timing-function",
        "value": "cubic-bezier(0.2, 0, 0.38, 0.9)"
      },
      {
        "part": "label",
        "channel": "cursor",
        "value": "pointer"
      },
      {
        "part": "label",
        "channel": "text-rendering",
        "value": "auto"
      },
      {
        "part": "accordion__wrapper",
        "channel": "max-height",
        "value": "fit-content"
      },
      {
        "part": "accordion__wrapper",
        "channel": "transition-duration",
        "value": "0.11s"
      },
      {
        "part": "accordion__wrapper",
        "channel": "transition-timing-function",
        "value": "cubic-bezier(0, 0, 0.38, 0.9)"
      }
    ],
    "state": [
      {
        "state": "disabled",
        "part": "accordion__heading",
        "channel": "cursor",
        "value": "not-allowed"
      },
      {
        "state": "disabled",
        "part": "icon",
        "channel": "cursor",
        "value": "not-allowed"
      },
      {
        "state": "disabled",
        "part": "label",
        "channel": "cursor",
        "value": "not-allowed"
      }
    ]
  },
  "codeOnlyChannels": [
    {
      "part": "root",
      "channel": "list-style-type",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "none",
      "distinctValues": 1
    },
    {
      "part": "root",
      "channel": "unicode-bidi",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "isolate",
      "distinctValues": 1
    },
    {
      "part": "accordion__item",
      "channel": "list-style-type",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "none",
      "distinctValues": 1
    },
    {
      "part": "accordion__item",
      "channel": "unicode-bidi",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "isolate",
      "distinctValues": 1
    },
    {
      "part": "accordion__heading",
      "channel": "flex-direction",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "row",
      "distinctValues": 2
    },
    {
      "part": "accordion__heading",
      "channel": "list-style-type",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "none",
      "distinctValues": 1
    },
    {
      "part": "icon",
      "channel": "line-height",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "normal",
      "distinctValues": 1
    },
    {
      "part": "icon",
      "channel": "list-style-type",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "none",
      "distinctValues": 1
    },
    {
      "part": "icon",
      "channel": "overflow-clip-margin",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "content-box",
      "distinctValues": 1
    },
    {
      "part": "icon",
      "channel": "transform",
      "reason": "declared-channel value outside the bounded grammar — named residue (v15)",
      "sample": "matrix(0, -1, 1, 0, 0, 0)",
      "distinctValues": 1
    },
    {
      "part": "label",
      "channel": "list-style-type",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "none",
      "distinctValues": 1
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "list-style-type",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "max-height",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "min-height",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "min-width",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "opacity",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "overflow-x",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "overflow-y",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "transition-behavior",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "transition-duration",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper-2",
      "channel": "transition-timing-function",
      "reason": "part absent in every default-state combo (interaction-only part) — state rounds own it",
      "sample": "<no default-state observation>",
      "distinctValues": 0
    },
    {
      "part": "accordion__wrapper",
      "channel": "list-style-type",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "none",
      "distinctValues": 1
    },
    {
      "part": "accordion__wrapper",
      "channel": "transition-behavior",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "allow-discrete",
      "distinctValues": 1
    },
    {
      "part": "label-2",
      "channel": "list-style-type",
      "reason": "value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today",
      "sample": "none",
      "distinctValues": 1
    }
  ],
  "stateOverflow": [
    {
      "state": "disabled",
      "part": "icon",
      "channel": "transform",
      "sample": "matrix(0, 1, -1, 0, 0, 0)",
      "reason": "value shape outside mintable kinds and outside the declared vocabulary"
    }
  ],
  "overflowBindings": [
    {
      "part": "icon",
      "channel": "border-bottom-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "border-left-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "border-right-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "border-top-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "caret-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "column-rule-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "fill",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "row-rule-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "text-decoration-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "icon",
      "channel": "text-emphasis-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "border-bottom-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "border-left-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "border-right-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "border-top-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "caret-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "column-rule-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "outline-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "row-rule-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "text-decoration-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "label",
      "channel": "text-emphasis-color",
      "state": "disabled",
      "ref": "{imported.shared.color-16161640}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "accordion__heading",
      "channel": "box-shadow",
      "state": "focus-visible",
      "ref": "{imported.accordion.accordion-heading.box-shadow-state-focus-visible}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    },
    {
      "part": "accordion__heading",
      "channel": "z-index",
      "state": "focus-visible",
      "ref": "{imported.accordion.accordion-heading.z-index-state-focus-visible}",
      "refusal": "v13 Part.states carries plain color-kind refs only on non-root parts"
    }
  ],
  "pairwiseRefusals": [],
  "pseudoParts": {
    "_reason": "S5 (DESIGN §5.4): pseudo-element decor has no anatomy spelling — captured, receipted, not carried",
    "findings": [],
    "totalFindings": 0
  },
  "bindingContradictions": [],
  "interactionOnDisabled": [
    "interaction-on-disabled-changed: start.unset.disabled hover accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: start.unset.disabled hover accordion__item.border-top-color",
    "interaction-on-disabled-changed: start.unset.disabled active accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: start.unset.disabled active accordion__item.border-top-color",
    "interaction-on-disabled-changed: start.sm.disabled hover accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: start.sm.disabled hover accordion__item.border-top-color",
    "interaction-on-disabled-changed: start.sm.disabled active accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: start.sm.disabled active accordion__item.border-top-color",
    "interaction-on-disabled-changed: start.md.disabled hover accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: start.md.disabled hover accordion__item.border-top-color",
    "interaction-on-disabled-changed: start.md.disabled active accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: start.md.disabled active accordion__item.border-top-color",
    "interaction-on-disabled-changed: start.lg.disabled hover accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: start.lg.disabled hover accordion__item.border-top-color",
    "interaction-on-disabled-changed: start.lg.disabled active accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: start.lg.disabled active accordion__item.border-top-color",
    "interaction-on-disabled-changed: end.unset.disabled hover accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: end.unset.disabled hover accordion__item.border-top-color",
    "interaction-on-disabled-changed: end.unset.disabled active accordion__item.border-bottom-color",
    "interaction-on-disabled-changed: end.unset.disabled active accordion__item.border-top-color"
  ],
  "structureReceipts": [
    "union-part-added: start.unset.disabled__default @0.1 (div|accordion__wrapper|sr-only)",
    "union-part-added: start.unset.disabled__default @0.1.0 (div|accordion__content)",
    "part-missing: start.unset.disabled__default accordion__wrapper",
    "part-missing: start.unset.disabled__default label-2",
    "part-missing: start.unset.disabled__hover accordion__wrapper",
    "part-missing: start.unset.disabled__hover label-2",
    "part-missing: start.unset.disabled__focus-visible accordion__wrapper",
    "part-missing: start.unset.disabled__focus-visible label-2",
    "part-missing: start.unset.disabled__active accordion__wrapper",
    "part-missing: start.unset.disabled__active label-2",
    "part-missing: start.sm.disabled__default accordion__wrapper",
    "part-missing: start.sm.disabled__default label-2",
    "part-missing: start.sm.disabled__hover accordion__wrapper",
    "part-missing: start.sm.disabled__hover label-2",
    "part-missing: start.sm.disabled__focus-visible accordion__wrapper",
    "part-missing: start.sm.disabled__focus-visible label-2",
    "part-missing: start.sm.disabled__active accordion__wrapper",
    "part-missing: start.sm.disabled__active label-2",
    "part-missing: start.md.disabled__default accordion__wrapper",
    "part-missing: start.md.disabled__default label-2",
    "part-missing: start.md.disabled__hover accordion__wrapper",
    "part-missing: start.md.disabled__hover label-2",
    "part-missing: start.md.disabled__focus-visible accordion__wrapper",
    "part-missing: start.md.disabled__focus-visible label-2",
    "part-missing: start.md.disabled__active accordion__wrapper",
    "part-missing: start.md.disabled__active label-2",
    "part-missing: start.lg.disabled__default accordion__wrapper",
    "part-missing: start.lg.disabled__default label-2",
    "part-missing: start.lg.disabled__hover accordion__wrapper",
    "part-missing: start.lg.disabled__hover label-2",
    "part-missing: start.lg.disabled__focus-visible accordion__wrapper",
    "part-missing: start.lg.disabled__focus-visible label-2",
    "part-missing: start.lg.disabled__active accordion__wrapper",
    "part-missing: start.lg.disabled__active label-2",
    "part-missing: end.unset.disabled__default accordion__wrapper",
    "part-missing: end.unset.disabled__default label-2",
    "part-missing: end.unset.disabled__hover accordion__wrapper",
    "part-missing: end.unset.disabled__hover label-2",
    "part-missing: end.unset.disabled__focus-visible accordion__wrapper",
    "part-missing: end.unset.disabled__focus-visible label-2",
    "part-missing: end.unset.disabled__active accordion__wrapper",
    "part-missing: end.unset.disabled__active label-2",
    "part-missing: end.sm.disabled__default accordion__wrapper",
    "part-missing: end.sm.disabled__default label-2",
    "part-missing: end.sm.disabled__hover accordion__wrapper",
    "part-missing: end.sm.disabled__hover label-2",
    "part-missing: end.sm.disabled__focus-visible accordion__wrapper",
    "part-missing: end.sm.disabled__focus-visible label-2",
    "part-missing: end.sm.disabled__active accordion__wrapper",
    "part-missing: end.sm.disabled__active label-2",
    "part-missing: end.md.disabled__default accordion__wrapper",
    "part-missing: end.md.disabled__default label-2",
    "part-missing: end.md.disabled__hover accordion__wrapper",
    "part-missing: end.md.disabled__hover label-2",
    "part-missing: end.md.disabled__focus-visible accordion__wrapper",
    "part-missing: end.md.disabled__focus-visible label-2",
    "part-missing: end.md.disabled__active accordion__wrapper",
    "part-missing: end.md.disabled__active label-2",
    "part-missing: end.lg.disabled__default accordion__wrapper",
    "part-missing: end.lg.disabled__default label-2",
    "part-missing: end.lg.disabled__hover accordion__wrapper",
    "part-missing: end.lg.disabled__hover label-2",
    "part-missing: end.lg.disabled__focus-visible accordion__wrapper",
    "part-missing: end.lg.disabled__focus-visible label-2",
    "part-missing: end.lg.disabled__active accordion__wrapper",
    "part-missing: end.lg.disabled__active label-2"
  ],
  "styledChannelReceipts": [
    "block-root-width-admitted: root — display:block root fills its container in CSS; the captured stage width joins fusion (stage-dependent, receipted — the canvas card draws at the captured block width instead of hugging its text)",
    "control-fallback: no control for <ul> — span control used (part root)",
    "control-fallback: no control for <li> — span control used (part accordion__item)",
    "control-fallback: no control for <svg> — span control used (part icon)",
    "control-fallback: no control for <path> — span control used (part part-0-0-0-0)",
    "text-part-typography-carried: label — font-weight carried even though equal to the control baseline (context-inherited typography IS the rendered truth; the generated surfaces have no Polaris body context, and the canvas otherwise draws its own 14px/500 defaults — round 5c)",
    "text-part-typography-carried: label-3 — font-size/line-height/font-weight carried even though equal to the control baseline (context-inherited typography IS the rendered truth; the generated surfaces have no Polaris body context, and the canvas otherwise draws its own 14px/500 defaults — round 5c)",
    "text-part-typography-carried: label-2 — font-size/line-height/font-weight carried even though equal to the control baseline (context-inherited typography IS the rendered truth; the generated surfaces have no Polaris body context, and the canvas otherwise draws its own 14px/500 defaults — round 5c)",
    "webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (accordion__heading); -webkit-text-stroke-color (accordion__heading)",
    "webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.",
    "orphan-mint-refused: label-3 was REFUSED by the anatomy promotion (it is not a part of the promoted contract), so its 9 styled channel(s) do not mint — before this door they minted leaves the contract could never reference, and `figma bundle` shipped them as Figma variables nothing binds (task #42). The named promotion refusal is in anatomyPromotion.refusals.",
    "inheritance-check-rejected: accordion__heading.caret-color — values track ancestor \"accordion__item\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: accordion__heading.color — values track ancestor \"accordion__item\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: accordion__heading.font-size — values track ancestor \"accordion__item\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: icon.fill — values track ancestor \"accordion__heading\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: icon.line-height — values track ancestor \"accordion__heading\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: label-2.font-size — values track ancestor \"accordion__wrapper\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: label-2.font-weight — values track ancestor \"accordion__wrapper\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: label-2.line-height — values track ancestor \"accordion__wrapper\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: label.font-weight — values track ancestor \"accordion__heading\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: label.letter-spacing — values track ancestor \"accordion__heading\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)",
    "inheritance-check-rejected: label.line-height — values track ancestor \"accordion__heading\" but that ancestor carries the channel nowhere; the binding STAYS (dropping it would bind the channel nowhere)"
  ],
  "frontierReceipts": [
    "closed-shadow-root-suspected: Accordion:end.lg.disabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.lg.disabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.lg.disabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.lg.disabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.lg.enabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.lg.enabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.lg.enabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.lg.enabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.md.disabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.md.disabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.md.disabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.md.disabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.md.enabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.md.enabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.md.enabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.md.enabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.sm.disabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.sm.disabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.sm.disabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.sm.disabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.sm.enabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.sm.enabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.sm.enabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.sm.enabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.unset.disabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.unset.disabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.unset.disabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.unset.disabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.unset.enabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.unset.enabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.unset.enabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:end.unset.enabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.lg.disabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.lg.disabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.lg.disabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.lg.disabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.lg.enabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.lg.enabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.lg.enabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.lg.enabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.md.disabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.md.disabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.md.disabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.md.disabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.md.enabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.md.enabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.md.enabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.md.enabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.sm.disabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.sm.disabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.sm.disabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.sm.disabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.sm.enabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.sm.enabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.sm.enabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.sm.enabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.unset.disabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.unset.disabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.unset.disabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.unset.disabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.unset.enabled__active>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.unset.enabled__default>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.unset.enabled__focus-visible>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-suspected: Accordion:start.unset.enabled__hover>li>button>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script",
    "closed-shadow-root-limit: 64 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.",
    "pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::marker (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.",
    "pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight",
    "pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, \"::backdrop\") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit",
    "pseudo-elements-refused: 2 (::selection, ::backdrop)"
  ],
  "anatomyJoin": {
    "computed": [
      {
        "part": "root",
        "join": "matched"
      },
      {
        "part": "accordion__item",
        "join": "computed-only"
      },
      {
        "part": "accordion__heading",
        "join": "computed-only"
      },
      {
        "part": "icon",
        "join": "computed-only"
      },
      {
        "part": "part-0-0-0-0",
        "join": "computed-only"
      },
      {
        "part": "label",
        "join": "computed-only"
      },
      {
        "part": "accordion__wrapper-2",
        "join": "computed-only"
      },
      {
        "part": "label-3",
        "join": "computed-only"
      },
      {
        "part": "accordion__wrapper",
        "join": "computed-only"
      },
      {
        "part": "label-2",
        "join": "computed-only"
      }
    ],
    "staticOnly": []
  },
  "anatomyPromotion": {
    "_note": "Round 4 DOM-anatomy promotion: computed-only elements carried as REAL parts (extract/computed/anatomy.ts); svg subtrees carried as reconstructed icon assets; presence facts via visibleWhen/stylesWhen; refusals named.",
    "partsCarried": [
      "root",
      "accordion__item",
      "accordion__heading",
      "icon",
      "label",
      "accordion__wrapper-2",
      "accordion__wrapper",
      "label-2"
    ],
    "svgAssets": [
      "accordion-icon"
    ],
    "receipts": [
      "svg-viewbox-reconstructed: Accordion.icon@start.unset.enabled — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)",
      "svg-viewbox-reconstructed: Accordion.icon@start.sm.enabled — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)",
      "svg-viewbox-reconstructed: Accordion.icon@start.md.enabled — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)",
      "svg-viewbox-reconstructed: Accordion.icon@start.lg.enabled — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)",
      "svg-viewbox-reconstructed: Accordion.icon@end.unset.enabled — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)",
      "svg-viewbox-reconstructed: Accordion.icon@end.sm.enabled — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)",
      "svg-viewbox-reconstructed: Accordion.icon@end.md.enabled — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)",
      "svg-viewbox-reconstructed: Accordion.icon@end.lg.enabled — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)",
      "block-level-display-carried: accordion__item = \"list-item\" — a block-level box in CSS block flow; carried in the declared registry so the canvas lowers it as a stack rather than the emitter's row default",
      "literal-text-carried: label = \"Accordion title\" (no text prop sample matches — the mounted value is carried verbatim)",
      "sr-only-carried-as-hidden: accordion__wrapper-2 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)",
      "literal-text-carried: label-2 = \"Details body copy for the Carbon round.\" (no text prop sample matches — the mounted value is carried verbatim)"
    ],
    "refusals": []
  },
  "enrichmentNotes": [
    "layout enriched: accordion__heading.layout.align = center (uniform computed keyword — the schema's own vocabulary)",
    "layout enriched: accordion__heading.layout.justify = start (uniform computed keyword — the schema's own vocabulary)",
    "declared fact carried: accordion__item.border-bottom-style = solid (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__item.border-top-style = solid (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__item.transition-duration = 0.11s (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__item.transition-property = border-color (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__item.transition-timing-function = cubic-bezier(0.2, 0, 0.38, 0.9) (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.appearance = none (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.border-bottom-style = none (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.border-left-style = none (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.border-right-style = none (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.border-top-style = none (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.cursor = pointer (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.position = relative (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.text-align = start (v15 declared vocabulary — canvas-drawable)",
    "declared fact carried: accordion__heading.transition-duration = 0.11s (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.transition-property = background-color (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__heading.transition-timing-function = cubic-bezier(0.2, 0, 0.38, 0.9) (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: icon.cursor = pointer (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: icon.font-feature-settings = normal (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: icon.outline-style = solid (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: icon.overflow-x = hidden (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: icon.overflow-y = hidden (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: icon.text-rendering = auto (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: icon.transition-duration = 0.11s (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: icon.transition-timing-function = cubic-bezier(0.2, 0, 0.38, 0.9) (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: label.cursor = pointer (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: label.text-rendering = auto (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__wrapper.max-height = fit-content (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__wrapper.transition-duration = 0.11s (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared fact carried: accordion__wrapper.transition-timing-function = cubic-bezier(0, 0, 0.38, 0.9) (v15 declared vocabulary — declared-not-drawn on canvas)",
    "declared state fact carried: [disabled] accordion__heading.cursor = not-allowed (v15 declared vocabulary)",
    "declared state fact carried: [disabled] icon.cursor = not-allowed (v15 declared vocabulary)",
    "declared state fact carried: [disabled] label.cursor = not-allowed (v15 declared vocabulary)"
  ]
}
