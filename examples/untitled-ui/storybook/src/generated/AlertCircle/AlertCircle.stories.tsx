/**
 * GENERATED FILE — DO NOT EDIT.
 * Source of truth: contracts/alert-circle.contract.json (ds.alert-circle v0.1.0)
 * Regenerate with: npm run generate
 */
import type { Meta, StoryObj } from '@storybook/react-vite';
import { AlertCircle } from './AlertCircle';

const meta = {
  title: 'Components/AlertCircle',
  component: AlertCircle,
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component:
          'STUB contract auto-proposed for the nested "alert-circle" instances of _Input field base — the child set was not imported. Props are the observed applied values ONLY; anatomy and styling are NOT captured (dump v1 stops at instance boundaries); the root renders the OBSERVED bounding box and primary paint (dump v1.5) as honest provisional geometry. Import the child set to replace this stub.',
      },
    },
  },
  argTypes: {},
  args: {},
} satisfies Meta<typeof AlertCircle>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Playground: Story = {};
