# MUI minted tokens — promotion receipt

Generated by `examples/tailwind/scripts/promote-floor.mjs`.

- **25 leaves source-aliased** to the library's own CSS-variable-named tokens (value-verified twice: capture + promotion)
- **246 leaves kept literal** (no verified source reference)
- **0 named alias refusals**

- **14 authored fact(s) applied** from `examples/tailwind/authored-facts.json` — reviewed hand-authoring the capture cannot carry; every row names its cause, and a row the capture has since learned to carry REFUSES the promotion by name:
  - alert prop dismissable: edit description, bindings.code.prop — The seed bound the BOOLEAN `dismissable` to the `onDismiss` callback prop, so the authored React received a boolean where Flowbite expects a function and the dismiss control never clicked (NORTH-STAR functional gate, 968958cd). The boolean now binds to Flowbite's own `dismissable` prop; the callback moves to the declared `dismiss` event below. The capture reads DOM truth, not the React prop surface, so it cannot see this binding.
  - alert.dismiss: set fields element, attrs (after description); edit description — The dismiss control is a real <button aria-label="Dismiss"> in Flowbite's DOM, and the declared `dismiss` event triggers on it — the authored React needs the element and the accessible name to be clickable and announced. The dismiss part is CANVAS-DEVELOPED anatomy (the captured default renders no dismiss button: Flowbite mounts it only when onDismiss is passed), so no capture plane carries the element or its attributes.
  - alert: set fields events (after anatomy) — Declared event (core/emit-react.ts execute gate, 968958cd): the `onDismiss` callback is a code-surface fact with no canvas expression and no computed-style footprint — the capture drives prop combos and pseudo-class states, never a click handler. Placed after `anatomy` to keep the committed byte order.
  - toggleswitch @semantics: set fields role, roleException — Flowbite's ToggleSwitch renders <button role="switch"> with no native checkbox; the authored React must carry the ARIA role (and the named exception to the checkbox expectation) for the `toggle` event's aria-checked flip to be correct. The capture's semantics read is element-only; roles are not a computed style.
  - toggleswitch.root @layout: set fields align — The flex root's cross-axis alignment (`align-items: center`, Flowbite's `items-center`) — the track and the label sit on one centre line. The capture lowered the root to `display: flex` and carried no align fact for it; the authored React clipped the label against the track without it (968958cd).
  - toggleswitch: set fields events (after anatomy) — Declared event (core/emit-react.ts execute gate, 968958cd): `onToggle` flips the `checked` variant axis and mirrors it to aria-checked — a code-surface fact the capture cannot see (it reads computed style per prop combo, never a click). Placed after `anatomy` to keep the committed byte order.
  - alert @bindings.figma.anchors: set fields nodeId (after fileKey); edit fileKey, componentSetKey — The live demo set for this contract is node 120:1979 on Figma file 59mLQlOMiD5w5za6SUcoO5 (NORTH-STAR's demo file); its key is 27fce6232d10fdd74f9f87d46c91ab29ecc98c68. Verified READ-ONLY against the live file on 2026-08-23 (REST nodes?ids=…&plugin_data=shared: the node is that set, its `key` is this key, and `ds_contracts/contractId` equals the contract id; the key also equals the `key` field of extract/figma/fixtures/flowbite-eight.dump.json). The capture reads the library's DOM, so it cannot know where the contract's canvas twin lives; nothing writes the anchor back after a recorded apply yet (docs/23 §D.30), so the identity is authored here and re-derived on every promotion.
  - badge @bindings.figma.anchors: set fields nodeId (after fileKey); edit fileKey, componentSetKey — The live demo set for this contract is node 120:2098 on Figma file 59mLQlOMiD5w5za6SUcoO5 (NORTH-STAR's demo file); its key is 8c36b6b475b56aa2920113b250ac5c7186320b2d. Verified READ-ONLY against the live file on 2026-08-23 (REST nodes?ids=…&plugin_data=shared: the node is that set, its `key` is this key, and `ds_contracts/contractId` equals the contract id; the key also equals the `key` field of extract/figma/fixtures/flowbite-eight.dump.json). The capture reads the library's DOM, so it cannot know where the contract's canvas twin lives; nothing writes the anchor back after a recorded apply yet (docs/23 §D.30), so the identity is authored here and re-derived on every promotion.
  - button @bindings.figma.anchors: set fields nodeId (after fileKey); edit fileKey, componentSetKey — The live demo set for this contract is node 120:2203 on Figma file 59mLQlOMiD5w5za6SUcoO5 (NORTH-STAR's demo file); its key is 8c1a78adb634d4277cd96bb53b275ca45885ff9a. Verified READ-ONLY against the live file on 2026-08-23 (REST nodes?ids=…&plugin_data=shared: the node is that set, its `key` is this key, and `ds_contracts/contractId` equals the contract id; the key also equals the `key` field of extract/figma/fixtures/flowbite-eight.dump.json). The capture reads the library's DOM, so it cannot know where the contract's canvas twin lives; nothing writes the anchor back after a recorded apply yet (docs/23 §D.30), so the identity is authored here and re-derived on every promotion.
  - card @bindings.figma.anchors: set fields nodeId (after fileKey); edit fileKey, componentSetKey — The live demo set for this contract is node 120:1999 on Figma file 59mLQlOMiD5w5za6SUcoO5 (NORTH-STAR's demo file) — a standalone COMPONENT; the key is its component key; its key is 7b5427afe8d5766e80457327b0b04a50544808ed. Verified READ-ONLY against the live file on 2026-08-23 (REST nodes?ids=…&plugin_data=shared: the node is that set, its `key` is this key, and `ds_contracts/contractId` equals the contract id; the key also equals the `key` field of extract/figma/fixtures/flowbite-eight.dump.json). The capture reads the library's DOM, so it cannot know where the contract's canvas twin lives; nothing writes the anchor back after a recorded apply yet (docs/23 §D.30), so the identity is authored here and re-derived on every promotion.
  - helpertext @bindings.figma.anchors: set fields nodeId (after fileKey); edit fileKey, componentSetKey — The live demo set for this contract is node 120:2014 on Figma file 59mLQlOMiD5w5za6SUcoO5 (NORTH-STAR's demo file); its key is 9fda06f5ff7b04b37f7c325edc3d12c2ffdeb39c. Verified READ-ONLY against the live file on 2026-08-23 (REST nodes?ids=…&plugin_data=shared: the node is that set, its `key` is this key, and `ds_contracts/contractId` equals the contract id; the key also equals the `key` field of extract/figma/fixtures/flowbite-eight.dump.json). The capture reads the library's DOM, so it cannot know where the contract's canvas twin lives; nothing writes the anchor back after a recorded apply yet (docs/23 §D.30), so the identity is authored here and re-derived on every promotion.
  - kbd @bindings.figma.anchors: set fields nodeId (after fileKey); edit fileKey, componentSetKey — The live demo set for this contract is node 120:1982 on Figma file 59mLQlOMiD5w5za6SUcoO5 (NORTH-STAR's demo file) — a standalone COMPONENT; the key is its component key; its key is b2a08e2c5421c4d41e8920f1c4b85f9a85ab99c9. Verified READ-ONLY against the live file on 2026-08-23 (REST nodes?ids=…&plugin_data=shared: the node is that set, its `key` is this key, and `ds_contracts/contractId` equals the contract id; the key also equals the `key` field of extract/figma/fixtures/flowbite-eight.dump.json). The capture reads the library's DOM, so it cannot know where the contract's canvas twin lives; nothing writes the anchor back after a recorded apply yet (docs/23 §D.30), so the identity is authored here and re-derived on every promotion.
  - label @bindings.figma.anchors: set fields nodeId (after fileKey); edit fileKey, componentSetKey — The live demo set for this contract is node 120:1996 on Figma file 59mLQlOMiD5w5za6SUcoO5 (NORTH-STAR's demo file); its key is 76023d1204c57c7a0e07f86fb342f6c7a0536934. Verified READ-ONLY against the live file on 2026-08-23 (REST nodes?ids=…&plugin_data=shared: the node is that set, its `key` is this key, and `ds_contracts/contractId` equals the contract id; the key also equals the `key` field of extract/figma/fixtures/flowbite-eight.dump.json). The capture reads the library's DOM, so it cannot know where the contract's canvas twin lives; nothing writes the anchor back after a recorded apply yet (docs/23 §D.30), so the identity is authored here and re-derived on every promotion.
  - toggleswitch @bindings.figma.anchors: set fields nodeId (after fileKey); edit fileKey, componentSetKey — The live demo set for this contract is node 120:2047 on Figma file 59mLQlOMiD5w5za6SUcoO5 (NORTH-STAR's demo file); its key is 08442ebe555baa908cd7ab07097bfcd58dd5fccc. Verified READ-ONLY against the live file on 2026-08-23 (REST nodes?ids=…&plugin_data=shared: the node is that set, its `key` is this key, and `ds_contracts/contractId` equals the contract id; the key also equals the `key` field of extract/figma/fixtures/flowbite-eight.dump.json). The capture reads the library's DOM, so it cannot know where the contract's canvas twin lives; nothing writes the anchor back after a recorded apply yet (docs/23 §D.30), so the identity is authored here and re-derived on every promotion.
