{
  "__note": "LIBRARY MANIFEST \u2014 the per-library half of the whole code\u2192canvas pipeline (docs/21). `ds-contracts promote --config <this>` runs the promote stage; `ds-contracts onboard --adopt examples/tailwind` runs capture \u2192 promote \u2192 emit \u2192 bundle \u2192 publish from it. packages/cli/src/promote.ts is the shared engine-free pipeline; nothing here is library-specific code.",
  "library": "tailwind",
  "exampleDir": "examples/tailwind",
  "captureOut": "extract/computed/out/tailwind",
  "dtcg": "examples/tailwind/tokens/tailwind.dtcg.json",
  "mintedOut": "examples/tailwind/tokens/tailwind-minted.dtcg.json",
  "mintedDoc": "examples/tailwind/tokens/MINTED.md",
  "components": [
    "button",
    "badge",
    "card",
    "alert",
    "toggleswitch",
    "helpertext",
    "label",
    "kbd"
  ],
  "contractStem": {},
  "contractVersion": "0.2.0",
  "promoterPath": "examples/tailwind/scripts/promote-floor.mjs",
  "possessive": "the library's",
  "__note:mintedDocTitle": "SIC \u2014 the committed examples/tailwind/tokens/MINTED.md is titled 'MUI minted tokens'. The Tailwind promote script was copied from MUI's and the heading was never retitled. Preserved verbatim so the generalization is provably byte-identical; retitling it is a one-word edit to this field, and it should be made deliberately, not as a side effect of refactoring.",
  "mintedDocTitle": "MUI minted tokens",
  "capture": {
    "config": "extract/computed/configs/tailwind.json",
    "harness": "examples/tailwind/.tw-sandbox"
  },
  "seeds": "examples/tailwind/contracts-seed",
  "__note:authored": "AUTHORED FACTS LEDGER — reviewed hand-authored facts the capture cannot carry (Alert dismiss event + dismissable rebinding + dismiss <button aria-label>, ToggleSwitch toggle event + role=switch + root align — commit 968958cd had edited the promoted contracts directly; since 2026-08-23 also the eight sets' live Figma identity, bindings.figma.anchors fileKey/nodeId/componentSetKey, verified read-only against file 59mLQlOMiD5w5za6SUcoO5 — docs/23 §D.30). promote.ts applies it after the computed contract and before the resolution guard, so the committed contracts stay re-derivable; every row names its cause and a stale row refuses by name.",
  "authored": "examples/tailwind/authored-facts.json",
  "emit": {
    "out": "examples/tailwind/figma",
    "icons": "examples/tailwind/assets/icons"
  },
  "bundle": {
    "out": "examples/tailwind/figma/tailwind.bundle.json",
    "name": "Tailwind"
  }
}
