# Proposal notes — Eventz, the real-variable round

Generated by `npx tsx examples/eventz-vars/eventz-pipeline.mts --write`, byte-verified
against a rebuild from `dumps/MERGED.json`.

**328 named decisions across 7 set(s).** Every one is a fact the canvas
draws; a note says what happened to it. Nothing here is silent.

## Refusal classes

| n | class |
|---|---|
| 6 | CARRIED as statesByProp — a state binding that is also a function of an enum axis (v17) |
| 3 | state promoted but nothing recoverable |
| 4 | state override cannot unset a channel |
| 16 | variable name outside the token-ref grammar (U+2024) |
| 19 | binding inconsistent across variants |
| 3 | alpha rides a bound variable |
| 53 | minted — no variable bound to this fact |

Eventz encodes interaction state as a variant axis (`state=default|hover|active|focus`)
CROSSED with `variant=primary|knockout|secondary|bare`, so its hover background is a
function of TWO axes. That combination used to be the largest refusal in this table:
`tokensByProp` carries a per-variant ref and `states` carries a per-state ref, and there
was no per-state-PER-VARIANT form, so Button and Icon Button lost their ENTIRE hover and
active planes. v17 adds `statesByProp` and they carry — with the designer's own unrelated
token names (comp/button/primary/…/hover for primary, …/knockout-hover for knockout),
which is exactly why a substituted ref could never have reached them.

The `per-state-per-variant` refusal row is kept in the table on purpose: it should read
zero, and a non-zero count means the carry regressed.

## Atoms/Badge

- semantics.element defaulted to "div" — element/role/ARIA are not drawn on the canvas and the name/axis inference table matched nothing; set the real host element
- Atoms/Badge:root fill: bound in 3/15 variants (variant=brand, hasIcon=true, hasLabel=true, variant=brand, hasIcon=true, hasLabel=false, variant=brand, hasIcon=false, hasLabel=true) — inconsistent, not proposed
- Atoms/Badge:root paddingLeft: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Badge:root paddingRight: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Badge:root: left/right padding bindings differ — padding-inline is not representable; carried as separate padding-left/padding-right channels
- Atoms/Badge:root paddingTop: variable name "spacing/0․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Badge:root paddingBottom: variable name "spacing/0․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Badge:root: top/bottom padding bindings differ — padding-block is not representable; carried as separate padding-top/padding-bottom channels
- Atoms/Badge:root itemSpacing: variable name "spacing/0․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Badge:root: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Atoms/Badge:root/Icon: present exactly where "hasIcon" is true — proposed as visibleWhen { prop: hasIcon } (boolean axis, truthy form)
- Atoms/Badge:root/Icon: nested instance name "Icons/Placeholder" contains characters a contract id cannot carry — stub id sanitized to "ds.icons-placeholder" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Atoms/Badge:root/Icon: nested instance of "Icons/Placeholder" has no known contract — component ref proposed as "ds.icons-placeholder" with a STUB child contract auto-proposed alongside (childStubs; API from observed applied values only, anatomy not captured — import the real child set to replace it)
- Atoms/Badge:root/Icon: fixed props of "Icons/Placeholder" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Atoms/Badge:root/Label: present exactly where "hasLabel" is true — proposed as visibleWhen { prop: hasLabel } (boolean axis, truthy form)
- Atoms/Badge:root/Label text fill: bindings differ across variants without correlating to any variant axis: color.content.inverse vs color.neutral.950
- Atoms/Badge:root/Label: rides text style "caption/lg-allcaps-bold" which is not a token-derived style — typography not proposed
- Atoms/Badge:root: root width is DRAWN FIXED in 5/15 variants and HUG in the rest — minted as root width with the fixed planes' observed bbox and `fit-content` on the hug planes (the sizing MODE each plane actually draws; a pinned hug measure would freeze a box sized for the DEFAULT content and clip or float whatever a host puts in it)
- Atoms/Badge:root: root height is DRAWN FIXED in 5/15 variants and HUG in the rest — minted as root height with the fixed planes' observed bbox and `fit-content` on the hug planes (the sizing MODE each plane actually draws; a pinned hug measure would freeze a box sized for the DEFAULT content and clip or float whatever a host puts in it)
- prop `hasIcon`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `hasLabel`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `label`: the single text prop carries the component's main content — repo contracts bind main content to code prop "children" (ds.button convention); adopt by setting bindings.code.prop to "children" when this is the label (note-only, nothing renamed mechanically)
- contract name: drawn set name "Atoms/Badge" is not a PascalCase component name — proposed as "AtomsBadge" (the canvas set keeps its own name; the componentSetKey/nodeId anchors carry identity)
- contract id: drawn set name "Atoms/Badge" contains characters a contract id cannot carry — id proposed as "ds.atoms-badge" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the canvas set keeps its own name and the componentSetKey/nodeId anchors carry identity
- MINTED {imported.atoms-badge.label.font-size} = 12px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Badge:root/Label font-size
- MINTED {imported.atoms-badge.label.font-weight} = 700 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Badge:root/Label font-weight
- MINTED {imported.atoms-badge.label.line-height} = 18px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Badge:root/Label line-height
- MINTED {imported.atoms-badge.root.width.true} = fit-content — a SIZING MODE, not a measure: this plane HUGS its content on the canvas, so the leaf states the mode instead of freezing a measurement of the default content. Nothing to rename (no design token names HUG); the canvas leg reads it back as Figma HUG sizing and upserts no variable for it; bound at: Atoms/Badge:root width (hasLabel=true)
- MINTED {imported.atoms-badge.root.width.false} = 22px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Badge:root width (hasLabel=false)
- MINTED {imported.atoms-badge.root.height.true} = fit-content — a SIZING MODE, not a measure: this plane HUGS its content on the canvas, so the leaf states the mode instead of freezing a measurement of the default content. Nothing to rename (no design token names HUG); the canvas leg reads it back as Figma HUG sizing and upserts no variable for it; bound at: Atoms/Badge:root height (hasLabel=true)
- MINTED {imported.atoms-badge.root.height.false} = 22px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Badge:root height (hasLabel=false)
- stub ds.icons-placeholder: observed paint var-bound on some occurrence(s) but not one variable across all — background not carried on the stub geometry, review
- stub ds.icons-placeholder: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-icons-placeholder.root.width} = 15px — stub geometry (the "Icons/Placeholder" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-placeholder width
- MINTED {imported.stub-icons-placeholder.root.height} = 15px — stub geometry (the "Icons/Placeholder" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-placeholder height

## Atoms/Button

- semantics: element "button" inferred from the set name "Atoms/Button" — inference is mechanical (name/axis table), review
- variant axis "state" (default|hover|active|focus) IS the platform's interaction states, not API — promoted: the axis is NOT a prop; anatomy and base facts come from the 8 default-state variant(s); hover→hover, active→active, focus→focus-visible propose root state overrides
- Atoms/Button: 4 state-axis variant(s) wrapped an instance of the set's own base component — flattened before state diffing (same rule as the default variants); their captured componentProperties only FILL defaults of props the base anatomy already discovered
- Atoms/Button:root fill: paint opacity 0.05000000074505806 rides the bound variable "comp/button/color/background/default" — alpha is not representable on a token ref; binding proposed at full opacity, review
- Atoms/Button:root background-color: bindings are a function of variant axis "variant" by VALUE (default primary={comp.button.primary.color.background.default}; knockout={comp.button.color.background.knockout}, secondary={comp.button.color.background.default}, bare={color.background.none}) — carried as tokensByProp overrides (v10; the token names do not spell the axis values, so the substituted-ref shape cannot carry them)
- Atoms/Button:root stroke: bound in 4/8 variants (variant=primary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=false, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Button:root paddingTop: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Button:root paddingBottom: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Button:root: top/bottom padding bindings differ — padding-block is not representable; carried as separate padding-top/padding-bottom channels
- Atoms/Button:root strokeTopWeight: bound in 6/8 variants (variant=primary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=false, variant=bare, isDisabled=false, variant=bare, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Button:root strokeRightWeight: bound in 6/8 variants (variant=primary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=false, variant=bare, isDisabled=false, variant=bare, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Button:root strokeBottomWeight: bound in 6/8 variants (variant=primary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=false, variant=bare, isDisabled=false, variant=bare, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Button:root strokeLeftWeight: bound in 6/8 variants (variant=primary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=false, variant=bare, isDisabled=false, variant=bare, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Button:root: stroke weight bindings are not uniform — border-width not representable, review
- Atoms/Button:root opacity: bindings differ across variants without correlating to any variant axis: theme.opacity.default vs theme.opacity.disabled
- Atoms/Button:root: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Atoms/Button:root/startIcon: INSTANCE_SWAP-bound instance without a dedicated wrapper frame — slot proposed without layout, review
- Atoms/Button:root/startIcon: slot "startIcon" preferredValues name 1 component key(s) with no in-scope contract (b26c053080bd5f845829beb8a434724e954a0008) — not carried into `accepts` (import the referenced set(s) to resolve them by key)
- Atoms/Button:root/startIcon: nested instance name "Icons/Play" contains characters a contract id cannot carry — stub id sanitized to "ds.icons-play" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Atoms/Button:root/startIcon: slot "startIcon" design-time content "Icons/Play" proposed as defaultContent [ds.icons-play] — a STUB rendering the OBSERVED geometry only (dump v1.5 bbox + primary paint; PROVISIONAL — import the real child set to replace it)
- Atoms/Button:root/startIcon: visibility bound to BOOLEAN "hasStartIcon" — proposed as prop `hasStartIcon` (default true: the property definition's defaultValue, dump v1.5)
- Atoms/Button:root/Label color: bindings are a function of variant axis "variant" by VALUE (default primary={comp.button.primary.color.content.default}; knockout={comp.button.color.content.default}, secondary={comp.button.color.content.default}, bare={comp.button.color.content.default}) — carried as tokensByProp overrides (v10; the token names do not spell the axis values, so the substituted-ref shape cannot carry them)
- Atoms/Button:root/Label: rides text style "body/sm" which is not a token-derived style — typography not proposed
- Atoms/Button:root/endIcon: INSTANCE_SWAP-bound instance without a dedicated wrapper frame — slot proposed without layout, review
- Atoms/Button:root/endIcon: slot "endIcon" preferredValues name 1 component key(s) with no in-scope contract (b26c053080bd5f845829beb8a434724e954a0008) — not carried into `accepts` (import the referenced set(s) to resolve them by key)
- Atoms/Button:root/endIcon: nested instance name "Icons/Pause" contains characters a contract id cannot carry — stub id sanitized to "ds.icons-pause" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Atoms/Button:root/endIcon: slot "endIcon" design-time content "Icons/Pause" proposed as defaultContent [ds.icons-pause] — a STUB rendering the OBSERVED geometry only (dump v1.5 bbox + primary paint; PROVISIONAL — import the real child set to replace it)
- Atoms/Button:root/endIcon: visibility bound to BOOLEAN "hasEndIcon" — proposed as prop `hasEndIcon` (default true: the property definition's defaultValue, dump v1.5)
- Atoms/Button:root: root width HUGS in every variant — carried as the literal `width: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 109px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- Atoms/Button:root: root height HUGS in every variant — carried as the literal `height: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 32px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- Atoms/Button: 1 observed character override(s) in this set's dump were NOT carried because no proposed part reaches the instance that holds them ("Label" = "Label") — the usual cause is a part that lives only in variants the proposal excludes (an interaction-state group, a filtered mode); named here so the absence is never read as "nothing was drawn"
- base instance "Atoms/Button": property "variant" is one of the set's own variant axes (pinned to primary, knockout, secondary, bare in the flattened variant(s)) — not promoted
- base instance "Atoms/Button": property "isDisabled" is one of the set's own variant axes (pinned to false in the flattened variant(s)) — not promoted
- Atoms/Button:root fill (state hover): bindings are a function of variant axis "variant" by VALUE (primary={comp.button.primary.color.background.hover}, knockout={comp.button.color.background.knockout-hover}, secondary={comp.button.color.background.hover}, bare={comp.button.color.background.hover}) — carried as statesByProp (v17; the token names do not spell the axis values, so neither a substituted ref nor a single state ref can carry them)
- Atoms/Button:root: stroke differs in state "hover" but is absent in some of its variant(s) — a state override cannot unset a channel; NAMED, not proposed (review)
- Atoms/Button:root/Label fill (state hover): bindings are a function of variant axis "variant" by VALUE (primary={comp.button.primary.color.content.hover}, knockout={comp.button.color.content.hover}, secondary={comp.button.color.content.hover}, bare={comp.button.color.content.hover}) — carried as statesByProp (v17)
- Atoms/Button:root fill (state active): bindings are a function of variant axis "variant" by VALUE (primary={comp.button.primary.color.background.active}, knockout={comp.button.color.background.knockout-active}, secondary={comp.button.color.background.default}, bare={color.background.none}) — carried as statesByProp (v17; the token names do not spell the axis values, so neither a substituted ref nor a single state ref can carry them)
- Atoms/Button:root: stroke differs in state "active" but is absent in some of its variant(s) — a state override cannot unset a channel; NAMED, not proposed (review)
- Atoms/Button:root/Label fill (state active): bindings are a function of variant axis "variant" by VALUE (primary={comp.button.primary.color.content.active}, knockout={comp.button.color.content.active}, secondary={comp.button.color.content.default}, bare={comp.button.color.content.active}) — carried as statesByProp (v17)
- Atoms/Button:root/Focus ring: drawn only in the focus state and carries a stroke — inverted to focus-visible outline overrides (outline-color/outline-width); its own corner radius is not carried (the outline follows the root's shape + offset), review
- prop `isDisabled`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `text`: the single text prop carries the component's main content — repo contracts bind main content to code prop "children" (ds.button convention); adopt by setting bindings.code.prop to "children" when this is the label (note-only, nothing renamed mechanically)
- contract name: drawn set name "Atoms/Button" is not a PascalCase component name — proposed as "AtomsButton" (the canvas set keeps its own name; the componentSetKey/nodeId anchors carry identity)
- contract id: drawn set name "Atoms/Button" contains characters a contract id cannot carry — id proposed as "ds.atoms-button" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the canvas set keeps its own name and the componentSetKey/nodeId anchors carry identity
- MINTED {imported.atoms-button.label.font-size} = 14px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Button:root/Label font-size
- MINTED {imported.atoms-button.label.font-weight} = 500 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Button:root/Label font-weight
- MINTED {imported.atoms-button.label.line-height} = 20px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Button:root/Label line-height
- MINTED {imported.atoms-button.state-focus-visible.outline-width} = 2px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Button:root (state focus-visible) outline-width
- figmaStatePreviews: true — regenerating the canvas draws the promoted states as a "State" preview axis (values Default|Hover|Active|Focus Visible, the shared spelling rules) — a RENAME relative to the imported axis "state" (default|hover|active|focus); the contract vocabulary carries no custom state-axis spellings, so the original spelling lives in this note and in the anchors' set
- stub ds.icons-play: observed paint var-bound on some occurrence(s) but not one variable across all — background not carried on the stub geometry, review
- stub ds.icons-play: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-icons-play.root.width} = 20px — stub geometry (the "Icons/Play" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-play width
- MINTED {imported.stub-icons-play.root.height} = 20px — stub geometry (the "Icons/Play" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-play height
- stub ds.icons-pause: observed paint var-bound on some occurrence(s) but not one variable across all — background not carried on the stub geometry, review
- stub ds.icons-pause: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-icons-pause.root.width} = 20px — stub geometry (the "Icons/Pause" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-pause width
- MINTED {imported.stub-icons-pause.root.height} = 20px — stub geometry (the "Icons/Pause" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-pause height

## Atoms/Checkbox

- semantics: element "input" ("checkbox"; the type="checkbox" attribute is not canvas-recoverable — author it) inferred from the set name "Atoms/Checkbox" — inference is mechanical (name/axis table), review
- variant axis "state" (default|focus|hover) IS the platform's interaction states, not API — promoted: the axis is NOT a prop; anatomy and base facts come from the 4 default-state variant(s); focus→focus-visible, hover→hover propose root state overrides
- Atoms/Checkbox:root opacity: bound in 2/4 variants (isChecked=false, isDisabled=true, isChecked=true, isDisabled=true) — inconsistent, not proposed
- Atoms/Checkbox:root: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Atoms/Checkbox:root/Icons/Checkbox: nested instance name "Icons/Checkbox" contains characters a contract id cannot carry — stub id sanitized to "ds.icons-checkbox" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Atoms/Checkbox:root/Icons/Checkbox: nested instance of "Icons/Checkbox" has no known contract — component ref proposed as "ds.icons-checkbox" with a STUB child contract auto-proposed alongside (childStubs; API from observed applied values only, anatomy not captured — import the real child set to replace it)
- Atoms/Checkbox:root/Icons/Checkbox: fixed props of "Icons/Checkbox" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Atoms/Checkbox:root/Icons/Checkbox: applied prop "state" of the nested "Icons/Checkbox" varies across variants (unselected, selected) without tracking any enum axis — first value "unselected" carried, review
- Atoms/Checkbox:root/content: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Atoms/Checkbox:root/content/Label: rides text style "body/sm" which is not a token-derived style — typography not proposed
- Atoms/Checkbox:root/content/.Inline hint: nested instance name ".Inline hint" contains characters a contract id cannot carry — stub id sanitized to "ds.inline-hint" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Atoms/Checkbox:root/content/.Inline hint: nested instance of ".Inline hint" has no known contract — component ref proposed as "ds.inline-hint" with a STUB child contract auto-proposed alongside (childStubs; API from observed applied values only, anatomy not captured — import the real child set to replace it)
- Atoms/Checkbox:root/content/.Inline hint: fixed props of ".Inline hint" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Atoms/Checkbox:root/content/.Inline hint: visibility bound to BOOLEAN "hasHint" — proposed as prop `hasHint` (default true: the property definition's defaultValue, dump v1.5)
- Atoms/Checkbox:root: root width is DRAWN FIXED in every variant — the observed dimension (240px, dump v1.5 bbox) is proposed as a minted root token (the drawn value is the only witness; rename against your real tokens)
- Atoms/Checkbox:root: root height HUGS in every variant — carried as the literal `height: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 38px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- Atoms/Checkbox:root: opacity differs in state "focus-visible" with bound variables in play — bound number-channel state inversion is not implemented; NAMED, review
- Atoms/Checkbox:root/Focus ring: drawn only in the focus state and carries a stroke — inverted to focus-visible outline overrides (outline-color/outline-width); its own corner radius is not carried (the outline follows the root's shape + offset), review
- prop `isChecked`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `isDisabled`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `label`: the single text prop carries the component's main content — repo contracts bind main content to code prop "children" (ds.button convention); adopt by setting bindings.code.prop to "children" when this is the label (note-only, nothing renamed mechanically)
- contract name: drawn set name "Atoms/Checkbox" is not a PascalCase component name — proposed as "AtomsCheckbox" (the canvas set keeps its own name; the componentSetKey/nodeId anchors carry identity)
- contract id: drawn set name "Atoms/Checkbox" contains characters a contract id cannot carry — id proposed as "ds.atoms-checkbox" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the canvas set keeps its own name and the componentSetKey/nodeId anchors carry identity
- MINTED {imported.atoms-checkbox.content-label.font-size} = 14px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Checkbox:root/content/Label font-size
- MINTED {imported.atoms-checkbox.content-label.font-weight} = 500 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Checkbox:root/content/Label font-weight
- MINTED {imported.atoms-checkbox.content-label.line-height} = 20px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Checkbox:root/content/Label line-height
- MINTED {imported.atoms-checkbox.root.width} = 240px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Checkbox:root width
- MINTED {imported.atoms-checkbox.state-focus-visible.outline-width} = 2px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Checkbox:root (state focus-visible) outline-width
- state "hover": promoted from the axis but no root or part override was recoverable — state not declared (its variants render identically to default, or every channel refused by name above)
- figmaStatePreviews: true — regenerating the canvas draws the promoted states as a "State" preview axis (values Default|Focus Visible, the shared spelling rules) — a RENAME relative to the imported axis "state" (default|focus|hover); the contract vocabulary carries no custom state-axis spellings, so the original spelling lives in this note and in the anchors' set
- stub ds.icons-checkbox: observed paint var-bound on some occurrence(s) but not one variable across all — background not carried on the stub geometry, review
- stub ds.icons-checkbox: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-icons-checkbox.root.width} = 20px — stub geometry (the "Icons/Checkbox" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-checkbox width
- MINTED {imported.stub-icons-checkbox.root.height} = 20px — stub geometry (the "Icons/Checkbox" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-checkbox height
- stub ds.inline-hint: observed primary paint is bound to variable "color/background/default" on every occurrence — background-color carried as {color.background.default} (resolves through the captured-token layer; dump v1.7 instancePrimaryFill)
- stub ds.inline-hint: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box + primary paint) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-inline-hint.root.width} = 216px — stub geometry (the ".Inline hint" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.inline-hint width
- MINTED {imported.stub-inline-hint.root.height} = 18px — stub geometry (the ".Inline hint" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.inline-hint height

## Atoms/Icon Button

- semantics: element "button" inferred from the set name "Atoms/Icon Button" — inference is mechanical (name/axis table), review
- variant axis "state" (default|active|focus|hover) IS the platform's interaction states, not API — promoted: the axis is NOT a prop; anatomy and base facts come from the 10 default-state variant(s); active→active, focus→focus-visible, hover→hover propose root state overrides
- Atoms/Icon Button: 5 state-axis variant(s) wrapped an instance of the set's own base component — flattened before state diffing (same rule as the default variants); their captured componentProperties only FILL defaults of props the base anatomy already discovered
- Atoms/Icon Button:root fill: paint opacity 0.05000000074505806 rides the bound variable "comp/button/color/background/default" — alpha is not representable on a token ref; binding proposed at full opacity, review
- Atoms/Icon Button:root background-color: bindings are a function of variant axis "variant" by VALUE (default primary={comp.button.primary.color.background.default}; knockout={comp.button.color.background.knockout}, secondary={comp.button.color.background.default}, bare={color.background.none}, bareKnockout={color.background.none}) — carried as tokensByProp overrides (v10; the token names do not spell the axis values, so the substituted-ref shape cannot carry them)
- Atoms/Icon Button:root stroke: bound in 4/10 variants (variant=primary, isDisabled=false, variant=secondary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Icon Button:root paddingLeft: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Icon Button:root paddingRight: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Icon Button:root: left/right padding bindings differ — padding-inline is not representable; carried as separate padding-left/padding-right channels
- Atoms/Icon Button:root paddingTop: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Icon Button:root paddingBottom: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Icon Button:root: top/bottom padding bindings differ — padding-block is not representable; carried as separate padding-top/padding-bottom channels
- Atoms/Icon Button:root strokeTopWeight: bound in 4/10 variants (variant=primary, isDisabled=false, variant=secondary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Icon Button:root strokeRightWeight: bound in 4/10 variants (variant=primary, isDisabled=false, variant=secondary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Icon Button:root strokeBottomWeight: bound in 4/10 variants (variant=primary, isDisabled=false, variant=secondary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Icon Button:root strokeLeftWeight: bound in 4/10 variants (variant=primary, isDisabled=false, variant=secondary, isDisabled=false, variant=primary, isDisabled=true, variant=secondary, isDisabled=true) — inconsistent, not proposed
- Atoms/Icon Button:root: stroke weight bindings are not uniform — border-width not representable, review
- Atoms/Icon Button:root opacity: bindings differ across variants without correlating to any variant axis: theme.opacity.default vs theme.opacity.disabled
- Atoms/Icon Button:root: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Atoms/Icon Button:root/Icon: INSTANCE_SWAP-bound instance without a dedicated wrapper frame — slot proposed without layout, review
- Atoms/Icon Button:root/Icon: slot "icon" preferredValues name 1 component key(s) with no in-scope contract (b26c053080bd5f845829beb8a434724e954a0008) — not carried into `accepts` (import the referenced set(s) to resolve them by key)
- Atoms/Icon Button:root/Icon: nested instance of "Icons/Placeholder" LINKED to ds.icons-placeholder by componentSetKey 8f0ab06f0098a262cb887252f56e0ecbe530cca0 (dump v1.5 — rename-safe: the key matches the contract's anchors, whatever either side is named)
- Atoms/Icon Button:root/Icon: fixed props of "Icons/Placeholder" canonicalized through ds.icons-placeholder's bindings
- Atoms/Icon Button:root/Icon: slot "icon" design-time content "Icons/Placeholder" proposed as defaultContent [ds.icons-placeholder] (linked contract in scope, dump v1.5)
- Atoms/Icon Button:root: root width HUGS in every variant — carried as the literal `width: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 32px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- Atoms/Icon Button:root: root height HUGS in every variant — carried as the literal `height: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 32px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- base instance "Atoms/Icon Button": property "variant" is one of the set's own variant axes (pinned to primary, knockout, secondary, bare, bare knockout in the flattened variant(s)) — not promoted
- base instance "Atoms/Icon Button": property "isDisabled" is one of the set's own variant axes (pinned to false, true in the flattened variant(s)) — not promoted
- Atoms/Icon Button:root fill (state active): bindings are a function of variant axis "variant" by VALUE (primary={comp.button.primary.color.background.active}, knockout={comp.button.color.background.knockout-active}, secondary={comp.button.color.background.default}, bare={color.background.none}, bareKnockout={color.background.none}) — carried as statesByProp (v17; the token names do not spell the axis values, so neither a substituted ref nor a single state ref can carry them)
- Atoms/Icon Button:root: stroke differs in state "active" but is absent in some of its variant(s) — a state override cannot unset a channel; NAMED, not proposed (review)
- Atoms/Icon Button:root fill (state focus-visible): token paths differ in depth: comp.button.primary.color.background.default vs comp.button.color.background.knockout vs comp.button.color.background.default vs color.background.none
- Atoms/Icon Button:root/Focus ring: drawn only in the focus state and carries a stroke — inverted to focus-visible outline overrides (outline-color/outline-width); its own corner radius is not carried (the outline follows the root's shape + offset), review
- Atoms/Icon Button:root fill (state hover): bindings are a function of variant axis "variant" by VALUE (primary={comp.button.primary.color.background.hover}, knockout={comp.button.color.background.knockout-hover}, secondary={comp.button.color.background.hover}, bare={comp.button.color.background.hover}, bareKnockout={comp.button.color.background.hover}) — carried as statesByProp (v17; the token names do not spell the axis values, so neither a substituted ref nor a single state ref can carry them)
- Atoms/Icon Button:root: stroke differs in state "hover" but is absent in some of its variant(s) — a state override cannot unset a channel; NAMED, not proposed (review)
- slot "icon": first non-optional slot in tree order — judged the DEFAULT slot (name `children`); rename if it is not the main content
- prop `isDisabled`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- contract name: drawn set name "Atoms/Icon Button" is not a PascalCase component name — proposed as "AtomsIconButton" (the canvas set keeps its own name; the componentSetKey/nodeId anchors carry identity)
- contract id: drawn set name "Atoms/Icon Button" contains characters a contract id cannot carry — id proposed as "ds.atoms-icon-button" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the canvas set keeps its own name and the componentSetKey/nodeId anchors carry identity
- MINTED {imported.atoms-icon-button.state-focus-visible.outline-width} = 2px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Icon Button:root (state focus-visible) outline-width
- figmaStatePreviews: true — regenerating the canvas draws the promoted states as a "State" preview axis (values Default|Hover|Active|Focus Visible, the shared spelling rules) — a RENAME relative to the imported axis "state" (default|active|focus|hover); the contract vocabulary carries no custom state-axis spellings, so the original spelling lives in this note and in the anchors' set

## Atoms/Input

- semantics: element "input" inferred from the set name "Atoms/Input" — inference is mechanical (name/axis table), review
- variant axis "state" (default|hover|focus) IS the platform's interaction states, not API — promoted: the axis is NOT a prop; anatomy and base facts come from the 6 default-state variant(s); hover→hover, focus→focus-visible propose root state overrides
- Atoms/Input:root opacity: bound in 2/6 variants (isFilled=false, hasError=false, isDisabled=true, isFilled=true, hasError=false, isDisabled=true) — inconsistent, not proposed
- Atoms/Input:root: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Atoms/Input:root/.Field label: nested instance name ".Field label" contains characters a contract id cannot carry — stub id sanitized to "ds.field-label" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Atoms/Input:root/.Field label: nested instance of ".Field label" has no known contract — component ref proposed as "ds.field-label" with a STUB child contract auto-proposed alongside (childStubs; API from observed applied values only, anatomy not captured — import the real child set to replace it)
- Atoms/Input:root/.Field label: fixed props of ".Field label" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Atoms/Input:root/.Field label: visibility bound to BOOLEAN "hasLabel" — proposed as prop `hasLabel` (default true: the property definition's defaultValue, dump v1.5)
- Atoms/Input:root/horizontal stack fill: paint opacity 0.05000000074505806 rides the bound variable "comp/form/color/background/default" — alpha is not representable on a token ref; binding proposed at full opacity, review
- Atoms/Input:root/horizontal stack stroke: token paths differ in depth: comp.form.color.border.default vs comp.form.color.border.utility.danger
- Atoms/Input:root/horizontal stack paddingTop: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Input:root/horizontal stack paddingBottom: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Input:root/horizontal stack: top/bottom padding bindings differ — padding-block is not representable; carried as separate padding-top/padding-bottom channels
- Atoms/Input:root/horizontal stack: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Atoms/Input:root/horizontal stack/startIcon: INSTANCE_SWAP-bound instance without a dedicated wrapper frame — slot proposed without layout, review
- Atoms/Input:root/horizontal stack/startIcon: slot "startIcon" preferredValues name 1 component key(s) with no in-scope contract (b26c053080bd5f845829beb8a434724e954a0008) — not carried into `accepts` (import the referenced set(s) to resolve them by key)
- Atoms/Input:root/horizontal stack/startIcon: nested instance of "Icons/Placeholder" LINKED to ds.icons-placeholder by componentSetKey 8f0ab06f0098a262cb887252f56e0ecbe530cca0 (dump v1.5 — rename-safe: the key matches the contract's anchors, whatever either side is named)
- Atoms/Input:root/horizontal stack/startIcon: fixed props of "Icons/Placeholder" canonicalized through ds.icons-placeholder's bindings
- Atoms/Input:root/horizontal stack/startIcon: slot "startIcon" design-time content "Icons/Placeholder" proposed as defaultContent [ds.icons-placeholder] (linked contract in scope, dump v1.5)
- Atoms/Input:root/horizontal stack/startIcon: visibility bound to BOOLEAN "hasStartIcon" — proposed as prop `hasStartIcon` (default false: the property definition's defaultValue, dump v1.5)
- Atoms/Input:root/horizontal stack/Input content text fill: bindings differ across variants without correlating to any variant axis: color.content.weak vs color.content.default
- Atoms/Input:root/horizontal stack/Input content: rides text style "body/sm" which is not a token-derived style — typography not proposed
- Atoms/Input:root/horizontal stack/endIcon: INSTANCE_SWAP-bound instance without a dedicated wrapper frame — slot proposed without layout, review
- Atoms/Input:root/horizontal stack/endIcon: slot "endIcon" preferredValues name 1 component key(s) with no in-scope contract (b26c053080bd5f845829beb8a434724e954a0008) — not carried into `accepts` (import the referenced set(s) to resolve them by key)
- Atoms/Input:root/horizontal stack/endIcon: nested instance of "Icons/Placeholder" LINKED to ds.icons-placeholder by componentSetKey 8f0ab06f0098a262cb887252f56e0ecbe530cca0 (dump v1.5 — rename-safe: the key matches the contract's anchors, whatever either side is named)
- Atoms/Input:root/horizontal stack/endIcon: fixed props of "Icons/Placeholder" canonicalized through ds.icons-placeholder's bindings
- Atoms/Input:root/horizontal stack/endIcon: slot "endIcon" design-time content "Icons/Placeholder" proposed as defaultContent [ds.icons-placeholder] (linked contract in scope, dump v1.5)
- Atoms/Input:root/horizontal stack/endIcon: visibility bound to BOOLEAN "hasEndIcon" — proposed as prop `hasEndIcon` (default false: the property definition's defaultValue, dump v1.5)
- Atoms/Input:root/.Inline danger: present exactly where "hasError" is true — proposed as visibleWhen { prop: hasError } (boolean axis, truthy form)
- Atoms/Input:root/.Inline danger: nested instance name ".Inline danger" contains characters a contract id cannot carry — stub id sanitized to "ds.inline-danger" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Atoms/Input:root/.Inline danger: nested instance of ".Inline danger" has no known contract — component ref proposed as "ds.inline-danger" with a STUB child contract auto-proposed alongside (childStubs; API from observed applied values only, anatomy not captured — import the real child set to replace it)
- Atoms/Input:root/.Inline danger: fixed props of ".Inline danger" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Atoms/Input:root/.Inline hint: present exactly where "hasError" is false — the visibleWhen vocabulary has no negated form, so the condition is inexpressible; kept unconditional (declared fidelity limit), review
- Atoms/Input:root/.Inline hint: nested instance of ".Inline hint" LINKED to ds.inline-hint by componentSetKey f36998ba91948f5337a78544a49eeec99236e3d7 (dump v1.5 — rename-safe: the key matches the contract's anchors, whatever either side is named)
- Atoms/Input:root/.Inline hint: fixed props of ".Inline hint" canonicalized through ds.inline-hint's bindings
- Atoms/Input:root/.Inline hint: observed subtree paint (var color/background/default, dump v1.7 instancePrimaryFill) on an instance LINKED to ds.inline-hint — the child contract owns its paint; a per-usage paint override is not representable on a component ref (ledgered, review if the linked contract's paint differs)
- Atoms/Input:root/.Inline hint: visibility bound to BOOLEAN "hasHint" — proposed as prop `hasHint` (default true: the property definition's defaultValue, dump v1.5)
- Atoms/Input:root: root width is DRAWN FIXED in every variant — the observed dimension (268px, dump v1.5 bbox) is proposed as a minted root token (the drawn value is the only witness; rename against your real tokens)
- Atoms/Input:root: root height HUGS in every variant — carried as the literal `height: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 76px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- Atoms/Input:root/horizontal stack stroke (state hover): token paths differ in depth: comp.form.color.border.hover vs comp.form.color.border.utility.danger
- prop `isFilled`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `hasError`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `isDisabled`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `content`: the single text prop carries the component's main content — repo contracts bind main content to code prop "children" (ds.button convention); adopt by setting bindings.code.prop to "children" when this is the label (note-only, nothing renamed mechanically)
- contract name: drawn set name "Atoms/Input" is not a PascalCase component name — proposed as "AtomsInput" (the canvas set keeps its own name; the componentSetKey/nodeId anchors carry identity)
- contract id: drawn set name "Atoms/Input" contains characters a contract id cannot carry — id proposed as "ds.atoms-input" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the canvas set keeps its own name and the componentSetKey/nodeId anchors carry identity
- MINTED {imported.atoms-input.horizontal-stack-input-content.font-size} = 14px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Input:root/horizontal stack/Input content font-size
- MINTED {imported.atoms-input.horizontal-stack-input-content.font-weight} = 500 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Input:root/horizontal stack/Input content font-weight
- MINTED {imported.atoms-input.horizontal-stack-input-content.line-height} = 20px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Input:root/horizontal stack/Input content line-height
- MINTED {imported.atoms-input.root.width} = 268px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Input:root width
- state "focus-visible": promoted from the axis but no root or part override was recoverable — state not declared (its variants render identically to default, or every channel refused by name above)
- Atoms/Input:root/horizontalStack: state "hover" part-level override proposed — background-color: {comp.form.color.background.hover} (P18 v13; formerly the STYLE-FIDELITY B7 named gap)
- figmaStatePreviews: true — regenerating the canvas draws the promoted states as a "State" preview axis (values Default|Hover, the shared spelling rules) — a RENAME relative to the imported axis "state" (default|hover|focus); the contract vocabulary carries no custom state-axis spellings, so the original spelling lives in this note and in the anchors' set
- stub ds.field-label: observed primary paint is bound to variable "color/background/default" on every occurrence — background-color carried as {color.background.default} (resolves through the captured-token layer; dump v1.7 instancePrimaryFill)
- stub ds.field-label: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box + primary paint) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-field-label.root.width} = 268px — stub geometry (the ".Field label" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.field-label width
- MINTED {imported.stub-field-label.root.height} = 18px — stub geometry (the ".Field label" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.field-label height
- stub ds.inline-danger: observed primary paint is bound to variable "color/background/default" on every occurrence — background-color carried as {color.background.default} (resolves through the captured-token layer; dump v1.7 instancePrimaryFill)
- stub ds.inline-danger: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box + primary paint) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-inline-danger.root.width} = 268px — stub geometry (the ".Inline danger" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.inline-danger width
- MINTED {imported.stub-inline-danger.root.height} = 18px — stub geometry (the ".Inline danger" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.inline-danger height

## Atoms/Tag

- semantics: element "button" inferred STRUCTURALLY — the set carries an interaction-state variant axis (hover/pressed/… are platform states of an interactive element) and the name gave no signal; review
- variant axis "state" (default|focus|hover) IS the platform's interaction states, not API — promoted: the axis is NOT a prop; anatomy and base facts come from the 5 default-state variant(s); focus→focus-visible, hover→hover propose root state overrides
- Atoms/Tag:root fill: token paths differ in depth: color.background.brand vs color.background.none vs color.background.weak vs comp.button.color.background.default
- Atoms/Tag:root stroke: bound in 1/5 variants (isInteractive=true, variant=parent, isActive=true) — inconsistent, not proposed
- Atoms/Tag:root paddingLeft: bindings differ across variants without correlating to any variant axis: spacing.3 vs spacing.4
- Atoms/Tag:root paddingRight: bindings differ across variants without correlating to any variant axis: spacing.3 vs spacing.4
- Atoms/Tag:root: left/right padding bindings differ — padding-inline is not representable; carried as separate padding-left/padding-right channels
- Atoms/Tag:root paddingTop: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Tag:root paddingBottom: variable name "spacing/1․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Atoms/Tag:root: top/bottom padding bindings differ — padding-block is not representable; carried as separate padding-top/padding-bottom channels
- Atoms/Tag:root topLeftRadius: token paths differ in depth: 8 vs component.border.radius.rounded-full
- Atoms/Tag:root topRightRadius: token paths differ in depth: 8 vs component.border.radius.rounded-full
- Atoms/Tag:root bottomLeftRadius: token paths differ in depth: 8 vs component.border.radius.rounded-full
- Atoms/Tag:root bottomRightRadius: token paths differ in depth: 8 vs component.border.radius.rounded-full
- Atoms/Tag:root: corner radii bindings are not uniform — border-radius not representable, review
- Atoms/Tag:root strokeTopWeight: bound in 2/5 variants (isInteractive=true, variant=parent, isActive=true, isInteractive=false, variant=parent, isActive=false) — inconsistent, not proposed
- Atoms/Tag:root strokeRightWeight: bound in 2/5 variants (isInteractive=true, variant=parent, isActive=true, isInteractive=false, variant=parent, isActive=false) — inconsistent, not proposed
- Atoms/Tag:root strokeBottomWeight: bound in 2/5 variants (isInteractive=true, variant=parent, isActive=true, isInteractive=false, variant=parent, isActive=false) — inconsistent, not proposed
- Atoms/Tag:root strokeLeftWeight: bound in 2/5 variants (isInteractive=true, variant=parent, isActive=true, isInteractive=false, variant=parent, isActive=false) — inconsistent, not proposed
- Atoms/Tag:root: stroke weight bindings are not uniform — border-width not representable, review
- Atoms/Tag:root itemSpacing: bindings differ across variants without correlating to any variant axis: spacing.0 vs spacing.1
- Atoms/Tag:root opacity: bound in 1/5 variants (isInteractive=false, variant=parent, isActive=false) — inconsistent, not proposed
- Atoms/Tag:root: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Atoms/Tag:root/Label text fill: token paths differ in depth: color.background.default vs color.background.brand vs color.content.default vs color.content.weak vs comp.button.color.content.default
- Atoms/Tag:root/Label: typography varies across variants (fontSize 14, weight Bold/Medium) — no single text-style identity adopted (the first variant's value would be wrong for the others); font-size minted per variant where axis-correlated; font-weight minted per variant through the weight-name table where every name maps (unknown names stay NAMED) (review)
- Atoms/Tag:root/Icon: present exactly where "isInteractive" is false — the visibleWhen vocabulary has no negated form, so the condition is inexpressible; kept unconditional (declared fidelity limit), review
- Atoms/Tag:root/Icon: nested instance of "Icons/Placeholder" LINKED to ds.icons-placeholder by componentSetKey 8f0ab06f0098a262cb887252f56e0ecbe530cca0 (dump v1.5 — rename-safe: the key matches the contract's anchors, whatever either side is named)
- Atoms/Tag:root/Icon: fixed props of "Icons/Placeholder" canonicalized through ds.icons-placeholder's bindings
- Atoms/Tag:root/Icon: observed subtree paint (var button/color/content/default, dump v1.7 instancePrimaryFill) on an instance LINKED to ds.icons-placeholder — the child contract owns its paint; a per-usage paint override is not representable on a component ref (ledgered, review if the linked contract's paint differs)
- Atoms/Tag:root: root width HUGS in every variant — carried as the literal `width: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 62/61/66px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- Atoms/Tag:root: root height HUGS in every variant — carried as the literal `height: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 32px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- Atoms/Tag:root fill (state focus-visible): bindings differ across variants without correlating to any variant axis: color.background.brand vs color.background.none
- Atoms/Tag:root/Focus ring: drawn only in the focus state and carries a stroke — inverted to focus-visible outline overrides (outline-color/outline-width); its own corner radius is not carried (the outline follows the root's shape + offset), review
- Atoms/Tag:root fill (state hover): bindings differ across variants without correlating to any variant axis: color.background.brand-hover vs color.background.subtle-hover vs color.background.weak-hover
- Atoms/Tag:root/Label fill (state hover): bindings differ across variants without correlating to any variant axis: color.background.default vs color.background.brand-hover vs color.content.default-hover vs color.content.weak-hover
- prop `isInteractive`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `variant`: two-value axis [parent, child] kept as an ENUM (both states render truthfully on both surfaces); a code boolean is a compatible code-side binding — see extract/reconcile.ts bool⇄axis treatment
- prop `isActive`: true/false variant axis proposed as a boolean (extract/reconcile.ts bool⇄axis rule)
- prop `label`: the single text prop carries the component's main content — repo contracts bind main content to code prop "children" (ds.button convention); adopt by setting bindings.code.prop to "children" when this is the label (note-only, nothing renamed mechanically)
- contract name: drawn set name "Atoms/Tag" is not a PascalCase component name — proposed as "AtomsTag" (the canvas set keeps its own name; the componentSetKey/nodeId anchors carry identity)
- contract id: drawn set name "Atoms/Tag" contains characters a contract id cannot carry — id proposed as "ds.atoms-tag" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the canvas set keeps its own name and the componentSetKey/nodeId anchors carry identity
- Atoms/Tag:root/Label font-weight: resolved values differ across variants without correlating to any variant axis or axis PAIR — a nested part carries at most a pair (one map axis + one placeholder); nothing minted, bind manually
- MINTED {imported.atoms-tag.label.font-size} = 14px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Tag:root/Label font-size
- MINTED {imported.atoms-tag.label.line-height} = 20px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Tag:root/Label line-height
- MINTED {imported.atoms-tag.state-focus-visible.outline-width} = 2px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Atoms/Tag:root (state focus-visible) outline-width
- state "hover": promoted from the axis but no root or part override was recoverable — state not declared (its variants render identically to default, or every channel refused by name above)
- figmaStatePreviews: true — regenerating the canvas draws the promoted states as a "State" preview axis (values Default|Focus Visible, the shared spelling rules) — a RENAME relative to the imported axis "state" (default|focus|hover); the contract vocabulary carries no custom state-axis spellings, so the original spelling lives in this note and in the anchors' set
- UNBOUND Atoms/Tag:root/Label fontWeight = 700 — no token invented; nearest tokens by value: (none found)

## Molecules/Alert

- semantics.element defaulted to "div" — element/role/ARIA are not drawn on the canvas and the name/axis inference table matched nothing; set the real host element
- Molecules/Alert:root: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Molecules/Alert:root/container: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Molecules/Alert:root/container: visibility bound to BOOLEAN "hasIcon" — proposed as prop `hasIcon` (default true: the property definition's defaultValue, dump v1.5)
- Molecules/Alert:root/container/Icon: nested instance name "Icons/Check Circle" contains characters a contract id cannot carry — stub id sanitized to "ds.icons-check-circle" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Molecules/Alert:root/container/Icon: nested instance of "Icons/Check Circle" has no known contract — component ref proposed as "ds.icons-check-circle" with a STUB child contract auto-proposed alongside (childStubs; API from observed applied values only, anatomy not captured — import the real child set to replace it)
- Molecules/Alert:root/container/Icon: fixed props of "Icons/Check Circle" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Molecules/Alert:root/horizontal stack itemSpacing: variable name "spacing/0․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Molecules/Alert:root/horizontal stack: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Molecules/Alert:root/horizontal stack/horizontal stack: part name "horizontalStack" already names another part of this contract (part names are contract-wide: CSS classes, swap layers, and note paths key on them) — renamed to "horizontalStack2" (rule: first drawn part keeps the name; later collisions take the parent-derived prefix, else an ordinal suffix)
- Molecules/Alert:root/horizontal stack/horizontal stack: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Molecules/Alert:root/horizontal stack/horizontal stack/Title: rides text style "body/md-bold" which is not a token-derived style — typography not proposed
- Molecules/Alert:root/horizontal stack/horizontal stack/Description: rides text style "body/sm" which is not a token-derived style — typography not proposed
- Molecules/Alert:root/horizontal stack/Atoms/Text Link: nested instance name "Atoms/Text Link" contains characters a contract id cannot carry — stub id sanitized to "ds.atoms-text-link" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Molecules/Alert:root/horizontal stack/Atoms/Text Link: nested instance of "Atoms/Text Link" has no known contract — component ref proposed as "ds.atoms-text-link" with a STUB child contract auto-proposed alongside (childStubs; API from observed applied values only, anatomy not captured — import the real child set to replace it)
- Molecules/Alert:root/horizontal stack/Atoms/Text Link: fixed props of "Atoms/Text Link" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Molecules/Alert:root/horizontal stack/Atoms/Text Link: visibility bound to BOOLEAN "hasLink" — proposed as prop `hasLink` (default true: the property definition's defaultValue, dump v1.5)
- Molecules/Alert:root/container 2: bound variable on "counterAxisSpacing" has no contract vocabulary — binding NAMED, not proposed (review)
- Molecules/Alert:root/container 2: visibility bound to BOOLEAN "isDismissible" — proposed as prop `isDismissible` (default true: the property definition's defaultValue, dump v1.5)
- Molecules/Alert:root/container 2/Icon: part name "Icon" already names another part of this contract (part names are contract-wide: CSS classes, swap layers, and note paths key on them) — renamed to "container2Icon" (rule: first drawn part keeps the name; later collisions take the parent-derived prefix, else an ordinal suffix)
- Molecules/Alert:root/container 2/Icon: nested instance name "Icons/Close" contains characters a contract id cannot carry — stub id sanitized to "ds.icons-close" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the original spelling stays on the stub's name/description and in this note
- Molecules/Alert:root/container 2/Icon: nested instance of "Icons/Close" has no known contract — component ref proposed as "ds.icons-close" with a STUB child contract auto-proposed alongside (childStubs; API from observed applied values only, anatomy not captured — import the real child set to replace it)
- Molecules/Alert:root/container 2/Icon: fixed props of "Icons/Close" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Molecules/Alert:root: a DROP_SHADOW stack (up to 2 layers) proposed as a comma-separated box-shadow value (dump v1.2) — CSS surfaces render it; the canvas preview and the Figma sync script project it as a native DROP_SHADOW effect (dump v1.3)
- Molecules/Alert:root: root width is DRAWN FIXED in every variant — the observed dimension (390px, dump v1.5 bbox) is proposed as a minted root token (the drawn value is the only witness; rename against your real tokens)
- Molecules/Alert:root: root height HUGS in every variant — carried as the literal `height: fit-content` (v16 grammar), the CSS twin of Figma HUG. The drawn 86px is a measurement of the DEFAULT content, not a design value, so it is NOT minted; the emitted box is content-sized and no longer cross-stretches when this component is nested inside another
- contract name: drawn set name "Molecules/Alert" is not a PascalCase component name — proposed as "MoleculesAlert" (the canvas set keeps its own name; the componentSetKey/nodeId anchors carry identity)
- contract id: drawn set name "Molecules/Alert" contains characters a contract id cannot carry — id proposed as "ds.molecules-alert" (rule: lowercase kebab, illegal characters → hyphens, runs collapsed, edge hyphens stripped, digit-led/empty gets "c"); the canvas set keeps its own name and the componentSetKey/nodeId anchors carry identity
- MINTED {imported.molecules-alert.root.max-width} = 880px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root max-width
- MINTED {imported.molecules-alert.horizontal-stack-horizontal-stack-title.font-size} = 16px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root/horizontal stack/horizontal stack/Title font-size
- MINTED {imported.molecules-alert.horizontal-stack-horizontal-stack-title.font-weight} = 700 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root/horizontal stack/horizontal stack/Title font-weight
- MINTED {imported.molecules-alert.horizontal-stack-horizontal-stack-title.line-height} = 20px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root/horizontal stack/horizontal stack/Title line-height
- MINTED {imported.molecules-alert.horizontal-stack-horizontal-stack-description.font-size} = 14px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root/horizontal stack/horizontal stack/Description font-size
- MINTED {imported.molecules-alert.horizontal-stack-horizontal-stack-description.font-weight} = 500 — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root/horizontal stack/horizontal stack/Description font-weight
- MINTED {imported.molecules-alert.horizontal-stack-horizontal-stack-description.line-height} = 20px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root/horizontal stack/horizontal stack/Description line-height
- MINTED {imported.molecules-alert.root.box-shadow} = 0px 12px 60px #00000026, 0px 12px 32px -16px #0009321f — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root box-shadow
- MINTED {imported.molecules-alert.root.width} = 390px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Molecules/Alert:root width
- stub ds.icons-check-circle: observed paint var-bound on some occurrence(s) but not one variable across all — background not carried on the stub geometry, review
- stub ds.icons-check-circle: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-icons-check-circle.root.width} = 16px — stub geometry (the "Icons/Check Circle" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-check-circle width
- MINTED {imported.stub-icons-check-circle.root.height} = 16px — stub geometry (the "Icons/Check Circle" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-check-circle height
- stub ds.atoms-text-link: observed primary paint is bound to variable "comp/button/primary/color/content/default" on every occurrence — background-color carried as {comp.button.primary.color.content.default} (resolves through the captured-token layer; dump v1.7 instancePrimaryFill)
- stub ds.atoms-text-link: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box + primary paint) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-atoms-text-link.root.width} = 37px — stub geometry (the "Atoms/Text Link" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.atoms-text-link width
- MINTED {imported.stub-atoms-text-link.root.height} = 20px — stub geometry (the "Atoms/Text Link" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.atoms-text-link height
- stub ds.icons-close: observed primary paint is bound to variable "color/content/inverse" on every occurrence — background-color carried as {color.content.inverse} (resolves through the captured-token layer; dump v1.7 instancePrimaryFill)
- stub ds.icons-close: renders HONEST OBSERVED GEOMETRY (dump v1.5 bounding box + primary paint) via minted imported.stub-* tokens — a correctly-sized box, NOT the child's anatomy (still not captured); import the real child set to replace it
- MINTED {imported.stub-icons-close.root.width} = 16px — stub geometry (the "Icons/Close" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-close width
- MINTED {imported.stub-icons-close.root.height} = 16px — stub geometry (the "Icons/Close" instances' OBSERVED box/paint, dump v1.5; provisional) — bound at: stub ds.icons-close height
