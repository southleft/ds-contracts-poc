{
  "$schema": "./contract.schema.json",
  "id": "fluent.card",
  "name": "Card",
  "version": "0.1.0",
  "status": "draft",
  "description": "SEED contract for the FLUENT 2 round — props/axes only; anatomy is promoted from captured DOM truth. Subject: @fluentui/react-components@9.74.5 (Griffel CSS-in-JS; 65-package family pinned by the committed lockfile sha256 c3b230dfbd8abd68408fefed9c8abc0e0fd46722faf8652e16e5c038452e0536 — examples/fluent/RECON.md §1). EVERY enum default below is HAND-TRANSCRIBED from the package rollup's own @default/@defaultvalue JSDoc tag: the react-tsx extractor keeps the description prose but emits no `default` field (RECON §3a), so left to the drafter two of twelve components would have captured their whole grid around a base combo the library never renders. @default tags: appearance=filled, size=medium, orientation=vertical, focusMode=off (pinned), selected=false (pinned). CardHeader/CardPreview have no readable props (RECON §3b — the slot API), so they are COMPOSITION, not components.",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "appearance",
      "type": {
        "enum": [
          "filled",
          "filled-alternative",
          "outline",
          "subtle"
        ]
      },
      "default": "filled",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Appearance",
          "values": {
            "filled": "Filled",
            "filled-alternative": "Filled Alternative",
            "outline": "Outline",
            "subtle": "Subtle"
          }
        },
        "code": {
          "prop": "appearance"
        }
      }
    },
    {
      "name": "size",
      "type": {
        "enum": [
          "small",
          "medium",
          "large"
        ]
      },
      "default": "medium",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Size",
          "values": {
            "small": "Small",
            "medium": "Medium",
            "large": "Large"
          }
        },
        "code": {
          "prop": "size"
        }
      }
    },
    {
      "name": "orientation",
      "type": {
        "enum": [
          "horizontal",
          "vertical"
        ]
      },
      "default": "vertical",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Orientation",
          "values": {
            "horizontal": "Horizontal",
            "vertical": "Vertical"
          }
        },
        "code": {
          "prop": "orientation"
        }
      }
    }
  ],
  "states": [],
  "anatomy": {
    "root": {}
  },
  "anchors": {
    "figma": {
      "fileKey": null,
      "componentSetKey": null
    },
    "code": {
      "importPath": "@fluentui/react-components",
      "export": "Card"
    }
  }
}
