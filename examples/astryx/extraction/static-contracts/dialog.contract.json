{
  "$schema": "./contract.schema.json",
  "id": "astryx.dialog",
  "name": "Dialog",
  "version": "0.1.0",
  "status": "draft",
  "description": "PROPOSED contract extracted from examples/astryx/.astryx-sandbox/node_modules/@astryxdesign/core/src/Dialog/Dialog.tsx (react-tsx adapter) — API surface only; anatomy, tokens, and design bindings await reconciliation and human review.",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "isOpen",
      "description": "Whether the dialog is open.",
      "type": "boolean",
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Is Open"
        },
        "code": {
          "prop": "isOpen"
        }
      }
    },
    {
      "name": "isInline",
      "description": "Renders dialog content inline without the <dialog> element, backdrop,\nmodal behavior, or dialog-managed autofocus. Intended for documentation\npreviews and showcases only — not for production UIs. The dialog will not\ntrap focus or respond to Escape.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Is Inline"
        },
        "code": {
          "prop": "isInline"
        }
      }
    },
    {
      "name": "variant",
      "description": "The variant of the dialog.\n- standard: Normal dialog with configurable dimensions\n- fullscreen: Takes up the entire viewport",
      "type": {
        "enum": [
          "standard",
          "fullscreen"
        ]
      },
      "default": "standard",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Variant",
          "values": {
            "standard": "Standard",
            "fullscreen": "Fullscreen"
          }
        },
        "code": {
          "prop": "variant"
        }
      }
    },
    {
      "name": "purpose",
      "description": "Configures how the dialog enables dismissals.\n- required: Disables all exit methods (for mandatory flows)\n- form: Prevents backdrop click, allows Escape key\n- info: Allows all exit methods (Escape and backdrop click)",
      "type": {
        "enum": [
          "required",
          "form",
          "info"
        ]
      },
      "default": "info",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Purpose",
          "values": {
            "required": "Required",
            "form": "Form",
            "info": "Info"
          }
        },
        "code": {
          "prop": "purpose"
        }
      }
    }
  ],
  "events": [
    {
      "name": "openChange",
      "description": "Callback fired when the dialog visibility changes.\nCalled with `false` when the dialog requests to be hidden.\nBehavior depends on the `purpose` prop:\n- required: Never called automatically\n- form: Called on Escape key only\n- info: Called on Escape key and backdrop click",
      "bindings": {
        "code": {
          "prop": "onOpenChange"
        }
      },
      "trigger": "root"
    }
  ],
  "states": [],
  "anatomy": {
    "root": {}
  },
  "anchors": {
    "figma": {
      "fileKey": null,
      "componentSetKey": null
    },
    "code": {
      "importPath": "examples/astryx/.astryx-sandbox/node_modules/@astryxdesign/core/src/Dialog/Dialog",
      "export": "Dialog"
    }
  }
}
