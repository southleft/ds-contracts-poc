{
  "$schema": "./contract.schema.json",
  "id": "astryx.button",
  "name": "Button",
  "version": "0.1.0",
  "status": "draft",
  "description": "PROPOSED contract extracted from examples/astryx/.astryx-sandbox/node_modules/@astryxdesign/core/src/Button/Button.tsx (react-tsx adapter) — API surface only; anatomy, tokens, and design bindings await reconciliation and human review.",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "type",
      "description": "HTML button type attribute.",
      "type": {
        "enum": [
          "button",
          "submit",
          "reset"
        ]
      },
      "default": "button",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Type",
          "values": {
            "button": "Button",
            "submit": "Submit",
            "reset": "Reset"
          }
        },
        "code": {
          "prop": "type"
        }
      }
    },
    {
      "name": "name",
      "description": "HTML name attribute for form submission.",
      "type": "text",
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Name"
        },
        "code": {
          "prop": "name"
        }
      }
    },
    {
      "name": "form",
      "description": "Associates the button with a form element by ID.",
      "type": "text",
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Form"
        },
        "code": {
          "prop": "form"
        }
      }
    },
    {
      "name": "label",
      "description": "Accessible label for the button (required for accessibility).\nRendered as visible text by default. When `isIconOnly` is true,\nused as aria-label instead.",
      "type": "text",
      "required": true,
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Label"
        },
        "code": {
          "prop": "label"
        }
      }
    },
    {
      "name": "variant",
      "description": "The visual style variant of the button.",
      "type": {
        "enum": [
          "primary",
          "secondary",
          "ghost",
          "destructive"
        ]
      },
      "default": "secondary",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Variant",
          "values": {
            "primary": "Primary",
            "secondary": "Secondary",
            "ghost": "Ghost",
            "destructive": "Destructive"
          }
        },
        "code": {
          "prop": "variant"
        }
      }
    },
    {
      "name": "size",
      "description": "The size of the button.",
      "type": {
        "enum": [
          "sm",
          "md",
          "lg"
        ]
      },
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Size",
          "values": {
            "sm": "Sm",
            "md": "Md",
            "lg": "Lg"
          }
        },
        "code": {
          "prop": "size"
        }
      }
    },
    {
      "name": "isDisabled",
      "description": "Whether the button is disabled.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Is Disabled"
        },
        "code": {
          "prop": "isDisabled"
        }
      }
    },
    {
      "name": "isLoading",
      "description": "Whether the button is in a loading state.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Is Loading"
        },
        "code": {
          "prop": "isLoading"
        }
      }
    },
    {
      "name": "isInterruptible",
      "description": "Keep the button interactive while a `clickAction` is pending. The loading\nstate still renders the spinner and `aria-busy`, but the button is not\ndisabled and the in-flight action is not deduped — so a re-click lands and\ninterrupts the previous action with a fresh one. Use for interruptible\nactions (e.g. a toggle whose action can be re-triggered before the previous\none settles), not fire-once actions (submit/save/pay).",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Is Interruptible"
        },
        "code": {
          "prop": "isInterruptible"
        }
      }
    },
    {
      "name": "isIconOnly",
      "description": "When true, renders as a square icon-only button with `label` as aria-label.\nRequires `icon` to be provided.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Is Icon Only"
        },
        "code": {
          "prop": "isIconOnly"
        }
      }
    },
    {
      "name": "tooltip",
      "description": "Tooltip text shown on hover.",
      "type": "text",
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Tooltip"
        },
        "code": {
          "prop": "tooltip"
        }
      }
    },
    {
      "name": "href",
      "description": "When provided, renders the button as a link (`<a>` or custom component).\nWhen the button is disabled, still renders as `<button>` regardless of href\n(disabled links are an accessibility anti-pattern).",
      "type": "text",
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Href"
        },
        "code": {
          "prop": "href"
        }
      }
    },
    {
      "name": "target",
      "description": "HTML target attribute for the link. Only applies when `href` is provided.",
      "type": "text",
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Target"
        },
        "code": {
          "prop": "target"
        }
      }
    },
    {
      "name": "rel",
      "description": "HTML rel attribute for the link. Only applies when `href` is provided.",
      "type": "text",
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Rel"
        },
        "code": {
          "prop": "rel"
        }
      }
    }
  ],
  "states": [],
  "anatomy": {
    "root": {}
  },
  "anchors": {
    "figma": {
      "fileKey": null,
      "componentSetKey": null
    },
    "code": {
      "importPath": "examples/astryx/.astryx-sandbox/node_modules/@astryxdesign/core/src/Button/Button",
      "export": "Button"
    }
  }
}
