{
  "$schema": "./contract.schema.json",
  "id": "conformance.svg-glyph-axes-ragged",
  "name": "CaseSvgGlyphAxesRagged",
  "version": "0.1.0",
  "status": "draft",
  "description": "SEED contract for the CSS/DOM conformance fixture case \"svg-glyph-axes-ragged\" (svg). The fixture varies CSS CONSTRUCTS, not prop spaces, so the prop space is deliberately EMPTY: every axis the capture would enumerate is held at nothing, and the whole measurement is the computed truth of one mounted rendering. Construct: glyph markup whose value sets are RAGGED across two axes — one glyph on the (plain,sm)/(marked,lg) diagonal, two others on the opposite one",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "tone",
      "type": {
        "enum": [
          "plain",
          "marked"
        ]
      },
      "default": "plain",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Tone",
          "values": {
            "plain": "Plain",
            "marked": "Marked"
          }
        },
        "code": {
          "prop": "tone"
        }
      }
    },
    {
      "name": "size",
      "type": {
        "enum": [
          "sm",
          "lg"
        ]
      },
      "default": "sm",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Size",
          "values": {
            "sm": "Sm",
            "lg": "Lg"
          }
        },
        "code": {
          "prop": "size"
        }
      }
    }
  ],
  "states": [],
  "anatomy": {
    "root": {}
  },
  "bindings": {
    "figma": {
      "anchors": {
        "fileKey": null,
        "componentSetKey": null
      }
    },
    "code": {
      "anchors": {
        "importPath": "@ds-contracts/conformance",
        "export": "CaseSvgGlyphAxesRagged"
      }
    }
  }
}
