{
  "$schema": "./contract.schema.json",
  "id": "conformance.repeat-siblings-three",
  "name": "CaseRepeatSiblingsThree",
  "version": "0.1.0",
  "status": "draft",
  "description": "SEED contract for the CSS/DOM conformance fixture case \"repeat-siblings-three\" (repetition). The fixture varies CSS CONSTRUCTS, not prop spaces, so the prop space is deliberately EMPTY: every axis the capture would enumerate is held at nothing, and the whole measurement is the computed truth of one mounted rendering. Construct: 3 adjacent same-shape siblings, the LAST carrying a distinct paint (rgb(191, 64, 32))",
  "semantics": {
    "element": "div"
  },
  "props": [],
  "states": [],
  "anatomy": {
    "root": {}
  },
  "bindings": {
    "figma": {
      "anchors": {
        "fileKey": null,
        "componentSetKey": null
      }
    },
    "code": {
      "anchors": {
        "importPath": "@ds-contracts/conformance",
        "export": "CaseRepeatSiblingsThree"
      }
    }
  }
}
