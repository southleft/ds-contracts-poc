{
  "$schema": "./contract.schema.json",
  "id": "conformance.pseudo-inset-ring-two-axis",
  "name": "CasePseudoInsetRingTwoAxis",
  "version": "0.1.0",
  "status": "draft",
  "description": "SEED contract for the CSS/DOM conformance fixture case \"pseudo-inset-ring-two-axis\" (pseudo-elements). The fixture varies CSS CONSTRUCTS, not prop spaces, so the prop space is deliberately EMPTY: every axis the capture would enumerate is held at nothing, and the whole measurement is the computed truth of one mounted rendering. Construct: a COINCIDENT INSET RING pseudo — ::after at inset 0, transparent fill, uniform 1px border, host radius — whose ring COLOUR is a product of TWO enum axes while its four side WIDTHS are uniform across every combo (fluent Badge's `root::after`)",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "appearance",
      "type": {
        "enum": [
          "filled",
          "outline",
          "tint"
        ]
      },
      "default": "filled",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Appearance",
          "values": {
            "filled": "Filled",
            "outline": "Outline",
            "tint": "Tint"
          }
        },
        "code": {
          "prop": "appearance"
        }
      }
    },
    {
      "name": "tone",
      "type": {
        "enum": [
          "brand",
          "danger"
        ]
      },
      "default": "brand",
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Tone",
          "values": {
            "brand": "Brand",
            "danger": "Danger"
          }
        },
        "code": {
          "prop": "tone"
        }
      }
    }
  ],
  "states": [],
  "anatomy": {
    "root": {}
  },
  "bindings": {
    "figma": {
      "anchors": {
        "fileKey": null,
        "componentSetKey": null
      }
    },
    "code": {
      "anchors": {
        "importPath": "@ds-contracts/conformance",
        "export": "CasePseudoInsetRingTwoAxis"
      }
    }
  }
}
