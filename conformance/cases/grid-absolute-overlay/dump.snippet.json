{
  "_marker": "design-side fixture for the canvasToCode direction of \"grid-absolute-overlay\" (dump v1.17 grammar — extract/figma/types.ts DumpGrid/cell). Hand-authored from the A1 probe receipts (docs/research/grid-recon-probes.md P13) and re-decided by the composition round (layout-grammar-proposal.md G9). RESOLVED 2026-08-08 by the composition round (layout-grammar-proposal.md G5′/G9). This snippet is no longer a pinned red: the GRID is carried whole — tracks, gaps and every sibling placement — and the absolute child rides the EXISTING out-of-flow door: carryAbsPlacement inverts dump `abs` into position:absolute plus minted px insets, losing no coordinate, exactly as absolute children are carried under every other parent kind. What is refused is only the abs -> Part.overlay EDGE inversion, and it is refused PERMANENTLY and by name (grid-overlay-edge-inversion, G9.1) on ARITY rather than on a Plugin API dead end: OverlaySchema is four edge values with NO offset channel while the canvas fact is a point in R² plus a 3x3 constraints lattice, so every total map either drops the offsets (a silent geometry rewrite) or invents a channel the schema does not have. Nobody should re-probe Figma looking for that door. P13's dump gate is untouched: the ABSOLUTE child still carries `abs` and NO `cell`, so no 0,0 placement is invented, and the referee's all-or-none placement rule (G2) now excludes out-of-flow parts generally (overlay OR absolute) instead of knowing only about `overlay` — the referee matching the instrument. Measured by the registered eval `grid-canvas-conformance`; the case is now declared green with no observedCheck pin.",
  "direction": {
    "expect": "CARRIED",
    "note": "QUIRK: absolute children still REPORT anchors (0,0) — readers gate anchor reads on layoutPositioning !== 'ABSOLUTE'; the child carries out-of-flow via `abs` (G9.2) and the edge inversion is refused by name (grid-overlay-edge-inversion, G9.1)"
  },
  "note": "P13 quirk on record: the absolute child reports anchors (0,0); the dump producer gates cell capture on layoutPositioning !== ABSOLUTE. The grid carries whole; the child carries out-of-flow through `abs`.",
  "node": {
    "name": "CaseGridAbsoluteOverlay",
    "type": "COMPONENT",
    "layout": "grid",
    "grid": {
      "rows": [
        {
          "px": 24
        },
        {
          "px": 24
        }
      ],
      "columns": [
        {
          "fr": 1
        },
        {
          "fr": 1
        }
      ],
      "rowGap": 0,
      "columnGap": 0
    },
    "children": [
      {
        "name": "a",
        "type": "FRAME",
        "cell": {
          "row": 0,
          "column": 0
        }
      },
      {
        "name": "b",
        "type": "FRAME",
        "layoutPositioning": "ABSOLUTE",
        "x": 4,
        "y": 4
      }
    ]
  }
}
