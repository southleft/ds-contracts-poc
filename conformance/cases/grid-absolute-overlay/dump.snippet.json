{
  "_marker": "design-side fixture for the canvasToCode direction of \"grid-absolute-overlay\" (dump v1.17 grammar — extract/figma/types.ts DumpGrid/cell). Hand-authored from the same A1 probe receipts as the case declaration (docs/research/grid-recon-probes.md). GATED SINCE 2026-08-08 — THIS SNIPPET IS NO LONGER AWAITING ITS INSTRUMENT. The canvasToCode reader EXISTS and has since commit 0161ef9f: core/propose-figma.ts carries gridCarriageOf, invertGridLayout, attachGridPlacement and hoistGridAreas (extract/figma/propose.ts re-exports the engine). This snippet is mechanically expanded into the full dump v1.17 grammar as extract/figma/conformance/cases/grid-absolute-overlay.dump.json — the expansion adds no grid fact, it only spells `layout:\"grid\"` as the DumpLayout block around the snippet's own `grid` object — and is measured against the hand-authored denominator in extract/figma/conformance/MANIFEST.json by the REGISTERED eval `grid-canvas-conformance` (npm run eval). VERDICT THERE TODAY: CARRIED declared, pinned FAIL-EXPECTED-RED — the doc model calls it CARRIED and the engine does not do it yet; the status quo is pinned in the manifest's observedCheck, so the loss is a named finding that breaks loudly the day it changes, not a silence. The closed loop is the sibling registered eval `grid-roundtrip-identity`: contract → emit-figma-script → the strict Figma mock → THE PLUGIN'S OWN dump script (figma-sync/plugin/ui.html #dump-source) → proposeFromDump → the contract again, asserted for IDENTITY on the P8 bento and red-tested on the canvas half (a span rewritten on the built node must change the recovered contract). WHY THE OLD MARKER WAS WRONG: it opened with a \"nothing reads this snippet yet\" claim and stated that extract/figma/propose.ts reads no grid facts. That absence was the INSTRUMENT'S, not the engine's. core/propose-figma.ts contains three literal NUL bytes (composite-key separators in template literals), so file(1) classes it as binary data and a plain `grep` SKIPS it entirely — `grep -c grid core/propose-figma.ts` prints nothing and exits 1, which reads as \"zero hits\" while `grep -ac grid` on the same file returns 63. The gap was measured through a grep that never opened the file. PINNED FINDING: the dump gate is correct (the ABSOLUTE child carries `abs` and NO cell, so no 0,0 placement is invented — exactly what P13 demands), but the reader then refuses the WHOLE grid because it has no abs→overlay inversion: Part.overlay spells attachment as the v7 EDGE enum (top|bottom|start|end) while the canvas fact is a raw x/y inside a cell, and choosing an edge would be an invented fact. The refusal was rewritten this round to name THAT cause instead of blaming the child for a cell the dump was right not to capture.",
  "direction": {
    "expect": "CARRIED",
    "note": "QUIRK: absolute children still REPORT anchors (0,0) — readers must gate anchor reads on layoutPositioning !== 'ABSOLUTE' or they invent a placement"
  },
  "note": "P13 quirk on record: the absolute child reports anchors (0,0); the dump producer gates cell capture on layoutPositioning !== ABSOLUTE.",
  "node": {
    "name": "CaseGridAbsoluteOverlay",
    "type": "COMPONENT",
    "layout": "grid",
    "grid": {
      "rows": [
        {
          "px": 24
        },
        {
          "px": 24
        }
      ],
      "columns": [
        {
          "fr": 1
        },
        {
          "fr": 1
        }
      ],
      "rowGap": 0,
      "columnGap": 0
    },
    "children": [
      {
        "name": "a",
        "type": "FRAME",
        "cell": {
          "row": 0,
          "column": 0
        }
      },
      {
        "name": "b",
        "type": "FRAME",
        "layoutPositioning": "ABSOLUTE",
        "x": 4,
        "y": 4
      }
    ]
  }
}
