{
  "_marker": "design-side fixture for the canvasToCode direction of \"grid-auto-flow-row\" (dump v1.17 grammar — extract/figma/types.ts DumpGrid/cell). Hand-authored from the same A1 probe receipts as the case declaration (docs/research/grid-recon-probes.md). GATED SINCE 2026-08-08 — THIS SNIPPET IS NO LONGER AWAITING ITS INSTRUMENT. The canvasToCode reader EXISTS and has since commit 0161ef9f: core/propose-figma.ts carries gridCarriageOf, invertGridLayout, attachGridPlacement and hoistGridAreas (extract/figma/propose.ts re-exports the engine). This snippet is mechanically expanded into the full dump v1.17 grammar as extract/figma/conformance/cases/grid-auto-flow-row.dump.json — the expansion adds no grid fact, it only spells `layout:\"grid\"` as the DumpLayout block around the snippet's own `grid` object — and is measured against the hand-authored denominator in extract/figma/conformance/MANIFEST.json by the REGISTERED eval `grid-canvas-conformance` (npm run eval). VERDICT THERE TODAY: CARRIED declared, pinned FAIL-EXPECTED-RED — the doc model calls it CARRIED and the engine does not do it yet; the status quo is pinned in the manifest's observedCheck, so the loss is a named finding that breaks loudly the day it changes, not a silence. The closed loop is the sibling registered eval `grid-roundtrip-identity`: contract → emit-figma-script → the strict Figma mock → THE PLUGIN'S OWN dump script (figma-sync/plugin/ui.html #dump-source) → proposeFromDump → the contract again, asserted for IDENTITY on the P8 bento and red-tested on the canvas half (a span rewritten on the built node must change the recovered contract). WHY THE OLD MARKER WAS WRONG: it opened with a \"nothing reads this snippet yet\" claim and stated that extract/figma/propose.ts reads no grid facts. That absence was the INSTRUMENT'S, not the engine's. core/propose-figma.ts contains three literal NUL bytes (composite-key separators in template literals), so file(1) classes it as binary data and a plain `grep` SKIPS it entirely — `grep -c grid core/propose-figma.ts` prints nothing and exits 1, which reads as \"zero hits\" while `grep -ac grid` on the same file returns 63. The gap was measured through a grep that never opened the file. PINNED FINDING: G5 lets the contract carry flow ONLY with `rows` omitted, so this snippet's two declared 16px row tracks have nowhere to land — carrying flow would silently redraw them as the emitter derivation ({fr:1} × ceil(children/columns)) on the next round trip, the P9 rewrite the grammar exists to prevent. The reader therefore admits flow only when the drawn rows ALREADY equal that derivation, and a flow grid that does round-trips exactly (proven in the grid-roundtrip-identity eval, section 4). Closing this needs a grammar decision — a flow spelling that can carry declared row sizes — not a reader patch.",
  "direction": {
    "expect": "CARRIED",
    "note": "anchors read back computed row-major from order — deterministic; carriage conditional on occupied rows == declared rows, else grid-implicit-tracks fires"
  },
  "note": "ROW_AUTO_FLOW: no cells captured — order IS the fact (P5); carriage conditional on occupied rows == declared rows else grid-implicit-tracks fires.",
  "node": {
    "name": "CaseGridAutoFlowRow",
    "type": "COMPONENT",
    "layout": "grid",
    "grid": {
      "rows": [
        {
          "px": 16
        },
        {
          "px": 16
        }
      ],
      "columns": [
        {
          "fr": 1
        },
        {
          "fr": 1
        }
      ],
      "rowGap": 0,
      "columnGap": 0,
      "flow": "row"
    },
    "children": [
      {
        "name": "a",
        "type": "FRAME"
      },
      {
        "name": "b",
        "type": "FRAME"
      },
      {
        "name": "c",
        "type": "FRAME"
      }
    ]
  }
}
