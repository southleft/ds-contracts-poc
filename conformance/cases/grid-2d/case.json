{
  "id": "grid-2d",
  "feature": "layout",
  "construct": "display: grid + grid-template-columns: 1fr 1fr",
  "why": "Two-dimensional grid is the layout every data-dense DS ships. STAGED WIDEN, NOT LANDED: the A1 grid recon (docs/research/grid-recon-probes.md P1/P8) removed the PLATFORM limit — layoutMode GRID exists and tracks read back byte-exactly — and the A2 engine carries EXPLICITLY-PLACED grids (grid-bento-span-matrix passes as CARRIED). But this case's children are AUTO-PLACED, and placement-from-order promotion (G5) is deliberately not in the engine this round, so the measured disposition is still the named refusal. The frozen subset (spec/conformance/subset-v0.1.json) keeps REFUSED until this case measures CARRIED; declaring the widen before the engine lands it would be exactly the silent widen the subset's change rule forbids.",
  "expect": "REFUSED",
  "expectName": "grid-two-dimensional",
  "expectWhere": "LEDGER.md (DOM-anatomy promotion refusals — the flex-era fence names grid-two-dimensional for auto-placed 2-D grids pending G5 promotion)",
  "observable": {
    "part": "root",
    "channel": "display",
    "capturedValue": "grid"
  },
  "canvas": {
    "expect": "PRESENT",
    "note": "layoutMode GRID + gridColumnSizes [{FLEX,1},{FLEX,1}] — the grid DEFAULT, reads back exactly (P1/P8). The canvas CAN hold this construct now; the refusal is the ENGINE's auto-placement gap (G5), no longer a canvas limit — PRESENT also keeps a future engine carry non-harmful (good news, wrong manifest)."
  },
  "blockStage": true
}
