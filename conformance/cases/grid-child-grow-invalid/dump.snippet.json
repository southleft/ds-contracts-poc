{
  "_marker": "design-side fixture for the canvasToCode direction of \"grid-child-grow-invalid\" (dump v1.17 grammar — extract/figma/types.ts DumpGrid/cell). Hand-authored from the same A1 probe receipts as the case declaration (docs/research/grid-recon-probes.md). GATED SINCE 2026-08-08 — THIS SNIPPET IS NO LONGER AWAITING ITS INSTRUMENT. The canvasToCode reader EXISTS and has since commit 0161ef9f: core/propose-figma.ts carries gridCarriageOf, invertGridLayout, attachGridPlacement and hoistGridAreas (extract/figma/propose.ts re-exports the engine). This snippet is mechanically expanded into the full dump v1.17 grammar as extract/figma/conformance/cases/grid-child-grow-invalid.dump.json — the expansion adds no grid fact, it only spells `layout:\"grid\"` as the DumpLayout block around the snippet's own `grid` object — and is measured against the hand-authored denominator in extract/figma/conformance/MANIFEST.json by the REGISTERED eval `grid-canvas-conformance` (npm run eval). VERDICT THERE TODAY: CARRIED / PASS — the reader meets the documentation model for this construct. The closed loop is the sibling registered eval `grid-roundtrip-identity`: contract → emit-figma-script → the strict Figma mock → THE PLUGIN'S OWN dump script (figma-sync/plugin/ui.html #dump-source) → proposeFromDump → the contract again, asserted for IDENTITY on the P8 bento and red-tested on the canvas half (a span rewritten on the built node must change the recovered contract). WHY THE OLD MARKER WAS WRONG: it opened with a \"nothing reads this snippet yet\" claim and stated that extract/figma/propose.ts reads no grid facts. That absence was the INSTRUMENT'S, not the engine's. core/propose-figma.ts contains three literal NUL bytes (composite-key separators in template literals), so file(1) classes it as binary data and a plain `grep` SKIPS it entirely — `grep -c grid core/propose-figma.ts` prints nothing and exits 1, which reads as \"zero hits\" while `grep -ac grid` on the same file returns 63. The gap was measured through a grep that never opened the file. NOTE: the canvasToCode REFUSAL here is of the GROW fact, not of the grid. layoutGrow on a grid child is P4's silent-accept hazard, so the dump grammar gives it no channel at all — the fact cannot reach the reader — and the manifest's `absent` probe measures that no flex-grow was invented from anything else. The grid itself carries.",
  "direction": {
    "expect": "REFUSED",
    "note": "reader must NOT emit flex-grow from layoutGrow when parent mode is GRID"
  },
  "note": "canvasToCode REFUSED: the reader must NOT emit flex-grow from layoutGrow when the parent mode is GRID (P4: layoutGrow accepted without error, no layout effect).",
  "node": {
    "name": "CaseGridChildGrowInvalid",
    "type": "COMPONENT",
    "layout": "grid",
    "grid": {
      "rows": [
        {
          "px": 16
        }
      ],
      "columns": [
        {
          "fr": 1
        },
        {
          "fr": 1
        }
      ],
      "rowGap": 0,
      "columnGap": 0
    },
    "children": [
      {
        "name": "a",
        "type": "FRAME",
        "cell": {
          "row": 0,
          "column": 0
        },
        "layoutGrow": 1
      },
      {
        "name": "b",
        "type": "FRAME",
        "cell": {
          "row": 0,
          "column": 1
        }
      }
    ]
  }
}
