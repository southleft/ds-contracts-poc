{
  "_marker": "CHILD-WIDER-THAN-PARENT BASELINE — a committed per-library defect count that may only DECREASE. `overflows` counts in-flow children wider than their parent with a non-text cause; `textCaused` counts the exempted class (the corpus-wide text-wrapping gap, docs/22) separately so it can never flatter the first number. Both ratchet TWO-SIDED: an increase is a new defect, a decrease is a fix that must be re-recorded here with its cause named. Re-record deliberately with `node scripts/child-wider.mjs --write` and say what moved.",
  "recordedAt": "2026-08-26",
  "rows": [
    {
      "library": "altitude",
      "scripts": 8,
      "overflows": 0,
      "textCaused": 0,
      "marginBox": 0,
      "cause": "",
      "detail": []
    },
    {
      "library": "antd",
      "scripts": 12,
      "overflows": 0,
      "textCaused": 0,
      "marginBox": 0,
      "cause": "",
      "detail": []
    },
    {
      "library": "astryx",
      "scripts": 13,
      "overflows": 0,
      "textCaused": 6,
      "marginBox": 0,
      "cause": "textCaused(6): 33 -> 6 on the census/stack-to-main merge — an IMPROVEMENT, re-recorded so the number cannot regrow into the slack. The old 33 was the exact-conversion wave's Banner title/description rows, TextInput label/helper runs and the source-geometry Slider labels. MEASURED per script into one shared mock (scripts are identity-dependent, so per-script isolation is not a valid instrument here): checkbox-input +2, switch +4, and EVERY OTHER astryx script is now zero — banner, text-input, slider and progress-bar all closed. The merged engine is the cause: RC1's layout-axis door plus the stack's re-derived corpus (cb13a56f) give those runs a real direction and width instead of the harness-floor geometry that made them hug. overflows stays a HARD ZERO.",
      "detail": []
    },
    {
      "library": "carbon",
      "scripts": 10,
      "overflows": 0,
      "textCaused": 8,
      "marginBox": 0,
      "cause": "textCaused(8): all eight are the corpus-wide text-wrapping gap in Accordion (docs/22). Carbon's NON-text overflows were closed by the live-defect round (9c30204, D3) and its own compile receipt keeps a HARD zero on them — this row is the exempted class only.",
      "detail": []
    },
    {
      "library": "fluent",
      "scripts": 11,
      "overflows": 2,
      "textCaused": 34,
      "marginBox": 41,
      "cause": "overflows(2) unchanged — Switch's `label` at 66px inside a 59px parent on the two `Label Position=Above` cells, where the stacked layout gives the label a narrower column than its own hug width. The Checkbox glyph is STILL missing (examples/fluent/assets/icons ships only dialog-part-0-1-0.svg and switch-indicator.svg) and is still named here; only the instrument stopped inflating it at c27e3fe0. textCaused(34) unchanged. marginBox(41): 43 -> 41 on the census/stack-to-main merge — an IMPROVEMENT, re-recorded. MEASURED per script: card 40, dialog 1, and nothing else. The two that closed are on the grid-lowered parts main's grid work re-derived (cardheader.display / grid-template-rows leave fluent/Card's mismatch table by 192 cells in the same round). All are NEGATIVE-MARGIN paint-outside, not overflows — the margin-box wrapper reserving less layout space than its child paints is what a negative margin is for.",
      "detail": [
        "\"label\" 66 > parent \"Checked=Unchecked, Label Position=Above, State=Default\" 59 (Checked=Unchecked, Label Position=Above, State=Default)",
        "\"label\" 66 > parent \"Checked=Checked, Label Position=Above, State=Default\" 59 (Checked=Checked, Label Position=Above, State=Default)"
      ]
    },
    {
      "library": "mui",
      "scripts": 31,
      "overflows": 2,
      "textCaused": 14,
      "marginBox": 2,
      "cause": "overflows(2): Wave 5 denominator — `label` paints 75px inside a 74px Size=Medium/Small hug cell (Size=Medium + Size=Small). textCaused(14): corpus-wide text-wrapping gap (docs/22). 56→14 on 2026-08-22 re-record, and it is a FIX already reviewed, not today’s re-emission: commit 16889547 (2026-08-17, VISUAL-REVIEW-2026-08-17.md “Lin/k wrap in every cell”) dropped `imported.link.root.width` — the 30.22px capture-font pin — from link.contract.json, so Link’s label hugs one line and all 42 link rows (one hugging label run per variant of the 42-variant set) closed at once; the baseline sat stale for five days because nothing re-ran the ratchet. Verified by bisect (2b0fceff 56 → 16889547 14) and a fresh emission with the 2026-08-22 emitter (`version` stamp + Disabled-opacity unbind) measures the SAME 2/14/2 — the emitter edits did not move geometry. History: grew 14→56 at the exact-conversion wave landing, when MUI's scripts were finally re-emitted against the wave engine (they had sat stale; the freshness gate is what caught it): the non-truncating-text HUG rule (Carbon Tabs fix, refined by FC-TEXT-FILL-ALIGNMENT so alignment-displaced text still FILLs) makes hugging label runs measurable where the old FILL lowering masked them. Attribution NOW: text-field 6, accordion 4 (AccordionDetails body remains the canonical case), autocomplete 2, select 2 (link’s 42 closed, above). marginBox(2): Autocomplete POPUP indicator margin-right -2 (unchanged from task-#38).",
      "detail": [
        "\"label\" 75 > parent \"Size=Medium\" 74 (Size=Medium)",
        "\"label\" 75 > parent \"Size=Small\" 74 (Size=Small)"
      ]
    },
    {
      "library": "polaris",
      "scripts": 12,
      "overflows": 0,
      "textCaused": 29,
      "marginBox": 58,
      "cause": "textCaused(29): 26 -> 29 on the census/stack-to-main merge — GREW by 3, and it is a fact that was SILENTLY ABSENT now being COUNTED, the same pattern this baseline already records for fluent's Tooltip. MEASURED per script: banner 8, checkbox 3, radio-button 2, tag 8, text-field 8. checkbox and radio-button are exactly the rows the rejected-sets round (#39) fixed: `fuse.page-inherited-ink-admitted` now admits the label's ink where the styled-channel door used to drop it for equalling a page-inked control, so the label text run is DRAWN for the first time and joins the corpus-wide text-wrapping class (docs/22). A drawn run that hugs is visible; an undrawn one was invisible AND uncounted. marginBox(58) unchanged — all negative-margin paint-outside. Polaris keeps ZERO real child-wider overflows.",
      "detail": []
    },
    {
      "library": "shadcn",
      "scripts": 11,
      "overflows": 0,
      "textCaused": 2,
      "marginBox": 0,
      "cause": "textCaused(2): 0 -> 2 on the census/stack-to-main merge — GREW by 2, both in ALERT and both the same not-a-regression shape as polaris above. MEASURED per script: alert +2, every other shadcn script zero. shadcn.alert is one of the rows the rejected-sets round moved (`fuse.reset-supplied-border-color-admitted` — shadcn's `* { border-color: var(--border) }` reset), so the Alert now draws a real bordered surface and its title/description runs are measured against the interior rather than the old border-less box. The INSTRUMENT-GAP this row carried before still stands and is repeated here because re-recording must not quietly drop it: the per-component compile-receipt run witnesses ONE real overflow this row does not carry — Tooltip's arrow `part-0-0-0-0` (a lucide <polygon>) at the mock's default 100px inside its 10px icon frame, refused by name as svg-child-outside-grammar. countChildWider only scans set-parented cells, and shadcn's Tooltip builds as a STANDALONE component beside 8 sets, so it is structurally outside this instrument's denominator. This row's overflows zero UNDERSTATES by exactly that one named overflow. See examples/shadcn/PROVENANCE.md.",
      "detail": []
    },
    {
      "library": "tailwind",
      "scripts": 8,
      "overflows": 0,
      "textCaused": 10,
      "marginBox": 0,
      "cause": "textCaused(10): 0 -> 10, and it is NEW STEMS, not a regression. The 2026-08-09 baseline recorded FIVE scripts; the kit climb landed HelperText, Label and Kbd in 8afae937 (2026-08-15), so this row now measures EIGHT. Attribution, MEASURED per script rather than inferred: helper-text 5 + label 5 = 10, kbd 0, and all five original stems (alert, badge, button, card, toggle-switch) stay at 0. All ten are the corpus-wide text-wrapping gap (docs/22 §\"Text wrapping is not implemented\") — hugging text runs in the two text-shaped stems that joined. overflows stays a HARD ZERO: no non-text child of any Flowbite stem paints wider than its parent, and that number did not move when the set grew by three.",
      "detail": []
    }
  ]
}
