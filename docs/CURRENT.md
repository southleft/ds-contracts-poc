# React ↔ contracts ↔ Figma

**Public project status · updated 2026-09-17 · v1 is not complete.**

V1 targets React; Lit/Web Components are parked for V1.1. The goal is to connect a team's code and design libraries through a shared contract: code becomes editable design components, design becomes reusable code, and supported changes can be repaired in either direction. This includes composed component sets such as data tables, forms and dialogs, with nested components, slots, instance swaps and other properties.

This page is the current status and outcome-based work order. It is also rendered in the playground and documentation site at `/system`. Earlier plans and dated measurements do not override it.

For installation and a step-by-step walkthrough from each starting point, read the [user journey guide](../docs/USER-JOURNEYS.md). The local app exposes it at `/start`; the documentation site at `/get-started/`. These entry points now link to available import and source-inspection actions and name unfinished delivery steps.

## The whole loop

![V1 workflows: React to contract to editable Figma; Figma to contract to reusable React; and changes through comparison, authorized repair and independent verification.](assets/product-loop.svg)

1. **Observe the original.** Record the actual source revision or Figma identity, states, themes, fonts and assets. Confirm that the reference is correctly styled.
2. **Derive a contract.** Carry supported APIs, anatomy, tokens, variants and composition into a shared representation. Name missing or ambiguous facts.
3. **Generate the target.** Compile the contract into native editable Figma elements or reusable code through the existing emitters.
4. **Verify the result independently.** Compare the actual native canvas or running code with the original, including structure, token bindings, content, editability and declared behavior.
5. **Maintain the agreement.** When either side changes, compare both with the last verified baseline, apply authorized repairs, and verify again. A repeat run must make no changes.

These steps describe the intended product workflow. The full application integration remains unfinished.

## Where we are

**Current milestone: qualify the first React-led journey after completing its composed native delivery.** The app opens the original styled React implementation for the fixed Button, Checkbox and composed Card cohort. Source validation records the originals, checks authored style/font/state witnesses, replays archived resources without network fallback, and checks that intentionally broken references are rejected. Through the application and companion plugin, the composed Card now creates one operation-scoped native graph containing its seven dependencies and six parent variants. Independent readback authenticated the parent's structure, its 169 scoped variables, nested instance identities and main links, forwarded Checkbox state and caller-owned content; the seven dependency mains are verified by identity only, and their internals remain unverified against the plan. Inherited slots the parent leaves unfilled must mirror the main exactly. Recognizable Card text is a review observation, not a readback result. A live Figma proof changed one generated instance title while its sibling and source main stayed unchanged. Repeating preparation returned the same completed operation, with one operation page and one parent set. The app now shows the unchanged original beside the native default variant at one image pixel per CSS pixel, with all six variants expandable, and reports source authenticity, structural status and editability separately from still-unqualified visual fidelity. A second read-only readback one hour after creation exposed that Figma re-identifies caller content inside nested instance slots after a reload; the independent verifier now resolves those rows by their durable allocation stamps and slot topology without consulting names, and both saved readbacks verify with all 209 nodes retained. A fresh read-only readback through the companion on 2026-09-18, after that fix, again collected 209 nodes, passed supported-structure verification and returned all six variant images with layout bounds against an unchanged source. The same day, read-only readbacks through the companion re-observed the retained 63-main Button family (191 nodes, 63 variant images) and the corrected twelve-variant Checkbox (44 nodes, 12 variant images); both matched the postconditions of their last applied corrections with no further write. Visual fidelity, responsive behavior and interaction qualification remain unfinished.

| Area | Demonstrated today | Remaining product gap |
| --- | --- | --- |
| Original React source | A host-configured source preset opens ten original cases. The app exposes source checks, archived replay, measured trees, negative controls and side-by-side source images. Changed inputs or altered evidence invalidate readiness. | General repository onboarding, broader theme/state coverage and the admission boundary into conversion. |
| React → editable Figma | The local app creates native root variants and a closed composed Card graph through the companion plugin. The graph contains seven dependency components, six parent variants, bound variables, forwarded Boolean state, native nested instances and directly editable caller text. Independent readback of the parent passed, including after Figma re-identified slot content; dependency mains are checked by identity only. A live edit stayed isolated to one instance, and repeat preparation created no duplicate operation or component set. Dependencies that themselves carry caller content in nested slots are refused by inventory. One application review shows the unchanged original beside the native default variant at original pixel scale. | Qualify fidelity and behavior across the frozen cohort with a deterministic score, and reduce first-run latency. Unsupported nested property representations must continue to refuse by name. |
| Figma → reusable React | Readers propose contracts and deterministic emitters generate React. Both React emitters preserve declared root-toggle indeterminate state, disabled behavior and controlled values; an omitted state without a default remains unset. Declared next-value callbacks let consumers update controlled root and nested toggles, including triggers with literal text or slots. A separate declared initializer preserves default-only inputs without turning them into controlled state. A clean-consumer check now packages generated output, installs it into an isolated Vite consumer, mounts every variant, exercises the TEXT-bound prop and variant switching, checks whether undeclared children are refused or discarded, and scores each variant against Figma's render. On the designer-authored Altitude Badge set the library installed and behaved except that undeclared React children are accepted and discarded (a named failure), five dot variants scored 0% under the limit, and the five text variants, once both readers carried the designer's 1 px letter spacing instead of naming it a loss, render 57.27 px against Figma's 57 px: three pass the limit and two remain just above it (5.26 % and 6.90 %). The composed Altitude Tabs set installed with two auto-proposed child stubs and rendered its items from the design sample, its Stretch axis is no longer ledgered inert (the header's Stretch-only cross-axis FILL is carried as a per-variant `width: 100%` rule and the `variant` class is composed, though the rule moves no pixel: the header already rendered 439 px wide in both variants, and the list's and tabs' Stretch-only primary-axis FILL has no per-variant `grow` spelling and is named, so the tabs still render 58.86 px each in both variants), but the panel's sample content was not carried, and both variants failed the limit (5.33 % and 5.0049 %, unchanged); the header's per-side strokes are carried since dump v1.34, while the active tab's per-side indicator on an instance stub is named, not drawn. | Composition, instance swaps, declared behavior beyond text and variant props, accessibility measurement and the in-app review remain unfinished; two text-bearing Badge variants remain just above the image limit; the consumer check loads no font of its own and relies on the host having the design's family installed. |
| Changes and repair | The app applied reviewed opacity corrections to Checkbox variants, a fixed-height correction to a Button main, shadow-stack corrections to two Card mains, and SVG stroke corrections to six Checkbox vectors, with independent readbacks and preserved node identities. Channel diffs, three-way merge rules and proposal planning also exist. | Corrections cover opacity, literal dimensions on supported empty flex roots, supported root shadow stacks, and uniform strokes in simple viewBox-only SVG paths. Consecutive corrections retain a verified baseline chain; unresolved writes and conflicting changes refuse further application. The app can also repair the exact unrequested invisible default root paint on an otherwise valid draft; all other observed fields must match. The general sync spine still plans without applying. A code-only source change, a same-channel conflict, an interrupted write and an unchanged repeat are now live-proven on one stateful set (see the acceptance ledger); a design-only change is named and its agreement with changed code is verified without a canvas write; generating the code change, rollback through the app, and the same journey on a root family remain open. |
| Release readiness | Shared engine checks and bounded evidence are available. | Complete both journeys, repair/recovery and the independent cohort before qualifying V1. |

**No complete journey cohort has yet met the current v1 criteria.** Existing evidence reduces implementation uncertainty, but it is not a percentage of product completion. We do not have an evidence-backed completion date.

New root drafts can use the current compiler with unchanged, verified source observations, without repeating the source capture. Their prepared output is pinned before creation. Existing operations retain their original identity and evidence; compiler changes do not authorize replacing them.

Both preselected Card arrangements now have populated native comparisons and pass independent supported-structure readback. The header/body/footer case reused five existing child mains without a corrective edit; the other includes seven children with Checkbox and Button. Their exported images pass the existing 5% antialias-tolerant image-difference limit at 2.52% and 1.68%, respectively. That scorer trims surrounding whitespace; the app separately aligns images by measured layout origins. These are two rendered-case results, not qualification of the full cohort, responsive behavior or the complete journey.

All twelve saved Checkbox initial-state control exports now pass the existing image limit, with 0% antialias-tolerant difference. The application corrected six SVG stroke weights on the existing nodes, preserving their paths, bindings, identities and the earlier disabled-opacity correction. A separate native readback verified the complete saved structure and produced fresh exports. The shared compiler now imports SVGs at their intended viewport so new output scales strokes correctly. These native control-only results do not establish label relationships, keyboard behavior or completion of the full journey. Source validation now separately exercises associated-label activation and the Space key on the four original Checkbox cases, including indeterminate and disabled states. All eight actions were observed through the application, followed by exact restoration of the original screenshots and measured trees. Generated-output behavior remains unqualified.

The application has also generated the 24 observed Button initial states with native text, content-sized width, fixed height and bound variables. **This family remains visually unqualified:** seven exports exceed the existing image-difference limit. The application repaired unrequested invisible default root fills on the 24 retained mains, preserving all 74 node identities. A separate readback now verifies supported structure; the original failed observation remains in its journal. Fresh exports retain the same seven image failures. The selected disabled/default rendering passes the image limit at 2.58%, which does not override those failures. The application can now align these state comparisons using recorded source and native layout bounds at original pixel scale. A read-only export added bounds for all 24 retained mains without changing any nodes or PNG bytes. A diagnostic using those fixed origins localizes all remaining above-tolerance pixel differences to the native text regions; this does not establish font equivalence or override the existing failed image scores. Static text support checks font availability before allocation and verifies text, font and token identity independently. Wrapping, constrained or mixed fixed/content-sized initial-state widths remain refused.

The retained default Button comparison now passes the existing image limit at 3.92% antialias-tolerant difference, improved from 10.18%, against the unchanged 5% limit and original source image. Through the application and companion, a reviewed correction added editable inset background layers to the 63 existing Button mains, preserving all 128 existing nodes and their content slots. The retained comparison adopted its inherited paint layer through a separate reviewed correction and passed independent structure readback. All five nodes from its last verified state remain; its layout remains 116 × 36 px against the original 115.81 × 36 px. The application displays both images at original pixel scale.

The application can also compare another source case using the existing native family when its source identity, non-variant inputs and complete root matrix match. Secondary and Icon-with-text now reuse the original 63-main Button family and pass independent native readback, with image differences of 2.86% and 3.59% against the same 5% limit. Their source images and content records are separate; no additional component set was created. Repeating Icon-with-text preparation reopened the same completed operation without another native dispatch. Disabled differs and is refused by this reuse rule.

This demonstrates the shared uniform padding-box rule and migration for the retained default case. It does not qualify the Disabled Button case, general content editability and state behavior, the complete journey, or reverse reconstruction of the paint layer. Direct radius-token edits still require recompilation of the derived inner radius. Checks cover repeat execution, lost acknowledgements, concurrent edits and journal recovery, but the complete two-way recovery journey still needs live proof.

The composed Card's current child mapping now resolves all seven children against independently verified native mains. The application repaired the existing nested Button's background layer after its earlier height correction, preserving allocation ownership, and corrected the existing Card-context Checkbox's SVG stroke width. It did not substitute the standalone Checkbox, whose inherited typography differs. Current child reuse and the retained composed instance now pass independent structure verification. The shared comparison compiler distinguishes inherited background layers from caller content at nested component boundaries and preserves descendant mappings. Through the app, a guarded metadata repair adopted the existing inherited layer; fresh readback retained all 29 recorded nodes, with no additions or removals, and the original 360 × 200 px layout. Editable parent-to-child property mappings, visual qualification and the complete journey remain required.

### What the React application can do now

The local `/sources` page opens a fixed ten-case shadcn cohort: Button, Checkbox and a composed Card. **Validate React sources** checks the originals, theme, fonts and selected states against an isolated archived replay; representative cases must reject deliberately missing or hidden content and assets. This is a configured source preset, not arbitrary repository onboarding. The [React V1 scope](../docs/REACT-V1-SCOPE.md) defines the cohort and required outcomes.

**Inspect React APIs** reads installed declarations and original JSX into incomplete contract proposals. It preserves supported typed values, omission, defaults and source-proven caller-content slots. Callback signatures retain their parameters and return requirements. Callbacks with arguments remain outside the proposal until their behavior is observed; matching finite state-input types are shown as candidates, without guessing whether an input controls updates or only the initial state. Unsupported APIs and ambiguous content remain explicit. **Trace React structure** pairs component ownership with unchanged original renders, then observes admitted finite string/null property combinations in an isolated copy. Shared rules derive root layout, source token identities and supported fixed sizing. Boolean/runtime states, descendant styling and broader CSS remain incomplete.

**Inspect state and callback behavior** now runs targeted original-source trials from the saved structure observation. For the current Checkbox, the application observed `checked` controlling state and `defaultChecked` supplying only the initial state, with `onCheckedChange` returning the next boolean value. Twelve trials cover all three supplied values, label and Space activation twice, live input updates, and exact restoration of source rendering and ownership. The application now derives a separate React behavior draft from this report and the matching initial appearance archive. It emits `checked`, an initial-only `defaultChecked`, and `onCheckedChange`, retaining the original static contract. Both React generators exercise these declared semantics in a real consumer. **Try generated React beside the original** opens the emitted control in the app with initializer, controlled-update, disabled and callback controls alongside the unchanged original case. This preview uses emitted inline styles and does not import source CSS; external fonts and caller composition remain absent. Controlled-source visual comparisons, associated label composition, a clean installed consumer and native metadata preservation still need qualification.

**Inspect editable Figma roots** connects those observations to the existing native compiler and operation journal. Prepare a supported case, connect the companion in the authorized file, then choose **Create and inspect native draft**. The app records token creation, token readback, component creation and independent node readback. It checks identities, layout, properties, bindings and supported shadow stacks; native exports are displayed as diagnostics. One live Button operation retained its 63 root variants and 121 variables through a server/plugin restart and read-only reinspection. No contract was accepted and visual fidelity remains unverified.

**The next gap is visual qualification and one cohesive application review.** The app now displays a populated React Card comparison containing all seven mapped child instances, including Checkbox and Button, beside the unchanged original. Both layouts measure 360 × 200 px. The same 28 owned native nodes pass independent supported-structure readback after a guarded correction restored the nested Checkbox’s 16 px height binding and removed an extra descendant variable mode. Original creation and failed-attempt records remain intact; the correction allocated no new nodes. The shared writer preserves these inherited values on new source-owned instances, and the repair resolves Figma’s changed slot-child IDs through their existing allocation markers and topology. Tests cover repeat no-op, conflicts, rollback, forged identities and bundled execution.

Reusable mains retain editable slots; source-owned initial-state components retain their own internal content. Child mappings use the actual observed inputs and styling, including nested context, rather than assuming a standalone observation is interchangeable. This bounded path requires verified source dimensions, fonts and SVG evidence. Other property values and interactions remain unqualified. Existing composition rules preserve supported nested scalar properties in React and expose child controls in Figma. Boolean values that map to child variants forward as native instance properties. Caller-owned text inside a nested slot remains directly editable on the canvas with deterministic contract identity because Figma cannot lift that text control through the instance boundary; unsupported representations still refuse.

**The first React-led journey is not complete.** The Card comparison now includes its outer outline and shadow after a guarded correction to the surrounding comparison frame; typography differences remain. The source and native layout origins align using observed export bounds at original pixel scale. The component’s own clipping and all existing node identities are preserved. **Compare text measurements** replays the unchanged original and shows its painted font face and text width alongside the saved native text box. Matching family names or close widths do not establish identical font files or pixel fidelity. Responsive composition, interaction behavior, broader source onboarding and full visual fidelity remain unqualified. Restoring saved operation records reuses checked source evidence within each synchronous display request. One local measurement of the same 14 saved operations improved from 26.5 to 16.8 seconds with unchanged results; this remains too slow for an acceptable first-run experience. A saved update-image request improved from 2.16 to 0.09 seconds with identical PNG bytes by removing unused current-source compilation from historical image delivery. Journal, identity and image integrity checks remain in place. No display evidence is reused by later requests or native write authorization. Correct the remaining shared conversion defects and that delay before qualifying this journey; then proceed to the design-led clean React consumer.

The shared engine supports editable grid content, declared tracks and managed extra-row sizing, plus parent-relative widths for nested content roots. Reusable mains stay empty. Native comparisons retain main links and token bindings; checked reverse extraction preserves reusable React layout rules instead of sample dimensions. These capabilities have isolated native and browser verification. The application now carries source-proven column stretch into a CardFooter main without freezing its measured width. The Card header now uses a bounded intrinsic-row lowering through the application. Comparison preparation admits explicit unit cells only when they exactly match the reusable grid carrier’s row-flow placement; reordered cells, spans and alignment overrides remain unsupported. Complete nested composition remains unfinished.

The application review distinguishes current CSS grid constraints from rendered pixel tracks. It now lowers a source-proven single implicit column with intrinsic rows for auto-height, horizontal block content in a definite-width parent. Browser checks cover wrapping, added/removed content and different widths; this does not qualify arbitrary min-content grids or native visual fidelity. Text-only block roots now have a shared native projection that retains block layout when regenerated as React. Source sizing follows the qualified parent context rather than copying a measured width; mixed inline content remains unsupported. The app can now select a child main by its exact observed inputs and source styling, or prepare a child root when a standalone observation covers different inputs. Omitted inputs remain distinct from Boolean false. Live inspection exposed a lost source-declared height in the nested Button. The shared child-root projection now retains authenticated fixed dimensions. Through the app, a guarded correction changed the existing main from 3 to 36 px tall, preserved its identity and centered editable slot, and passed two independent readbacks with one write. Composition can reuse that verified correction while preserving the original creation record. Nested runtime/state qualification and the complete composed comparison remain unfinished. Managed native writes materialize extra rows, while direct canvas child insertion can require companion reconciliation. Full-width comparisons require a definite containing column or grid; unsupported parent contexts refuse.

The shared native writer also preserves explicit literal dimensions on empty frames during creation and update. Live readback confirmed the intended root and child sizes. That trial still failed full structural verification on an unsupported literal fill; dimension preservation alone does not qualify the output.

**Prepare caller-content comparison** now reads the unchanged original behind a saved operation, records the fonts actually used for its text and any SVG viewports, and compiles a comparison draft through the shared engine. It preserves text/icon order and distinguishes SVG stroke color from inherited fill. The app reports preparation and unsupported facts separately from native structure. **Prepare native comparison operation** then connects that content to the same durable journal and companion plugin. **Create and inspect native comparison** creates a separate instance of the existing main and independently checks its content, tokens, main linkage and unchanged parent. One live Button text comparison passed, including read-only reinspection after reconnecting; creation ran once. **Measure original comparison frame** independently measures the unchanged original, then crops archived pixels for an unscaled side-by-side comparison. The full original remains available; measured source and native layout dimensions stay visible. Framing survives reload and rejects changed originals. The live Button measures 115.81 × 36 px in React and 116 × 36 px in Figma. Its corrected default-case image comparison passes the existing limit, as reported above; the separate initial-state family retains seven visual failures. Runtime states, reusable nested mappings and the composed cohort remain unfinished. This action does not fill reusable mains or repeat the property matrix. The saved fully opaque Button comparison has since passed fresh read-only inspection through a narrowly checked compatibility rule. Its original receipt remains intact; broader compiler migrations remain unqualified.

**Inspect initial states** now reuses the saved source archive to vary finite caller-supplied inputs on fresh React mounts. The app saves the observed states and original-pixel previews, verifies restoration, and reopens the same result without rerunning the matrix. A live Checkbox action observed the supported initial input combinations, including omission. The app also derives one unaccepted contract draft with the complete observed input domain and conditional content. The Checkbox draft has 12 native variant specifications, with booleans and omission preserved. It also retains directly observed root CSS-variable names across those states and displays them in the app; descendant identities, aliases and themes remain unqualified. Shared anatomy rules retain verified unpainted absolute boxes as editable geometry and annotate pointer behavior separately. The shared writer and independent reader now account for supported shapes and SVG descendants, with duplicate and partial-allocation checks in synthetic execution. The app now pins the saved initial-state evidence into its native operation journal. One live Checkbox operation created 12 editable native variants and passed independent structural readback, including a read-only repeat after reconnecting. Its original and native state images are available together in the app without resizing. This verifies the supported initial-state structure, not visual fidelity, keyboard behavior or interaction preservation. Live comparison exposed a fidelity defect: the disabled checked source has opacity 0.5, while the saved native main reads back as opacity 1. The shared computed-channel map now retains node opacity, and recompiling the unchanged observations produces the correct disabled values. Native execution regressions reject incorrect or missing opacity readback. The app’s **Review compiler update** action now pins a proposal listing the four existing disabled variants and their opacity changes. The app now delivers that pinned correction through the companion: fresh preflight, one guarded update and a separate independent readback. The live correction changed the four existing disabled nodes to opacity 0.5, retained the same 44 owned nodes, and passed a second read-only inspection without another write. Original creation evidence and fresh exports remain visible separately. Controlled companion tests cover conflicts, source changes, restart, lost acknowledgements and interrupted reads; the engine also tests rollback on assignment and postcondition failures. An interrupted write with no saved result remains blocked against automatic replay. The same delivery workflow now supports literal fixed dimensions on supported empty flex roots, including the resulting slot alignment. It rejects populated roots, incompatible layout, bound-dimension changes and unrelated native edits. This bounded repair does not qualify the complete two-way recovery journey or visual fidelity. Descendant token provenance and absolute-box coordinate equivalence remain open. The action currently requires a previously prepared root operation to locate its source archive; general source onboarding is unfinished.

Evidence remains private. Reloading unchanged originals restores saved native operations from their pinned evidence after a server restart; reinspection reads the same native objects. The saved fully opaque Button comparison also reopened after the opacity compiler correction and passed a fresh read-only inspection without duplicate creation. This narrowly verified compatibility rule does not qualify general compiler migrations. The source-validation panel does not yet restore its earlier validation result. Broader source admission, visual qualification and repair/recovery remain required.

The shared shadow parser now handles supported OKLab/OKLCH colors in ordered CSS shadow stacks. Through the app, a reviewed correction restored the retained Card mains’ ring and elevation effects; separate native readback verified the two existing mains and unchanged surrounding structure. The app can reinspect the retained populated comparison against verified main corrections while preserving its original creation records and allocations. A fresh live Card readback passed supported-structure verification with all 28 owned nodes retained. The side-by-side view still shows a clipped outer shadow: the app-created comparison frame clips at the instance bounds. Typography differences and visual qualification also remain open; structural readback does not qualify the composed journey.

### Build rules that compose

The caller-content review records native HTML label-to-control relationships separately from appearance. The application can now assemble the observed Card into a React parent and seven nested components, using source identities and sealed observations. Its preview renders two independent copies with caller-provided IDs, editable captions and observed checkbox behavior. Live checks verified label and Space activation, independent state, disabled suppression and mount-only initial values. The source remains unchanged. Stateful children can now be inspected inside their original parent: the Card’s Checkbox has contextual initial-state and callback observations, with full-original restoration. The regenerated composition uses those observations and reports no source-context differences. Native property mapping, broader property/layout coverage and a clean installed consumer remain unfinished.

The Figma compiler can resolve declared finite child initializers into fresh-mount design variants, including omitted inputs, child defaults and controlled-value precedence. Its shared writer populates a child instance’s existing slot with caller text and nested components, preserving the child main. Instances are attached before filling their slots; a live native test found that moving an already-filled instance into another slot invalidates its private sublayers. Test-host checks cover flex/grid layout, nested slots, inherited typography, amendment and duplicate-free repeats; graph readback verifies only slots directly under a dependency's root and refuses deeper slot locations by name. Full-width grid content inherits its established parent width, and slot sizing is refreshed after placement. The application now delivers the source-derived graph, as described above; reverse reconstruction remains unqualified.

**Check native composition** compiles the saved source-derived parent and its dependencies through the shared Figma engine. Each component retains a separate token and asset identity, including when names collide. The Card’s context-matched graph compiles eight components at its observed 360 px width, including the Checkbox’s twelve initial-state variants. The application names unsupported native property bindings separately from available direct text properties; this check creates no Figma objects. Reusing existing child mains, companion delivery and independent live verification remain required.

The scoped native draft writer accepts supported text properties bound to component-owned text. Its independent reader checks names, defaults and every binding. Caller text retains its observed font across component boundaries. **Parent text-property bindings inside instance slots are not possible in Figma; the composed graph represents that caller text as directly editable canvas content instead.** An earlier live trial produced all six parent variants, but Figma refused these bindings. A separate probe found that binding before slot insertion also fails: insertion clears the reference, and changing the property leaves the text unchanged. The writer now refuses these mappings before allocation instead of producing ineffective controls; the requested contract mappings remain intact. Partial trial output was removed. Ordinary direct text properties and freely editable slot content are distinct native capabilities, as described in [Figma’s component-property documentation](https://developers.figma.com/docs/plugins/working-with-component-properties/). The delivered graph now uses that canvas-content representation, and a live title edit stayed local to one instance; property-panel editing of slot text remains unavailable by Figma's design.

Both React emitters also preserve contract-declared caller content through nested, unconstrained child slots. Generated-code browser checks verify independent child state, label activation, changing IDs and content, disabled controls and conditional composition. Automatic source projection and native slot population remain separate, unfinished integration steps; this shared rule is not a completed application journey.

A contract describes the component's parts, layout, tokens, properties, content and references to other components. Readers derive supported facts from React source or native Figma data; generators compile those facts into the other surface. The conversion path requires no AI. Optional AI proposals must pass the same validation as user-authored input.

A finite set of rules can describe many compositions. A table should reuse the rules for rows, cells, selection controls, slots and nested menus. Development must test those rules and their interactions rather than add a bespoke converter for every component name. New examples should expose a missing shared rule or verify an existing one.

This does not make arbitrary React programs convertible. Source patterns and styling systems need explicit support. Nor can a Figma drawing supply sorting logic, data fetching or validation rules. Existing code behavior needs a verified preservation boundary; design-only behavior needs a declared, tested implementation. Unresolved facts remain visible rather than receiving guessed values.

### Lit and Web Components: paused for planned V1.1

The configured Altitude flow and its existing records remain available under the collapsed Lit evaluation archive on `/sources`. They are not the React starting point or a prerequisite for React V1. Its native Button inspection is bounded evidence; Checkbox source observations do not establish native conversion. Neither qualifies a complete Web Components journey.

Preserve the current adapters, fixtures, tests, source captures and operation identities. Archive detailed working logs privately; keep reproducible checks and signed evidence intact. Resume framework-specific integration after the React V1 exits below, beginning with fresh source/dependency checks and a comparison against the then-current shared rules. Existing regression gates still run; pausing expansion does not waive them. V1.1 is a planned scope, not a release date or a support claim.

## Plan and measures of success

| Order | Milestone | Exit evidence | Status |
| --- | --- | --- | --- |
| 1 | Bound and verify React intake | Document supported React syntax and styling; pin source/dependencies; load original styles/fonts/assets; verify states and reject deliberately broken inputs. | Fixed styled cohort verified; general repository intake remains open. |
| 2 | Complete React → Figma through the app | Simple, stateful and composed React components through the same shared rules; editable native output; original/native screenshots and structural/token/property checks; no manual role maps or pasted scripts; repeat makes no changes. | **Active integration milestone.** |
| 3 | Complete Figma → reusable React | Design-only input; clean consumer installs the generated library; content APIs, variants, tokens, accessibility and declared behavior work; independent browser comparison. | Engines exist; full journey unqualified. |
| 4 | Repair an existing React/Figma pair safely | Real edits on either side; authorized changes applied; conflicting/stale edits handled; interruption/retry/rollback demonstrated; fresh verification; repeat makes no changes. | Foundations exist; full repair loop unqualified. |
| 5 | Qualify release readiness | Repeat the same journeys on independently selected supported React compositions, without component-specific conversion code or operator intervention; reproducible evidence and explicit unsupported cases. | Not yet reached. |

### V1 acceptance evidence, 2026-09-18

The owner's React-only V1 asks for five demonstrations. Each row names what the application shows today, where the evidence is, and the concrete gap. None of these rows is a release grade.

| Criterion | Demonstrated through the application | Evidence | Concrete gap |
| --- | --- | --- | --- |
| 1 · React → contract → editable native Figma for simple, stateful and composed components | Button (simple), Checkbox (stateful, 12 initial-state variants) and the composed Card graph (seven dependencies, six parent variants, 169 scoped variables) are created through the companion plugin from the verified shadcn cohort; the parent structure passes independent readback, including after Figma re-identified slot content and in a fresh live readback on 2026-09-18 after the verifier fix; same-day read-only readbacks of the retained Button family (191 nodes, 63 images) and the corrected Checkbox (44 nodes, 12 images) matched their last applied corrections without another write; a live title edit stayed local to one instance; repeat preparation created no duplicates; the app shows the unchanged original beside the native default variant. | `/sources` → composed Card → **Check native composition** → **Create and inspect native graph**; private operation journal `4e6e22c3…` and update journals `c620c0e9…` (Button) and `0b978ebf…` (Checkbox) in the Evaluations file. | Visual fidelity is unqualified (seven Button initial-state exports still exceed the image limit; Card typography differs); dependency mains are verified by identity only; two-level caller content and deeper slot locations refuse by name; first-run latency remains too slow. |
| 2 · Designer-authored Figma → contract → working React in a clean consumer | `npm run design:consumer:check` packages generated output, installs it into an isolated Vite consumer, mounts every variant, exercises text, array and variant props, refuses undeclared children by type, and scores each variant against Figma's render. Altitude Badge installed and behaved; Altitude Tabs (composed, two child stubs) installed and rendered its items from the design sample. | `recipe/evidence/design-led-consumer/{altitude-badge,altitude-tabs}/receipt.json`. | Badge: the 53 px against 57 px label was dropped letter spacing, not font metrics. Both Figma readers named the designer's 1 px tracking a loss while the proposer, the `letter-spacing` channel and the CSS emitter already carried it; they now carry it (dump v1.33). Re-measured on the real file: labels render 57.27 px against 57 px and image difference fell from 15.8–18.8 % to 3.2–6.9 %. Eight of ten variants pass; **neutral (5.26 %) and info (6.90 %) labels are still above the 5 % limit**, where the 57.27 px box captures as 58 px against Figma's 57 plus antialiasing. The limit is unchanged. Tabs still fails: stubs carry no panel content, and its Stretch axis, no longer ledgered inert, still draws nothing different. The header's Stretch-only cross-axis FILL is a pure function of the `Variant` axis and is now carried as `.variant-stretch … { width: 100% }` through `literalsByProp`, so `variant-axis-inert-ledgered:variant` left the receipt and switching the prop changes the rendered class; but the header already rendered 439 px wide in both variants (a column root with no `align-items` stretches it), so both scores are unchanged to the digit (5.33 % default, 5.0049 % stretch). What would move pixels is the list's and each tab's Stretch-only FILL along a ROW parent's primary axis: that is `layout.grow`, the per-variant layout vocabulary has no `grow`, and it is now named in the proposal instead of silent; the tabs render 58.86 px each in both variants against Figma's 146.33 px under Stretch. Per-side strokes are now carried: both Figma readers write the header's drawn top 1 / right 0 / bottom 1 / left 0 weights as `strokeWeights` (dump v1.34) instead of naming them a loss beside a uniform weight of 0, and they reach the CSS as `border-<side>-width` literals, so the header rule that rendered as an invisible zero-width border is now drawn. Re-measured on the real file: image difference fell from 5.67 % to 5.33 % (default) and from 6.14 % to 5.00 % (stretch, 5.0049 %), the consumer's height now matches Figma's 176 px (was 174), and it draws 2.5 % ink against the design's 12.7 % (was 1.4 %). **Both variants are still above the unchanged 5 % limit.** The active tab's bottom-only 2 px indicator sits on an INSTANCE stub, whose geometry carries one uniform weight only: it is named in the proposal, not drawn. The check is a CLI, not yet an application review. |
| 3 · Updates both ways, conflicts, interruption recovery, duplicate-free repeats | **Live on 2026-09-18, through the app and the companion in the Evaluations file, on the existing twelve-variant Checkbox set.** *Code-only change:* editing the original source (`disabled:opacity-50` → `-40`, stylesheet rebuilt) no longer orphans the native component. A recorded **source succession** lets the existing operation follow a later sealed observation of the same case; **Review compiler update** then proposed 0.5 → 0.4 on the same four existing nodes, the canvas showed 40 %, and two independent readbacks agreed. Restoring the source byte-for-byte produced the reverse change on the same nodes. An intermediate revision whose stylesheet had not been rebuilt was observed truthfully (0.5 → 1) and not applied. *Same-channel conflict:* a designer's 80 % on one of those nodes refused the write at preflight by name (`native-update-opacity-conflict:<node>`); no write claim was created. *Interrupted write:* the app was killed at the instant of write dispatch. After restart the write was not re-sent; **Resolve by reading the canvas** found the nodes untouched, and a second write under its own numbered claim was independently verified. In that run the second write followed automatically; an adversarial review of the change showed that a second or stalled companion could then execute both writes, so the rule was tightened before merge: the companion must now ask the app before executing any write and is refused once a canvas read is judging it, an untouched write stops and waits for an explicit operator decision before a new one is sent, and a write that had begun is never treated as dead. *Repeat:* reviewing, preparing and inspecting again planned nothing, reopened the same operation and wrote nothing (one write in the journal). *Design-only change:* a designer set the four disabled variants to 60 % in Figma. **Read design changes from the canvas** named exactly those four values (0.5 → 0.6) while the update stayed verified and nothing was written. After the developer changed the source to render 60 %, the same update path found both surfaces in agreement: the write program reported `no-op` with nothing changed, the independent readback verified 0.6, and the shared baseline advanced with zero canvas writes. Source and canvas were then restored. | Private journals `27f378de…` and `ea0e4d6d…` under `private/source-native-updates/`, succession journal under `private/source-native-successions/`, index `private/two-way-live-2026-09-18/README.md`; rules and regressions in `source-reference/native-source-succession*.ts`, `native-update-write-outcome.test.ts`, `core/native-contract-update.test.ts` (float32). | **The design-led direction names a change and verifies agreement; it does not write code.** The developer edits the hand-written React; the tool reports what the designer changed and confirms when both surfaces agree. That read exists only on the tip of a correction chain, not yet on an operation that has never been corrected. Updates cover only the bounded correction channels (opacity, literal dimensions on empty flex roots, root shadow stacks, simple SVG strokes, background layers); any other source difference refuses by name. Root families seal a content-derived component name at observation time, so an edit to a root's own matrix refuses (`bound-token-change-unsupported`); initial-state sets compile under the existing identity. Re-reading a *parent* operation after a written correction used to break that operation's correction chain; the app now refuses that read by name (`react-parent-observation-superseded-by-correction`) and points to the latest correction instead. The Button root family was re-read before the guard existed and its chain is still unusable: repairing it needs the chain to be replayed from the parent journal as it stood when the first correction was planned. This run also exposed and fixed a write defect: a value float32 cannot hold exactly (0.4) was written correctly, then misread as a conflict. If the companion had been granted permission to begin a write and then died before executing it, the update stays `update-recovery-required` for good and freezes that component's correction chain: there is no operator attestation yet that declares such a write dead. The native listing is slow enough to block the app while it is being read (polling it made each companion step take about a minute; read from the journal, a step took about five seconds). |
| 4 · An independent component family without component-specific fixes | Radix Themes avatar, switch and checkbox minted and scored under the 5 % bar through the F1 recipe, not the application; the design-led consumer check ran on Altitude Badge and Tabs with no per-component code. **Through the application, 2026-09-18:** a separate source workspace declared its own cases (`ds-contracts.react.json`: shadcn Badge, Alert and Switch; no application code names them) and went through the Sources page and the companion in the Evaluations file. All seven cases validated against workspace-authored witnesses with 5 of 5 negative controls rejected per subject (the validator first refused a witness that was wrong). Badge: a seven-variant native root family with 65 variables and a native content slot was created and its structure independently read back; with its real content the native instance is 44 × 20 against the original 43.875 × 20 and scores 6.25 % against the unchanged 5 % limit, every beyond-antialias pixel inside the text glyphs. Switch: nine initial-state variants compiled and were created. | [F1 Radix round](../parity/receipts/v1/F1-RADIX-ROUND.md); `recipe/evidence/design-led-consumer/`.; `source-reference/README.md` (declared cases); live evidence is private. | No independent family has completed either journey through the application. On the React side no member passes the visual limit: Badge misses it on glyph rendering alone; Alert stops at a named refusal because a top-level root whose own display is `grid` gets no contract layout; Switch's independent component readback refuses the transparent `::after` hit area, read back one float32 step from its planned position under a parent of fractional height. The family shares shadcn's stylesheet with the built-in cohort, so a second code-led library is unproven; no update cycle was run on it; a saved initial-state inspection cannot be observed again after an observer change; absolute children are placed from the frame origin where CSS places them from the padding edge. The design-led family results above fail fidelity. |
| 5 · Installation, current documentation, supported limitations, acceptance evidence | Installation and the three entry paths are in the [user journey guide](../docs/USER-JOURNEYS.md); supported rules and refusals are named in this page and [React V1 scope](../docs/REACT-V1-SCOPE.md); this table is the acceptance ledger. | This page; `npm run docs:check` validates gated numbers and links. | Onboarding an arbitrary React repository is not supported; the configured shadcn preset is the only code-led source. |

Conversion stays deterministic and needs no AI at runtime: readers, compilers, writers and verifiers are pure functions of authenticated inputs, and every result above is reproducible from its recorded evidence.

### React V1 acceptance surface

These are requirements to verify, not a list of features already qualified:

| Capability | Required evidence |
| --- | --- |
| Layout and styling | Supported sizing, alignment, spacing, typography and paint preserve their meaning; original and target renders are independently compared. Unsupported CSS is named. |
| Tokens and themes | Token identities, values, aliases and supported modes survive conversion and edits. |
| Properties and state | Text, enums, booleans and omitted values retain their semantics; declared interactions are tested separately from appearance. |
| Content and composition | Nested identities, parent-to-child property mappings, editable text, named slots and supported instance choices survive. Include a table, form or dialog-scale composition. |
| Delivery and maintenance | A clean consumer installs generated React; Figma output remains editable; authorized updates work both ways; conflicts, interruption, rollback and unchanged repeats are verified. |

Do not expand every property combination blindly. Cover each supported rule, its known interactions and representative compositions, then exercise an independently selected cohort. If a combination is unverified or unsupported, report it. A component-specific exception does not establish a general rule.

For every milestone, report:

- **Outcome:** what a user can now complete, through which interface.
- **Coverage:** selected components, states, themes and composition features, including failures and refusals.
- **Fidelity and usability:** original and target renders plus independent semantic, structural and editability checks.
- **Manual intervention:** setup, mappings, scripts or repairs still required.
- **Reliability:** changed inputs, missing prerequisites, conflicts, interruption and repeat behavior.

Do not tune source styling or comparison thresholds to make a candidate pass. Native Figma evidence must come from actual native nodes, not an HTML imitation. Preserve existing signed evidence and leave release approval to the owners.

## Architecture and boundaries

The contract records supported semantics: identity, properties, anatomy, token references and modes, layout, content/slots, component references and declared behavior. Rendering representations are projections of that contract. They do not become competing sources of truth.

Code-led contracts may retain an immutable reference to a host-verified original runtime. Only qualified editable channels may alter that runtime; preservation does not make every design edit effective. Design-only observations cannot supply undeclared business logic or authorize executable dependencies.

| Connection | Responsibility |
| --- | --- |
| Figma REST | Read file/node observations for imports and drift checks. |
| Companion Figma plugin | Execute the shared engine's native operations and return observations. The development plugin connects to the local app on port 5181 using an operation-specific capability; native qualification is still pending. |
| Figma MCP / Desktop Bridge | Engineering access for native execution and screenshots. A successful engineering probe does not prove the application journey. |
| CLI and local application service | Invoke the same deterministic readers, compilers and verification logic; retain operation state. |

The paused Lit source workflow uses a configured sibling library. Each run must check the actual source and dependency identities; old captures do not establish current-source parity. Engineering writes are limited to Scratch and the user-provided DS Contracts Evaluations file. Existing operations remain bound to their original file; a new target requires a new operation identity. Altitude and CBDS remain read-only references.

### Implementation map

| Responsibility | Existing implementation to reuse |
| --- | --- |
| Browser engine imports and emitters | `playground/vite.config.ts`, `playground/src/engine/emitters.ts`, `core/index.ts` |
| React and Figma contract proposals | `playground/src/engine/code-import.ts`, `playground/src/engine/figma-import.ts`, `extract/figma/rest/fetch.ts` |
| Contract-to-Figma programs | `core/emit-figma-script.ts` |
| Canonical recipe canvas IR and writer | `recipe/figma-ir.ts`, `recipe/figma-writer-runtime.ts` |
| React source intake, paused Lit workflow and reusable observation infrastructure | `source-reference/`, especially `source-reference/compile.ts` |
| Application-owned source candidate jobs | `source-reference/service.ts`, `source-reference/candidate-jobs.ts`, `source-reference/candidate-run.ts` |
| Verified evidence selection and candidate inventory | `source-reference/binding-jobs.ts`, `source-reference/candidate-report.ts`, `source-reference/button-candidate-semantics.ts` |
| Unaccepted source-owned visual candidate | `source-reference/source-bound-anatomy.ts`, `source-reference/source-visual-seed.ts`, `source-reference/source-visual-contract.ts` |
| Verified original runtime preparation and React lowering | `source-reference/runtime-artifact.ts`, `core/runtime-emission.ts`, `core/emit-react.ts` |
| Runtime identity preservation and adoption refusal | `core/runtime-reference.ts`, `packages/core/src/contract-provenance.ts` |
| Actual plugin host | `figma-sync/plugin/engine/entry.ts`, `figma-sync/plugin/code.js` |
| Drift classification and merge computation | `core/channel-diff.ts`, `core/three-way-merge.ts` |
| Observation, baseline ledger and plan-only coordinator | `sync/observe.ts`, `sync/ledger.ts`, `sync/spine.ts` |

## How drift must be detected and repaired

Compare fresh code and canvas observations with the last mutually verified baseline.

- **Neither changed:** report a verified no-op.
- **One changed:** propose its supported changes and apply only under recorded authority.
- **Both changed:** combine compatible edits; report unresolved same-channel conflicts.
- **Inputs became stale:** reobserve and replan before writing.
- **Execution failed or was interrupted:** preserve operation and target identities; recover or roll back owned changes under checked preconditions. Do not advance the baseline on partial success.
- **Evidence is unavailable:** report the missing source, font, asset or connection. Unknown state is not synchronized state.

These are required product behaviors. The full loop remains unqualified.

## Keeping the public status current

Every pull request should state the user-visible outcome, validation, remaining limits and next milestone step. Update this page when usability, coverage or milestone status changes; say explicitly when a change improves internal machinery only. A merge does not imply deployment or a published package.

Keep the README focused on the product and present usability. Keep detailed experiment logs, handoffs and superseded working narratives in the gitignored `private/` archive. Reproducible fixtures, CI inputs, public technical references and signed evidence remain with their checks; housekeeping must not break reproducibility or rewrite grades.

When resuming work, read this page, [AGENTS.md](../AGENTS.md) and [CONTRIBUTING.md](../CONTRIBUTING.md), then verify the current worktree, source and PR state. After two attempts without new evidence, diagnose and replan.

### Checking these pages

`npm run docs:check` validates public claims and links. `npm run test:playground` includes canonical-document integration checks. `npm run site:build` builds the documentation site. The desktop/mobile documentation smoke test is `npm run test:product-overview:browser` with local servers on ports 5181 and 5182. These checks validate documentation surfaces, not conversion readiness.
