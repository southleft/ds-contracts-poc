{
  "$schema": "./contract.schema.json",
  "id": "sl.sl-dropdown",
  "name": "SlDropdown",
  "version": "0.1.0",
  "status": "draft",
  "description": "PROPOSED contract extracted from extract/pilots/shoelace/custom-elements.json (components/dropdown/dropdown.js) (cem adapter) — API surface only; anatomy, tokens, and design bindings await reconciliation and human review.",
  "semantics": {
    "element": "div"
  },
  "props": [
    {
      "name": "open",
      "description": "Indicates whether or not the dropdown is open. You can toggle this attribute to show and hide the dropdown, or you\ncan use the `show()` and `hide()` methods and this attribute will reflect the dropdown's open state.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Open"
        },
        "code": {
          "prop": "open"
        }
      }
    },
    {
      "name": "disabled",
      "description": "Disables the dropdown so the panel will not open.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Disabled"
        },
        "code": {
          "prop": "disabled"
        }
      }
    },
    {
      "name": "stay-open-on-select",
      "description": "By default, the dropdown is closed when an item is selected. This attribute will keep it open instead. Useful for\ndropdowns that allow for multiple interactions.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Stay Open On Select"
        },
        "code": {
          "prop": "stay-open-on-select"
        }
      }
    },
    {
      "name": "distance",
      "description": "The distance in pixels from which to offset the panel away from its trigger.",
      "type": "number",
      "default": 0,
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Distance"
        },
        "code": {
          "prop": "distance"
        }
      }
    },
    {
      "name": "skidding",
      "description": "The distance in pixels from which to offset the panel along its trigger.",
      "type": "number",
      "default": 0,
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Skidding"
        },
        "code": {
          "prop": "skidding"
        }
      }
    },
    {
      "name": "hoist",
      "description": "Enable this option to prevent the panel from being clipped when the component is placed inside a container with\n`overflow: auto|scroll`. Hoisting uses a fixed positioning strategy that works in many, but not all, scenarios.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Hoist"
        },
        "code": {
          "prop": "hoist"
        }
      }
    },
    {
      "name": "sync",
      "description": "Syncs the popup width or height to that of the trigger element.",
      "type": {
        "enum": [
          "width",
          "height",
          "both"
        ]
      },
      "bindings": {
        "figma": {
          "kind": "VARIANT",
          "property": "Sync",
          "values": {
            "width": "Width",
            "height": "Height",
            "both": "Both"
          }
        },
        "code": {
          "prop": "sync"
        }
      }
    },
    {
      "name": "stayOpenOnSelect",
      "description": "By default, the dropdown is closed when an item is selected. This attribute will keep it open instead. Useful for\ndropdowns that allow for multiple interactions.",
      "type": "boolean",
      "default": false,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Stay Open On Select"
        },
        "code": {
          "prop": "stayOpenOnSelect"
        }
      }
    }
  ],
  "events": [
    {
      "name": "show",
      "description": "Emitted when the dropdown opens. (CEM event \"sl-show\")",
      "bindings": {
        "code": {
          "prop": "onShow"
        }
      },
      "trigger": "root"
    },
    {
      "name": "afterShow",
      "description": "Emitted after the dropdown opens and all animations are complete. (CEM event \"sl-after-show\")",
      "bindings": {
        "code": {
          "prop": "onAfterShow"
        }
      },
      "trigger": "root"
    },
    {
      "name": "hide",
      "description": "Emitted when the dropdown closes. (CEM event \"sl-hide\")",
      "bindings": {
        "code": {
          "prop": "onHide"
        }
      },
      "trigger": "root"
    },
    {
      "name": "afterHide",
      "description": "Emitted after the dropdown closes and all animations are complete. (CEM event \"sl-after-hide\")",
      "bindings": {
        "code": {
          "prop": "onAfterHide"
        }
      },
      "trigger": "root"
    }
  ],
  "states": [],
  "anatomy": {
    "root": {}
  },
  "anchors": {
    "figma": {
      "fileKey": null,
      "componentSetKey": null
    },
    "code": {
      "importPath": "extract/pilots/shoelace/custom-elements.json (components/dropdown/dropdown.js)",
      "export": "SlDropdown"
    }
  }
}
