/**
 * Eval fixture — second rollup, so the refusal has to COUNT and NAME more
 * than one file (a ledger of one is indistinguishable from a hardcoded
 * sentence).
 */
export declare interface WidgetInternalProps {
    unstable_slot?: unknown;
}
