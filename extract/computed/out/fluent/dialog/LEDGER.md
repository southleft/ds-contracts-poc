# Delta ledger — computed floor × static layer (Dialog)

Generated by extract/computed/run.ts against @fluentui/react-components@9.74.5 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Dialog`.

- captures: **3** (3 combos, full-cartesian × 4 interactions), channels enumerated per element: **475** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|` (matched) → **dialogbody-2** `div|DialogBody` (computed-only) → **label-4** `h2|DialogTitle` (computed-only) → **dialogtitle__action** `div|DialogTitle__action` (computed-only) → **part-0-1-0** `button|` (computed-only) → **icon** `svg|Icon` (computed-only) → **part-0-1-0-0-0** `path|` (computed-only) → **label-5** `div|DialogContent` (computed-only) → **dialogactions-2** `div|DialogActions` (computed-only) → **label-6** `button|Button` (computed-only) → **dialogsurface__backdrop** `div|DialogSurface__backdrop` (computed-only) → **dialogsurface** `div|DialogSurface` (computed-only) → **part-1-0** `i|` (computed-only) → **dialogbody** `div|DialogBody` (computed-only) → **label** `h2|DialogTitle` (computed-only) → **label-2** `div|DialogContent` (computed-only) → **dialogactions** `div|DialogActions` (computed-only) → **label-3** `button|Button` (computed-only) → **part-1-2** `i|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **15** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **1** · named refusals: **4**
- asset: `dialog-part-0-1-0.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- svg-viewbox-reconstructed: Dialog.part-0-1-0@non-modal — 0 0 20 20 from computed size 20×20 (path extent 16.1; viewBox is not a computed style — named reconstruction)
- presence-carried: dialogbody-2 (1/3 combos) →  hidden-when modalType=modal, modalType=alert
- literal-text-carried: label-4 = "Dialog title" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-4 (1/3 combos) →  hidden-when modalType=modal, modalType=alert
- presence-carried: dialogtitle__action (1/3 combos) →  hidden-when modalType=modal, modalType=alert
- presence-carried: part-0-1-0 (1/3 combos) →  hidden-when modalType=modal, modalType=alert
- literal-text-carried: label-5 = "Dialog content copy for the Fluent round" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-5 (1/3 combos) →  hidden-when modalType=modal, modalType=alert
- presence-carried: dialogactions-2 (1/3 combos) →  hidden-when modalType=modal, modalType=alert
- literal-text-carried: label-6 = "Close" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label-6 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- presence-carried: label-6 (1/3 combos) →  hidden-when modalType=modal, modalType=alert
- grid-promotion-fallback: dialogbody-2 — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; the flex-era lowering decides (named)
- presence-carried: dialogsurface__backdrop (2/3 combos) →  hidden-when modalType=non-modal
- presence-carried: dialogsurface (2/3 combos) →  hidden-when modalType=non-modal
- presence-carried: dialogbody (2/3 combos) →  hidden-when modalType=non-modal
- literal-text-carried: label = "Dialog title" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label (2/3 combos) →  hidden-when modalType=non-modal
- literal-text-carried: label-2 = "Dialog content copy for the Fluent round" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-2 (2/3 combos) →  hidden-when modalType=non-modal
- presence-carried: dialogactions (2/3 combos) →  hidden-when modalType=non-modal
- literal-text-carried: label-3 = "Close" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label-3 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- presence-carried: label-3 (2/3 combos) →  hidden-when modalType=non-modal
- grid-promotion-fallback: dialogbody — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; the flex-era lowering decides (named)
- **REFUSED**: grid-two-dimensional: grid resolves 3 columns × 3 rows — a two-dimensional grid has no auto-layout spelling (one axis would have to be invented); no layout carried (named refusal) (part dialogbody-2)
- **REFUSED**: non-painting-part: part-1-0 <i> renders NO INK in any combo it appears in (content-visibility: hidden) and no descendant paints — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)
- **REFUSED**: grid-two-dimensional: grid resolves 3 columns × 3 rows — a two-dimensional grid has no auto-layout spelling (one axis would have to be invented); no layout carried (named refusal) (part dialogbody)
- **REFUSED**: non-painting-part: part-1-2 <i> renders NO INK in any combo it appears in (content-visibility: hidden) and no descendant paints — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)

## Read-boundary frontier (conformance round — every line here used to be silence)

- read-boundary-not-read-on-portal-path: Dialog sweeps through capturePortalRoots (two-phase baseline diff), which normalizes each root inside its own reader and never runs readBoundaryReceipts — so the text-fill fold and the closed-shadow-root signature are NOT MEASURED for this component. Zero lines above means UNREAD, not "none found"; a named residual of the molecule round, surfaced by the scope-independence fix (before it, this component displayed its SIBLINGS' folds and looked read).
- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (label-3, label-6, part-0-1-0); -webkit-text-stroke-color (label-3, label-6, part-0-1-0)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **98** → WITH folding: **98** (18 folded channels; every fold a named receipt).

- part-0-1-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-1-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-1-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-1-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-1-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-1-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **98** · bindings: 160 base + 0 state
- shape: 139 uniform · 21 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **44**
  - dialogbody-2.box-sizing = `border-box`
  - part-0-1-0.border-bottom-style = `none`
  - part-0-1-0.border-left-style = `none`
  - part-0-1-0.border-right-style = `none`
  - part-0-1-0.border-top-style = `none`
  - part-0-1-0.box-sizing = `content-box`
  - part-0-1-0.cursor = `pointer`
  - part-0-1-0.font-family = `"Segoe UI", "Segoe UI Web (West European)", -apple-system, "system-ui", Roboto, "Helvetica Neue", sans-serif`
  - part-0-1-0.position = `relative`
  - part-0-1-0.text-align = `left`
  - label-5.box-sizing = `border-box`
  - label-5.overflow-x = `auto`
  - label-5.overflow-y = `auto`
  - dialogactions-2.box-sizing = `border-box`
  - label-6.border-bottom-style = `solid`
  - label-6.border-left-style = `solid`
  - label-6.border-right-style = `solid`
  - label-6.border-top-style = `solid`
  - label-6.font-family = `"Segoe UI", "Segoe UI Web (West European)", -apple-system, "system-ui", Roboto, "Helvetica Neue", sans-serif`
  - label-6.overflow-x = `hidden`
  - label-6.overflow-y = `hidden`
  - label-6.transition-duration = `0.1s`
  - label-6.transition-property = `background, border, color`
  - label-6.transition-timing-function = `cubic-bezier(0.33, 0, 0.67, 1)`
  - dialogsurface.border-bottom-style = `solid`
  - dialogsurface.border-left-style = `solid`
  - dialogsurface.border-right-style = `solid`
  - dialogsurface.border-top-style = `solid`
  - dialogsurface.box-sizing = `border-box`
  - dialogbody.box-sizing = `border-box`
  - label-2.box-sizing = `border-box`
  - label-2.overflow-x = `auto`
  - label-2.overflow-y = `auto`
  - dialogactions.box-sizing = `border-box`
  - label-3.border-bottom-style = `solid`
  - label-3.border-left-style = `solid`
  - label-3.border-right-style = `solid`
  - label-3.border-top-style = `solid`
  - label-3.font-family = `"Segoe UI", "Segoe UI Web (West European)", -apple-system, "system-ui", Roboto, "Helvetica Neue", sans-serif`
  - label-3.overflow-x = `hidden`
  - label-3.overflow-y = `hidden`
  - label-3.transition-duration = `0.1s`
  - label-3.transition-property = `background, border, color`
  - label-3.transition-timing-function = `cubic-bezier(0.33, 0, 0.67, 1)`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **54**
  - root.border-bottom-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.border-left-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.border-right-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.border-top-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.box-sizing — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `content-box`, 2 distinct value(s))
  - root.max-height — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.max-width — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.position — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `absolute`, 2 distinct value(s))
  - root.scale — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 2 distinct value(s))
  - root.z-index — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `auto`, 2 distinct value(s))
  - dialogbody-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `grid`, 1 distinct value(s))
  - dialogbody-2.grid-template-columns — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `257px 257px 20px`, 1 distinct value(s))
  - dialogbody-2.grid-template-rows — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `28px 28px 32px`, 1 distinct value(s))
  - label-4.grid-column-end — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `3`, 1 distinct value(s))
  - label-4.grid-column-start — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - label-4.grid-row-end — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - label-4.grid-row-start — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - label-4.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <h2>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <h2> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - dialogtitle__action.align-self — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `start`, 1 distinct value(s))
  - dialogtitle__action.grid-column-start — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `3`, 1 distinct value(s))
  - dialogtitle__action.grid-row-end — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - dialogtitle__action.grid-row-start — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - dialogtitle__action.justify-self — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `end`, 1 distinct value(s))
  - part-0-1-0.appearance — declared-channel value outside the bounded grammar — named residue (v15) (sample: `button`, 1 distinct value(s))
  - label-5.grid-column-end — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `4`, 1 distinct value(s))
  - label-5.grid-column-start — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - label-5.grid-row-end — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `2`, 1 distinct value(s))
  - label-5.grid-row-start — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `2`, 1 distinct value(s))
  - dialogactions-2.grid-column-end — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `4`, 1 distinct value(s))
  - dialogactions-2.grid-column-start — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `2`, 1 distinct value(s))
  - dialogactions-2.grid-row-end — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `3`, 1 distinct value(s))
  - dialogactions-2.grid-row-start — grid placement not carried: parent dialogbody-2 declined structured grid promotion — child "label-4" grid-row "1 / 1": grid line `1 / 1` — end line must follow start line (1-based); not extracted; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `3`, 1 distinct value(s))
  - dialogactions-2.justify-self — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `end`, 1 distinct value(s))
  - label-6.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - dialogsurface__backdrop.position — declared-channel value outside the bounded grammar — named residue (v15) (sample: `fixed`, 1 distinct value(s))
  - dialogsurface.position — declared-channel value outside the bounded grammar — named residue (v15) (sample: `fixed`, 1 distinct value(s))
  - dialogbody.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `grid`, 1 distinct value(s))
  - dialogbody.grid-template-columns — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `267px 267px 0px`, 1 distinct value(s))
  - dialogbody.grid-template-rows — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `28px 28px 32px`, 1 distinct value(s))
  - label.grid-column-end — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `4`, 1 distinct value(s))
  - label.grid-column-start — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - label.grid-row-end — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - label.grid-row-start — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - label.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <h2>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <h2> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-2.grid-column-end — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `4`, 1 distinct value(s))
  - label-2.grid-column-start — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - label-2.grid-row-end — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `2`, 1 distinct value(s))
  - label-2.grid-row-start — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `2`, 1 distinct value(s))
  - dialogactions.grid-column-end — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `4`, 1 distinct value(s))
  - dialogactions.grid-column-start — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `2`, 1 distinct value(s))
  - dialogactions.grid-row-end — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `3`, 1 distinct value(s))
  - dialogactions.grid-row-start — grid placement not carried: parent dialogbody declined structured grid promotion — computed grid-template-columns "267px 267px 0px" yields no carriageable track list; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `3`, 1 distinct value(s))
  - dialogactions.justify-self — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `end`, 1 distinct value(s))
  - label-3.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **82.353%** (364/442 cells; 0/12 combo×state rows fully equal)
- pixel: 0/0 pairs perfect at threshold 0 · 0/0 at the AA point (mean AA NOT MEASURED, max NOT MEASURED; 0/0 pairs MEASURED — 0 size-mismatched, 12 with no original screenshot, none averaged)

