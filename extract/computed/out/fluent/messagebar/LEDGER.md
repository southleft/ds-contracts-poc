# Delta ledger — computed floor × static layer (MessageBar)

Generated by extract/computed/run.ts against @fluentui/react-components@9.74.5 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component MessageBar`.

- captures: **32** (8 combos, full-cartesian × 4 interactions), channels enumerated per element: **472** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|MessageBar` (matched) → **icon** `div|MessageBar__icon` (computed-only) → **icon-2** `svg|Icon` (computed-only) → **part-0-0-0** `path|` (computed-only) → **body** `div|MessageBarBody` (computed-only) → **label** `span|MessageBarTitle` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **4** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **4** · named refusals: **1**
- asset: `message-bar-icon-error.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `message-bar-icon-info.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `message-bar-icon-success.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `message-bar-icon-warning.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- svg-currentcolor-folded: MessageBar.icon@info.square — the glyph's authored fill rgba(97, 97, 97, 1) EQUALS the svg's inherited `color` (rgba(97, 97, 97, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: MessageBar.icon@info.square — 0 0 20 20 from computed size 20×20 (path extent 18.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: MessageBar.icon@info.rounded — the glyph's authored fill rgba(97, 97, 97, 1) EQUALS the svg's inherited `color` (rgba(97, 97, 97, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: MessageBar.icon@info.rounded — 0 0 20 20 from computed size 20×20 (path extent 18.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: MessageBar.icon@success.square — the glyph's authored fill rgba(14, 112, 14, 1) EQUALS the svg's inherited `color` (rgba(14, 112, 14, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: MessageBar.icon@success.square — 0 0 20 20 from computed size 20×20 (path extent 18.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: MessageBar.icon@success.rounded — the glyph's authored fill rgba(14, 112, 14, 1) EQUALS the svg's inherited `color` (rgba(14, 112, 14, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: MessageBar.icon@success.rounded — 0 0 20 20 from computed size 20×20 (path extent 18.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: MessageBar.icon@warning.square — the glyph's authored fill rgba(188, 75, 9, 1) EQUALS the svg's inherited `color` (rgba(188, 75, 9, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: MessageBar.icon@warning.square — 0 0 20 20 from computed size 20×20 (path extent 18.1; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: MessageBar.icon@warning.rounded — the glyph's authored fill rgba(188, 75, 9, 1) EQUALS the svg's inherited `color` (rgba(188, 75, 9, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: MessageBar.icon@warning.rounded — 0 0 20 20 from computed size 20×20 (path extent 18.1; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: MessageBar.icon@error.square — the glyph's authored fill rgba(177, 14, 28, 1) EQUALS the svg's inherited `color` (rgba(177, 14, 28, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: MessageBar.icon@error.square — 0 0 20 20 from computed size 20×20 (path extent 18.1; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: MessageBar.icon@error.rounded — the glyph's authored fill rgba(177, 14, 28, 1) EQUALS the svg's inherited `color` (rgba(177, 14, 28, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: MessageBar.icon@error.rounded — 0 0 20 20 from computed size 20×20 (path extent 18.1; viewBox is not a computed style — named reconstruction)
- literal-text-carried: label = "Descriptive title" (no text prop sample matches — the mounted value is carried verbatim)
- grid-tracks-declared: root.grid-template-rows carries the DECLARED list "1fr" — verified against computed "34px" (1 tracks; every px track agrees ≤0.5px; fr/fit spellings resolve to used px in computed style and are carried from source)
- grid-tracks-declared: root.grid-template-columns carries the DECLARED list "auto 1fr auto auto" — verified against computed "28px 118.641px 0px 0px" (4 tracks; every px track agrees ≤0.5px; fr/fit spellings resolve to used px in computed style and are carried from source)
- grid-tracks-lowered: root.grid-template-columns grid track `auto` LOWERED to {fit: true} — the canvas track enum has no AUTO (P2b: "Expected 'FLEX' | 'FIXED' | 'HUG', received 'AUTO'"); HUG / fit-content(100%) (P14) is the carried content-sized spelling
- grid-tracks-lowered: root.grid-template-columns grid track `auto` LOWERED to {fit: true} — the canvas track enum has no AUTO (P2b: "Expected 'FLEX' | 'FIXED' | 'HUG', received 'AUTO'"); HUG / fit-content(100%) (P14) is the carried content-sized spelling
- grid-tracks-lowered: root.grid-template-columns grid track `auto` LOWERED to {fit: true} — the canvas track enum has no AUTO (P2b: "Expected 'FLEX' | 'FIXED' | 'HUG', received 'AUTO'"); HUG / fit-content(100%) (P14) is the carried content-sized spelling
- grid-promoted: root — 1×4 declared tracks carried as layout.rows/columns, 4 named area(s) (G4: names are contract-owned slot anchors); 0 child placement(s) (0-based cells, spans, per-cell align — A2 G1/G2/G4)
- **REFUSED**: pseudo-content-not-canvas-ink: label::after carries content " " — the bounded decor grammar promotes EMPTY-content decor boxes only. A glyph drawn by an icon-font ligature (Bootstrap/FontAwesome/Material carets, chevrons, close ×s) is real ink that does NOT reach the canvas; named refusal

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::after (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (body, icon, icon-2, label, part-0-0-0, root); -webkit-text-stroke-color (body, icon, icon-2, label, part-0-0-0, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **83** → WITH folding: **43** (56 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- body.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **43** · bindings: 32 base + 0 state
- shape: 22 uniform · 10 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **17**
  - root.border-bottom-style = `solid`
  - root.border-left-style = `solid`
  - root.border-right-style = `solid`
  - root.border-top-style = `solid`
  - root.box-sizing = `border-box`
  - root.font-family = `"Segoe UI", "Segoe UI Web (West European)", -apple-system, "system-ui", Roboto, "Helvetica Neue", sans-serif`
  - root.text-align = `left`
  - root.text-wrap-mode = `nowrap`
  - icon.font-family = `"Segoe UI", "Segoe UI Web (West European)", -apple-system, "system-ui", Roboto, "Helvetica Neue", sans-serif`
  - icon.text-align = `left`
  - icon.text-wrap-mode = `nowrap`
  - body.font-family = `"Segoe UI", "Segoe UI Web (West European)", -apple-system, "system-ui", Roboto, "Helvetica Neue", sans-serif`
  - body.text-align = `left`
  - body.text-wrap-mode = `nowrap`
  - label.font-family = `"Segoe UI", "Segoe UI Web (West European)", -apple-system, "system-ui", Roboto, "Helvetica Neue", sans-serif`
  - label.text-align = `left`
  - label.text-wrap-mode = `nowrap`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **9**
  - root.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `center`, 1 distinct value(s))
  - icon.grid-column-end — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `icon`, 1 distinct value(s))
  - icon.grid-column-start — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `icon`, 1 distinct value(s))
  - icon.grid-row-end — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `icon`, 1 distinct value(s))
  - icon.grid-row-start — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `icon`, 1 distinct value(s))
  - body.grid-column-end — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `body`, 1 distinct value(s))
  - body.grid-column-start — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `body`, 1 distinct value(s))
  - body.grid-row-end — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `body`, 1 distinct value(s))
  - body.grid-row-start — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `body`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **99.010%** (3200/3232 cells; 0/32 combo×state rows fully equal)
- pixel: 0/32 pairs perfect at threshold 0 · 0/32 at the AA point (mean AA 1.464%, max 1.465%; 32/32 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

