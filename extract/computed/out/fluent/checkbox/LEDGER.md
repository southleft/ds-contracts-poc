# Delta ledger — computed floor × static layer (Checkbox)

Generated by extract/computed/run.ts against @fluentui/react-components@9.74.5 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Checkbox`.

- captures: **96** (24 combos, full-cartesian × 4 interactions), channels enumerated per element: **475** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `span|Checkbox` (matched) → **input** `input|Checkbox__input|sr-only` (computed-only) → **indicator** `div|Checkbox__indicator` (computed-only) → **icon** `svg|Icon` (computed-only) → **part-1-0-0** `path|` (computed-only) → **label** `label|Checkbox__label.Label` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **6** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **1**
- svg-viewbox-reconstructed: Checkbox.indicator@checked.medium.square.enabled — 0 0 12 12 from computed size 12×12 (path extent 10.1; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Checkbox.indicator@checked.medium.circular.enabled — 0 0 12 12 from computed size 12×12 (path extent 10.1; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Checkbox.indicator@checked.large.square.enabled — 0 0 16 16 from computed size 16×16 (path extent 14.3; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Checkbox.indicator@checked.large.circular.enabled — 0 0 16 16 from computed size 16×16 (path extent 14.3; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Checkbox.indicator@mixed.medium.square.enabled — 0 0 12 12 from computed size 12×12 (path extent 10.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-bumped: Checkbox.indicator@mixed.medium.circular.enabled — computed size 12 < path extent 18; viewBox reconstructed as 0 0 18 18 (named reconstruction)
- svg-viewbox-reconstructed: Checkbox.indicator@mixed.large.square.enabled — 0 0 16 16 from computed size 16×16 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-bumped: Checkbox.indicator@mixed.large.circular.enabled — computed size 16 < path extent 18; viewBox reconstructed as 0 0 18 18 (named reconstruction)
- root-display-carried: flex
- sr-only-carried-as-hidden: input is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- aspect-carried: indicator computed width == height in every enabled combo (16/20px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- aspect-carried: icon computed width == height in every enabled combo (12/16px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- state-axis-presence-dropped: icon absent under disabled but the contract has no "disabled" prop — factor dropped, part renders in that plane (named residue)
- presence-carried: icon (8/12 combos) →  hidden-when checked=unchecked
- state-axis-presence-dropped: part-1-0-0 absent under disabled but the contract has no "disabled" prop — factor dropped, part renders in that plane (named residue)
- presence-carried: part-1-0-0 (8/12 combos) →  hidden-when checked=unchecked
- literal-text-carried: label = "Checkbox" (no text prop sample matches — the mounted value is carried verbatim)
- **REFUSED**: svg-content-multi-axis: Checkbox.indicator — markup varies over more than one axis; asset refused (part still promoted as a box)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-suspected: Checkbox:checked.large.circular.disabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.circular.disabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.circular.disabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.circular.disabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.circular.enabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.circular.enabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.circular.enabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.circular.enabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.square.disabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.square.disabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.square.disabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.square.disabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.square.enabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.square.enabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.square.enabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.large.square.enabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.circular.disabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.circular.disabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.circular.disabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.circular.disabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.circular.enabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.circular.enabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.circular.enabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.circular.enabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.square.disabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.square.disabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.square.disabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.square.disabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.square.enabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.square.enabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.square.enabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:checked.medium.square.enabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x6 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.circular.disabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 13x13 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.circular.disabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 13x13 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.circular.disabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 13x13 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.circular.disabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 13x13 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.circular.enabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 13x13 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.circular.enabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 13x13 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.circular.enabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 13x13 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.circular.enabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 13x13 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.square.disabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.square.disabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.square.disabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.square.disabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.square.enabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.square.enabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.square.enabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.large.square.enabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.circular.disabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x10 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.circular.disabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x10 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.circular.disabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x10 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.circular.disabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x10 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.circular.enabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x10 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.circular.enabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x10 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.circular.enabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x10 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.circular.enabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 10x10 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.square.disabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.square.disabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.square.disabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.square.disabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.square.enabled__active>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.square.enabled__default>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.square.enabled__focus-visible>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Checkbox:mixed.medium.square.enabled__hover>div>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-limit: 64 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::after (1 part(s)), ::placeholder (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (icon, indicator, input, label, part-1-0-0, root); -webkit-text-stroke-color (icon, indicator, input, label, part-1-0-0, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **302** → WITH folding: **151** (53 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- input.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- indicator.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- indicator.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- indicator.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- indicator.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- indicator.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- indicator.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.height → folds into `font-size` (em ratio 1) [em-tracking] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.width → folds into `font-size` (em ratio 1) [em-tracking] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [active] icon.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] icon.border-left-color rides its source fact
- fold-carries-state-delta: [active] icon.border-right-color rides its source fact
- fold-carries-state-delta: [active] icon.border-top-color rides its source fact
- fold-carries-state-delta: [active] icon.caret-color rides its source fact
- fold-carries-state-delta: [active] icon.column-rule-color rides its source fact
- fold-carries-state-delta: [active] icon.outline-color rides its source fact
- fold-carries-state-delta: [active] icon.row-rule-color rides its source fact
- fold-carries-state-delta: [active] icon.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] icon.text-emphasis-color rides its source fact
- fold-carries-state-delta: [active] indicator.caret-color rides its source fact
- fold-carries-state-delta: [active] indicator.column-rule-color rides its source fact
- fold-carries-state-delta: [active] indicator.outline-color rides its source fact
- fold-carries-state-delta: [active] indicator.row-rule-color rides its source fact
- fold-carries-state-delta: [active] indicator.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] indicator.text-emphasis-color rides its source fact
- fold-carries-state-delta: [active] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] label.border-left-color rides its source fact
- fold-carries-state-delta: [active] label.border-right-color rides its source fact
- fold-carries-state-delta: [active] label.border-top-color rides its source fact
- fold-carries-state-delta: [active] label.caret-color rides its source fact
- fold-carries-state-delta: [active] label.column-rule-color rides its source fact
- fold-carries-state-delta: [active] label.outline-color rides its source fact
- fold-carries-state-delta: [active] label.row-rule-color rides its source fact
- fold-carries-state-delta: [active] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.border-left-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.border-right-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.border-top-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.caret-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.column-rule-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.outline-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.row-rule-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] part-1-0-0.text-emphasis-color rides its source fact
- fold-carries-state-delta: [active] root.caret-color rides its source fact
- fold-carries-state-delta: [active] root.column-rule-color rides its source fact
- fold-carries-state-delta: [active] root.outline-color rides its source fact
- fold-carries-state-delta: [active] root.row-rule-color rides its source fact
- fold-carries-state-delta: [active] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] root.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] icon.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] icon.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] icon.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] icon.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] icon.caret-color rides its source fact
- fold-carries-state-delta: [disabled] icon.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] icon.outline-color rides its source fact
- fold-carries-state-delta: [disabled] icon.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] icon.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] icon.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] indicator.caret-color rides its source fact
- fold-carries-state-delta: [disabled] indicator.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] indicator.outline-color rides its source fact
- fold-carries-state-delta: [disabled] indicator.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] indicator.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] indicator.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] input.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] input.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] input.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] input.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] input.caret-color rides its source fact
- fold-carries-state-delta: [disabled] input.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] input.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] input.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] input.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] label.caret-color rides its source fact
- fold-carries-state-delta: [disabled] label.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label.outline-color rides its source fact
- fold-carries-state-delta: [disabled] label.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.caret-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.outline-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] part-1-0-0.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] root.caret-color rides its source fact
- fold-carries-state-delta: [disabled] root.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] root.outline-color rides its source fact
- fold-carries-state-delta: [disabled] root.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] root.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] icon.border-bottom-color rides its source fact
- fold-carries-state-delta: [hover] icon.border-left-color rides its source fact
- fold-carries-state-delta: [hover] icon.border-right-color rides its source fact
- fold-carries-state-delta: [hover] icon.border-top-color rides its source fact
- fold-carries-state-delta: [hover] icon.caret-color rides its source fact
- fold-carries-state-delta: [hover] icon.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] icon.outline-color rides its source fact
- fold-carries-state-delta: [hover] icon.row-rule-color rides its source fact
- fold-carries-state-delta: [hover] icon.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] icon.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] indicator.caret-color rides its source fact
- fold-carries-state-delta: [hover] indicator.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] indicator.outline-color rides its source fact
- fold-carries-state-delta: [hover] indicator.row-rule-color rides its source fact
- fold-carries-state-delta: [hover] indicator.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] indicator.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [hover] label.border-left-color rides its source fact
- fold-carries-state-delta: [hover] label.border-right-color rides its source fact
- fold-carries-state-delta: [hover] label.border-top-color rides its source fact
- fold-carries-state-delta: [hover] label.caret-color rides its source fact
- fold-carries-state-delta: [hover] label.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] label.outline-color rides its source fact
- fold-carries-state-delta: [hover] label.row-rule-color rides its source fact
- fold-carries-state-delta: [hover] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.border-bottom-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.border-left-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.border-right-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.border-top-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.caret-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.outline-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.row-rule-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] part-1-0-0.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] root.caret-color rides its source fact
- fold-carries-state-delta: [hover] root.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] root.outline-color rides its source fact
- fold-carries-state-delta: [hover] root.row-rule-color rides its source fact
- fold-carries-state-delta: [hover] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] root.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **151** · bindings: 66 base + 48 state
- shape: 62 uniform · 48 per-axis · 0 per-axis-pair · 4 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **25**
  - root.cursor = `pointer`
  - root.max-width = `fit-content`
  - root.position = `relative`
  - input.appearance = `auto`
  - input.box-sizing = `border-box`
  - input.cursor = `pointer`
  - input.font-family = `Arial`
  - input.position = `absolute`
  - input.text-align = `start`
  - indicator.border-bottom-style = `solid`
  - indicator.border-left-style = `solid`
  - indicator.border-right-style = `solid`
  - indicator.border-top-style = `solid`
  - indicator.box-sizing = `border-box`
  - indicator.cursor = `pointer`
  - indicator.overflow-x = `hidden`
  - indicator.overflow-y = `hidden`
  - indicator.pointer-events = `none`
  - icon.cursor = `pointer`
  - icon.overflow-x = `hidden`
  - icon.overflow-y = `hidden`
  - icon.pointer-events = `none`
  - part-1-0-0.cursor = `pointer`
  - part-1-0-0.pointer-events = `none`
  - label.cursor = `pointer`
- state declared facts: **5**
  - [disabled] indicator.cursor = `default`
  - [disabled] input.cursor = `default`
  - [disabled] label.cursor = `default`
  - [disabled] root.cursor = `default`
  - [focus-visible] input.outline-style = `auto`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **8**
  - root.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - input.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <input>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <input> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - indicator.align-self — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `flex-start`, 1 distinct value(s))
  - icon.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `content-box`, 1 distinct value(s))
  - part-1-0-0.d — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `path("M 9.76 3.2 C 10.06 3.49 10.08 3.96 9.8 4.26 L 5.55 8.76 A 0.75 0.75 0 0 1 4.47 8.78 `, 5 distinct value(s))
  - part-1-0-0.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - part-1-0-0.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - label.align-self — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <label>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <label> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `center`, 1 distinct value(s))
- state channels outside mintable kinds: **14**
  - [active] icon.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(15, 84, 140, 1)`)
  - [active] icon.fill — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(15, 84, 140, 1)`)
  - [active] part-1-0-0.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(15, 84, 140, 1)`)
  - [active] part-1-0-0.fill — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(15, 84, 140, 1)`)
  - [disabled] icon.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(189, 189, 189, 1)`)
  - [disabled] icon.cursor — declared-channel state delta on 8/12 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `default`)
  - [disabled] icon.fill — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(189, 189, 189, 1)`)
  - [disabled] part-1-0-0.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(189, 189, 189, 1)`)
  - [disabled] part-1-0-0.cursor — declared-channel state delta on 8/12 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `default`)
  - [disabled] part-1-0-0.fill — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(189, 189, 189, 1)`)
  - [hover] icon.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(17, 94, 163, 1)`)
  - [hover] icon.fill — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(17, 94, 163, 1)`)
  - [hover] part-1-0-0.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(17, 94, 163, 1)`)
  - [hover] part-1-0-0.fill — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(17, 94, 163, 1)`)
- refused/overflow bindings: **28**
  - {"part":"icon","channel":"color","refusal":"resolved values differ across variants without correlating to any variant axis or axis PAIR — a nested part carries at most a pair (one map axis + one placeholder); nothing minted, bind manually"}
  - {"part":"icon","channel":"fill","refusal":"resolved values differ across variants without correlating to any variant axis or axis PAIR — a nested part carries at most a pair (one map axis + one placeholder); nothing minted, bind manually"}
  - {"part":"part-1-0-0","channel":"color","refusal":"resolved values differ across variants without correlating to any variant axis or axis PAIR — a nested part carries at most a pair (one map axis + one placeholder); nothing minted, bind manually"}
  - {"part":"part-1-0-0","channel":"fill","refusal":"resolved values differ across variants without correlating to any variant axis or axis PAIR — a nested part carries at most a pair (one map axis + one placeholder); nothing minted, bind manually"}
  - {"part":"indicator","channel":"background-color","state":"active","ref":"{imported.checkbox.indicator.background-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-bottom-color","state":"active","ref":"{imported.checkbox.indicator.border-bottom-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-left-color","state":"active","ref":"{imported.checkbox.indicator.border-left-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-right-color","state":"active","ref":"{imported.checkbox.indicator.border-right-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-top-color","state":"active","ref":"{imported.checkbox.indicator.border-top-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"color","state":"active","ref":"{imported.checkbox.indicator.color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"fill","state":"active","ref":"{imported.checkbox.indicator.fill-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-bottom-color","state":"disabled","ref":"{imported.shared.color-e0e0e0}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-left-color","state":"disabled","ref":"{imported.shared.color-e0e0e0}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-right-color","state":"disabled","ref":"{imported.shared.color-e0e0e0}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-top-color","state":"disabled","ref":"{imported.shared.color-e0e0e0}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"fill","state":"disabled","ref":"{imported.shared.color-bdbdbd}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"input","channel":"outline-color","state":"disabled","ref":"{imported.checkbox.input.outline-color-state-disabled}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"input","channel":"outline-color","state":"focus-visible","ref":"{imported.checkbox.input.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"input","channel":"outline-offset","state":"focus-visible","ref":"{imported.checkbox.input.outline-offset-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"input","channel":"outline-width","state":"focus-visible","ref":"{imported.checkbox.input.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"background-color","state":"hover","ref":"{imported.checkbox.indicator.background-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-bottom-color","state":"hover","ref":"{imported.checkbox.indicator.border-bottom-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-left-color","state":"hover","ref":"{imported.checkbox.indicator.border-left-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-right-color","state":"hover","ref":"{imported.checkbox.indicator.border-right-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"border-top-color","state":"hover","ref":"{imported.checkbox.indicator.border-top-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"color","state":"hover","ref":"{imported.checkbox.indicator.color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"indicator","channel":"fill","state":"hover","ref":"{imported.checkbox.indicator.fill-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"color","state":"hover","ref":"{imported.checkbox.label.color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **67.195%** (8644/12864 cells; 0/96 combo×state rows fully equal)
- pixel: 3/96 pairs perfect at threshold 0 · 8/96 at the AA point (mean AA 0.517%, max 1.639%; 96/96 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

