# Delta ledger — computed floor × static layer (Alert)

Generated by extract/computed/run.ts against @chakra-ui/react@3.37.0 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Alert`.

- captures: **16** (4 combos, full-cartesian × 4 interactions), channels enumerated per element: **1004** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|alert__root` (matched) → **part-0** `span|` (computed-only) → **icon** `svg|` (computed-only) → **part-0-0-0** `path|` (computed-only) → **alert__content** `div|alert__content` (computed-only) → **label** `div|alert__title` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **4** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **4** · named refusals: **0**
- asset: `alert-part-0-error.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `alert-part-0-info.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `alert-part-0-success.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `alert-part-0-warning.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- svg-currentcolor-folded: Alert.part-0@info — the glyph's authored fill rgba(23, 61, 166, 1) EQUALS the svg's inherited `color` (rgba(23, 61, 166, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-bumped: Alert.part-0@info — computed size 20 < path extent 22; viewBox reconstructed as 0 0 22 22 (named reconstruction)
- svg-currentcolor-folded: Alert.part-0@success — the glyph's authored fill rgba(17, 105, 50, 1) EQUALS the svg's inherited `color` (rgba(17, 105, 50, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-bumped: Alert.part-0@success — computed size 20 < path extent 22; viewBox reconstructed as 0 0 22 22 (named reconstruction)
- svg-currentcolor-folded: Alert.part-0@warning — the glyph's authored fill rgba(146, 49, 10, 1) EQUALS the svg's inherited `color` (rgba(146, 49, 10, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-bumped: Alert.part-0@warning — computed size 20 < path extent 22; viewBox reconstructed as 0 0 22 22 (named reconstruction)
- svg-currentcolor-folded: Alert.part-0@error — the glyph's authored fill rgba(153, 25, 25, 1) EQUALS the svg's inherited `color` (rgba(153, 25, 25, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-bumped: Alert.part-0@error — computed size 20 < path extent 22; viewBox reconstructed as 0 0 22 22 (named reconstruction)
- root-display-carried: flex
- literal-text-carried: label = "This is an alert — check it out!" (no text prop sample matches — the mounted value is carried verbatim)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 6 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-box-align (part-0); -webkit-box-pack (part-0); -webkit-font-smoothing (alert__content, icon, label, part-0, part-0-0-0, root); -webkit-tap-highlight-color (alert__content, icon, label, part-0, part-0-0-0, root); -webkit-text-fill-color (alert__content, icon, label, part-0, part-0-0-0, root); -webkit-text-stroke-color (alert__content, icon, label, part-0, part-0-0-0, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **132** → WITH folding: **36** (36 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert__content.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert__content.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert__content.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert__content.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert__content.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert__content.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **36** · bindings: 55 base + 0 state
- shape: 50 uniform · 5 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **33**
  - root.border-bottom-style = `solid`
  - root.border-left-style = `solid`
  - root.border-right-style = `solid`
  - root.border-top-style = `solid`
  - root.box-sizing = `border-box`
  - root.font-family = `Inter, -apple-system, "system-ui", "Segoe UI", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"`
  - root.font-feature-settings = `"cv11"`
  - root.position = `relative`
  - root.text-rendering = `optimizelegibility`
  - part-0.border-bottom-style = `solid`
  - part-0.border-left-style = `solid`
  - part-0.border-right-style = `solid`
  - part-0.border-top-style = `solid`
  - part-0.box-sizing = `border-box`
  - part-0.font-family = `Inter, -apple-system, "system-ui", "Segoe UI", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"`
  - part-0.font-feature-settings = `"cv11"`
  - part-0.text-rendering = `optimizelegibility`
  - alert__content.border-bottom-style = `solid`
  - alert__content.border-left-style = `solid`
  - alert__content.border-right-style = `solid`
  - alert__content.border-top-style = `solid`
  - alert__content.box-sizing = `border-box`
  - alert__content.font-family = `Inter, -apple-system, "system-ui", "Segoe UI", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"`
  - alert__content.font-feature-settings = `"cv11"`
  - alert__content.text-rendering = `optimizelegibility`
  - label.border-bottom-style = `solid`
  - label.border-left-style = `solid`
  - label.border-right-style = `solid`
  - label.border-top-style = `solid`
  - label.box-sizing = `border-box`
  - label.font-family = `Inter, -apple-system, "system-ui", "Segoe UI", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"`
  - label.font-feature-settings = `"cv11"`
  - label.text-rendering = `optimizelegibility`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **89**
  - root.--bg-currentcolor — CSS custom property, not a styled channel — `--bg-currentcolor` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-100 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-100` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-200 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-200` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#bfdbfe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#bfdbfe`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-300 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-300` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#a3cfff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#a3cfff`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-400 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-400` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#60a5fa`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#60a5fa`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-50 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-50` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#eff6ff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#eff6ff`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-500 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-500` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-600 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-600` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#2563eb`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#2563eb`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-700 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-700` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#173da6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#173da6`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-800 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-800` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#1a3478`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#1a3478`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-900 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-900` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#14204a`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#14204a`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-950 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-950` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#0c142e`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#0c142e`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-border — CSS custom property, not a styled channel — `--chakra-colors-color-palette-border` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-contrast — CSS custom property, not a styled channel — `--chakra-colors-color-palette-contrast` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `white`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `white`, 1 distinct value(s))
  - root.--chakra-colors-color-palette-emphasized — CSS custom property, not a styled channel — `--chakra-colors-color-palette-emphasized` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#a3cfff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#a3cfff`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-fg — CSS custom property, not a styled channel — `--chakra-colors-color-palette-fg` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#173da6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#173da6`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-focus-ring — CSS custom property, not a styled channel — `--chakra-colors-color-palette-focus-ring` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-muted — CSS custom property, not a styled channel — `--chakra-colors-color-palette-muted` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#bfdbfe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#bfdbfe`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-solid — CSS custom property, not a styled channel — `--chakra-colors-color-palette-solid` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#2563eb`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#2563eb`, 4 distinct value(s))
  - root.--chakra-colors-color-palette-subtle — CSS custom property, not a styled channel — `--chakra-colors-color-palette-subtle` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - root.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - root.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - part-0.--bg-currentcolor — CSS custom property, not a styled channel — `--bg-currentcolor` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-100 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-100` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-200 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-200` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#bfdbfe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#bfdbfe`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-300 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-300` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#a3cfff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#a3cfff`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-400 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-400` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#60a5fa`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#60a5fa`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-50 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-50` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#eff6ff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#eff6ff`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-500 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-500` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-600 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-600` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#2563eb`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#2563eb`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-700 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-700` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#173da6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#173da6`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-800 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-800` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#1a3478`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#1a3478`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-900 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-900` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#14204a`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#14204a`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-950 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-950` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#0c142e`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#0c142e`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-border — CSS custom property, not a styled channel — `--chakra-colors-color-palette-border` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-contrast — CSS custom property, not a styled channel — `--chakra-colors-color-palette-contrast` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `white`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `white`, 1 distinct value(s))
  - part-0.--chakra-colors-color-palette-emphasized — CSS custom property, not a styled channel — `--chakra-colors-color-palette-emphasized` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#a3cfff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#a3cfff`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-fg — CSS custom property, not a styled channel — `--chakra-colors-color-palette-fg` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#173da6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#173da6`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-focus-ring — CSS custom property, not a styled channel — `--chakra-colors-color-palette-focus-ring` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-muted — CSS custom property, not a styled channel — `--chakra-colors-color-palette-muted` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#bfdbfe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#bfdbfe`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-solid — CSS custom property, not a styled channel — `--chakra-colors-color-palette-solid` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#2563eb`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#2563eb`, 4 distinct value(s))
  - part-0.--chakra-colors-color-palette-subtle — CSS custom property, not a styled channel — `--chakra-colors-color-palette-subtle` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - part-0.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - part-0.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - alert__content.--bg-currentcolor — CSS custom property, not a styled channel — `--bg-currentcolor` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-100 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-100` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-200 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-200` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#bfdbfe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#bfdbfe`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-300 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-300` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#a3cfff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#a3cfff`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-400 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-400` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#60a5fa`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#60a5fa`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-50 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-50` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#eff6ff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#eff6ff`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-500 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-500` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-600 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-600` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#2563eb`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#2563eb`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-700 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-700` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#173da6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#173da6`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-800 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-800` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#1a3478`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#1a3478`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-900 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-900` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#14204a`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#14204a`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-950 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-950` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#0c142e`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#0c142e`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-border — CSS custom property, not a styled channel — `--chakra-colors-color-palette-border` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-contrast — CSS custom property, not a styled channel — `--chakra-colors-color-palette-contrast` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `white`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `white`, 1 distinct value(s))
  - alert__content.--chakra-colors-color-palette-emphasized — CSS custom property, not a styled channel — `--chakra-colors-color-palette-emphasized` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#a3cfff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#a3cfff`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-fg — CSS custom property, not a styled channel — `--chakra-colors-color-palette-fg` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#173da6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#173da6`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-focus-ring — CSS custom property, not a styled channel — `--chakra-colors-color-palette-focus-ring` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-muted — CSS custom property, not a styled channel — `--chakra-colors-color-palette-muted` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#bfdbfe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#bfdbfe`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-solid — CSS custom property, not a styled channel — `--chakra-colors-color-palette-solid` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#2563eb`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#2563eb`, 4 distinct value(s))
  - alert__content.--chakra-colors-color-palette-subtle — CSS custom property, not a styled channel — `--chakra-colors-color-palette-subtle` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - alert__content.flex-basis — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `0%`, 1 distinct value(s))
  - alert__content.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - alert__content.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - label.--bg-currentcolor — CSS custom property, not a styled channel — `--bg-currentcolor` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-100 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-100` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-200 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-200` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#bfdbfe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#bfdbfe`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-300 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-300` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#a3cfff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#a3cfff`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-400 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-400` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#60a5fa`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#60a5fa`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-50 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-50` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#eff6ff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#eff6ff`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-500 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-500` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-600 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-600` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#2563eb`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#2563eb`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-700 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-700` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#173da6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#173da6`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-800 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-800` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#1a3478`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#1a3478`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-900 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-900` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#14204a`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#14204a`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-950 — CSS custom property, not a styled channel — `--chakra-colors-color-palette-950` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#0c142e`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#0c142e`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-border — CSS custom property, not a styled channel — `--chakra-colors-color-palette-border` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-contrast — CSS custom property, not a styled channel — `--chakra-colors-color-palette-contrast` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `white`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `white`, 1 distinct value(s))
  - label.--chakra-colors-color-palette-emphasized — CSS custom property, not a styled channel — `--chakra-colors-color-palette-emphasized` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#a3cfff`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#a3cfff`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-fg — CSS custom property, not a styled channel — `--chakra-colors-color-palette-fg` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#173da6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#173da6`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-focus-ring — CSS custom property, not a styled channel — `--chakra-colors-color-palette-focus-ring` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#3b82f6`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#3b82f6`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-muted — CSS custom property, not a styled channel — `--chakra-colors-color-palette-muted` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#bfdbfe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#bfdbfe`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-solid — CSS custom property, not a styled channel — `--chakra-colors-color-palette-solid` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#2563eb`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#2563eb`, 4 distinct value(s))
  - label.--chakra-colors-color-palette-subtle — CSS custom property, not a styled channel — `--chakra-colors-color-palette-subtle` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `#dbeafe`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `#dbeafe`, 4 distinct value(s))
  - label.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - label.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **57.005%** (1888/3312 cells; 0/16 combo×state rows fully equal)
- pixel: 0/16 pairs perfect at threshold 0 · 0/16 at the AA point (mean AA 2.101%, max 2.111%; 16/16 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

