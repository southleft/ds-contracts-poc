# Delta ledger — computed floor × static layer (Tabs)

Generated by extract/computed/run.ts against @shadcn-sandbox/ui@0.0.1 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Tabs`.

- captures: **4** (1 combos, full-cartesian × 4 interactions), channels enumerated per element: **564** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|` (matched) → **part-0** `div|` (computed-only) → **label** `button|` (computed-only) → **label-2** `button|` (computed-only) → **label-3** `button|` (computed-only) → **label-4** `div|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **6** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **3**
- root-display-carried: flex
- literal-text-carried: label = "Overview" (no text prop sample matches — the mounted value is carried verbatim)
- literal-text-carried: label-2 = "Activity" (no text prop sample matches — the mounted value is carried verbatim)
- literal-text-carried: label-3 = "Settings" (no text prop sample matches — the mounted value is carried verbatim)
- literal-text-carried: label-4 = "Overview panel copy for the shadcn round" (no text prop sample matches — the mounted value is carried verbatim)
- **REFUSED**: pseudo-decor-hidden-in-combo: label::after is not drawn in every combo (opacity 0, box 79.5312x2) — the component's own hidden state, NOT a grammar limit
- **REFUSED**: pseudo-decor-hidden-in-combo: label-2::after is not drawn in every combo (opacity 0, box 66.8594x2) — the component's own hidden state, NOT a grammar limit
- **REFUSED**: pseudo-decor-hidden-in-combo: label-3::after is not drawn in every combo (opacity 0, box 70.9062x2) — the component's own hidden state, NOT a grammar limit

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::after (3 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (label-2, label-3, part-0); -webkit-text-stroke-color (label-2, label-3, part-0)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **29** → WITH folding: **32** (16 folded channels; every fold a named receipt).

- part-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [active] label-2.caret-color rides its source fact
- fold-carries-state-delta: [active] label-2.column-rule-color rides its source fact
- fold-carries-state-delta: [active] label-2.row-rule-color rides its source fact
- fold-carries-state-delta: [active] label-2.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] label-2.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] label-2.caret-color rides its source fact
- fold-carries-state-delta: [hover] label-2.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] label-2.row-rule-color rides its source fact
- fold-carries-state-delta: [hover] label-2.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] label-2.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **32** · bindings: 108 base + 9 state
- shape: 117 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **24**
  - label.border-bottom-style = `solid`
  - label.border-left-style = `solid`
  - label.border-right-style = `solid`
  - label.border-top-style = `solid`
  - label.position = `relative`
  - label.text-wrap-mode = `nowrap`
  - label.transition-duration = `0.15s`
  - label.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - label-2.border-bottom-style = `solid`
  - label-2.border-left-style = `solid`
  - label-2.border-right-style = `solid`
  - label-2.border-top-style = `solid`
  - label-2.position = `relative`
  - label-2.text-wrap-mode = `nowrap`
  - label-2.transition-duration = `0.15s`
  - label-2.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - label-3.border-bottom-style = `solid`
  - label-3.border-left-style = `solid`
  - label-3.border-right-style = `solid`
  - label-3.border-top-style = `solid`
  - label-3.position = `relative`
  - label-3.text-wrap-mode = `nowrap`
  - label-3.transition-duration = `0.15s`
  - label-3.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
- state declared facts: **1**
  - [focus-visible] label.outline-style = `solid`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **6**
  - label.--tw-shadow — CSS custom property, not a styled channel — `--tw-shadow` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)`, 1 distinct value(s))
  - label.flex-basis — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `0%`, 1 distinct value(s))
  - label-2.flex-basis — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `0%`, 1 distinct value(s))
  - label-3.flex-basis — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `0%`, 1 distinct value(s))
  - label-4.--tw-outline-style — CSS custom property, not a styled channel — `--tw-outline-style` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `none`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `none`, 1 distinct value(s))
  - label-4.flex-basis — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `0%`, 1 distinct value(s))
- state channels outside mintable kinds: **1**
  - [focus-visible] label.--tw-ring-shadow — CSS custom property, not a styled channel — the same door the base plane applies: a custom property is a token DECLARATION the library sets on this element (its plumbing), never a rendered fact; the rendered state delta rides the consuming channel (sample: `0 0 0 calc(3px + 0px) color-mix(in oklab, oklch(0.708 0 0) 50%, transparent)`)
- refused/overflow bindings: **7**
  - {"part":"label","channel":"border-bottom-color","state":"focus-visible","ref":"{imported.shared.color-a1a1a1}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"border-left-color","state":"focus-visible","ref":"{imported.shared.color-a1a1a1}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"border-right-color","state":"focus-visible","ref":"{imported.shared.color-a1a1a1}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"border-top-color","state":"focus-visible","ref":"{imported.shared.color-a1a1a1}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"box-shadow","state":"focus-visible","ref":"{imported.tabs.label.box-shadow-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"outline-color","state":"focus-visible","ref":"{imported.shared.color-a1a1a1}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"outline-width","state":"focus-visible","ref":"{imported.tabs.label.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **82.120%** (519/632 cells; 0/4 combo×state rows fully equal)
- pixel: 0/4 pairs perfect at threshold 0 · 0/4 at the AA point (mean AA 3.227%, max 5.452%; 4/4 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

