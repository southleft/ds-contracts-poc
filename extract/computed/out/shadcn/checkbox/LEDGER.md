# Delta ledger — computed floor × static layer (Checkbox)

Generated by extract/computed/run.ts against @shadcn-sandbox/ui@0.0.1 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Checkbox`.

- captures: **24** (6 combos, full-cartesian × 4 interactions), channels enumerated per element: **561** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `button|` (matched) → **part-0** `span|` (computed-only) → **icon** `svg|` (computed-only) → **part-0-0-0** `path|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **2** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **1** · named refusals: **1**
- asset: `checkbox-part-0.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- svg-stroke-glyph-fill-none: Checkbox.part-0@checked.enabled — path fill 'none' PRESERVED on a stroke-drawn glyph (SVG's initial fill is BLACK, so an absent fill attribute would fill the open path into a blob when the asset renders standalone or through the canvas importer; the stroke channels above carry the paint)
- svg-viewbox-bumped: Checkbox.part-0@checked.enabled — computed size 14 < path extent 20; viewBox reconstructed as 0 0 20 20 (named reconstruction)
- svg-stroke-glyph-fill-none: Checkbox.part-0@indeterminate.enabled — path fill 'none' PRESERVED on a stroke-drawn glyph (SVG's initial fill is BLACK, so an absent fill attribute would fill the open path into a blob when the asset renders standalone or through the canvas importer; the stroke channels above carry the paint)
- svg-viewbox-bumped: Checkbox.part-0@indeterminate.enabled — computed size 14 < path extent 20; viewBox reconstructed as 0 0 20 20 (named reconstruction)
- root-display-carried: flex
- state-axis-presence-dropped: part-0 absent under disabled but the contract has no "disabled" prop — factor dropped, part renders in that plane (named residue)
- presence-carried: part-0 (2/3 combos) →  hidden-when checked=unchecked
- **REFUSED**: pseudo-decor-outside-grammar: root::after occupies 38x30 at position:absolute but paints nothing the grammar reads (background-color rgba(0, 0, 0, 0), border 0px oklch(0.145 0 0)) — NOT promoted

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::after (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 3 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-tap-highlight-color (icon, part-0, part-0-0-0, root); -webkit-text-fill-color (icon, part-0, part-0-0-0, root); -webkit-text-stroke-color (icon, part-0, part-0-0-0, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **68** → WITH folding: **39** (20 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **39** · bindings: 42 base + 7 state
- shape: 42 uniform · 7 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **19**
  - root.border-bottom-style = `solid`
  - root.border-left-style = `solid`
  - root.border-right-style = `solid`
  - root.border-top-style = `solid`
  - root.font-family = `"Inter Variable", sans-serif`
  - root.position = `relative`
  - root.transition-duration = `0.15s`
  - root.transition-property = `box-shadow`
  - root.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - part-0.border-bottom-style = `solid`
  - part-0.border-left-style = `solid`
  - part-0.border-right-style = `solid`
  - part-0.border-top-style = `solid`
  - part-0.box-sizing = `border-box`
  - part-0.cursor = `default`
  - part-0.font-family = `"Inter Variable", sans-serif`
  - part-0.pointer-events = `none`
  - part-0.text-align = `center`
  - part-0.transition-property = `none`
- state declared facts: **1**
  - [disabled] root.cursor = `not-allowed`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **8**
  - root.--tw-outline-style — CSS custom property, not a styled channel — `--tw-outline-style` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `none`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `none`, 1 distinct value(s))
  - root.--tw-shadow — CSS custom property, not a styled channel — `--tw-shadow` is a token DECLARATION the library sets on this element, not a rendered fact of the component. Its value may well be mintable (this one is `0 1px 2px 0 rgb(0 0 0 / 0.05)`); the refusal is the CHANNEL's nature, not the value's shape. The token belongs to the library's token system and is carried there if anywhere; the component contract has no channel for "declares a custom property" and should not invent one. (sample: `0 1px 2px 0 rgb(0 0 0 / 0.05)`, 1 distinct value(s))
  - root.appearance — declared-channel value outside the bounded grammar — named residue (v15) (sample: `button`, 1 distinct value(s))
  - root.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - part-0.align-content — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `center`, 1 distinct value(s))
  - part-0.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `grid`, 1 distinct value(s))
  - part-0.justify-content — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `center`, 1 distinct value(s))
  - part-0.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
- state channels outside mintable kinds: **2**
  - [disabled] part-0.cursor — declared-channel state delta on 2/3 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `not-allowed`)
  - [focus-visible] root.--tw-ring-shadow — CSS custom property, not a styled channel — the same door the base plane applies: a custom property is a token DECLARATION the library sets on this element (its plumbing), never a rendered fact; the rendered state delta rides the consuming channel (sample: `0 0 0 calc(3px + 0px) color-mix(in oklab, oklch(0.708 0 0) 50%, transparent)`)
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **68.707%** (808/1176 cells; 0/24 combo×state rows fully equal)
- pixel: 7/24 pairs perfect at threshold 0 · 8/24 at the AA point (mean AA 0.182%, max 0.446%; 24/24 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

