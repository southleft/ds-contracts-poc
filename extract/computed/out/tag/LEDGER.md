# Delta ledger — computed floor × static layer (Tag)

Generated by extract/computed/run.ts against @shopify/polaris@13.9.5 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Tag`.

- captures: **128** (32 combos, full-cartesian × 4 interactions), channels enumerated per element: **961** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `span|Tag` (matched) → **link** `a|Tag__Link` (computed-only) → **text-2** `span|Text` (computed-only) → **label-2** `span|Tag__Text` (computed-only) → **text** `span|Text` (computed-only) → **label** `span|Tag__Text` (matched) → **part-1** `span|` (computed-only) → **button** `button|Tag__Button|sr-only` (computed-only) → **icon** `span|Icon` (computed-only) → **icon-2** `svg|Icon__Svg` (computed-only) → **part-2-0-0-0** `path|` (computed-only) → **button-2** `button|Tag__Button` (computed-only) → **icon-3** `span|Icon` (computed-only) → **icon-4** `svg|Icon__Svg` (computed-only) → **part-1-0-0-0** `path|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **10** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **1** · named refusals: **4**
- asset: `tag-icon-3.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- presence-prop-added: removable (boolean, default false; library prop onRemove)
- presence-prop-added: clickable (boolean, default false; library prop onClick)
- presence-prop-added: linked (boolean, default false; library prop url)
- svg-viewbox-reconstructed: Tag.icon@large.on.off.off.enabled — 0 0 20 20 from computed size 20×20 (path extent 13.8; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Tag.icon@large.on.off.on.enabled — 0 0 20 20 from computed size 20×20 (path extent 13.8; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Tag.icon-3@none.on.off.off.enabled — 0 0 18 18 from computed size 18×18 (path extent 13.8; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Tag.icon-3@none.on.off.on.enabled — 0 0 18 18 from computed size 18×18 (path extent 13.8; viewBox is not a computed style — named reconstruction)
- root-display-carried: flex (overrides reviewed "inline-flex" — computed truth wins display, receipted)
- grid-lowering: link grid 1×1 → flex column, cross-axis stretch from justify-items:normal (measured track counts)
- presence-carried: link (4/16 combos) → visibleWhen linked hidden-when clickable, disabled
- presence-carried: text-2 (4/16 combos) → visibleWhen linked hidden-when clickable, disabled
- child-content-carried: label-2 holds the children sample text — bound to minted text prop "children" (MUI CardContent class)
- presence-carried: label-2 (4/16 combos) → visibleWhen linked hidden-when clickable, disabled
- presence-complement-carried: text absent in 4/32 default-interaction combos — absence factors as a product; carried as ORDERED stylesWhen (hide on linked; cascade-restored on clickable, disabled) — verified against every captured combo (round 5c complement-of-product spelling)
- presence-complement-carried: label absent in 4/32 default-interaction combos — absence factors as a product; carried as ORDERED stylesWhen (hide on linked; cascade-restored on clickable, disabled) — verified against every captured combo (round 5c complement-of-product spelling)
- presence-carried: part-1 (4/16 combos) →  base-hidden, shown-when size=large hidden-when clickable, disabled
- sr-only-carried-as-hidden: button is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- presence-carried: button-2 (2/16 combos) → visibleWhen removable hidden-when size=large, clickable, disabled
- presence-carried: icon-3 (2/16 combos) → visibleWhen removable hidden-when size=large, clickable, disabled
- optional-adornment-unset-materialized: size — defaultless enum gates a present-only-when-set part; unset value "none" added to the enum as the DEFAULT so a PLAIN (adornment-absent) variant is enumerated and the base-hidden part renders nothing there (round 5f — S2 unset extended from styling to STRUCTURE)
- **REFUSED**: pseudo-decor-outside-grammar: link::after is painted by a mechanism the grammar cannot read (box-shadow: rgba(0, 91, 211, 1) 0px 0px 0px -1px) — the decor grammar reads background-color alpha and border rings only; the box is NOT promoted
- **REFUSED**: pseudo-decor-outside-grammar: button-2::after is painted by a mechanism the grammar cannot read (box-shadow: rgba(0, 91, 211, 1) 0px 0px 0px -1px) — the decor grammar reads background-color alpha and border rings only; the box is NOT promoted
- **REFUSED**: pseudo-decor-outside-grammar: root::after is painted by a mechanism the grammar cannot read (box-shadow: rgba(0, 91, 211, 1) 0px 0px 0px -1px) — the decor grammar reads background-color alpha and border rings only; the box is NOT promoted
- **REFUSED**: orphan-asset-dropped: icon asset "tag-icon" was reconstructed from the svg subtree of part "icon", which this promotion then REFUSED (see the named refusal above) — the asset belongs to no part of the promoted contract and is not carried. Before this door it was committed to the library's assets/icons/ directory and referenced by nothing (measured: examples/mui/assets/icons/autocomplete-autocomplete-clearindicator.svg).

### Human-acked decisions re-applied (gate scores resolved bindings)

- decision re-applied: label.line-height [base] → {p.font-line-height-500}

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-suspected: Tag:large.on.off.off.disabled__active>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.off.disabled__default>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.off.disabled__focus-visible>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.off.disabled__hover>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.off.enabled__active>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.off.enabled__default>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.off.enabled__focus-visible>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.off.enabled__hover>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.on.disabled__active>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.on.disabled__default>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.on.disabled__focus-visible>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.on.disabled__hover>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.on.enabled__active>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.on.enabled__default>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.on.enabled__focus-visible>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:large.on.off.on.enabled__hover>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 9x9 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.off.disabled__active>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.off.disabled__default>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.off.disabled__focus-visible>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.off.disabled__hover>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.off.enabled__active>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.off.enabled__default>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.off.enabled__focus-visible>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.off.enabled__hover>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.on.disabled__active>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.on.disabled__default>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.on.disabled__focus-visible>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.on.disabled__hover>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.on.enabled__active>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.on.enabled__default>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.on.enabled__focus-visible>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Tag:none.on.off.on.enabled__hover>button>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 8x8 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-limit: 32 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::after (4 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 3 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-decorations-in-effect (link); -webkit-text-fill-color (button, button-2, icon, icon-2, icon-3, icon-4, link, part-1-0-0-0, part-2-0-0-0); -webkit-text-stroke-color (button, button-2, icon, icon-2, icon-3, icon-4, link, part-1-0-0-0, part-2-0-0-0)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **64** → WITH folding: **66** (90 folded channels; every fold a named receipt).

- link.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- link.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- button-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [focus-visible] button-2.border-bottom-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.border-left-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.border-right-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.border-top-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.caret-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.column-rule-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.outline-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.row-rule-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.text-decoration-color rides its source fact
- fold-carries-state-delta: [focus-visible] button-2.text-emphasis-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.border-bottom-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.border-left-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.border-right-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.border-top-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.caret-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.column-rule-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.outline-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.row-rule-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.text-decoration-color rides its source fact
- fold-carries-state-delta: [focus-visible] icon-3.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

132/160 carried-binding cells string-equal to the browser-probed token value (no tolerance).

### Binding contradictions (12 — the review queue, see review-queue.json; 0 untriaged)

- `none.off.off.off.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `none.off.on.off.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `none.off.on.on.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `none.on.off.off.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `none.on.on.off.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `none.on.on.on.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `large.off.off.off.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `large.off.on.off.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `large.off.on.on.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `large.on.off.off.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `large.on.on.off.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)
- `large.on.on.on.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: REAL STALE BINDING caught by the floor: Tag.module.css sets line-height {p.font-line-height-500} (20px) on its own inner .Polaris-Tag__Text span, overriding the carried Text-primitive bodySm chain (16px) the promotion bound; the showcase's verify.ts compared the Text root element, not the inner span the floor names `label` — resolve via the review queue (--to {p.font-line-height-500}; six tokens share the 20px value, so the explicit ref is required)

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **66** · bindings: 97 base + 20 state
- shape: 104 uniform · 6 per-axis · 4 per-axis-pair · 3 refused (uncorrelated — nothing minted, named)
- set-plane-literal-carried: root.min-height — unmintable at base (auto …) but uniform per defaultless-axis plane over the presence-off slice: size=large → 24px (presence planes stay named residue — round 5c)

## Declared facts (v15 — carried, first-class)

- base declared facts: **36**
  - root.max-width = `100%`
  - root.position = `relative`
  - link.position = `relative`
  - link.text-decoration-line = `none`
  - text-2.cursor = `pointer`
  - text-2.overflow-x = `hidden`
  - text-2.overflow-y = `hidden`
  - text-2.text-overflow = `ellipsis`
  - text-2.text-wrap-mode = `nowrap`
  - label-2.cursor = `pointer`
  - label-2.text-wrap-mode = `nowrap`
  - text.overflow-x = `hidden`
  - text.overflow-y = `hidden`
  - text.text-overflow = `ellipsis`
  - text.text-wrap-mode = `nowrap`
  - label.text-wrap-mode = `nowrap`
  - button.appearance = `none`
  - button.border-bottom-style = `none`
  - button.border-left-style = `none`
  - button.border-right-style = `none`
  - button.border-top-style = `none`
  - button.cursor = `pointer`
  - button.position = `absolute`
  - button-2.appearance = `none`
  - button-2.border-bottom-style = `none`
  - button-2.border-left-style = `none`
  - button-2.border-right-style = `none`
  - button-2.border-top-style = `none`
  - button-2.cursor = `pointer`
  - button-2.position = `relative`
  - icon-3.cursor = `pointer`
  - icon-3.font-feature-settings = `normal`
  - icon-3.max-height = `100%`
  - icon-3.max-width = `100%`
  - icon-3.text-align = `center`
  - icon-3.text-rendering = `auto`
- state declared facts: **1**
  - [disabled] root.transition-property = `none`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **16**
  - root.cursor — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `auto`, 2 distinct value(s))
  - root.font-feature-settings — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `"calt" 0`, 2 distinct value(s))
  - root.min-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `auto`, 2 distinct value(s))
  - root.outline-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.text-align — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `start`, 2 distinct value(s))
  - root.text-rendering — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `optimizelegibility`, 2 distinct value(s))
  - link.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `grid`, 1 distinct value(s))
  - link.text-decoration — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - text.cursor — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `auto`, 2 distinct value(s))
  - text.font-feature-settings — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `"calt" 0`, 2 distinct value(s))
  - text.text-align — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `start`, 2 distinct value(s))
  - text.text-rendering — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `optimizelegibility`, 2 distinct value(s))
  - label.cursor — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `auto`, 2 distinct value(s))
  - label.font-feature-settings — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `"calt" 0`, 2 distinct value(s))
  - label.text-align — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `start`, 2 distinct value(s))
  - label.text-rendering — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `optimizelegibility`, 2 distinct value(s))
- state channels outside mintable kinds: **80**
  - [active] button-2.background-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(204, 204, 204, 1)`)
  - [active] link.background-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(204, 204, 204, 1)`)
  - [active] link.text-decoration — value shape outside mintable kinds and outside the declared vocabulary (sample: `underline`)
  - [active] link.text-decoration-line — declared-channel state delta on 4/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `underline`)
  - [active] part-1.background-image — default-state values not kind-compatible for padding — cannot correlate (sample: `linear-gradient(to left, rgba(227, 227, 227, 1) 0%, rgba(0, 0, 0, 0) 100%)`)
  - [active] part-1.bottom — default-state values not kind-compatible for padding — cannot correlate (sample: `0px`)
  - [active] part-1.left — default-state values not kind-compatible for padding — cannot correlate (sample: `43.9219px`)
  - [active] part-1.min-height — default-state values not kind-compatible for padding — cannot correlate (sample: `0px`)
  - [active] part-1.min-width — default-state values not kind-compatible for padding — cannot correlate (sample: `0px`)
  - [active] part-1.pointer-events — declared-channel state delta on 4/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [active] part-1.position — declared-channel state delta on 4/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `absolute`)
  - [active] part-1.right — default-state values not kind-compatible for padding — cannot correlate (sample: `20px`)
  - [active] part-1.top — default-state values not kind-compatible for padding — cannot correlate (sample: `0px`)
  - [active] root.outline-style — declared-channel state delta on 8/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [disabled] button-2.cursor — declared-channel state delta on 2/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `default`)
  - [disabled] button-2.pointer-events — declared-channel state delta on 2/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [disabled] button.background-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(227, 227, 227, 1)`)
  - [disabled] button.cursor — declared-channel state delta on 2/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `default`)
  - [disabled] button.pointer-events — declared-channel state delta on 2/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [disabled] icon-3.cursor — declared-channel state delta on 2/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `default`)
  - [disabled] icon-3.pointer-events — declared-channel state delta on 2/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [disabled] label.border-bottom-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.border-left-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.border-right-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.border-top-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.caret-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.column-rule-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.cursor — declared-channel state delta on 8/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `default`)
  - [disabled] label.outline-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.pointer-events — declared-channel state delta on 8/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [disabled] label.row-rule-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.text-decoration-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] label.text-emphasis-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.border-bottom-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.border-left-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.border-right-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.border-top-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.caret-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.column-rule-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.outline-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.row-rule-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.text-decoration-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] part-1.text-emphasis-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] root.cursor — declared-channel state delta on 8/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `default`)
  - [disabled] root.pointer-events — declared-channel state delta on 8/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [disabled] text.border-bottom-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.border-left-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.border-right-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.border-top-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.caret-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.column-rule-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.cursor — declared-channel state delta on 8/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `default`)
  - [disabled] text.outline-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.pointer-events — declared-channel state delta on 8/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [disabled] text.row-rule-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.text-decoration-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [disabled] text.text-emphasis-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(181, 181, 181, 1)`)
  - [focus-visible] button-2.background-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(212, 212, 212, 1)`)
  - [focus-visible] button-2.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(48, 48, 48, 1)`)
  - [focus-visible] icon-3.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(48, 48, 48, 1)`)
  - [focus-visible] link.outline-offset — default-state values not kind-compatible for padding — cannot correlate (sample: `1px`)
  - [focus-visible] link.text-decoration — value shape outside mintable kinds and outside the declared vocabulary (sample: `underline`)
  - [focus-visible] link.text-decoration-line — declared-channel state delta on 4/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `underline`)
  - [focus-visible] root.outline-style — declared-channel state delta on 8/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [hover] button-2.background-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(204, 204, 204, 1)`)
  - [hover] link.background-color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(212, 212, 212, 1)`)
  - [hover] link.text-decoration — value shape outside mintable kinds and outside the declared vocabulary (sample: `underline`)
  - [hover] link.text-decoration-line — declared-channel state delta on 4/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `underline`)
  - [hover] part-1.background-image — default-state values not kind-compatible for padding — cannot correlate (sample: `linear-gradient(to left, rgba(227, 227, 227, 1) 0%, rgba(0, 0, 0, 0) 100%)`)
  - [hover] part-1.bottom — default-state values not kind-compatible for padding — cannot correlate (sample: `0px`)
  - [hover] part-1.left — default-state values not kind-compatible for padding — cannot correlate (sample: `43.9219px`)
  - [hover] part-1.min-height — default-state values not kind-compatible for padding — cannot correlate (sample: `0px`)
  - [hover] part-1.min-width — default-state values not kind-compatible for padding — cannot correlate (sample: `0px`)
  - [hover] part-1.pointer-events — declared-channel state delta on 4/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `none`)
  - [hover] part-1.position — declared-channel state delta on 4/16 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `absolute`)
  - [hover] part-1.right — default-state values not kind-compatible for padding — cannot correlate (sample: `20px`)
  - [hover] part-1.top — default-state values not kind-compatible for padding — cannot correlate (sample: `0px`)
- refused/overflow bindings: **13**
  - {"part":"root","channel":"font-weight","ref":"{imported.tag.root.font-weight.{clickable}}","refusal":"presence-prop-driven styling (clickable) — boolean tokensByProp has no spelling (round 4 residue)"}
  - {"part":"root","channel":"outline-color","ref":"{imported.tag.root.outline-color.{clickable}}","refusal":"presence-prop-driven styling (clickable) — boolean tokensByProp has no spelling (round 4 residue)"}
  - {"part":"root","channel":"outline-width","ref":"{imported.tag.root.outline-width.{clickable}}","refusal":"presence-prop-driven styling (clickable) — boolean tokensByProp has no spelling (round 4 residue)"}
  - {"part":"root","channel":"padding-left","refusal":"resolved values differ across variants without correlating to any variant axis (or axis pair/triple) — nothing minted; bind manually"}
  - {"part":"root","channel":"padding-right","refusal":"resolved values differ across variants without correlating to any variant axis (or axis pair/triple) — nothing minted; bind manually"}
  - {"part":"root","channel":"width","refusal":"resolved values differ across variants without correlating to any variant axis (or axis pair/triple) — nothing minted; bind manually"}
  - {"part":"button","channel":"background-color","ref":"{imported.tag.button.background-color.{linked}}","refusal":"presence-prop-driven styling (linked) — boolean tokensByProp has no spelling (round 4 residue)"}
  - {"part":"icon-3","channel":"color","ref":"{imported.shared.color-8a8a8a}","refusal":"inheritance-only channel with uncarried nested state deltas — the captured value equals this part's ancestor on EVERY plane, and its per-state deltas exceed the nested Part.states vocabulary (plain color-kind refs only); binding the base value would pin all state planes to the resting colour, so the channel stays UNCARRIED and CSS inheritance from the ancestor renders it (leaves exist in the minted tree)"}
  - {"part":"root","channel":"background-color","state":"active","ref":"{imported.tag.root.background-color-state-active.{removable}.{clickable}.{linked}}","refusal":"presence-prop state ref — boolean substitution has no spelling (round 4 residue)"}
  - {"part":"root","channel":"outline-color","state":"disabled","ref":"{imported.tag.root.outline-color-state-disabled.{clickable}}","refusal":"presence-prop state ref — boolean substitution has no spelling (round 4 residue)"}
  - {"part":"root","channel":"padding-left","state":"disabled","ref":"{imported.tag.root.padding-left-state-disabled.{size}.{removable}.{clickable}}","refusal":"presence-prop state ref — boolean substitution has no spelling (round 4 residue)"}
  - {"part":"root","channel":"padding-right","state":"disabled","ref":"{imported.tag.root.padding-right-state-disabled.{size}.{removable}.{clickable}}","refusal":"presence-prop state ref — boolean substitution has no spelling (round 4 residue)"}
  - {"part":"root","channel":"background-color","state":"hover","ref":"{imported.tag.root.background-color-state-hover.{clickable}.{linked}}","refusal":"presence-prop state ref — boolean substitution has no spelling (round 4 residue)"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **81.016%** (5996/7401 cells; 0/128 combo×state rows fully equal)
- pixel: 0/128 pairs perfect at threshold 0 · 0/128 at the AA point (mean AA 0.510%, max 2.011%; 128/128 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

