# Delta ledger — computed floor × static layer (Button)

Generated by extract/computed/run.ts against @shopify/polaris@13.9.5 in Chromium 148.0.7778.96. Regenerate: `npm run extract:computed -- --harness <dir> --component Button`.

- captures: **480** (120 combos, full-cartesian × 4 interactions), channels enumerated per element: **942** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `button|Button` (matched) → **label** `span|` (matched)
- axes held fixed at defaults (receipted, not enumerated): textAlign

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **863** → WITH folding: **298** (17 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [active] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] label.border-left-color rides its source fact
- fold-carries-state-delta: [active] label.border-right-color rides its source fact
- fold-carries-state-delta: [active] label.border-top-color rides its source fact
- fold-carries-state-delta: [active] label.caret-color rides its source fact
- fold-carries-state-delta: [active] label.column-rule-color rides its source fact
- fold-carries-state-delta: [active] label.outline-color rides its source fact
- fold-carries-state-delta: [active] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [active] root.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] root.border-left-color rides its source fact
- fold-carries-state-delta: [active] root.border-right-color rides its source fact
- fold-carries-state-delta: [active] root.border-top-color rides its source fact
- fold-carries-state-delta: [active] root.caret-color rides its source fact
- fold-carries-state-delta: [active] root.column-rule-color rides its source fact
- fold-carries-state-delta: [active] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] root.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] label.caret-color rides its source fact
- fold-carries-state-delta: [disabled] label.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label.outline-color rides its source fact
- fold-carries-state-delta: [disabled] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] root.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] root.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] root.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] root.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] root.caret-color rides its source fact
- fold-carries-state-delta: [disabled] root.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] root.text-emphasis-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.border-left-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.border-right-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.border-top-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.caret-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.column-rule-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.outline-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [focus-visible] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [focus-visible] root.border-bottom-color rides its source fact
- fold-carries-state-delta: [focus-visible] root.border-left-color rides its source fact
- fold-carries-state-delta: [focus-visible] root.border-right-color rides its source fact
- fold-carries-state-delta: [focus-visible] root.border-top-color rides its source fact
- fold-carries-state-delta: [focus-visible] root.caret-color rides its source fact
- fold-carries-state-delta: [focus-visible] root.column-rule-color rides its source fact
- fold-carries-state-delta: [focus-visible] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [focus-visible] root.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [hover] label.border-left-color rides its source fact
- fold-carries-state-delta: [hover] label.border-right-color rides its source fact
- fold-carries-state-delta: [hover] label.border-top-color rides its source fact
- fold-carries-state-delta: [hover] label.caret-color rides its source fact
- fold-carries-state-delta: [hover] label.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] label.outline-color rides its source fact
- fold-carries-state-delta: [hover] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] root.border-bottom-color rides its source fact
- fold-carries-state-delta: [hover] root.border-left-color rides its source fact
- fold-carries-state-delta: [hover] root.border-right-color rides its source fact
- fold-carries-state-delta: [hover] root.border-top-color rides its source fact
- fold-carries-state-delta: [hover] root.caret-color rides its source fact
- fold-carries-state-delta: [hover] root.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] root.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

740/816 carried-binding cells string-equal to the browser-probed token value (no tolerance).

### Binding contradictions (76 — the review queue, see review-queue.json; 0 untriaged)

- `plain.none.slim.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.none.slim.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.none.medium.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.none.medium.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.critical.micro.enabled` root.color (color) {p.color-text-link}: expected `rgba(0, 91, 211, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `plain.critical.slim.enabled` root.color (color) {p.color-text-link}: expected `rgba(0, 91, 211, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `plain.critical.slim.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.critical.slim.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.critical.medium.enabled` root.color (color) {p.color-text-link}: expected `rgba(0, 91, 211, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `plain.critical.medium.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.critical.medium.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.critical.large.enabled` root.color (color) {p.color-text-link}: expected `rgba(0, 91, 211, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `plain.success.micro.enabled` root.color (color) {p.color-text-link}: expected `rgba(0, 91, 211, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `plain.success.slim.enabled` root.color (color) {p.color-text-link}: expected `rgba(0, 91, 211, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `plain.success.slim.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.success.slim.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.success.medium.enabled` root.color (color) {p.color-text-link}: expected `rgba(0, 91, 211, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `plain.success.medium.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.success.medium.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `plain.success.large.enabled` root.color (color) {p.color-text-link}: expected `rgba(0, 91, 211, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.none.micro.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.none.slim.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.none.medium.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.none.large.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.critical.micro.enabled` root.background-color (background-color) {p.color-bg-fill-brand}: expected `rgba(48, 48, 48, 1)` observed `rgba(199, 10, 36, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.critical.micro.enabled` root.box-shadow (box-shadow) {p.shadow-button-primary}: expected `rgba(0, 0, 0, 0.8) 0px -1px 0px 1px inset, rgba(48, 48, 48, 1) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset` observed `rgba(142, 31, 11, 0.8) 0px -1px 0px 1px inset, rgba(181, 38, 11, 0.8) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.35) 0px 0.5px 0px 1.5px inset`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.critical.micro.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.critical.slim.enabled` root.background-color (background-color) {p.color-bg-fill-brand}: expected `rgba(48, 48, 48, 1)` observed `rgba(199, 10, 36, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.critical.slim.enabled` root.box-shadow (box-shadow) {p.shadow-button-primary}: expected `rgba(0, 0, 0, 0.8) 0px -1px 0px 1px inset, rgba(48, 48, 48, 1) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset` observed `rgba(142, 31, 11, 0.8) 0px -1px 0px 1px inset, rgba(181, 38, 11, 0.8) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.35) 0px 0.5px 0px 1.5px inset`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.critical.slim.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.critical.medium.enabled` root.background-color (background-color) {p.color-bg-fill-brand}: expected `rgba(48, 48, 48, 1)` observed `rgba(199, 10, 36, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.critical.medium.enabled` root.box-shadow (box-shadow) {p.shadow-button-primary}: expected `rgba(0, 0, 0, 0.8) 0px -1px 0px 1px inset, rgba(48, 48, 48, 1) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset` observed `rgba(142, 31, 11, 0.8) 0px -1px 0px 1px inset, rgba(181, 38, 11, 0.8) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.35) 0px 0.5px 0px 1.5px inset`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.critical.medium.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.critical.large.enabled` root.background-color (background-color) {p.color-bg-fill-brand}: expected `rgba(48, 48, 48, 1)` observed `rgba(199, 10, 36, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.critical.large.enabled` root.box-shadow (box-shadow) {p.shadow-button-primary}: expected `rgba(0, 0, 0, 0.8) 0px -1px 0px 1px inset, rgba(48, 48, 48, 1) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset` observed `rgba(142, 31, 11, 0.8) 0px -1px 0px 1px inset, rgba(181, 38, 11, 0.8) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.35) 0px 0.5px 0px 1.5px inset`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.critical.large.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.success.micro.enabled` root.background-color (background-color) {p.color-bg-fill-brand}: expected `rgba(48, 48, 48, 1)` observed `rgba(4, 123, 93, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.success.micro.enabled` root.box-shadow (box-shadow) {p.shadow-button-primary}: expected `rgba(0, 0, 0, 0.8) 0px -1px 0px 1px inset, rgba(48, 48, 48, 1) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset` observed `rgba(12, 81, 50, 0.8) 0px -1px 0px 1px inset, rgba(19, 111, 69, 0.8) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.success.micro.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.success.slim.enabled` root.background-color (background-color) {p.color-bg-fill-brand}: expected `rgba(48, 48, 48, 1)` observed `rgba(4, 123, 93, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.success.slim.enabled` root.box-shadow (box-shadow) {p.shadow-button-primary}: expected `rgba(0, 0, 0, 0.8) 0px -1px 0px 1px inset, rgba(48, 48, 48, 1) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset` observed `rgba(12, 81, 50, 0.8) 0px -1px 0px 1px inset, rgba(19, 111, 69, 0.8) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.success.slim.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.success.medium.enabled` root.background-color (background-color) {p.color-bg-fill-brand}: expected `rgba(48, 48, 48, 1)` observed `rgba(4, 123, 93, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.success.medium.enabled` root.box-shadow (box-shadow) {p.shadow-button-primary}: expected `rgba(0, 0, 0, 0.8) 0px -1px 0px 1px inset, rgba(48, 48, 48, 1) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset` observed `rgba(12, 81, 50, 0.8) 0px -1px 0px 1px inset, rgba(19, 111, 69, 0.8) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.success.medium.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `primary.success.large.enabled` root.background-color (background-color) {p.color-bg-fill-brand}: expected `rgba(48, 48, 48, 1)` observed `rgba(4, 123, 93, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.success.large.enabled` root.box-shadow (box-shadow) {p.shadow-button-primary}: expected `rgba(0, 0, 0, 0.8) 0px -1px 0px 1px inset, rgba(48, 48, 48, 1) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset` observed `rgba(12, 81, 50, 0.8) 0px -1px 0px 1px inset, rgba(19, 111, 69, 0.8) 0px 0px 0px 1px inset, rgba(255, 255, 255, 0.25) 0px 0.5px 0px 1.5px inset`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `primary.success.large.enabled` label.font-weight (font-weight) {p.font-weight-medium}: expected `550` observed `650`
  - NAMED CAUSE: composition-owned typography: the primary variant label renders Text fontWeight 650 (semibold); the carried binding is font-weight-medium (550) — same composition-owned class
- `secondary.critical.micro.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `secondary.critical.slim.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `secondary.critical.medium.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `secondary.critical.large.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `secondary.success.micro.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `secondary.success.slim.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `secondary.success.medium.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `secondary.success.large.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `tertiary.critical.micro.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `tertiary.critical.slim.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `tertiary.critical.medium.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `tertiary.critical.large.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(142, 11, 33, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `tertiary.success.micro.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `tertiary.success.slim.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `tertiary.success.medium.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `tertiary.success.large.enabled` root.color (color) {p.color-text}: expected `rgba(48, 48, 48, 1)` observed `rgba(1, 75, 64, 1)`
  - NAMED CAUSE: tone×variant multi-axis condition (.tone*:is(.variant*)) — refused by name in the static promotion; the computed floor observes the resolved value and the mint pass carries it per-axis-pair
- `monochromePlain.none.slim.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.none.slim.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.none.medium.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.none.medium.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.critical.slim.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.critical.slim.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.critical.medium.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.critical.medium.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.success.slim.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.success.slim.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.success.medium.enabled` label.font-size (font-size) {p.text-body-sm-font-size}: expected `12px` observed `13px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion
- `monochromePlain.success.medium.enabled` label.line-height (line-height) {p.text-body-sm-font-line-height}: expected `16px` observed `20px`
  - NAMED CAUSE: composition-owned typography: the plain/monochromePlain variants render their label through the Text primitive at bodyMd (13px/20px); the carried binding is the bodySm default — child-component props conditioned on the parent variant, invisible to single-file static promotion

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **298** · bindings: 21 base + 24 state
- shape: 17 uniform · 8 per-axis · 15 per-axis-pair · 5 refused (uncorrelated — nothing minted, named)

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **22**
  - root.appearance — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.background-attachment — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `scroll`, 1 distinct value(s))
  - root.background-blend-mode — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `normal`, 1 distinct value(s))
  - root.background-clip — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `border-box`, 1 distinct value(s))
  - root.background-image — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.background-origin — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `padding-box`, 1 distinct value(s))
  - root.background-position — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `0% 0%`, 1 distinct value(s))
  - root.background-repeat — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `repeat`, 1 distinct value(s))
  - root.background-size — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `auto`, 1 distinct value(s))
  - root.border-bottom-style — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.border-left-style — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.border-right-style — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.border-top-style — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.cursor — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `pointer`, 1 distinct value(s))
  - root.font-feature-settings — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `"calt" 0`, 1 distinct value(s))
  - root.position — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `relative`, 1 distinct value(s))
  - root.text-rendering — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `optimizelegibility`, 1 distinct value(s))
  - root.touch-action — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `manipulation`, 1 distinct value(s))
  - root.user-select — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `none`, 1 distinct value(s))
  - label.cursor — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `pointer`, 1 distinct value(s))
  - label.text-align — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `center`, 1 distinct value(s))
  - label.user-select — value shape outside mintable kinds (color/px/number/shadow) — no schema channel today (sample: `none`, 1 distinct value(s))
- state channels outside mintable kinds: **20**
  - [active] label.transform — value shape outside mintable kinds (sample: `matrix(1, 0, 0, 1, 0, 1)`)
  - [active] root.box-shadow — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(0, 0, 0, 1) 0px 3px 0px 0px inset`)
  - [active] root.text-decoration — value shape outside mintable kinds (sample: `underline`)
  - [active] root.text-decoration-line — value shape outside mintable kinds (sample: `underline`)
  - [disabled] label.pointer-events — value shape outside mintable kinds (sample: `none`)
  - [disabled] root.background-attachment — value shape outside mintable kinds (sample: `scroll`)
  - [disabled] root.background-blend-mode — value shape outside mintable kinds (sample: `normal`)
  - [disabled] root.background-clip — value shape outside mintable kinds (sample: `border-box`)
  - [disabled] root.background-image — value shape outside mintable kinds (sample: `none`)
  - [disabled] root.background-origin — value shape outside mintable kinds (sample: `padding-box`)
  - [disabled] root.background-position — value shape outside mintable kinds (sample: `0% 0%`)
  - [disabled] root.background-repeat — value shape outside mintable kinds (sample: `repeat`)
  - [disabled] root.background-size — value shape outside mintable kinds (sample: `auto`)
  - [disabled] root.box-shadow — value shape outside mintable kinds (sample: `none`)
  - [disabled] root.pointer-events — value shape outside mintable kinds (sample: `none`)
  - [focus-visible] root.outline-style — value shape outside mintable kinds (sample: `solid`)
  - [focus-visible] root.text-decoration — value shape outside mintable kinds (sample: `underline`)
  - [focus-visible] root.text-decoration-line — value shape outside mintable kinds (sample: `underline`)
  - [hover] root.text-decoration — value shape outside mintable kinds (sample: `underline`)
  - [hover] root.text-decoration-line — value shape outside mintable kinds (sample: `underline`)
- refused/overflow bindings: **16**
  - {"part":"label","channel":"color","refusal":"resolved values differ across variants without correlating to any variant axis — nothing minted; bind manually"}
  - {"part":"label","channel":"color","state":"active","refusal":"resolved values differ across variants without correlating to any variant axis — nothing minted; bind manually"}
  - {"part":"label","channel":"fill","state":"active","refusal":"resolved values differ across variants without correlating to any variant axis — nothing minted; bind manually"}
  - {"part":"root","channel":"background-color","state":"active","ref":"{imported.button.root.background-color-state-active.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"color","state":"active","ref":"{imported.button.root.color-state-active.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"fill","state":"active","ref":"{imported.button.root.fill-state-active.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"outline-color","state":"active","ref":"{imported.button.root.outline-color-state-active.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"label","channel":"color","state":"disabled","ref":"{imported.button.label.color-state-disabled.{variant}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"color","state":"focus-visible","refusal":"resolved values differ across variants without correlating to any variant axis — nothing minted; bind manually"}
  - {"part":"root","channel":"background-color","state":"focus-visible","ref":"{imported.button.root.background-color-state-focus-visible.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"color","state":"focus-visible","ref":"{imported.button.root.color-state-focus-visible.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"outline-offset","state":"focus-visible","ref":"{imported.button.root.outline-offset-state-focus-visible.{variant}.{size}}","refusal":"state pair ref — root states carry ≤1 placeholder (S3 residue)"}
  - {"part":"label","channel":"color","state":"hover","refusal":"resolved values differ across variants without correlating to any variant axis — nothing minted; bind manually"}
  - {"part":"root","channel":"background-color","state":"hover","ref":"{imported.button.root.background-color-state-hover.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"color","state":"hover","ref":"{imported.button.root.color-state-hover.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"outline-color","state":"hover","ref":"{imported.button.root.outline-color-state-hover.{variant}.{tone}}","refusal":"state×tone set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **79.578%** (29412/36960 cells; 0/480 combo×state rows fully equal)
- pixel: 0/480 pairs perfect at threshold 0 · 0/480 at the AA point (mean AA 2.047%, max 13.188%)

