# Delta ledger — computed floor × static layer (Button)

Generated by extract/computed/run.ts against altitude-web-components@1.0.2 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Button`.

- captures: **20** (5 combos, full-cartesian × 4 interactions), channels enumerated per element: **795** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `button|button` (matched) → **label** `span|button__text` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **2** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**
- root-display-carried: inline-flex
- child-content-carried: label holds the children sample text — bound to minted text prop "children" (MUI CardContent class)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (label, root); -webkit-text-stroke-color (label, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **261** → WITH folding: **131** (13 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [hover] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [hover] label.border-left-color rides its source fact
- fold-carries-state-delta: [hover] label.border-right-color rides its source fact
- fold-carries-state-delta: [hover] label.border-top-color rides its source fact
- fold-carries-state-delta: [hover] label.caret-color rides its source fact
- fold-carries-state-delta: [hover] label.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] label.outline-color rides its source fact
- fold-carries-state-delta: [hover] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] root.caret-color rides its source fact
- fold-carries-state-delta: [hover] root.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] root.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **131** · bindings: 36 base + 13 state
- shape: 25 uniform · 24 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **8**
  - root.cursor = `pointer`
  - root.font-family = `"IBM Plex Sans", sans-serif`
  - root.position = `relative`
  - root.transition-duration = `0.2s`
  - root.transition-timing-function = `cubic-bezier(0.15, 0.99, 0.18, 0.99)`
  - label.box-sizing = `border-box`
  - label.cursor = `pointer`
  - label.text-align = `center`
- state declared facts: **1**
  - [focus-visible] root.outline-style = `solid`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **4**
  - root.border-bottom-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.border-left-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.border-right-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - root.border-top-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **11**
  - {"part":"label","channel":"color","ref":"{imported.button.label.color.{variant}}","refusal":"inheritance-only channel with uncarried nested state deltas — the captured value equals this part's ancestor on EVERY plane, and its per-state deltas exceed the nested Part.states vocabulary (plain color-kind refs only); binding the base value would pin all state planes to the resting colour, so the channel stays UNCARRIED and CSS inheritance from the ancestor renders it (leaves exist in the minted tree)"}
  - {"part":"label","channel":"color","state":"hover","ref":"{imported.button.label.color-state-hover.{variant}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"row-rule-color","state":"hover","ref":"{imported.button.label.row-rule-color-state-hover.{variant}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"root","channel":"background-color","state":"hover","ref":"{imported.button.root.background-color-state-hover.{variant}}","refusal":"state×variant set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-bottom-color","state":"hover","ref":"{imported.button.root.border-bottom-color-state-hover.{variant}}","refusal":"state×variant set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-left-color","state":"hover","ref":"{imported.button.root.border-left-color-state-hover.{variant}}","refusal":"state×variant set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-right-color","state":"hover","ref":"{imported.button.root.border-right-color-state-hover.{variant}}","refusal":"state×variant set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-top-color","state":"hover","ref":"{imported.button.root.border-top-color-state-hover.{variant}}","refusal":"state×variant set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"color","state":"hover","ref":"{imported.button.root.color-state-hover.{variant}}","refusal":"state×variant set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"outline-color","state":"hover","ref":"{imported.button.root.outline-color-state-hover.{variant}}","refusal":"state×variant set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"row-rule-color","state":"hover","ref":"{imported.button.root.row-rule-color-state-hover.{variant}}","refusal":"state×variant set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **74.766%** (957/1280 cells; 0/20 combo×state rows fully equal)
- pixel: 4/20 pairs perfect at threshold 0 · 5/20 at the AA point (mean AA 4.917%, max 10.280%; 20/20 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

