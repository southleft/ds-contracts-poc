# Delta ledger — computed floor × static layer (CasePseudoInsetRingAbsentHostClear)

Generated by extract/computed/run.ts against @ds-contracts/conformance@0.1.0 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component CasePseudoInsetRingAbsentHostClear`.

- captures: **24** (6 combos, full-cartesian × 4 interactions), channels enumerated per element: **479** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|root` (matched) → **label** `span|a` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **2** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **1**
- root-display-carried: flex
- literal-text-carried: label = "Badge" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- **REFUSED**: pseudo-decor-ring-folded: root::after is a COINCIDENT INSET RING — border-top-width / border-right-width / border-bottom-width / border-left-width = 1px, uniform on all four sides and identical in 4/6 default-interaction combos, transparent fill, the host's own radius, at 0,0 over the host's padding box. Its paint does not factor on ONE enum axis, so the decor grammar cannot promote it as its own part; the four UNIFORM widths are folded onto root as literals instead, where the border-{side}-color the contract ALREADY carries completes the pair. MEASURED-SAFE: the ring's border colour equals root's own on all four sides in 4/4 combos with a ring box, so no colour is guessed and a combo the host paints transparent stays bare. THE RING BOX IS ABSENT IN 2/6 COMBOS — ghost.brand, ghost.danger — and the fold is safe there ONLY because root's own border-{side}-color is transparent in every one of them, so the folded width paints nothing and the canvas stays bare exactly as CSS does

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::after (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **34** → WITH folding: **34** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **34** · bindings: 20 base + 0 state
- shape: 16 uniform · 0 per-axis · 4 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **5**
  - root.box-sizing = `border-box`
  - root.font-family = `monospace`
  - root.position = `relative`
  - label.box-sizing = `border-box`
  - label.font-family = `monospace`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **1**
  - label.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **100.000%** (624/624 cells; 24/24 combo×state rows fully equal)
- pixel: 0/24 pairs perfect at threshold 0 · 0/24 at the AA point (mean AA 0.387%, max 0.387%; 24/24 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

