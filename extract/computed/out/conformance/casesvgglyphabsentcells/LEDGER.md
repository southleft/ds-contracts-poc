# Delta ledger — computed floor × static layer (CaseSvgGlyphAbsentCells)

Generated by extract/computed/run.ts against @ds-contracts/conformance@0.1.0 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component CaseSvgGlyphAbsentCells`.

- captures: **24** (6 combos, full-cartesian × 4 interactions), channels enumerated per element: **479** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|root` (matched) → **a** `span|a` (computed-only) → **icon** `svg|` (computed-only) → **part-0-0-0** `path|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **2** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **2**
- svg-stroke-glyph-fill-none: CaseSvgGlyphAbsentCells.a@plain.sm — path fill 'none' PRESERVED on a stroke-drawn glyph (SVG's initial fill is BLACK, so an absent fill attribute would fill the open path into a blob when the asset renders standalone or through the canvas importer; the stroke channels above carry the paint)
- svg-viewbox-reconstructed: CaseSvgGlyphAbsentCells.a@plain.sm — 0 0 16 16 from computed size 16×16 (path extent 10.0; viewBox is not a computed style — named reconstruction)
- svg-stroke-glyph-fill-none: CaseSvgGlyphAbsentCells.a@marked.lg — path fill 'none' PRESERVED on a stroke-drawn glyph (SVG's initial fill is BLACK, so an absent fill attribute would fill the open path into a blob when the asset renders standalone or through the canvas importer; the stroke channels above carry the paint)
- svg-viewbox-reconstructed: CaseSvgGlyphAbsentCells.a@marked.lg — 0 0 16 16 from computed size 16×16 (path extent 10.0; viewBox is not a computed style — named reconstruction)
- svg-stroke-glyph-fill-none: CaseSvgGlyphAbsentCells.a@other.sm — path fill 'none' PRESERVED on a stroke-drawn glyph (SVG's initial fill is BLACK, so an absent fill attribute would fill the open path into a blob when the asset renders standalone or through the canvas importer; the stroke channels above carry the paint)
- svg-viewbox-reconstructed: CaseSvgGlyphAbsentCells.a@other.sm — 0 0 16 16 from computed size 16×16 (path extent 10.0; viewBox is not a computed style — named reconstruction)
- svg-stroke-glyph-fill-none: CaseSvgGlyphAbsentCells.a@other.lg — path fill 'none' PRESERVED on a stroke-drawn glyph (SVG's initial fill is BLACK, so an absent fill attribute would fill the open path into a blob when the asset renders standalone or through the canvas importer; the stroke channels above carry the paint)
- svg-viewbox-reconstructed: CaseSvgGlyphAbsentCells.a@other.lg — 0 0 16 16 from computed size 16×16 (path extent 13.0; viewBox is not a computed style — named reconstruction)
- root-display-carried: flex
- full-width-carried: a spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- full-width-carried: icon spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- **REFUSED**: svg-content-multi-axis: CaseSvgGlyphAbsentCells.a - markup varies over more than one axis; asset refused (part still promoted as a box) - the conjunction "plain-marked" is not a rectangle over the part's own presence domain: it would draw this glyph in 2 of the 6 combo(s) where "a" renders but the library does not (2 draw NO glyph at all: plain.lg, marked.sm; 0 draw a DIFFERENT glyph)
- **REFUSED**: part-presence-uncorrelated: icon present in 4/6 enabled combos and the presence set does not factor per-axis (nor does its ABSENCE — the round-5c complement spelling was tried) — part NOT promoted (named refusal; a phantom always-drawn part would be worse)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (a, icon, part-0-0-0); -webkit-text-stroke-color (a, icon, part-0-0-0)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **4** → WITH folding: **4** (30 folded channels; every fold a named receipt).

- a.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **4** · bindings: 4 base + 0 state
- shape: 4 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **4**
  - root.box-sizing = `border-box`
  - root.font-family = `monospace`
  - a.box-sizing = `border-box`
  - a.font-family = `monospace`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **0**
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **100.000%** (504/504 cells; 24/24 combo×state rows fully equal)
- pixel: 0/24 pairs perfect at threshold 0 · 8/24 at the AA point (mean AA 0.046%, max 0.143%; 24/24 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

