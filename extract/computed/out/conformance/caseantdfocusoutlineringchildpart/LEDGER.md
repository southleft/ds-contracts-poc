# Delta ledger — computed floor × static layer (CaseAntdFocusOutlineRingChildPart)

Generated by extract/computed/run.ts against @ds-contracts/conformance@0.1.0 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component CaseAntdFocusOutlineRingChildPart`.

- captures: **4** (1 combos, full-cartesian × 4 interactions), channels enumerated per element: **479** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `label|root` (matched) → **b** `span|b` (computed-only) → **c** `input|c|sr-only` (computed-only) → **a** `span|a` (computed-only) → **label** `span|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **5** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**
- root-display-carried: flex
- sr-only-carried-as-hidden: c is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- literal-text-carried: label = "Sample" (no text prop sample matches — the mounted value is carried verbatim)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::placeholder (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **17** → WITH folding: **19** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **19** · bindings: 48 base + 6 state
- shape: 54 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **22**
  - root.box-sizing = `border-box`
  - root.cursor = `default`
  - root.font-family = `monospace`
  - b.box-sizing = `border-box`
  - b.cursor = `default`
  - b.font-family = `monospace`
  - b.position = `relative`
  - c.appearance = `auto`
  - c.box-sizing = `border-box`
  - c.cursor = `pointer`
  - c.font-family = `monospace`
  - c.position = `absolute`
  - a.border-bottom-style = `solid`
  - a.border-left-style = `solid`
  - a.border-right-style = `solid`
  - a.border-top-style = `solid`
  - a.box-sizing = `border-box`
  - a.cursor = `default`
  - a.font-family = `monospace`
  - a.outline-style = `solid`
  - label.cursor = `default`
  - label.font-family = `monospace`
- state declared facts: **1**
  - [focus-visible] c.outline-style = `auto`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **1**
  - label.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **6**
  - {"part":"a","channel":"outline-color","state":"focus-visible","ref":"{imported.case-antd-focus-outline-ring-child-part.a.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"a","channel":"outline-offset","state":"focus-visible","ref":"{imported.case-antd-focus-outline-ring-child-part.a.outline-offset-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"a","channel":"outline-width","state":"focus-visible","ref":"{imported.case-antd-focus-outline-ring-child-part.a.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"c","channel":"outline-color","state":"focus-visible","ref":"{imported.case-antd-focus-outline-ring-child-part.c.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"c","channel":"outline-offset","state":"focus-visible","ref":"{imported.case-antd-focus-outline-ring-child-part.c.outline-offset-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"c","channel":"outline-width","state":"focus-visible","ref":"{imported.case-antd-focus-outline-ring-child-part.c.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **99.242%** (262/264 cells; 3/4 combo×state rows fully equal)
- pixel: 3/4 pairs perfect at threshold 0 · 3/4 at the AA point (mean AA 0.153%, max 0.612%; 4/4 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

