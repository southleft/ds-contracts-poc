# Delta ledger — computed floor × static layer (CasePositionAbsoluteInsets)

Generated by extract/computed/run.ts against @ds-contracts/conformance@0.1.0 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component CasePositionAbsoluteInsets`.

- captures: **4** (1 combos, full-cartesian × 4 interactions), channels enumerated per element: **476** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|root` (matched) → **a** `span|a` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **2** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **10** → WITH folding: **10** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **10** · bindings: 20 base + 0 state
- shape: 20 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **5**
  - root.box-sizing = `border-box`
  - root.font-family = `monospace`
  - root.position = `relative`
  - a.box-sizing = `border-box`
  - a.font-family = `monospace`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **0**
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **100.000%** (88/88 cells; 4/4 combo×state rows fully equal)
- pixel: 4/4 pairs perfect at threshold 0 · 4/4 at the AA point (mean AA 0.000%, max 0.000%; 4/4 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

