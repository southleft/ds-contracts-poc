# Delta ledger — computed floor × static layer (CaseGridSubgrid)

Generated by extract/computed/run.ts against @ds-contracts/conformance@0.1.0 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component CaseGridSubgrid`.

- captures: **4** (1 combos, full-cartesian × 4 interactions), channels enumerated per element: **479** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|root` (matched) → **a** `span|a` (computed-only) → **b** `i|b` (computed-only) → **c** `i|c` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **4** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **1**
- grid-promotion-fallback: a — declared grid-template-columns "subgrid" is a refused track construct — the G7 fence names it; the flex-era lowering decides (named)
- grid-tracks-declared: root.grid-template-columns carries the DECLARED list "1fr 1fr" — verified against computed "100px 100px" (2 tracks; every px track agrees ≤0.5px; fr/fit spellings resolve to used px in computed style and are carried from source)
- grid-promoted: root — 1×2 declared tracks carried as layout.rows/columns; 1 child placement(s) (0-based cells, spans, per-cell align — A2 G1/G2/G4)
- grid-axis-definite: root height carried as literals.height "fit-content" (G8) — the used box 24px equals the intrinsic track sum (24px), so the box IS its content and the canvas can hug it instead of keeping createFrame's FIXED 100 default (FC-GRID-ROOT-VSIZE)
- **REFUSED**: grid-subgrid: subgrid is refused — no track-inheritance property exists anywhere in the reflected Plugin API inventory (P1); a nested grid with copied literal tracks is a DIFFERENT fact and must not be passed off as subgrid (part a)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **8** → WITH folding: **8** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **8** · bindings: 11 base + 0 state
- shape: 11 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **10**
  - root.box-sizing = `border-box`
  - root.font-family = `monospace`
  - a.box-sizing = `border-box`
  - a.font-family = `monospace`
  - b.box-sizing = `border-box`
  - b.font-family = `monospace`
  - b.font-style = `italic`
  - c.box-sizing = `border-box`
  - c.font-family = `monospace`
  - c.font-style = `italic`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **6**
  - a.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `grid`, 1 distinct value(s))
  - a.grid-template-columns — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `subgrid [] [] []`, 1 distinct value(s))
  - b.grid-column-start — grid placement not carried: parent a declined structured grid promotion — declared grid-template-columns "subgrid" is a refused track construct — the G7 fence names it; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - b.grid-row-start — grid placement not carried: parent a declined structured grid promotion — declared grid-template-columns "subgrid" is a refused track construct — the G7 fence names it; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
  - c.grid-column-start — grid placement not carried: parent a declined structured grid promotion — declared grid-template-columns "subgrid" is a refused track construct — the G7 fence names it; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `2`, 1 distinct value(s))
  - c.grid-row-start — grid placement not carried: parent a declined structured grid promotion — declared grid-template-columns "subgrid" is a refused track construct — the G7 fence names it; an anchor/span of a grid the contract does not carry is a dead fact and is refused, not minted (sample: `1`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **78.788%** (104/132 cells; 0/4 combo×state rows fully equal)
- pixel: 0/4 pairs perfect at threshold 0 · 0/4 at the AA point (mean AA 7.813%, max 7.813%; 4/4 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

