# Delta ledger — computed floor × static layer (CasePositionSticky)

Generated by extract/computed/run.ts against @ds-contracts/conformance@0.1.0 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component CasePositionSticky`.

- captures: **4** (1 combos, full-cartesian × 4 interactions), channels enumerated per element: **476** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|root` (matched) → **label** `div|a` (computed-only) → **label-2** `div|b` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **3** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**
- literal-text-carried: label = "Header" (no text prop sample matches — the mounted value is carried verbatim)
- literal-text-carried: label-2 = "Body copy that overflows the container b" (no text prop sample matches — the mounted value is carried verbatim)

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **9** → WITH folding: **9** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **9** · bindings: 15 base + 2 state
- shape: 17 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **8**
  - root.box-sizing = `border-box`
  - root.font-family = `monospace`
  - root.overflow-x = `auto`
  - root.overflow-y = `auto`
  - label.box-sizing = `border-box`
  - label.font-family = `monospace`
  - label-2.box-sizing = `border-box`
  - label-2.font-family = `monospace`
- state declared facts: **1**
  - [focus-visible] root.outline-style = `auto`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **3**
  - label.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - label.position — declared-channel value outside the bounded grammar — named residue (v15) (sample: `sticky`, 1 distinct value(s))
  - label-2.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **96.000%** (96/100 cells; 0/4 combo×state rows fully equal)
- pixel: 0/4 pairs perfect at threshold 0 · 3/4 at the AA point (mean AA 0.771%, max 3.086%; 4/4 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

