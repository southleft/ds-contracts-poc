# Delta ledger — computed floor × static layer (CaseRepeatSiblingsThirtyone)

Generated by extract/computed/run.ts against @ds-contracts/conformance@0.1.0 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component CaseRepeatSiblingsThirtyone`.

- captures: **4** (1 combos, full-cartesian × 4 interactions), channels enumerated per element: **476** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|root` (matched) → **a** `span|a` (computed-only) → **a-2** `span|a` (computed-only) → **a-3** `span|a` (computed-only) → **a-4** `span|a` (computed-only) → **a-5** `span|a` (computed-only) → **a-6** `span|a` (computed-only) → **a-7** `span|a` (computed-only) → **a-8** `span|a` (computed-only) → **a-9** `span|a` (computed-only) → **a-10** `span|a` (computed-only) → **a-11** `span|a` (computed-only) → **a-12** `span|a` (computed-only) → **a-13** `span|a` (computed-only) → **a-14** `span|a` (computed-only) → **a-15** `span|a` (computed-only) → **a-16** `span|a` (computed-only) → **a-17** `span|a` (computed-only) → **a-18** `span|a` (computed-only) → **a-19** `span|a` (computed-only) → **a-20** `span|a` (computed-only) → **a-21** `span|a` (computed-only) → **a-22** `span|a` (computed-only) → **a-23** `span|a` (computed-only) → **a-24** `span|a` (computed-only) → **a-25** `span|a` (computed-only) → **a-26** `span|a` (computed-only) → **a-27** `span|a` (computed-only) → **a-28** `span|a` (computed-only) → **a-29** `span|a` (computed-only) → **a-30** `span|a` (computed-only) → **b** `span|b` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **32** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**
- root-display-carried: flex

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (a, a-10, a-11, a-12, a-13, a-14, a-15, a-16, a-17, a-18, a-19, a-2, a-20, a-21, a-22, a-23, a-24, a-25, a-26, a-27, a-28, a-29, a-3, a-30, a-4, a-5, a-6, a-7, a-8, a-9, b, root); -webkit-text-stroke-color (a, a-10, a-11, a-12, a-13, a-14, a-15, a-16, a-17, a-18, a-19, a-2, a-20, a-21, a-22, a-23, a-24, a-25, a-26, a-27, a-28, a-29, a-3, a-30, a-4, a-5, a-6, a-7, a-8, a-9, b, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **11** → WITH folding: **11** (192 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-4.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-4.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-4.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-4.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-4.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-4.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-5.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-5.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-5.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-5.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-5.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-5.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-6.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-6.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-6.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-6.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-6.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-6.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-7.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-7.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-7.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-7.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-7.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-7.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-8.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-8.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-8.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-8.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-8.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-8.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-9.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-9.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-9.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-9.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-9.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-9.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-10.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-10.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-10.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-10.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-10.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-10.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-11.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-11.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-11.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-11.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-11.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-11.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-12.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-12.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-12.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-12.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-12.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-12.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-13.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-13.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-13.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-13.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-13.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-13.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-14.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-14.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-14.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-14.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-14.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-14.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-15.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-15.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-15.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-15.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-15.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-15.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-16.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-16.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-16.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-16.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-16.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-16.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-17.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-17.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-17.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-17.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-17.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-17.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-18.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-18.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-18.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-18.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-18.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-18.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-19.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-19.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-19.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-19.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-19.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-19.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-20.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-20.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-20.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-20.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-20.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-20.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-21.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-21.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-21.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-21.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-21.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-21.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-22.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-22.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-22.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-22.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-22.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-22.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-23.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-23.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-23.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-23.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-23.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-23.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-24.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-24.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-24.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-24.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-24.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-24.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-25.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-25.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-25.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-25.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-25.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-25.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-26.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-26.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-26.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-26.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-26.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-26.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-27.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-27.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-27.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-27.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-27.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-27.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-28.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-28.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-28.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-28.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-28.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-28.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-29.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-29.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-29.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-29.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-29.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-29.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-30.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-30.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-30.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-30.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-30.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- a-30.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- b.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- b.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- b.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- b.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- b.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- b.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **11** · bindings: 232 base + 0 state
- shape: 232 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **64**
  - root.box-sizing = `border-box`
  - root.font-family = `monospace`
  - a.box-sizing = `border-box`
  - a.font-family = `monospace`
  - a-2.box-sizing = `border-box`
  - a-2.font-family = `monospace`
  - a-3.box-sizing = `border-box`
  - a-3.font-family = `monospace`
  - a-4.box-sizing = `border-box`
  - a-4.font-family = `monospace`
  - a-5.box-sizing = `border-box`
  - a-5.font-family = `monospace`
  - a-6.box-sizing = `border-box`
  - a-6.font-family = `monospace`
  - a-7.box-sizing = `border-box`
  - a-7.font-family = `monospace`
  - a-8.box-sizing = `border-box`
  - a-8.font-family = `monospace`
  - a-9.box-sizing = `border-box`
  - a-9.font-family = `monospace`
  - a-10.box-sizing = `border-box`
  - a-10.font-family = `monospace`
  - a-11.box-sizing = `border-box`
  - a-11.font-family = `monospace`
  - a-12.box-sizing = `border-box`
  - a-12.font-family = `monospace`
  - a-13.box-sizing = `border-box`
  - a-13.font-family = `monospace`
  - a-14.box-sizing = `border-box`
  - a-14.font-family = `monospace`
  - a-15.box-sizing = `border-box`
  - a-15.font-family = `monospace`
  - a-16.box-sizing = `border-box`
  - a-16.font-family = `monospace`
  - a-17.box-sizing = `border-box`
  - a-17.font-family = `monospace`
  - a-18.box-sizing = `border-box`
  - a-18.font-family = `monospace`
  - a-19.box-sizing = `border-box`
  - a-19.font-family = `monospace`
  - a-20.box-sizing = `border-box`
  - a-20.font-family = `monospace`
  - a-21.box-sizing = `border-box`
  - a-21.font-family = `monospace`
  - a-22.box-sizing = `border-box`
  - a-22.font-family = `monospace`
  - a-23.box-sizing = `border-box`
  - a-23.font-family = `monospace`
  - a-24.box-sizing = `border-box`
  - a-24.font-family = `monospace`
  - a-25.box-sizing = `border-box`
  - a-25.font-family = `monospace`
  - a-26.box-sizing = `border-box`
  - a-26.font-family = `monospace`
  - a-27.box-sizing = `border-box`
  - a-27.font-family = `monospace`
  - a-28.box-sizing = `border-box`
  - a-28.font-family = `monospace`
  - a-29.box-sizing = `border-box`
  - a-29.font-family = `monospace`
  - a-30.box-sizing = `border-box`
  - a-30.font-family = `monospace`
  - b.box-sizing = `border-box`
  - b.font-family = `monospace`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **1**
  - root.flex-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `wrap`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **99.796%** (1956/1960 cells; 0/4 combo×state rows fully equal)
- pixel: 0/4 pairs perfect at threshold 0 · 0/4 at the AA point (mean AA 25.078%, max 25.078%; 4/4 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

