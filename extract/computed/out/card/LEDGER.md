# Delta ledger — computed floor × static layer (Card)

Generated by extract/computed/run.ts against @astryxdesign/core@0.1.6 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Card`.

- captures: **52** (13 combos, full-cartesian × 4 interactions), channels enumerated per element: **658** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|card` (matched) · static-part-unrendered: body
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **1** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **1**
- root-display-carried: block via declared (overrides reviewed layout.display "flex" — computed truth wins display, receipted)
- **REFUSED**: static-part-unrendered: reviewed static part "body" has no rendered counterpart in ANY captured combo — dropped from the promoted anatomy (named; usually a conditional the sweep never triggered)

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **106** → WITH folding: **106** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

112/182 carried-binding cells string-equal to the browser-probed token value (no tolerance).

### Binding contradictions (70 — the review queue, see review-queue.json; 70 untriaged)

- `default` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `default` root.padding-block (padding-top) {spacing-4}: expected `16px` observed `15px`
  - **UNTRIAGED** — a defect until triaged
- `default` root.padding-block (padding-bottom) {spacing-4}: expected `16px` observed `15px`
  - **UNTRIAGED** — a defect until triaged
- `default` root.padding-inline (padding-left) {spacing-4}: expected `16px` observed `15px`
  - **UNTRIAGED** — a defect until triaged
- `default` root.padding-inline (padding-right) {spacing-4}: expected `16px` observed `15px`
  - **UNTRIAGED** — a defect until triaged
- `default` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `default` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `default` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `default` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `transparent` root.background-color (background-color) {color-background-body}: expected `rgba(241, 244, 247, 1)` observed `rgba(0, 0, 0, 0)`
  - **UNTRIAGED** — a defect until triaged
- `transparent` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `transparent` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `transparent` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `transparent` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `transparent` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `muted` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `muted` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `muted` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `muted` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `muted` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `blue` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `blue` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `blue` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `blue` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `blue` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `cyan` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `cyan` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `cyan` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `cyan` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `cyan` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `gray` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `gray` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `gray` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `gray` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `gray` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `green` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `green` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `green` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `green` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `green` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `orange` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `orange` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `orange` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `orange` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `orange` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `pink` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `pink` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `pink` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `pink` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `pink` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `purple` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `purple` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `purple` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `purple` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `purple` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `red` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `red` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `red` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `red` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `red` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `teal` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `teal` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `teal` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `teal` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `teal` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged
- `yellow` root.color (color) {color-text-primary}: expected `rgba(10, 19, 23, 1)` observed `rgba(0, 0, 0, 1)`
  - **UNTRIAGED** — a defect until triaged
- `yellow` root.gap (column-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `yellow` root.gap (row-gap) {spacing-3}: expected `12px` observed `normal`
  - **UNTRIAGED** — a defect until triaged
- `yellow` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `yellow` root.font-size (font-size) {font-size-base}: expected `14px` observed `16px`
  - **UNTRIAGED** — a defect until triaged

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **106** · bindings: 10 base + 0 state
- shape: 2 uniform · 8 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **2**
  - root.overflow-x = `clip`
  - root.overflow-y = `clip`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **0**
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **58.042%** (664/1144 cells; 0/52 combo×state rows fully equal)
- pixel: 0/52 pairs perfect at threshold 0 · 0/52 at the AA point (mean AA 12.925%, max 18.132%)

