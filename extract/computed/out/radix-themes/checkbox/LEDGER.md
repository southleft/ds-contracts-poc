# Delta ledger — computed floor × static layer (Checkbox)

Generated by extract/computed/run.ts against @radix-themes-sandbox/ui@0.0.1 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Checkbox`.

- captures: **216** (54 combos, full-cartesian × 4 interactions), channels enumerated per element: **1364** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `button|BaseCheckboxRoot.CheckboxRoot` (matched) → **icon** `svg|BaseCheckboxIndicator.CheckboxIndicator` (computed-only) → **part-0-0** `path|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **3** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **2**
- svg-currentcolor-folded: Checkbox.root@classic.1.checked.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@classic.1.checked.enabled — 0 0 9 9 from computed size 9×9 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@classic.1.indeterminate.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@classic.1.indeterminate.enabled — 0 0 9 9 from computed size 9×9 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@classic.2.checked.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@classic.2.checked.enabled — 0 0 10 10 from computed size 10×10 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@classic.2.indeterminate.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@classic.2.indeterminate.enabled — 0 0 10 10 from computed size 10×10 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@classic.3.checked.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@classic.3.checked.enabled — 0 0 12 12 from computed size 12×12 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@classic.3.indeterminate.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@classic.3.indeterminate.enabled — 0 0 12 12 from computed size 12×12 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@surface.1.checked.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@surface.1.checked.enabled — 0 0 9 9 from computed size 9×9 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@surface.1.indeterminate.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@surface.1.indeterminate.enabled — 0 0 9 9 from computed size 9×9 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@surface.2.checked.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@surface.2.checked.enabled — 0 0 10 10 from computed size 10×10 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@surface.2.indeterminate.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@surface.2.indeterminate.enabled — 0 0 10 10 from computed size 10×10 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@surface.3.checked.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@surface.3.checked.enabled — 0 0 12 12 from computed size 12×12 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@surface.3.indeterminate.enabled — the glyph's authored fill rgba(255, 255, 255, 1) EQUALS the svg's inherited `color` (rgba(255, 255, 255, 1)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@surface.3.indeterminate.enabled — 0 0 12 12 from computed size 12×12 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@soft.1.checked.enabled — the glyph's authored fill rgba(0, 43, 183, 0.773) EQUALS the svg's inherited `color` (rgba(0, 43, 183, 0.773)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@soft.1.checked.enabled — 0 0 9 9 from computed size 9×9 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@soft.1.indeterminate.enabled — the glyph's authored fill rgba(0, 43, 183, 0.773) EQUALS the svg's inherited `color` (rgba(0, 43, 183, 0.773)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@soft.1.indeterminate.enabled — 0 0 9 9 from computed size 9×9 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@soft.2.checked.enabled — the glyph's authored fill rgba(0, 43, 183, 0.773) EQUALS the svg's inherited `color` (rgba(0, 43, 183, 0.773)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@soft.2.checked.enabled — 0 0 10 10 from computed size 10×10 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@soft.2.indeterminate.enabled — the glyph's authored fill rgba(0, 43, 183, 0.773) EQUALS the svg's inherited `color` (rgba(0, 43, 183, 0.773)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@soft.2.indeterminate.enabled — 0 0 10 10 from computed size 10×10 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@soft.3.checked.enabled — the glyph's authored fill rgba(0, 43, 183, 0.773) EQUALS the svg's inherited `color` (rgba(0, 43, 183, 0.773)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@soft.3.checked.enabled — 0 0 12 12 from computed size 12×12 (path extent 9.0; viewBox is not a computed style — named reconstruction)
- svg-currentcolor-folded: Checkbox.root@soft.3.indeterminate.enabled — the glyph's authored fill rgba(0, 43, 183, 0.773) EQUALS the svg's inherited `color` (rgba(0, 43, 183, 0.773)) and is rewritten to currentColor, so the literal colour is NOT carried in the asset and the glyph paints whatever the consuming surface's colour chain says. When that chain arrives from the PAGE rather than the component (a theme provider setting `color` on an ancestor), the generated surface has nothing to inherit and the glyph draws black — the Polaris inherited-ink shape.
- svg-viewbox-reconstructed: Checkbox.root@soft.3.indeterminate.enabled — 0 0 12 12 from computed size 12×12 (path extent 8.3; viewBox is not a computed style — named reconstruction)
- root-display-carried: flex
- aspect-carried: icon computed width == height in every enabled combo (9/10/12px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- state-axis-presence-dropped: icon absent under disabled but the contract has no "disabled" prop — factor dropped, part renders in that plane (named residue)
- presence-carried: icon (18/27 combos) →  hidden-when checked=unchecked
- state-axis-presence-dropped: part-0-0 absent under disabled but the contract has no "disabled" prop — factor dropped, part renders in that plane (named residue)
- presence-carried: part-0-0 (18/27 combos) →  hidden-when checked=unchecked
- **REFUSED**: svg-content-multi-axis: Checkbox.root — markup varies over more than one axis; asset refused (part still promoted as a box)
- **REFUSED**: pseudo-decor-outside-grammar: root::before paints at position:static (20x20, opacity 1) — the bounded decor grammar reads position:absolute boxes only; an in-flow decor box is NOT promoted and NOT on the canvas

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::before (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 4 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-font-smoothing (icon, part-0-0, root); -webkit-tap-highlight-color (icon, part-0-0, root); -webkit-text-fill-color (icon, part-0-0, root); -webkit-text-stroke-color (icon, part-0-0, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **104** → WITH folding: **44** (30 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [disabled] icon.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] icon.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] icon.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] icon.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] icon.caret-color rides its source fact
- fold-carries-state-delta: [disabled] icon.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] icon.outline-color rides its source fact
- fold-carries-state-delta: [disabled] icon.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] icon.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] icon.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.caret-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.outline-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] part-0-0.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **44** · bindings: 37 base + 0 state
- shape: 25 uniform · 12 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **16**
  - root.appearance = `none`
  - root.border-bottom-style = `none`
  - root.border-left-style = `none`
  - root.border-right-style = `none`
  - root.border-top-style = `none`
  - root.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - root.position = `relative`
  - root.text-align = `start`
  - icon.cursor = `default`
  - icon.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - icon.overflow-x = `hidden`
  - icon.overflow-y = `hidden`
  - icon.pointer-events = `none`
  - part-0-0.cursor = `default`
  - part-0-0.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - part-0-0.pointer-events = `none`
- state declared facts: **1**
  - [disabled] root.cursor = `not-allowed`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **14**
  - root.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - root.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - root.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `top`, 1 distinct value(s))
  - icon.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `content-box`, 1 distinct value(s))
  - icon.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `break-word`, 1 distinct value(s))
  - icon.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `100%`, 1 distinct value(s))
  - icon.transform — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `matrix(1, 0, 0, 1, -4.5, -4.5)`, 3 distinct value(s))
  - part-0-0.clip-rule — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `evenodd`, 1 distinct value(s))
  - part-0-0.d — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `path("M 8.53547 0.62293 C 8.88226 0.849446 8.97976 1.3142 8.75325 1.66099 L 4.5083 8.1599 `, 2 distinct value(s))
  - part-0-0.fill-rule — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `evenodd`, 1 distinct value(s))
  - part-0-0.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - part-0-0.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `break-word`, 1 distinct value(s))
  - part-0-0.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `100%`, 1 distinct value(s))
  - part-0-0.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <path>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <path> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
- state channels outside mintable kinds: **6**
  - [disabled] icon.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(0, 8, 48, 0.275)`)
  - [disabled] icon.cursor — declared-channel state delta on 18/27 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `not-allowed`)
  - [disabled] icon.fill — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(0, 8, 48, 0.275)`)
  - [disabled] part-0-0.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(0, 8, 48, 0.275)`)
  - [disabled] part-0-0.cursor — declared-channel state delta on 18/27 combos — partial coverage cannot carry as a state selector (v15); named residue (sample: `not-allowed`)
  - [disabled] part-0-0.fill — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(0, 8, 48, 0.275)`)
- refused/overflow bindings: **2**
  - {"part":"part-0-0","channel":"color","ref":"{imported.checkbox.part-0-0.color.{variant}}","refusal":"inheritance-only channel with uncarried nested state deltas — the captured value equals this part's ancestor on EVERY plane, and its per-state deltas exceed the nested Part.states vocabulary (plain color-kind refs only); binding the base value would pin all state planes to the resting colour, so the channel stays UNCARRIED and CSS inheritance from the ancestor renders it (leaves exist in the minted tree)"}
  - {"part":"part-0-0","channel":"fill","ref":"{imported.checkbox.part-0-0.fill.{variant}}","refusal":"inheritance-only channel with uncarried nested state deltas — the captured value equals this part's ancestor on EVERY plane, and its per-state deltas exceed the nested Part.states vocabulary (plain color-kind refs only); binding the base value would pin all state planes to the resting colour, so the channel stays UNCARRIED and CSS inheritance from the ancestor renders it (leaves exist in the minted tree)"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **77.566%** (12789/16488 cells; 0/216 combo×state rows fully equal)
- pixel: 0/216 pairs perfect at threshold 0 · 12/216 at the AA point (mean AA 0.252%, max 1.073%; 216/216 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

