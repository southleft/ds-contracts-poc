# Delta ledger — computed floor × static layer (Tabs)

Generated by extract/computed/run.ts against @radix-themes-sandbox/ui@0.0.1 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Tabs`.

- captures: **4** (1 combos, full-cartesian × 4 interactions), channels enumerated per element: **1364** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|TabsRoot` (matched) → **basetablist** `div|BaseTabList.TabsList` (computed-only) → **basetablisttrigger** `button|BaseTabListTrigger.TabsTrigger` (computed-only) → **label** `span|BaseTabListTriggerInner.TabsTriggerInner` (computed-only) → **label-2** `span|BaseTabListTriggerInnerHidden.TabsTriggerInnerHidden` (computed-only) → **basetablisttrigger-2** `button|BaseTabListTrigger.TabsTrigger` (computed-only) → **label-3** `span|BaseTabListTriggerInner.TabsTriggerInner` (computed-only) → **label-4** `span|BaseTabListTriggerInnerHidden.TabsTriggerInnerHidden` (computed-only) → **basetablisttrigger-3** `button|BaseTabListTrigger.TabsTrigger` (computed-only) → **label-5** `span|BaseTabListTriggerInner.TabsTriggerInner` (computed-only) → **label-6** `span|BaseTabListTriggerInnerHidden.TabsTriggerInnerHidden` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **8** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **3**
- literal-text-carried: label = "Account" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- literal-text-carried: label-2 = "Account" (no text prop sample matches — the mounted value is carried verbatim)
- pseudo-decor-carried: basetablisttrigger::before → UNCONDITIONAL shape part "basetablisttrigger-before" (rect 85.672×2 at -0.00005000000000165983,38, fill rgba(62, 99, 221, 1); drawn in 1/1 default-interaction combos; geometry uniform, size uniform, paint uniform, rotation 0 — pseudo-decor v2)
- literal-text-carried: label-3 = "Documents" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label-3 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- literal-text-carried: label-4 = "Documents" (no text prop sample matches — the mounted value is carried verbatim)
- literal-text-carried: label-5 = "Settings" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label-5 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- literal-text-carried: label-6 = "Settings" (no text prop sample matches — the mounted value is carried verbatim)
- **REFUSED**: non-painting-part: label-2 <span> renders NO INK in any combo it appears in (visibility: hidden) and no descendant paints, yet it carries the text "Account" — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame with visible text on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)
- **REFUSED**: non-painting-part: label-4 <span> renders NO INK in any combo it appears in (visibility: hidden) and no descendant paints, yet it carries the text "Documents" — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame with visible text on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)
- **REFUSED**: non-painting-part: label-6 <span> renders NO INK in any combo it appears in (visibility: hidden) and no descendant paints, yet it carries the text "Settings" — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame with visible text on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::before (2 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 4 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-font-smoothing (basetablist, basetablisttrigger, basetablisttrigger-2, basetablisttrigger-3, label, label-2, label-3, label-4, label-5, label-6, root); -webkit-tap-highlight-color (basetablisttrigger, basetablisttrigger-2, basetablisttrigger-3, label, label-2, label-3, label-4, label-5, label-6); -webkit-text-fill-color (basetablist, basetablisttrigger, basetablisttrigger-2, basetablisttrigger-3, label, label-2, label-3, label-4, label-5, label-6, root); -webkit-text-stroke-color (basetablist, basetablisttrigger, basetablisttrigger-2, basetablisttrigger-3, label, label-2, label-3, label-4, label-5, label-6, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **40** → WITH folding: **45** (109 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablist.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- basetablisttrigger-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [active] basetablisttrigger-2.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.border-left-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.border-right-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.border-top-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.caret-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.column-rule-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.outline-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.row-rule-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger-2.text-emphasis-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.border-left-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.border-right-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.border-top-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.caret-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.column-rule-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.outline-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.row-rule-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] basetablisttrigger.text-emphasis-color rides its source fact
- fold-carries-state-delta: [active] label-3.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] label-3.border-left-color rides its source fact
- fold-carries-state-delta: [active] label-3.border-right-color rides its source fact
- fold-carries-state-delta: [active] label-3.border-top-color rides its source fact
- fold-carries-state-delta: [active] label-3.caret-color rides its source fact
- fold-carries-state-delta: [active] label-3.column-rule-color rides its source fact
- fold-carries-state-delta: [active] label-3.outline-color rides its source fact
- fold-carries-state-delta: [active] label-3.row-rule-color rides its source fact
- fold-carries-state-delta: [active] label-3.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] label-3.text-emphasis-color rides its source fact
- fold-carries-state-delta: [active] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] label.border-left-color rides its source fact
- fold-carries-state-delta: [active] label.border-right-color rides its source fact
- fold-carries-state-delta: [active] label.border-top-color rides its source fact
- fold-carries-state-delta: [active] label.caret-color rides its source fact
- fold-carries-state-delta: [active] label.column-rule-color rides its source fact
- fold-carries-state-delta: [active] label.row-rule-color rides its source fact
- fold-carries-state-delta: [active] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.border-bottom-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.border-left-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.border-right-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.border-top-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.caret-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.outline-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.row-rule-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] basetablisttrigger-2.text-emphasis-color rides its source fact
- fold-carries-state-delta: [hover] label-3.border-bottom-color rides its source fact
- fold-carries-state-delta: [hover] label-3.border-left-color rides its source fact
- fold-carries-state-delta: [hover] label-3.border-right-color rides its source fact
- fold-carries-state-delta: [hover] label-3.border-top-color rides its source fact
- fold-carries-state-delta: [hover] label-3.caret-color rides its source fact
- fold-carries-state-delta: [hover] label-3.column-rule-color rides its source fact
- fold-carries-state-delta: [hover] label-3.outline-color rides its source fact
- fold-carries-state-delta: [hover] label-3.row-rule-color rides its source fact
- fold-carries-state-delta: [hover] label-3.text-decoration-color rides its source fact
- fold-carries-state-delta: [hover] label-3.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **45** · bindings: 133 base + 19 state
- shape: 152 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **50**
  - root.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - basetablist.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - basetablist.overflow-x = `auto`
  - basetablist.overflow-y = `auto`
  - basetablist.text-wrap-mode = `nowrap`
  - basetablisttrigger.appearance = `none`
  - basetablisttrigger.border-bottom-style = `none`
  - basetablisttrigger.border-left-style = `none`
  - basetablisttrigger.border-right-style = `none`
  - basetablisttrigger.border-top-style = `none`
  - basetablisttrigger.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - basetablisttrigger.position = `relative`
  - basetablisttrigger.text-align = `start`
  - basetablisttrigger.text-wrap-mode = `nowrap`
  - basetablisttrigger.user-select = `none`
  - label.box-sizing = `border-box`
  - label.cursor = `default`
  - label.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - label.text-wrap-mode = `nowrap`
  - label.user-select = `none`
  - basetablisttrigger-2.appearance = `none`
  - basetablisttrigger-2.border-bottom-style = `none`
  - basetablisttrigger-2.border-left-style = `none`
  - basetablisttrigger-2.border-right-style = `none`
  - basetablisttrigger-2.border-top-style = `none`
  - basetablisttrigger-2.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - basetablisttrigger-2.position = `relative`
  - basetablisttrigger-2.text-align = `start`
  - basetablisttrigger-2.text-wrap-mode = `nowrap`
  - basetablisttrigger-2.user-select = `none`
  - label-3.box-sizing = `border-box`
  - label-3.cursor = `default`
  - label-3.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - label-3.text-wrap-mode = `nowrap`
  - label-3.user-select = `none`
  - basetablisttrigger-3.appearance = `none`
  - basetablisttrigger-3.border-bottom-style = `none`
  - basetablisttrigger-3.border-left-style = `none`
  - basetablisttrigger-3.border-right-style = `none`
  - basetablisttrigger-3.border-top-style = `none`
  - basetablisttrigger-3.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - basetablisttrigger-3.position = `relative`
  - basetablisttrigger-3.text-align = `start`
  - basetablisttrigger-3.text-wrap-mode = `nowrap`
  - basetablisttrigger-3.user-select = `none`
  - label-5.box-sizing = `border-box`
  - label-5.cursor = `default`
  - label-5.font-family = `-apple-system, "system-ui", "Segoe UI (Custom)", Roboto, "Helvetica Neue", "Open Sans (Custom)", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"`
  - label-5.text-wrap-mode = `nowrap`
  - label-5.user-select = `none`
- state declared facts: **1**
  - [focus-visible] label.outline-style = `solid`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **17**
  - root.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - root.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - basetablist.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - basetablist.scrollbar-width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - basetablist.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - basetablisttrigger.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - basetablisttrigger.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - label.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - label.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - basetablisttrigger-2.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - basetablisttrigger-2.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - label-3.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - label-3.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - basetablisttrigger-3.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - basetablisttrigger-3.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - label-5.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - label-5.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
- state channels outside mintable kinds: **1**
  - [active] label.letter-spacing — value shape outside mintable kinds and outside the declared vocabulary (sample: `normal`)
- refused/overflow bindings: **11**
  - {"part":"label-3","channel":"font-weight","state":"active","ref":"{imported.tabs.label-3.font-weight-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label-3","channel":"left","state":"active","ref":"{imported.tabs.label-3.left-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label-3","channel":"letter-spacing","state":"active","ref":"{imported.tabs.label-3.letter-spacing-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label-3","channel":"right","state":"active","ref":"{imported.tabs.label-3.right-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"font-weight","state":"active","ref":"{imported.tabs.label.font-weight-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"left","state":"active","ref":"{imported.tabs.label.left-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"outline-color","state":"active","ref":"{imported.shared.color-0007149f}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"right","state":"active","ref":"{imported.tabs.label.right-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"outline-color","state":"focus-visible","ref":"{imported.tabs.label.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"outline-offset","state":"focus-visible","ref":"{imported.tabs.label.outline-offset-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"label","channel":"outline-width","state":"focus-visible","ref":"{imported.tabs.label.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **88.986%** (1018/1144 cells; 0/4 combo×state rows fully equal)
- pixel: 0/4 pairs perfect at threshold 0 · 0/4 at the AA point (mean AA 0.455%, max 0.953%; 4/4 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

