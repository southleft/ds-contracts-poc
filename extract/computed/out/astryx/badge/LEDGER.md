# Delta ledger — computed floor × static layer (Badge)

Generated by extract/computed/run.ts against @astryxdesign/core@0.1.6 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Badge`.

- captures: **56** (14 combos, full-cartesian × 4 interactions), channels enumerated per element: **658** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `span|badge` (matched)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **1** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **140** → WITH folding: **14** (9 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

187/224 carried-binding cells string-equal to the browser-probed token value (no tolerance).

### Binding contradictions (37 — the review queue, see review-queue.json; 37 untriaged)

- `neutral` root.background-color (background-color) {color-background-gray}: expected `rgba(10, 19, 23, 0.2)` observed `rgba(5, 54, 89, 0.1)`
  - **UNTRIAGED** — a defect until triaged
- `neutral` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `neutral` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `info` root.background-color (background-color) {color-background-blue}: expected `rgba(1, 113, 227, 0.2)` observed `rgba(0, 100, 224, 1)`
  - **UNTRIAGED** — a defect until triaged
- `info` root.color (color) {color-text-blue}: expected `rgba(4, 47, 151, 1)` observed `rgba(255, 255, 255, 1)`
  - **UNTRIAGED** — a defect until triaged
- `info` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `info` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `success` root.background-color (background-color) {color-background-green}: expected `rgba(36, 187, 94, 0.2)` observed `rgba(13, 134, 38, 1)`
  - **UNTRIAGED** — a defect until triaged
- `success` root.color (color) {color-text-green}: expected `rgba(9, 68, 31, 1)` observed `rgba(255, 255, 255, 1)`
  - **UNTRIAGED** — a defect until triaged
- `success` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `success` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `warning` root.background-color (background-color) {color-background-orange}: expected `rgba(242, 121, 2, 0.2)` observed `rgba(233, 175, 8, 1)`
  - **UNTRIAGED** — a defect until triaged
- `warning` root.color (color) {color-text-orange}: expected `rgba(107, 34, 3, 1)` observed `rgba(10, 19, 23, 1)`
  - **UNTRIAGED** — a defect until triaged
- `warning` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `warning` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `error` root.background-color (background-color) {color-background-red}: expected `rgba(227, 25, 59, 0.2)` observed `rgba(227, 25, 59, 1)`
  - **UNTRIAGED** — a defect until triaged
- `error` root.color (color) {color-text-red}: expected `rgba(123, 2, 16, 1)` observed `rgba(255, 255, 255, 1)`
  - **UNTRIAGED** — a defect until triaged
- `error` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `error` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `blue` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `blue` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `cyan` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `cyan` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `green` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `green` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `orange` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `orange` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `pink` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `pink` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `purple` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `purple` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `red` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `red` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `teal` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `teal` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged
- `yellow` root.font-family (font-family) {font-family-body}: expected `-apple-system, "system-ui", "Segoe UI", Roboto, Helvetica, Arial, sans-serif` observed `Times`
  - **UNTRIAGED** — a defect until triaged
- `yellow` root.line-height (line-height) {imported.badge.root.line-height}: expected `24px` observed `20.0004px`
  - **UNTRIAGED** — a defect until triaged

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **14** · bindings: 1 base + 0 state
- shape: 0 uniform · 1 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **0**
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **0**
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **84.392%** (1276/1512 cells; 0/56 combo×state rows fully equal)
- pixel: 0/56 pairs perfect at threshold 0 · 0/56 at the AA point (mean AA 1.975%, max 2.497%)

