# Delta ledger — computed floor × static layer (TextInput)

Generated by extract/computed/run.ts against flowbite-react@0.12.17 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component TextInput`.

- captures: **60** (15 combos, full-cartesian × 4 interactions), channels enumerated per element: **567** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|` (matched) → **part-0** `div|` (computed-only) → **part-0-0** `input|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **3** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**
- root-display-carried: flex
- full-width-carried: part-0 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::placeholder (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (part-0-0); -webkit-text-stroke-color (part-0-0)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **133** → WITH folding: **110** (5 folded channels; every fold a named receipt).

- part-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **110** · bindings: 27 base + 14 state
- shape: 18 uniform · 23 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **9**
  - part-0.position = `relative`
  - part-0-0.appearance = `auto`
  - part-0-0.border-bottom-style = `solid`
  - part-0-0.border-left-style = `solid`
  - part-0-0.border-right-style = `solid`
  - part-0-0.border-top-style = `solid`
  - part-0-0.cursor = `text`
  - part-0-0.overflow-x = `clip`
  - part-0-0.overflow-y = `clip`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **0**
- state channels outside mintable kinds: **4**
  - [active] part-0-0.--tw-outline-style — CSS custom property, not a styled channel — the same door the base plane applies: a custom property is a token DECLARATION the library sets on this element (its plumbing), never a rendered fact; the rendered state delta rides the consuming channel (sample: `none`)
  - [active] part-0-0.--tw-ring-shadow — CSS custom property, not a styled channel — the same door the base plane applies: a custom property is a token DECLARATION the library sets on this element (its plumbing), never a rendered fact; the rendered state delta rides the consuming channel (sample: `0 0 0 calc(1px + 0px) oklch(71.5% 0.143 215.221)`)
  - [focus-visible] part-0-0.--tw-outline-style — CSS custom property, not a styled channel — the same door the base plane applies: a custom property is a token DECLARATION the library sets on this element (its plumbing), never a rendered fact; the rendered state delta rides the consuming channel (sample: `none`)
  - [focus-visible] part-0-0.--tw-ring-shadow — CSS custom property, not a styled channel — the same door the base plane applies: a custom property is a token DECLARATION the library sets on this element (its plumbing), never a rendered fact; the rendered state delta rides the consuming channel (sample: `0 0 0 calc(1px + 0px) oklch(71.5% 0.143 215.221)`)
- refused/overflow bindings: **14**
  - {"part":"part-0-0","channel":"border-bottom-color","state":"active","ref":"{imported.text-input.part-0-0.border-bottom-color-state-active.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"border-left-color","state":"active","ref":"{imported.text-input.part-0-0.border-left-color-state-active.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"border-right-color","state":"active","ref":"{imported.text-input.part-0-0.border-right-color-state-active.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"border-top-color","state":"active","ref":"{imported.text-input.part-0-0.border-top-color-state-active.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"box-shadow","state":"active","ref":"{imported.text-input.part-0-0.box-shadow-state-active.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"outline-color","state":"active","ref":"{imported.text-input.part-0-0.outline-color-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"outline-width","state":"active","ref":"{imported.text-input.part-0-0.outline-width-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"border-bottom-color","state":"focus-visible","ref":"{imported.text-input.part-0-0.border-bottom-color-state-focus-visible.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"border-left-color","state":"focus-visible","ref":"{imported.text-input.part-0-0.border-left-color-state-focus-visible.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"border-right-color","state":"focus-visible","ref":"{imported.text-input.part-0-0.border-right-color-state-focus-visible.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"border-top-color","state":"focus-visible","ref":"{imported.text-input.part-0-0.border-top-color-state-focus-visible.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"box-shadow","state":"focus-visible","ref":"{imported.text-input.part-0-0.box-shadow-state-focus-visible.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"outline-color","state":"focus-visible","ref":"{imported.text-input.part-0-0.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0","channel":"outline-width","state":"focus-visible","ref":"{imported.text-input.part-0-0.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **92.381%** (2328/2520 cells; 24/60 combo×state rows fully equal)
- pixel: 24/60 pairs perfect at threshold 0 · 30/60 at the AA point (mean AA 0.798%, max 3.184%; 60/60 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

