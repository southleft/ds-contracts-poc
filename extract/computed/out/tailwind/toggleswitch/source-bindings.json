{
  "_marker": "EMOTION/CSS-VARS READER — SOURCE facts: the library's own emitted CSS named these tokens (var references verified against captured computed values, one indirection hop followed). The promote step aliases matching minted leaves; never auto-applied here.",
  "component": "ToggleSwitch",
  "varPrefix": "--",
  "facts": [
    {
      "part": "label",
      "channel": "font-size",
      "combo": "sm.unchecked",
      "axisValues": {
        "sizing": "sm",
        "checked": "unchecked"
      },
      "token": "text-sm",
      "varName": "--text-sm",
      "anchor": {
        "selector": ".text-sm"
      }
    },
    {
      "part": "label",
      "channel": "font-size",
      "combo": "sm.checked",
      "axisValues": {
        "sizing": "sm",
        "checked": "checked"
      },
      "token": "text-sm",
      "varName": "--text-sm",
      "anchor": {
        "selector": ".text-sm"
      }
    },
    {
      "part": "label",
      "channel": "font-size",
      "combo": "md.unchecked",
      "axisValues": {
        "sizing": "md",
        "checked": "unchecked"
      },
      "token": "text-sm",
      "varName": "--text-sm",
      "anchor": {
        "selector": ".text-sm"
      }
    },
    {
      "part": "label",
      "channel": "font-size",
      "combo": "md.checked",
      "axisValues": {
        "sizing": "md",
        "checked": "checked"
      },
      "token": "text-sm",
      "varName": "--text-sm",
      "anchor": {
        "selector": ".text-sm"
      }
    },
    {
      "part": "label",
      "channel": "font-size",
      "combo": "lg.unchecked",
      "axisValues": {
        "sizing": "lg",
        "checked": "unchecked"
      },
      "token": "text-sm",
      "varName": "--text-sm",
      "anchor": {
        "selector": ".text-sm"
      }
    },
    {
      "part": "label",
      "channel": "font-size",
      "combo": "lg.checked",
      "axisValues": {
        "sizing": "lg",
        "checked": "checked"
      },
      "token": "text-sm",
      "varName": "--text-sm",
      "anchor": {
        "selector": ".text-sm"
      }
    },
    {
      "part": "label",
      "channel": "font-weight",
      "combo": "sm.unchecked",
      "axisValues": {
        "sizing": "sm",
        "checked": "unchecked"
      },
      "token": "font-weight-medium",
      "varName": "--font-weight-medium",
      "anchor": {
        "selector": ".font-medium"
      }
    },
    {
      "part": "label",
      "channel": "font-weight",
      "combo": "sm.checked",
      "axisValues": {
        "sizing": "sm",
        "checked": "checked"
      },
      "token": "font-weight-medium",
      "varName": "--font-weight-medium",
      "anchor": {
        "selector": ".font-medium"
      }
    },
    {
      "part": "label",
      "channel": "font-weight",
      "combo": "md.unchecked",
      "axisValues": {
        "sizing": "md",
        "checked": "unchecked"
      },
      "token": "font-weight-medium",
      "varName": "--font-weight-medium",
      "anchor": {
        "selector": ".font-medium"
      }
    },
    {
      "part": "label",
      "channel": "font-weight",
      "combo": "md.checked",
      "axisValues": {
        "sizing": "md",
        "checked": "checked"
      },
      "token": "font-weight-medium",
      "varName": "--font-weight-medium",
      "anchor": {
        "selector": ".font-medium"
      }
    },
    {
      "part": "label",
      "channel": "font-weight",
      "combo": "lg.unchecked",
      "axisValues": {
        "sizing": "lg",
        "checked": "unchecked"
      },
      "token": "font-weight-medium",
      "varName": "--font-weight-medium",
      "anchor": {
        "selector": ".font-medium"
      }
    },
    {
      "part": "label",
      "channel": "font-weight",
      "combo": "lg.checked",
      "axisValues": {
        "sizing": "lg",
        "checked": "checked"
      },
      "token": "font-weight-medium",
      "varName": "--font-weight-medium",
      "anchor": {
        "selector": ".font-medium"
      }
    }
  ],
  "skips": [
    "label.line-height: candidate --text-sm--line-height → \"text-sm--line-height\" = calc(1.25 / 0.875) names a DTCG leaf but the resolved value does NOT equal the captured computed value in any combo — the cascade approximation suggested a token the element did not render; binding dropped by name",
    "label.margin-inline-start: source declares `calc(var(--spacing) * 3)` — a calc() over var(--spacing). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost.",
    "label.margin-top: source declares `calc(var(--spacing) * 0.5)` — a calc() over var(--spacing). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost.",
    "part-0.height: source declares `calc(var(--spacing) * 7)` — a calc() over var(--spacing). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost.",
    "part-0.min-width: source declares `calc(var(--spacing) * 11)` — a calc() over var(--spacing). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost.",
    "part-0.width: source declares `calc(var(--spacing) * 11)` — a calc() over var(--spacing). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost."
  ],
  "shorthandCeiling": 1,
  "shorthandSkips": [
    "root.border-radius: SHORTHAND carrying var(--radius-lg) — computed style enumerates LONGHANDS only, so a shorthand with a var() (a CSS pending-substitution value) has no computed value to verify against; the token this declaration names is NOT carried on any longhand it sets (the shorthand ceiling, task #27)"
  ],
  "calcDeclarations": 5,
  "calcCeiling": 5
}
