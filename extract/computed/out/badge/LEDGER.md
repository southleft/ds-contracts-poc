# Delta ledger — computed floor × static layer (Badge)

Generated by extract/computed/run.ts against @shopify/polaris@13.9.5 in Chromium 148.0.7778.96. Regenerate: `npm run extract:computed -- --harness <dir> --component Badge`.

- captures: **240** (60 combos, full-cartesian × 4 interactions), channels enumerated per element: **942** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `span|Badge` (matched) → **label-2** `span|Text|sr-only` (computed-only) → **icon** `span|Badge__Icon` (computed-only) → **icon-2** `span|Icon` (computed-only) → **label-3** `span|Text|sr-only` (computed-only) → **icon-3** `svg|` (computed-only) → **part-0-0-1-0** `path|` (computed-only) → **label** `span|Text` (matched)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **7** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **3** · named refusals: **0**
- asset: `badge-icon-3-complete.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `badge-icon-3-incomplete.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `badge-icon-3-partiallycomplete.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- svg-viewbox-reconstructed: Badge.icon-3@none.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@none.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@none.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@info.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@info.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@info.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@success.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@success.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@success.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@warning.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@warning.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@warning.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@critical.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@critical.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@critical.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@attention.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@attention.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@attention.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@new.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@new.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@new.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@magic.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@magic.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@magic.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@info-strong.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@info-strong.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@info-strong.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@success-strong.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@success-strong.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@success-strong.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@warning-strong.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@warning-strong.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@warning-strong.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@critical-strong.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@critical-strong.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@critical-strong.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@attention-strong.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@attention-strong.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@attention-strong.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@read-only.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@read-only.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@read-only.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@enabled.incomplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@enabled.partiallyComplete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Badge.icon-3@enabled.complete — 0 0 20 20 from computed size 20×20 (path extent 14.0; viewBox is not a computed style — named reconstruction)
- root-display-carried: flex (overrides reviewed "inline-flex" — computed truth wins display, receipted)
- literal-text-carried: label-2 = "Info" (no text prop sample matches — the mounted value is carried verbatim)
- sr-only-carried-as-hidden: label-2 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- presence-carried: icon (45/60 combos) →  base-hidden, shown-when progress=incomplete, progress=partiallyComplete, progress=complete
- presence-carried: icon-2 (45/60 combos) →  base-hidden, shown-when progress=incomplete, progress=partiallyComplete, progress=complete
- literal-text-carried: label-3 = "Incomplete" (no text prop sample matches — the mounted value is carried verbatim)
- sr-only-carried-as-hidden: label-3 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- presence-carried: icon-3 (45/60 combos) →  base-hidden, shown-when progress=incomplete, progress=partiallyComplete, progress=complete

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **1033** → WITH folding: **223** (72 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

300/300 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **223** · bindings: 40 base + 0 state
- shape: 24 uniform · 14 per-axis · 0 per-axis-pair · 2 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **12**
  - label-2.overflow-x = `hidden`
  - label-2.overflow-y = `hidden`
  - label-2.position = `absolute`
  - label-2.text-wrap-mode = `nowrap`
  - icon-2.max-height = `100%`
  - icon-2.max-width = `100%`
  - label-3.overflow-x = `hidden`
  - label-3.overflow-y = `hidden`
  - label-3.position = `absolute`
  - label-3.text-wrap-mode = `nowrap`
  - icon-3.overflow-x = `hidden`
  - icon-3.overflow-y = `hidden`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **4**
  - label-2.clip-path — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `inset(50%)`, 1 distinct value(s))
  - label-3.clip-path — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `inset(50%)`, 1 distinct value(s))
  - icon-3.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `content-box`, 1 distinct value(s))
  - icon-3.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `top`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **2**
  - {"part":"label-2","channel":"color","refusal":"resolved values differ across variants without correlating to any variant axis — nothing minted; bind manually"}
  - {"part":"label-2","channel":"font-weight","refusal":"resolved values differ across variants without correlating to any variant axis — nothing minted; bind manually"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **97.327%** (21552/22144 cells; 8/240 combo×state rows fully equal)
- pixel: 0/240 pairs perfect at threshold 0 · 0/240 at the AA point (mean AA 1.523%, max 2.808%)

