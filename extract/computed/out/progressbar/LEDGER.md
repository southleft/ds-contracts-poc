# Delta ledger — computed floor × static layer (ProgressBar)

Generated by extract/computed/run.ts against @shopify/polaris@13.9.5 in Chromium 148.0.7778.96. Regenerate: `npm run extract:computed -- --harness <dir> --component ProgressBar`.

- captures: **48** (12 combos, full-cartesian × 4 interactions), channels enumerated per element: **942** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|ProgressBar` (matched) → **progress** `progress|ProgressBar__Progress` (computed-only) → **indicator** `div|ProgressBar__Indicator.ProgressBar__IndicatorAppearDone` (matched) → **label** `span|ProgressBar__Label` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **10** → WITH folding: **10** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

36/36 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **10** · bindings: 18 base + 0 state
- shape: 17 uniform · 1 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **5**
  - root.overflow-x = `hidden`
  - root.overflow-y = `hidden`
  - indicator.transition-duration = `0.5s`
  - indicator.transition-property = `transform`
  - indicator.transition-timing-function = `cubic-bezier(0.25, 0.1, 0.25, 1)`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **12**
  - progress.appearance — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `auto`, 1 distinct value(s))
  - progress.clip-path — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `inset(50%)`, 1 distinct value(s))
  - progress.overflow-x — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `hidden`, 1 distinct value(s))
  - progress.overflow-y — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `hidden`, 1 distinct value(s))
  - progress.position — declared-channel value outside the bounded grammar — named residue (v15) (sample: `absolute`, 1 distinct value(s))
  - progress.text-wrap-mode — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `nowrap`, 1 distinct value(s))
  - indicator.transform — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `matrix(0.25, 0, 0, 1, 0, 0)`, 1 distinct value(s))
  - label.clip-path — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `inset(50%)`, 1 distinct value(s))
  - label.overflow-x — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `hidden`, 1 distinct value(s))
  - label.overflow-y — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `hidden`, 1 distinct value(s))
  - label.position — declared-channel value outside the bounded grammar — named residue (v15) (sample: `absolute`, 1 distinct value(s))
  - label.text-wrap-mode — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `nowrap`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **13**
  - {"part":"progress","channel":"bottom","ref":"{imported.progress-bar.progress.bottom}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"progress","channel":"left","ref":"{imported.progress-bar.progress.left}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"progress","channel":"min-height","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"progress","channel":"min-width","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"progress","channel":"right","ref":"{imported.progress-bar.progress.right}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"progress","channel":"top","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"progress","channel":"vertical-align","ref":"{imported.progress-bar.progress.vertical-align}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"label","channel":"bottom","ref":"{imported.progress-bar.label.bottom.{size}}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"label","channel":"left","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"label","channel":"min-height","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"label","channel":"min-width","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"label","channel":"right","ref":"{imported.progress-bar.label.right}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"label","channel":"top","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **92.857%** (624/672 cells; 0/48 combo×state rows fully equal)
- pixel: 0/48 pairs perfect at threshold 0 · 0/48 at the AA point (mean AA 17.438%, max 29.938%)

