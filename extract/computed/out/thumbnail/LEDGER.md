# Delta ledger — computed floor × static layer (Thumbnail)

Generated by extract/computed/run.ts against @shopify/polaris@13.9.5 in Chromium 148.0.7778.96. Regenerate: `npm run extract:computed -- --harness <dir> --component Thumbnail`.

- captures: **16** (4 combos, full-cartesian × 4 interactions), channels enumerated per element: **942** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `span|Thumbnail` (matched) → **part-0** `img|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **14** → WITH folding: **14** (9 folded channels; every fold a named receipt).

- part-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

8/8 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **14** · bindings: 14 base + 0 state
- shape: 11 uniform · 3 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **4**
  - root.max-width = `100%`
  - root.overflow-x = `hidden`
  - root.overflow-y = `hidden`
  - root.position = `relative`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **6**
  - part-0.max-height — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `100%`, 1 distinct value(s))
  - part-0.max-width — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `100%`, 1 distinct value(s))
  - part-0.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `content-box`, 1 distinct value(s))
  - part-0.overflow-x — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `clip`, 1 distinct value(s))
  - part-0.overflow-y — declared channel on a computed-only (or ref/slot) part — adding parts is a curation decision, not a capture decision (sample: `clip`, 1 distinct value(s))
  - part-0.position — declared-channel value outside the bounded grammar — named residue (v15) (sample: `absolute`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **7**
  - {"part":"part-0","channel":"bottom","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"part-0","channel":"color","ref":"{imported.thumbnail.part-0.color}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"part-0","channel":"left","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"part-0","channel":"min-height","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"part-0","channel":"min-width","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"part-0","channel":"right","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}
  - {"part":"part-0","channel":"top","ref":"{imported.shared.size-0}","refusal":"computed-only part not present in the committed anatomy — adding parts is a curation decision, not a capture decision"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **100.000%** (224/224 cells; 16/16 combo×state rows fully equal)
- pixel: 0/16 pairs perfect at threshold 0 · 0/16 at the AA point (mean AA 9.244%, max 19.921%)

