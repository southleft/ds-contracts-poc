# Delta ledger — computed floor × static layer (Spinner)

Generated by extract/computed/run.ts against @shopify/polaris@13.9.5 in Chromium 148.0.7778.96. Regenerate: `npm run extract:computed -- --harness <dir> --component Spinner`.

- captures: **8** (2 combos, full-cartesian × 4 interactions), channels enumerated per element: **942** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `span|Spinner` (matched) → **icon** `svg|` (computed-only) → **part-0-0** `path|` (computed-only) · static-part-unrendered: glyphLarge, glyphSmall
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **1** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **2** · named refusals: **2**
- asset: `spinner-root-large.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `spinner-root-small.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- svg-viewbox-reconstructed: Spinner.root@small — 0 0 20 20 from computed size 20×20 (path extent 18.9; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Spinner.root@large — 0 0 44 44 from computed size 44×44 (path extent 43.5; viewBox is not a computed style — named reconstruction)
- **REFUSED**: static-part-unrendered: reviewed static part "glyphLarge" has no rendered counterpart in ANY captured combo — dropped from the promoted anatomy (named; usually a conditional the sweep never triggered)
- **REFUSED**: static-part-unrendered: reviewed static part "glyphSmall" has no rendered counterpart in ANY captured combo — dropped from the promoted anatomy (named; usually a conditional the sweep never triggered)

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **0** → WITH folding: **0** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **0** · bindings: 0 base + 0 state
- shape: 0 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **0**
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **0**
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **100.000%** (0/0 cells; 0/8 combo×state rows fully equal)
- pixel: 0/8 pairs perfect at threshold 0 · 0/8 at the AA point (mean AA 0.760%, max 1.035%)

