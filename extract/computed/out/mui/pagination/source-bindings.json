{
  "_marker": "EMOTION/CSS-VARS READER — SOURCE facts: the library's own emitted CSS named these tokens (var references verified against captured computed values, one indirection hop followed). The promote step aliases matching minted leaves; never auto-applied here.",
  "component": "Pagination",
  "varPrefix": "--mui-",
  "facts": [
    {
      "part": "buttonbase-root",
      "channel": "color",
      "combo": "",
      "axisValues": {},
      "token": "palette-action-disabled",
      "varName": "--mui-palette-action-disabled",
      "anchor": {
        "selector": ".css-1hezwcy-MuiButtonBase-root-MuiIconButton-root.Mui-disabled"
      }
    },
    {
      "part": "buttonbase-root-2",
      "channel": "color",
      "combo": "",
      "axisValues": {},
      "token": "palette-action-disabled",
      "varName": "--mui-palette-action-disabled",
      "anchor": {
        "selector": ".css-1hezwcy-MuiButtonBase-root-MuiIconButton-root.Mui-disabled"
      }
    },
    {
      "part": "icon",
      "channel": "color",
      "combo": "",
      "axisValues": {},
      "token": "palette-action-active",
      "varName": "--mui-palette-action-active",
      "anchor": {
        "selector": ".css-c4hhgn-MuiSvgIcon-root-MuiNativeSelect-root-MuiSelect-icon"
      }
    },
    {
      "part": "root",
      "channel": "color",
      "combo": "",
      "axisValues": {},
      "token": "palette-text-primary",
      "varName": "--mui-palette-text-primary",
      "anchor": {
        "selector": ".css-1rvaqho-MuiTablePagination-root"
      }
    }
  ],
  "skips": [
    "tablepagination-toolbar.padding-left: source declares `calc(3 * var(--mui-spacing))` — a calc() over var(--mui-spacing). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost.",
    "tablepagination-toolbar.padding-right: source declares `calc(3 * var(--mui-spacing))` — a calc() over var(--mui-spacing). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost.",
    "buttonbase-root-2.--IconButton-hoverBg: source declares `rgba(var(--mui-palette-action-activeChannel) / calc(var(--mui-palette-action-hoverOpacity)))` — a calc() over var(--mui-palette-action-activeChannel, --mui-palette-action-hoverOpacity). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost.",
    "buttonbase-root.--IconButton-hoverBg: source declares `rgba(var(--mui-palette-action-activeChannel) / calc(var(--mui-palette-action-hoverOpacity)))` — a calc() over var(--mui-palette-action-activeChannel, --mui-palette-action-hoverOpacity). The computed value is the RESULT of the arithmetic, not the token's own value, so no candidate can verify by value equality and the token NAME is not carried (the calc ceiling). The resolved pixel is correct; only the name is lost."
  ],
  "shorthandCeiling": 0,
  "shorthandSkips": [],
  "calcDeclarations": 4,
  "calcCeiling": 4
}
