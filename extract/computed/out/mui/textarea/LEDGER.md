# Delta ledger — computed floor × static layer (Textarea)

Generated by extract/computed/run.ts against @mui/material@9.2.0 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Textarea`.

- captures: **16** (4 combos, full-cartesian × 4 interactions), channels enumerated per element: **675** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|FormControl-root.TextField-root` (matched) → **label-3** `label|FormLabel-root.InputLabel-animated.InputLabel-formControl.InputLabel-root.InputLabel-shrink` (computed-only) → **label** `label|FormLabel-root.InputLabel-animated.InputLabel-formControl.InputLabel-root` (computed-only) → **inputbase-formcontrol** `div|InputBase-formControl.InputBase-multiline.InputBase-root.OutlinedInput-root` (computed-only) → **inputbase-input** `textarea|InputBase-input.OutlinedInput-input` (computed-only) → **inputbase-input-2** `textarea|InputBase-input.OutlinedInput-input` (computed-only) → **outlinedinput-notchedoutline** `fieldset|OutlinedInput-notchedOutline` (computed-only) → **part-1-2-0** `legend|` (computed-only) → **label-2** `span||sr-only` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **6** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **2**
- root-display-carried: inline-flex
- presence-carried: label-3 (1/2 combos) →  hidden-when content=empty, disabled
- presence-carried: label (1/2 combos) →  hidden-when content=value, disabled
- placeholder-attr-mounted: inputbase-input — the capture MOUNTED placeholder="Add a note" (fixedProps) and a computed-style capture reads no attributes, so the mounted string is carried as the contract's literal `attrs.placeholder`. It is the string that was on screen when this element's ::placeholder ink was measured; without it the canvas draws an EMPTY text node and the field reads as nothing at all.
- full-width-carried: inputbase-input spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- sr-only-carried-as-hidden: label-2 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- **REFUSED**: non-painting-part: inputbase-input-2 <textarea> renders NO INK in any combo it appears in (visibility: hidden) and no descendant paints — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)
- **REFUSED**: inert-overlay-wrapper: part-1-2-0 draws no box in any captured combo, carries no text/icon/shape of its own, and every one of its 1 child part(s) is declared display:none — a carried part that draws NOTHING; refused by name (Carbon's tooltip `popover` wrapper; census sibling of the portal round's stripInertPortalChildren)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::placeholder (2 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 5 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-box-align (inputbase-formcontrol); -webkit-font-smoothing (inputbase-formcontrol, inputbase-input, inputbase-input-2, label, label-2, label-3, outlinedinput-notchedoutline, part-1-2-0, root); -webkit-tap-highlight-color (inputbase-input, inputbase-input-2); -webkit-text-fill-color (inputbase-formcontrol, inputbase-input, inputbase-input-2, label, label-2, label-3, outlinedinput-notchedoutline, part-1-2-0, root); -webkit-text-stroke-color (inputbase-formcontrol, inputbase-input, inputbase-input-2, label, label-2, label-3, outlinedinput-notchedoutline, part-1-2-0, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **40** → WITH folding: **44** (86 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-formcontrol.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- inputbase-input-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- outlinedinput-notchedoutline.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- outlinedinput-notchedoutline.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- outlinedinput-notchedoutline.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- outlinedinput-notchedoutline.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- outlinedinput-notchedoutline.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- outlinedinput-notchedoutline.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-2-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [active] label-3.border-bottom-color rides its source fact
- fold-carries-state-delta: [active] label-3.border-left-color rides its source fact
- fold-carries-state-delta: [active] label-3.border-right-color rides its source fact
- fold-carries-state-delta: [active] label-3.border-top-color rides its source fact
- fold-carries-state-delta: [active] label-3.caret-color rides its source fact
- fold-carries-state-delta: [active] label-3.column-rule-color rides its source fact
- fold-carries-state-delta: [active] label-3.outline-color rides its source fact
- fold-carries-state-delta: [active] label-3.row-rule-color rides its source fact
- fold-carries-state-delta: [active] label-3.text-decoration-color rides its source fact
- fold-carries-state-delta: [active] label-3.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.caret-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.outline-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-formcontrol.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.caret-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.outline-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] inputbase-input.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.caret-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.outline-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] label-3.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] label.caret-color rides its source fact
- fold-carries-state-delta: [disabled] label.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label.outline-color rides its source fact
- fold-carries-state-delta: [disabled] label.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] label.text-emphasis-color rides its source fact
- fold-carries-state-delta: [disabled] outlinedinput-notchedoutline.caret-color rides its source fact
- fold-carries-state-delta: [disabled] outlinedinput-notchedoutline.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] outlinedinput-notchedoutline.outline-color rides its source fact
- fold-carries-state-delta: [disabled] outlinedinput-notchedoutline.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] outlinedinput-notchedoutline.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] outlinedinput-notchedoutline.text-emphasis-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.border-bottom-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.border-left-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.border-right-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.border-top-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.caret-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.column-rule-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.outline-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.row-rule-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.text-decoration-color rides its source fact
- fold-carries-state-delta: [focus-visible] label-3.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **44** · bindings: 88 base + 29 state
- shape: 113 uniform · 4 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **48**
  - root.box-sizing = `border-box`
  - root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - root.position = `relative`
  - label-3.box-sizing = `border-box`
  - label-3.cursor = `default`
  - label-3.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-3.overflow-x = `hidden`
  - label-3.overflow-y = `hidden`
  - label-3.text-overflow = `ellipsis`
  - label-3.text-wrap-mode = `nowrap`
  - label-3.transition-delay = `0s, 0s, 0s`
  - label-3.transition-duration = `0.2s, 0.2s, 0.2s`
  - label-3.transition-property = `color, transform, max-width`
  - label-3.transition-timing-function = `cubic-bezier(0, 0, 0.2, 1), cubic-bezier(0, 0, 0.2, 1), cubic-bezier(0, 0, 0.2, 1)`
  - label-3.user-select = `none`
  - label.box-sizing = `border-box`
  - label.cursor = `default`
  - label.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label.overflow-x = `hidden`
  - label.overflow-y = `hidden`
  - label.pointer-events = `none`
  - label.text-overflow = `ellipsis`
  - label.text-wrap-mode = `nowrap`
  - label.transform = `matrix(1, 0, 0, 1, 14, 16)`
  - label.transition-delay = `0s, 0s, 0s`
  - label.transition-duration = `0.2s, 0.2s, 0.2s`
  - label.transition-property = `color, transform, max-width`
  - label.transition-timing-function = `cubic-bezier(0, 0, 0.2, 1), cubic-bezier(0, 0, 0.2, 1), cubic-bezier(0, 0, 0.2, 1)`
  - inputbase-formcontrol.box-sizing = `border-box`
  - inputbase-formcontrol.cursor = `text`
  - inputbase-formcontrol.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - inputbase-formcontrol.position = `relative`
  - inputbase-input.appearance = `auto`
  - inputbase-input.cursor = `text`
  - inputbase-input.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - inputbase-input.overflow-x = `hidden`
  - inputbase-input.overflow-y = `hidden`
  - outlinedinput-notchedoutline.border-bottom-style = `solid`
  - outlinedinput-notchedoutline.border-left-style = `solid`
  - outlinedinput-notchedoutline.border-right-style = `solid`
  - outlinedinput-notchedoutline.border-top-style = `solid`
  - outlinedinput-notchedoutline.box-sizing = `border-box`
  - outlinedinput-notchedoutline.cursor = `text`
  - outlinedinput-notchedoutline.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - outlinedinput-notchedoutline.overflow-x = `hidden`
  - outlinedinput-notchedoutline.overflow-y = `hidden`
  - outlinedinput-notchedoutline.pointer-events = `none`
  - outlinedinput-notchedoutline.text-align = `left`
- state declared facts: **3**
  - [disabled] inputbase-formcontrol.cursor = `default`
  - [disabled] inputbase-input.cursor = `default`
  - [disabled] outlinedinput-notchedoutline.cursor = `default`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **17**
  - root.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - root.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `top`, 1 distinct value(s))
  - label-3.max-width — declared-channel value outside the bounded grammar — named residue (v15) (sample: `calc(133% - 32px)`, 1 distinct value(s))
  - label-3.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <label>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <label> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `100%`, 1 distinct value(s))
  - label-3.transform — declared-channel value outside the bounded grammar — named residue (v15) (sample: `matrix(0.75, 0, 0, 0.75, 14, -9)`, 1 distinct value(s))
  - label-3.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <label>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <label> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal, normal, normal`, 1 distinct value(s))
  - label.max-width — declared-channel value outside the bounded grammar — named residue (v15) (sample: `calc(100% - 24px)`, 1 distinct value(s))
  - label.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <label>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <label> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `100%`, 1 distinct value(s))
  - label.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <label>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <label> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal, normal, normal`, 1 distinct value(s))
  - inputbase-formcontrol.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - inputbase-input.animation-duration — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <textarea>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <textarea> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `0.01s`, 1 distinct value(s))
  - inputbase-input.animation-name — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <textarea>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <textarea> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `mui-auto-fill-cancel`, 1 distinct value(s))
  - inputbase-input.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <textarea>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <textarea> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `break-word`, 1 distinct value(s))
  - inputbase-input.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <textarea>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <textarea> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `100%`, 1 distinct value(s))
  - inputbase-input.white-space-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <textarea>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <textarea> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `preserve`, 1 distinct value(s))
  - outlinedinput-notchedoutline.min-width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <fieldset>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <fieldset> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `0%`, 1 distinct value(s))
  - outlinedinput-notchedoutline.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <fieldset>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <fieldset> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `100%`, 1 distinct value(s))
- state channels outside mintable kinds: **4**
  - [active] label-3.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(25, 118, 210, 1)`)
  - [disabled] label-3.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(0, 0, 0, 0.38)`)
  - [disabled] label.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(0, 0, 0, 0.38)`)
  - [focus-visible] label-3.color — default-state values not kind-compatible for padding — cannot correlate (sample: `rgba(25, 118, 210, 1)`)
- refused/overflow bindings: **26**
  - {"part":"inputbase-input","channel":"outline-width","state":"active","ref":"{imported.textarea.inputbase-input.outline-width-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-bottom-color","state":"active","ref":"{imported.shared.color-1976d2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-bottom-width","state":"active","ref":"{imported.shared.size-2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-left-color","state":"active","ref":"{imported.shared.color-1976d2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-left-width","state":"active","ref":"{imported.shared.size-2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-right-color","state":"active","ref":"{imported.shared.color-1976d2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-right-width","state":"active","ref":"{imported.shared.size-2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-top-color","state":"active","ref":"{imported.shared.color-1976d2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-top-width","state":"active","ref":"{imported.shared.size-2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-bottom-color","state":"disabled","ref":"{imported.shared.color-00000042}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-left-color","state":"disabled","ref":"{imported.shared.color-00000042}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-right-color","state":"disabled","ref":"{imported.shared.color-00000042}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-top-color","state":"disabled","ref":"{imported.shared.color-00000042}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"inputbase-input","channel":"outline-width","state":"focus-visible","ref":"{imported.textarea.inputbase-input.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-bottom-color","state":"focus-visible","ref":"{imported.shared.color-1976d2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-bottom-width","state":"focus-visible","ref":"{imported.shared.size-2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-left-color","state":"focus-visible","ref":"{imported.shared.color-1976d2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-left-width","state":"focus-visible","ref":"{imported.shared.size-2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-right-color","state":"focus-visible","ref":"{imported.shared.color-1976d2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-right-width","state":"focus-visible","ref":"{imported.shared.size-2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-top-color","state":"focus-visible","ref":"{imported.shared.color-1976d2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-top-width","state":"focus-visible","ref":"{imported.shared.size-2}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-bottom-color","state":"hover","ref":"{imported.shared.color-000000de}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-left-color","state":"hover","ref":"{imported.shared.color-000000de}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-right-color","state":"hover","ref":"{imported.shared.color-000000de}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"outlinedinput-notchedoutline","channel":"border-top-color","state":"hover","ref":"{imported.shared.color-000000de}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **74.540%** (1944/2608 cells; 0/16 combo×state rows fully equal)
- pixel: 0/16 pairs perfect at threshold 0 · 0/16 at the AA point (mean AA 2.242%, max 3.813%; 16/16 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

