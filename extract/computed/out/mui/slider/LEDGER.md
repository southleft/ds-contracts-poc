# Delta ledger — computed floor × static layer (Slider)

Generated by extract/computed/run.ts against @mui/material@9.2.0 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Slider`.

- captures: **48** (12 combos, full-cartesian × 4 interactions), channels enumerated per element: **675** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `span|Slider-root` (matched) → **slider-rail** `span|Slider-rail` (computed-only) → **slider-track** `span|Slider-track` (computed-only) → **slider-thumb** `span|Slider-thumb` (computed-only) → **part-2-0** `input|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **5** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**
- aspect-carried: slider-thumb computed width == height in every enabled combo (12/20px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- aspect-carried: part-2-0 computed width == height in every enabled combo (12/20px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- full-width-carried: part-2-0 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **331** → WITH folding: **116** (44 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-rail.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-track.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- slider-thumb.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-2-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **116** · bindings: 68 base + 5 state
- shape: 48 uniform · 25 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **20**
  - root.cursor = `pointer`
  - root.position = `relative`
  - root.touch-action = `none`
  - slider-rail.cursor = `pointer`
  - slider-track.cursor = `pointer`
  - slider-track.transition-delay = `0s, 0s, 0s, 0s`
  - slider-track.transition-duration = `0.15s, 0.15s, 0.15s, 0.15s`
  - slider-track.transition-property = `left, width, bottom, height`
  - slider-track.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1)`
  - slider-thumb.cursor = `pointer`
  - slider-thumb.transition-delay = `0s, 0s, 0s`
  - slider-thumb.transition-duration = `0.15s, 0.15s, 0.15s`
  - slider-thumb.transition-property = `box-shadow, left, bottom`
  - slider-thumb.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1)`
  - part-2-0.appearance = `auto`
  - part-2-0.cursor = `default`
  - part-2-0.font-family = `Arial`
  - part-2-0.overflow-x = `hidden`
  - part-2-0.overflow-y = `hidden`
  - part-2-0.text-wrap-mode = `nowrap`
- state declared facts: **1**
  - [focus-visible] part-2-0.outline-style = `auto`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **15**
  - slider-rail.transform — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `matrix(1, 0, 0, 1, 0, -1)`, 2 distinct value(s))
  - slider-track.border-bottom-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - slider-track.border-left-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - slider-track.border-right-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - slider-track.border-top-style — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `none`, 2 distinct value(s))
  - slider-track.transform — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `matrix(1, 0, 0, 1, 0, -1)`, 2 distinct value(s))
  - slider-track.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal, normal, normal, normal`, 1 distinct value(s))
  - slider-thumb.border-bottom-left-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - slider-thumb.border-bottom-right-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - slider-thumb.border-top-left-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - slider-thumb.border-top-right-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - slider-thumb.box-sizing — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `border-box`, 1 distinct value(s))
  - slider-thumb.transform — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `matrix(1, 0, 0, 1, -6, -6)`, 2 distinct value(s))
  - slider-thumb.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal, normal, normal`, 1 distinct value(s))
  - part-2-0.clip — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `rect(0px, 0px, 0px, 0px)`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **5**
  - {"part":"slider-thumb","channel":"box-shadow","state":"active","ref":"{imported.slider.slider-thumb.box-shadow-state-active.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"slider-thumb","channel":"z-index","state":"active","ref":"{imported.slider.slider-thumb.z-index-state-active}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-2-0","channel":"outline-color","state":"focus-visible","ref":"{imported.slider.part-2-0.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-2-0","channel":"outline-width","state":"focus-visible","ref":"{imported.slider.part-2-0.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"slider-thumb","channel":"box-shadow","state":"focus-visible","ref":"{imported.slider.slider-thumb.box-shadow-state-focus-visible.{color}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **89.448%** (6612/7392 cells; 0/48 combo×state rows fully equal)
- pixel: 0/48 pairs perfect at threshold 0 · 0/48 at the AA point (mean AA 4.592%, max 8.776%)

