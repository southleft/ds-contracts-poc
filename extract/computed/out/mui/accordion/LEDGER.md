# Delta ledger — computed floor × static layer (Accordion)

Generated by extract/computed/run.ts against @mui/material@9.2.0 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Accordion`.

- captures: **32** (8 combos, full-cartesian × 4 interactions), channels enumerated per element: **675** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|Accordion-gutters.Accordion-root.Paper-root` (matched) → **accordion-heading** `h3|Accordion-heading` (computed-only) → **accordionsummary-gutters** `button|AccordionSummary-gutters.AccordionSummary-root.ButtonBase-root` (computed-only) → **label** `span|AccordionSummary-content` (computed-only) → **collapse-root** `div|Collapse-root.Collapse-vertical` (computed-only) → **collapse-vertical** `div|Collapse-vertical.Collapse-wrapper` (computed-only) → **collapse-vertical-2** `div|Collapse-vertical.Collapse-wrapperInner` (computed-only) → **accordion-region** `div|Accordion-region` (computed-only) → **label-2** `div|AccordionDetails-root` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **9** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **1**
- literal-text-carried: label = "Accordion title" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- full-width-carried: collapse-vertical-2 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- literal-text-carried: label-2 = "Details body copy for the molecule round" (no text prop sample matches — the mounted value is carried verbatim)
- **REFUSED**: pseudo-decor-hidden-in-combo: root::before is not drawn in every combo (opacity 0, box nullx1) — the component's own hidden state, NOT a grammar limit

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::before (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 7 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-box-align (accordionsummary-gutters); -webkit-box-flex (label); -webkit-box-pack (accordionsummary-gutters); -webkit-font-smoothing (accordion-heading, accordion-region, accordionsummary-gutters, collapse-root, collapse-vertical, collapse-vertical-2, label, label-2, root); -webkit-tap-highlight-color (accordionsummary-gutters, label); -webkit-text-fill-color (accordion-heading, accordion-region, accordionsummary-gutters, collapse-root, collapse-vertical, collapse-vertical-2, label, label-2, root); -webkit-text-stroke-color (accordion-heading, accordion-region, accordionsummary-gutters, collapse-root, collapse-vertical, collapse-vertical-2, label, label-2, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **60** → WITH folding: **60** (86 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-heading.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordionsummary-gutters.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- collapse-vertical-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- accordion-region.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **60** · bindings: 92 base + 3 state
- shape: 75 uniform · 19 per-axis · 1 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)
- border-style-by-axis-carried: root.border-bottom-style varies by "variant" (elevation=none, outlined=solid) — carried as per-value stylesWhen rules (code: `.variant-<value> { border-bottom-style: … }`; canvas: a dashPattern on that variant's stroke)
- border-style-by-axis-carried: root.border-left-style varies by "variant" (elevation=none, outlined=solid) — carried as per-value stylesWhen rules (code: `.variant-<value> { border-left-style: … }`; canvas: a dashPattern on that variant's stroke)
- border-style-by-axis-carried: root.border-right-style varies by "variant" (elevation=none, outlined=solid) — carried as per-value stylesWhen rules (code: `.variant-<value> { border-right-style: … }`; canvas: a dashPattern on that variant's stroke)
- border-style-by-axis-carried: root.border-top-style varies by "variant" (elevation=none, outlined=solid) — carried as per-value stylesWhen rules (code: `.variant-<value> { border-top-style: … }`; canvas: a dashPattern on that variant's stroke)

## Declared facts (v15 — carried, first-class)

- base declared facts: **43**
  - root.border-bottom-style = `solid`
  - root.border-left-style = `solid`
  - root.border-right-style = `solid`
  - root.border-top-style = `solid`
  - root.box-sizing = `border-box`
  - root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - root.position = `relative`
  - root.transition-duration = `0.15s`
  - root.transition-property = `margin`
  - root.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - accordion-heading.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - accordionsummary-gutters.appearance = `none`
  - accordionsummary-gutters.border-bottom-style = `none`
  - accordionsummary-gutters.border-left-style = `none`
  - accordionsummary-gutters.border-right-style = `none`
  - accordionsummary-gutters.border-top-style = `none`
  - accordionsummary-gutters.cursor = `pointer`
  - accordionsummary-gutters.position = `relative`
  - accordionsummary-gutters.transition-delay = `0s, 0s`
  - accordionsummary-gutters.transition-duration = `0.15s, 0.15s`
  - accordionsummary-gutters.transition-property = `min-height, background-color`
  - accordionsummary-gutters.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1)`
  - accordionsummary-gutters.user-select = `none`
  - label.box-sizing = `border-box`
  - label.cursor = `pointer`
  - label.font-family = `Arial`
  - label.transition-duration = `0.15s`
  - label.transition-property = `margin`
  - label.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - label.user-select = `none`
  - collapse-root.box-sizing = `border-box`
  - collapse-root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - collapse-root.transition-duration = `0.3s`
  - collapse-root.transition-property = `height`
  - collapse-root.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - collapse-vertical.box-sizing = `border-box`
  - collapse-vertical.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - collapse-vertical-2.box-sizing = `border-box`
  - collapse-vertical-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - accordion-region.box-sizing = `border-box`
  - accordion-region.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-2.box-sizing = `border-box`
  - label-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
- state declared facts: **4**
  - [disabled] accordionsummary-gutters.cursor = `default`
  - [disabled] accordionsummary-gutters.pointer-events = `none`
  - [disabled] label.cursor = `default`
  - [disabled] label.pointer-events = `none`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **23**
  - root.overflow-anchor — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - accordion-heading.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <h3>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <h3> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - accordion-heading.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <h3>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <h3> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `100%`, 1 distinct value(s))
  - accordion-heading.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <h3>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <h3> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - accordion-heading.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <h3>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <h3> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - accordionsummary-gutters.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - accordionsummary-gutters.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal, normal`, 1 distinct value(s))
  - accordionsummary-gutters.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - label.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - collapse-root.overflow-x — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `hidden`, 2 distinct value(s))
  - collapse-root.overflow-y — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `hidden`, 2 distinct value(s))
  - collapse-root.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - collapse-root.visibility — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `hidden`, 2 distinct value(s))
  - collapse-vertical.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - collapse-vertical.visibility — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `hidden`, 2 distinct value(s))
  - collapse-vertical-2.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - collapse-vertical-2.visibility — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `hidden`, 2 distinct value(s))
  - accordion-region.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - accordion-region.visibility — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `hidden`, 2 distinct value(s))
  - label-2.text-size-adjust — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `100%`, 1 distinct value(s))
  - label-2.visibility — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `hidden`, 2 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **1**
  - {"part":"accordionsummary-gutters","channel":"opacity","state":"disabled","ref":"{imported.accordion.accordionsummary-gutters.opacity-state-disabled}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **90.943%** (6868/7552 cells; 0/32 combo×state rows fully equal)
- pixel: 0/32 pairs perfect at threshold 0 · 0/32 at the AA point (mean AA 21.512%, max 53.276%; 32/32 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

