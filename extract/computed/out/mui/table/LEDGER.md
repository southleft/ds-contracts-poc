# Delta ledger — computed floor × static layer (Table)

Generated by extract/computed/run.ts against @mui/material@9.2.0 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Table`.

- captures: **8** (2 combos, full-cartesian × 4 interactions), channels enumerated per element: **675** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `table|Table-root` (matched) → **tablehead-root** `thead|TableHead-root` (computed-only) → **tablerow-head** `tr|TableRow-head.TableRow-root` (computed-only) → **tablecell-head** `th|TableCell-head.TableCell-paddingCheckbox.TableCell-root` (computed-only) → **buttonbase-root** `span|ButtonBase-root.Checkbox-indeterminate.Checkbox-root` (computed-only) → **part-0-0-0-0-0** `input||sr-only` (computed-only) → **icon** `svg|SvgIcon-root` (computed-only) → **part-0-0-0-0-1-0** `path|` (computed-only) → **tablecell-head-2** `th|TableCell-head.TableCell-root` (computed-only) → **label** `span|ButtonBase-root.TableSortLabel-root` (computed-only) → **icon-2** `svg|SvgIcon-root.TableSortLabel-icon` (computed-only) → **part-0-0-1-0-0-0** `path|` (computed-only) → **label-2** `th|TableCell-head.TableCell-root` (computed-only) → **label-3** `th|TableCell-head.TableCell-root` (computed-only) → **label-4** `th|TableCell-alignRight.TableCell-head.TableCell-root` (computed-only) → **tablebody-root** `tbody|TableBody-root` (computed-only) → **tablerow-root** `tr|TableRow-root` (computed-only) → **tablecell-body** `td|TableCell-body.TableCell-paddingCheckbox.TableCell-root` (computed-only) → **buttonbase-root-2** `span|ButtonBase-root.Checkbox-root` (computed-only) → **part-1-0-0-0-0** `input||sr-only` (computed-only) → **icon-3** `svg|SvgIcon-root` (computed-only) → **part-1-0-0-0-1-0** `path|` (computed-only) → **label-5** `td|TableCell-body.TableCell-root` (computed-only) → **label-6** `td|TableCell-body.TableCell-root` (computed-only) → **label-7** `td|TableCell-body.TableCell-root` (computed-only) → **tablecell-alignright** `td|TableCell-alignRight.TableCell-body.TableCell-root` (computed-only) → **label-8** `button|ButtonBase-root.IconButton-root` (computed-only) → **tablerow-root-2** `tr|TableRow-root` (computed-only) → **tablecell-body-2** `td|TableCell-body.TableCell-paddingCheckbox.TableCell-root` (computed-only) → **buttonbase-root-3** `span|ButtonBase-root.Checkbox-root` (computed-only) → **part-1-1-0-0-0** `input||sr-only` (computed-only) → **icon-4** `svg|SvgIcon-root` (computed-only) → **part-1-1-0-0-1-0** `path|` (computed-only) → **label-9** `td|TableCell-body.TableCell-root` (computed-only) → **label-10** `td|TableCell-body.TableCell-root` (computed-only) → **label-11** `td|TableCell-body.TableCell-root` (computed-only) → **tablecell-alignright-2** `td|TableCell-alignRight.TableCell-body.TableCell-root` (computed-only) → **label-12** `button|ButtonBase-root.IconButton-root` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **34** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **4** · named refusals: **0**
- asset: `table-icon.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `table-icon-2.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `table-icon-3.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `table-icon-4.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- svg-viewbox-reconstructed: Table.icon@small — 0 0 24 24 from computed size 24×24 (path extent 21.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Table.icon@medium — 0 0 24 24 from computed size 24×24 (path extent 21.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-bumped: Table.icon-2@small — computed size 18 < path extent 20; viewBox reconstructed as 0 0 20 20 (named reconstruction)
- svg-viewbox-bumped: Table.icon-2@medium — computed size 18 < path extent 20; viewBox reconstructed as 0 0 20 20 (named reconstruction)
- svg-viewbox-reconstructed: Table.icon-3@small — 0 0 24 24 from computed size 24×24 (path extent 21.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Table.icon-3@medium — 0 0 24 24 from computed size 24×24 (path extent 21.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Table.icon-4@small — 0 0 24 24 from computed size 24×24 (path extent 21.0; viewBox is not a computed style — named reconstruction)
- svg-viewbox-reconstructed: Table.icon-4@medium — 0 0 24 24 from computed size 24×24 (path extent 21.0; viewBox is not a computed style — named reconstruction)
- table-lowering: root table → flex column, children stretch (row groups stack and fill the table width)
- table-lowering: tablehead-root table-header-group → flex column, children stretch (row groups stack and fill the table width); element <thead> → <div> role="rowgroup" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: tablerow-head table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: tablecell-head table-cell → flex row (counter axis center, main axis start from text-align:left); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- aspect-carried: buttonbase-root computed width == height in every enabled combo (24/42px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- sr-only-carried-as-hidden: part-0-0-0-0-0 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- full-width-carried: icon spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- table-lowering: tablecell-head-2 table-cell → flex row (counter axis center, main axis start from text-align:left); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label = "Name" (no text prop sample matches — the mounted value is carried verbatim)
- literal-text-carried: label-2 = "Role" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-2 table-cell → flex row (counter axis center, main axis start from text-align:left); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-3 = "Team" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-3 table-cell → flex row (counter axis center, main axis start from text-align:left); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-4 = "Actions" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-4 table-cell → flex row (counter axis center, main axis end from text-align:right); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: tablebody-root table-row-group → flex column, children stretch (row groups stack and fill the table width); element <tbody> → <div> role="rowgroup" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: tablerow-root table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: tablecell-body table-cell → flex row (counter axis center, main axis start from text-align:left); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- aspect-carried: buttonbase-root-2 computed width == height in every enabled combo (24/42px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- sr-only-carried-as-hidden: part-1-0-0-0-0 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- full-width-carried: icon-3 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- literal-text-carried: label-5 = "Frozen yoghurt" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-5 table-cell → flex row (counter axis center, main axis start from text-align:left); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-6 = "Designer" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-6 table-cell → flex row (counter axis center, main axis start from text-align:left); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-7 = "Platform" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-7 table-cell → flex row (counter axis center, main axis start from text-align:left); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: tablecell-alignright table-cell → flex row (counter axis center, main axis end from text-align:right); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-8 = "⋮" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: tablerow-root-2 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: tablecell-body-2 table-cell → flex row (counter axis center, main axis start from text-align:left); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- aspect-carried: buttonbase-root-3 computed width == height in every enabled combo (24/42px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- sr-only-carried-as-hidden: part-1-1-0-0-0 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- full-width-carried: icon-4 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- literal-text-carried: label-9 = "Ice cream sandwich" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-9 table-cell → flex row (counter axis center, main axis start from text-align:left); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-10 = "Engineer" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-10 table-cell → flex row (counter axis center, main axis start from text-align:left); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-11 = "Contracts" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-11 table-cell → flex row (counter axis center, main axis start from text-align:left); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: tablecell-alignright-2 table-cell → flex row (counter axis center, main axis end from text-align:right); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-12 = "⋮" (no text prop sample matches — the mounted value is carried verbatim)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-suspected: Table:medium__active>tbody>tr>td>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__active>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__active>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__default>tbody>tr>td>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__default>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__default>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__focus-visible>tbody>tr>td>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__focus-visible>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__focus-visible>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__hover>tbody>tr>td>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__hover>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:medium__hover>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__active>tbody>tr>td>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__active>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__active>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__default>tbody>tr>td>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__default>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__default>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__focus-visible>tbody>tr>td>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__focus-visible>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__focus-visible>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__hover>tbody>tr>td>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__hover>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 12x12 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-suspected: Table:small__hover>thead>tr>th>span>svg>path <path> — a non-replaced display:inline element with NO child nodes and no ::before/::after, whose CONTENT box is nevertheless non-zero — by CSS that is impossible unless something is rendering inside it that no walker can reach and a painted 18x18 box. A closed shadow root is undetectable by definition; this signature is its ABSENCE, and it is a NAMED REFUSAL rather than a silently-captured empty leaf. Nothing about this element's interior is captured or carried — the whole rendered box is unreachable from script
- closed-shadow-root-limit: 24 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::placeholder (3 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 5 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-box-align (buttonbase-root, buttonbase-root-2, buttonbase-root-3, label, label-12, label-8); -webkit-box-pack (buttonbase-root, buttonbase-root-2, buttonbase-root-3, label-12, label-8); -webkit-tap-highlight-color (buttonbase-root, buttonbase-root-2, buttonbase-root-3, icon, icon-2, icon-3, icon-4, label, label-12, label-8, part-0-0-0-0-0, part-0-0-0-0-1-0, part-0-0-1-0-0-0, part-1-0-0-0-0, part-1-0-0-0-1-0, part-1-1-0-0-0, part-1-1-0-0-1-0); -webkit-text-fill-color (buttonbase-root, buttonbase-root-2, buttonbase-root-3, icon, icon-2, icon-3, icon-4, label, label-10, label-11, label-12, label-2, label-3, label-4, label-5, label-6, label-7, label-8, label-9, part-0-0-0-0-1-0, part-0-0-1-0-0-0, part-1-0-0-0-1-0, part-1-1-0-0-1-0, tablecell-alignright, tablecell-alignright-2, tablecell-body, tablecell-body-2, tablecell-head, tablecell-head-2); -webkit-text-stroke-color (buttonbase-root, buttonbase-root-2, buttonbase-root-3, icon, icon-2, icon-3, icon-4, label, label-10, label-11, label-12, label-2, label-3, label-4, label-5, label-6, label-7, label-8, label-9, part-0-0-0-0-1-0, part-0-0-1-0-0-0, part-1-0-0-0-1-0, part-1-1-0-0-1-0, tablecell-alignright, tablecell-alignright-2, tablecell-body, tablecell-body-2, tablecell-head, tablecell-head-2)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **195** → WITH folding: **197** (275 folded channels; every fold a named receipt).

- tablecell-head.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0-0-1-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-head-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-1-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-4.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0-1-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-5.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-6.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-7.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-8.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-body-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- buttonbase-root-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-4.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-1-0-0-1-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-9.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-10.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-11.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tablecell-alignright-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-12.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **197** · bindings: 415 base + 3 state
- shape: 337 uniform · 81 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **147**
  - root.box-sizing = `border-box`
  - tablecell-head.border-bottom-style = `solid`
  - tablecell-head.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - tablecell-head.text-align = `left`
  - buttonbase-root.box-sizing = `border-box`
  - buttonbase-root.cursor = `pointer`
  - buttonbase-root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - buttonbase-root.position = `relative`
  - buttonbase-root.text-align = `left`
  - buttonbase-root.user-select = `none`
  - part-0-0-0-0-0.appearance = `auto`
  - part-0-0-0-0-0.box-sizing = `border-box`
  - part-0-0-0-0-0.cursor = `pointer`
  - part-0-0-0-0-0.font-family = `Arial`
  - part-0-0-0-0-0.position = `absolute`
  - part-0-0-0-0-0.user-select = `none`
  - icon.cursor = `pointer`
  - icon.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - icon.overflow-x = `hidden`
  - icon.overflow-y = `hidden`
  - icon.text-align = `left`
  - icon.transition-duration = `0.3s`
  - icon.transition-property = `fill`
  - icon.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - icon.user-select = `none`
  - tablecell-head-2.border-bottom-style = `solid`
  - tablecell-head-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - tablecell-head-2.text-align = `left`
  - label.box-sizing = `border-box`
  - label.cursor = `pointer`
  - label.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label.position = `relative`
  - label.text-align = `left`
  - label.user-select = `none`
  - icon-2.cursor = `pointer`
  - icon-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - icon-2.overflow-x = `hidden`
  - icon-2.overflow-y = `hidden`
  - icon-2.text-align = `left`
  - icon-2.transition-delay = `0s, 0s`
  - icon-2.transition-duration = `0.2s, 0.2s`
  - icon-2.transition-property = `opacity, transform`
  - icon-2.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1)`
  - icon-2.user-select = `none`
  - label-2.border-bottom-style = `solid`
  - label-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-2.text-align = `left`
  - label-3.border-bottom-style = `solid`
  - label-3.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-3.text-align = `left`
  - label-4.border-bottom-style = `solid`
  - label-4.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-4.text-align = `right`
  - tablecell-body.border-bottom-style = `solid`
  - tablecell-body.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - tablecell-body.text-align = `left`
  - buttonbase-root-2.box-sizing = `border-box`
  - buttonbase-root-2.cursor = `pointer`
  - buttonbase-root-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - buttonbase-root-2.position = `relative`
  - buttonbase-root-2.text-align = `left`
  - buttonbase-root-2.user-select = `none`
  - part-1-0-0-0-0.appearance = `auto`
  - part-1-0-0-0-0.box-sizing = `border-box`
  - part-1-0-0-0-0.cursor = `pointer`
  - part-1-0-0-0-0.font-family = `Arial`
  - part-1-0-0-0-0.position = `absolute`
  - part-1-0-0-0-0.user-select = `none`
  - icon-3.cursor = `pointer`
  - icon-3.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - icon-3.overflow-x = `hidden`
  - icon-3.overflow-y = `hidden`
  - icon-3.text-align = `left`
  - icon-3.transition-duration = `0.3s`
  - icon-3.transition-property = `fill`
  - icon-3.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - icon-3.user-select = `none`
  - label-5.border-bottom-style = `solid`
  - label-5.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-5.text-align = `left`
  - label-6.border-bottom-style = `solid`
  - label-6.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-6.text-align = `left`
  - label-7.border-bottom-style = `solid`
  - label-7.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-7.text-align = `left`
  - tablecell-alignright.border-bottom-style = `solid`
  - tablecell-alignright.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - tablecell-alignright.text-align = `right`
  - label-8.appearance = `none`
  - label-8.border-bottom-style = `none`
  - label-8.border-left-style = `none`
  - label-8.border-right-style = `none`
  - label-8.border-top-style = `none`
  - label-8.cursor = `pointer`
  - label-8.position = `relative`
  - label-8.transition-duration = `0.15s`
  - label-8.transition-property = `background-color`
  - label-8.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - label-8.user-select = `none`
  - tablecell-body-2.border-bottom-style = `solid`
  - tablecell-body-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - tablecell-body-2.text-align = `left`
  - buttonbase-root-3.box-sizing = `border-box`
  - buttonbase-root-3.cursor = `pointer`
  - buttonbase-root-3.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - buttonbase-root-3.position = `relative`
  - buttonbase-root-3.text-align = `left`
  - buttonbase-root-3.user-select = `none`
  - part-1-1-0-0-0.appearance = `auto`
  - part-1-1-0-0-0.box-sizing = `border-box`
  - part-1-1-0-0-0.cursor = `pointer`
  - part-1-1-0-0-0.font-family = `Arial`
  - part-1-1-0-0-0.position = `absolute`
  - part-1-1-0-0-0.user-select = `none`
  - icon-4.cursor = `pointer`
  - icon-4.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - icon-4.overflow-x = `hidden`
  - icon-4.overflow-y = `hidden`
  - icon-4.text-align = `left`
  - icon-4.transition-duration = `0.3s`
  - icon-4.transition-property = `fill`
  - icon-4.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - icon-4.user-select = `none`
  - label-9.border-bottom-style = `solid`
  - label-9.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-9.text-align = `left`
  - label-10.border-bottom-style = `solid`
  - label-10.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-10.text-align = `left`
  - label-11.border-bottom-style = `solid`
  - label-11.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-11.text-align = `left`
  - tablecell-alignright-2.border-bottom-style = `solid`
  - tablecell-alignright-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - tablecell-alignright-2.text-align = `right`
  - label-12.appearance = `none`
  - label-12.border-bottom-style = `none`
  - label-12.border-left-style = `none`
  - label-12.border-right-style = `none`
  - label-12.border-top-style = `none`
  - label-12.cursor = `pointer`
  - label-12.position = `relative`
  - label-12.transition-duration = `0.15s`
  - label-12.transition-property = `background-color`
  - label-12.transition-timing-function = `cubic-bezier(0.4, 0, 0.2, 1)`
  - label-12.user-select = `none`
- state declared facts: **1**
  - [focus-visible] part-0-0-0-0-0.outline-style = `auto`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **121**
  - root.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - root.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table`, 1 distinct value(s))
  - root.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablehead-root.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablehead-root.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-header-group`, 1 distinct value(s))
  - tablehead-root.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablehead-root.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablerow-head.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablerow-head.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - tablerow-head.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablerow-head.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablecell-head.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablecell-head.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - tablecell-head.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablecell-head.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - buttonbase-root.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - buttonbase-root.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - part-0-0-0-0-0.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - icon.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - icon.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `content-box`, 1 distinct value(s))
  - tablecell-head-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablecell-head-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - tablecell-head-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablecell-head-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - icon-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - icon-2.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `content-box`, 1 distinct value(s))
  - icon-2.transform — declared-channel value outside the bounded grammar — named residue (v15) (sample: `matrix(-1, 0, 0, -1, 0, 0)`, 1 distinct value(s))
  - icon-2.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal, normal`, 1 distinct value(s))
  - label-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label-3.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-3.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-3.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-3.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label-4.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-4.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-4.flex-direction — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `row-reverse`, 1 distinct value(s))
  - label-4.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-4.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablebody-root.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablebody-root.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row-group`, 1 distinct value(s))
  - tablebody-root.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablebody-root.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablerow-root.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablerow-root.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - tablerow-root.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablerow-root.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablecell-body.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablecell-body.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - tablecell-body.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablecell-body.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - buttonbase-root-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - buttonbase-root-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - part-1-0-0-0-0.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - icon-3.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - icon-3.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `content-box`, 1 distinct value(s))
  - label-5.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-5.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-5.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-5.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label-6.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-6.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-6.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-6.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label-7.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-7.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-7.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-7.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablecell-alignright.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablecell-alignright.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - tablecell-alignright.flex-direction — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `row-reverse`, 1 distinct value(s))
  - tablecell-alignright.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablecell-alignright.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label-8.border-bottom-left-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - label-8.border-bottom-right-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - label-8.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-8.border-top-left-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - label-8.border-top-right-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - label-8.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - label-8.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablerow-root-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablerow-root-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - tablerow-root-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablerow-root-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablecell-body-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablecell-body-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - tablecell-body-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablecell-body-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - buttonbase-root-3.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - buttonbase-root-3.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - part-1-1-0-0-0.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - icon-4.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - icon-4.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `content-box`, 1 distinct value(s))
  - label-9.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-9.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-9.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-9.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label-10.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-10.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-10.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-10.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label-11.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-11.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-11.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - label-11.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - tablecell-alignright-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - tablecell-alignright-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - tablecell-alignright-2.flex-direction — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `row-reverse`, 1 distinct value(s))
  - tablecell-alignright-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `isolate`, 1 distinct value(s))
  - tablecell-alignright-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - label-12.border-bottom-left-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - label-12.border-bottom-right-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - label-12.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-12.border-top-left-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - label-12.border-top-right-radius — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `50%`, 1 distinct value(s))
  - label-12.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - label-12.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **3**
  - {"part":"part-0-0-0-0-0","channel":"outline-color","state":"focus-visible","ref":"{imported.table.part-0-0-0-0-0.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0-0-0-0","channel":"outline-offset","state":"focus-visible","ref":"{imported.table.part-0-0-0-0-0.outline-offset-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"part-0-0-0-0-0","channel":"outline-width","state":"focus-visible","ref":"{imported.table.part-0-0-0-0-0.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **85.201%** (5262/6176 cells; 0/8 combo×state rows fully equal)
- pixel: 0/8 pairs perfect at threshold 0 · 0/8 at the AA point (mean AA 0.165%, max 0.213%; 8/8 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

