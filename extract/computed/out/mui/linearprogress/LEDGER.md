# Delta ledger — computed floor × static layer (LinearProgress)

Generated by extract/computed/run.ts against @mui/material@9.2.0 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component LinearProgress`.

- captures: **8** (2 combos, full-cartesian × 4 interactions), channels enumerated per element: **678** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `span|LinearProgress-indeterminate.LinearProgress-root` (matched) → **linearprogress-bar** `span|LinearProgress-bar.LinearProgress-bar1` (computed-only) → **linearprogress-bar-2** `span|LinearProgress-bar.LinearProgress-bar2` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **3** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**
- presence-carried: linearprogress-bar-2 (1/2 combos) →  hidden-when variant=determinate

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **16** → WITH folding: **16** (0 folded channels; every fold a named receipt).


## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **16** · bindings: 30 base + 0 state
- shape: 26 uniform · 4 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **8**
  - root.overflow-x = `hidden`
  - root.overflow-y = `hidden`
  - root.position = `relative`
  - linearprogress-bar.transition-property = `transform`
  - linearprogress-bar.transition-timing-function = `linear`
  - linearprogress-bar-2.transition-duration = `0.2s`
  - linearprogress-bar-2.transition-property = `transform`
  - linearprogress-bar-2.transition-timing-function = `linear`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **11**
  - linearprogress-bar.animation-duration — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `0s`, 2 distinct value(s))
  - linearprogress-bar.animation-iteration-count — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `infinite`, 2 distinct value(s))
  - linearprogress-bar.animation-name — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 2 distinct value(s))
  - linearprogress-bar.animation-timing-function — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `ease`, 2 distinct value(s))
  - linearprogress-bar.transform — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `matrix(1, 0, 0, 1, -172.8, 0)`, 2 distinct value(s))
  - linearprogress-bar.transition-duration — declared-channel value varies across combos — declared facts carry uniform values only (v15); named residue (sample: `0.4s`, 2 distinct value(s))
  - linearprogress-bar-2.animation-delay — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `1.15s`, 1 distinct value(s))
  - linearprogress-bar-2.animation-duration — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `2.1s`, 1 distinct value(s))
  - linearprogress-bar-2.animation-iteration-count — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `infinite`, 1 distinct value(s))
  - linearprogress-bar-2.animation-name — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `animation-19gglwu`, 1 distinct value(s))
  - linearprogress-bar-2.animation-timing-function — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `cubic-bezier(0.165, 0.84, 0.44, 1)`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **82.857%** (232/280 cells; 0/8 combo×state rows fully equal)
- pixel: 4/8 pairs perfect at threshold 0 · 4/8 at the AA point (mean AA 2.240%, max 4.479%; 8/8 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

