# Delta ledger — computed floor × static layer (Calendar)

Generated by extract/computed/run.ts against react-day-picker@10.0.1 in Chromium 149.0.7827.55. Regenerate: `npm run extract:computed -- --harness <dir> --component Calendar`.

- captures: **32** (8 combos, full-cartesian × 4 interactions), channels enumerated per element: **472** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|root` (matched) → **months** `div|months` (computed-only) → **nav** `nav|nav` (computed-only) → **button_previous** `button|button_previous` (computed-only) → **icon** `svg|chevron` (computed-only) → **part-0-0-0-0-0** `polygon|` (computed-only) → **button_next** `button|button_next` (computed-only) → **icon-2** `svg|chevron` (computed-only) → **part-0-0-1-0-0** `polygon|` (computed-only) → **month** `div|month` (computed-only) → **month_caption** `div|month_caption` (computed-only) → **dropdowns** `div|dropdowns` (computed-only) → **dropdown_root** `span|dropdown_root` (computed-only) → **dropdown** `select|dropdown.years_dropdown|sr-only` (computed-only) → **label-40** `option|` (computed-only) → **label-41** `option|` (computed-only) → **label-42** `option|` (computed-only) → **label-43** `option|` (computed-only) → **label-44** `option|` (computed-only) → **label-45** `option|` (computed-only) → **label-46** `option|` (computed-only) → **label-47** `option|` (computed-only) → **label-48** `option|` (computed-only) → **label-49** `option|` (computed-only) → **label-50** `option|` (computed-only) → **label-51** `option|` (computed-only) → **label-52** `option|` (computed-only) → **label-53** `option|` (computed-only) → **label-54** `option|` (computed-only) → **label-55** `option|` (computed-only) → **label-56** `option|` (computed-only) → **label-57** `option|` (computed-only) → **label-58** `option|` (computed-only) → **label-59** `option|` (computed-only) → **label-60** `option|` (computed-only) → **label-61** `option|` (computed-only) → **label-62** `option|` (computed-only) → **label-63** `option|` (computed-only) → **label-64** `option|` (computed-only) → **label-65** `option|` (computed-only) → **label-66** `option|` (computed-only) → **label-67** `option|` (computed-only) → **label-68** `option|` (computed-only) → **label-69** `option|` (computed-only) → **label-70** `option|` (computed-only) → **label-71** `option|` (computed-only) → **label-72** `option|` (computed-only) → **label-73** `option|` (computed-only) → **label-74** `option|` (computed-only) → **label-75** `option|` (computed-only) → **label-76** `option|` (computed-only) → **label-77** `option|` (computed-only) → **label-78** `option|` (computed-only) → **label-79** `option|` (computed-only) → **label-80** `option|` (computed-only) → **label-81** `option|` (computed-only) → **label-82** `option|` (computed-only) → **label-83** `option|` (computed-only) → **label-84** `option|` (computed-only) → **label-85** `option|` (computed-only) → **label-86** `option|` (computed-only) → **label-87** `option|` (computed-only) → **label-88** `option|` (computed-only) → **label-89** `option|` (computed-only) → **label-90** `option|` (computed-only) → **label-91** `option|` (computed-only) → **label-92** `option|` (computed-only) → **label-93** `option|` (computed-only) → **label-94** `option|` (computed-only) → **label-95** `option|` (computed-only) → **label-96** `option|` (computed-only) → **label-97** `option|` (computed-only) → **label-98** `option|` (computed-only) → **label-99** `option|` (computed-only) → **label-100** `option|` (computed-only) → **label-101** `option|` (computed-only) → **label-102** `option|` (computed-only) → **label-103** `option|` (computed-only) → **label-104** `option|` (computed-only) → **label-105** `option|` (computed-only) → **label-106** `option|` (computed-only) → **label-107** `option|` (computed-only) → **label-108** `option|` (computed-only) → **label-109** `option|` (computed-only) → **label-110** `option|` (computed-only) → **label-111** `option|` (computed-only) → **label-112** `option|` (computed-only) → **label-113** `option|` (computed-only) → **label-114** `option|` (computed-only) → **label-115** `option|` (computed-only) → **label-116** `option|` (computed-only) → **label-117** `option|` (computed-only) → **label-118** `option|` (computed-only) → **label-119** `option|` (computed-only) → **label-120** `option|` (computed-only) → **label-121** `option|` (computed-only) → **label-122** `option|` (computed-only) → **label-123** `option|` (computed-only) → **label-124** `option|` (computed-only) → **label-125** `option|` (computed-only) → **label-126** `option|` (computed-only) → **label-127** `option|` (computed-only) → **label-128** `option|` (computed-only) → **label-129** `option|` (computed-only) → **label-130** `option|` (computed-only) → **label-131** `option|` (computed-only) → **label-132** `option|` (computed-only) → **label-133** `option|` (computed-only) → **label-134** `option|` (computed-only) → **label-135** `option|` (computed-only) → **label-136** `option|` (computed-only) → **label-137** `option|` (computed-only) → **label-138** `option|` (computed-only) → **label-139** `option|` (computed-only) → **label-140** `option|` (computed-only) → **dropdown-2** `select|dropdown.months_dropdown|sr-only` (computed-only) → **label-141** `option|` (computed-only) → **label-142** `option|` (computed-only) → **label-143** `option|` (computed-only) → **label-144** `option|` (computed-only) → **label-145** `option|` (computed-only) → **label-146** `option|` (computed-only) → **label-147** `option|` (computed-only) → **label-148** `option|` (computed-only) → **label-149** `option|` (computed-only) → **label-150** `option|` (computed-only) → **label-151** `option|` (computed-only) → **label-152** `option|` (computed-only) → **label-153** `span|caption_label` (computed-only) → **icon-3** `svg|chevron` (computed-only) → **part-0-1-0-0-0-1-0-0** `polygon|` (computed-only) → **label-154** `span|` (computed-only) → **dropdown_root-2** `span|dropdown_root` (computed-only) → **dropdown-3** `select|dropdown.years_dropdown|sr-only` (computed-only) → **label-155** `option|` (computed-only) → **label-156** `option|` (computed-only) → **label-157** `option|` (computed-only) → **label-158** `option|` (computed-only) → **label-159** `option|` (computed-only) → **label-160** `option|` (computed-only) → **label-161** `option|` (computed-only) → **label-162** `option|` (computed-only) → **label-163** `option|` (computed-only) → **label-164** `option|` (computed-only) → **label-165** `option|` (computed-only) → **label-166** `option|` (computed-only) → **label-167** `option|` (computed-only) → **label-168** `option|` (computed-only) → **label-169** `option|` (computed-only) → **label-170** `option|` (computed-only) → **label-171** `option|` (computed-only) → **label-172** `option|` (computed-only) → **label-173** `option|` (computed-only) → **label-174** `option|` (computed-only) → **label-175** `option|` (computed-only) → **label-176** `option|` (computed-only) → **label-177** `option|` (computed-only) → **label-178** `option|` (computed-only) → **label-179** `option|` (computed-only) → **label-180** `option|` (computed-only) → **label-181** `option|` (computed-only) → **label-182** `option|` (computed-only) → **label-183** `option|` (computed-only) → **label-184** `option|` (computed-only) → **label-185** `option|` (computed-only) → **label-186** `option|` (computed-only) → **label-187** `option|` (computed-only) → **label-188** `option|` (computed-only) → **label-189** `option|` (computed-only) → **label-190** `option|` (computed-only) → **label-191** `option|` (computed-only) → **label-192** `option|` (computed-only) → **label-193** `option|` (computed-only) → **label-194** `option|` (computed-only) → **label-195** `option|` (computed-only) → **label-196** `option|` (computed-only) → **label-197** `option|` (computed-only) → **label-198** `option|` (computed-only) → **label-199** `option|` (computed-only) → **label-200** `option|` (computed-only) → **label-201** `option|` (computed-only) → **label-202** `option|` (computed-only) → **label-203** `option|` (computed-only) → **label-204** `option|` (computed-only) → **label-205** `option|` (computed-only) → **label-206** `option|` (computed-only) → **label-207** `option|` (computed-only) → **label-208** `option|` (computed-only) → **label-209** `option|` (computed-only) → **label-210** `option|` (computed-only) → **label-211** `option|` (computed-only) → **label-212** `option|` (computed-only) → **label-213** `option|` (computed-only) → **label-214** `option|` (computed-only) → **label-215** `option|` (computed-only) → **label-216** `option|` (computed-only) → **label-217** `option|` (computed-only) → **label-218** `option|` (computed-only) → **label-219** `option|` (computed-only) → **label-220** `option|` (computed-only) → **label-221** `option|` (computed-only) → **label-222** `option|` (computed-only) → **label-223** `option|` (computed-only) → **label-224** `option|` (computed-only) → **label-225** `option|` (computed-only) → **label-226** `option|` (computed-only) → **label-227** `option|` (computed-only) → **label-228** `option|` (computed-only) → **label-229** `option|` (computed-only) → **label-230** `option|` (computed-only) → **label-231** `option|` (computed-only) → **label-232** `option|` (computed-only) → **label-233** `option|` (computed-only) → **label-234** `option|` (computed-only) → **label-235** `option|` (computed-only) → **label-236** `option|` (computed-only) → **label-237** `option|` (computed-only) → **label-238** `option|` (computed-only) → **label-239** `option|` (computed-only) → **label-240** `option|` (computed-only) → **label-241** `option|` (computed-only) → **label-242** `option|` (computed-only) → **label-243** `option|` (computed-only) → **label-244** `option|` (computed-only) → **label-245** `option|` (computed-only) → **label-246** `option|` (computed-only) → **label-247** `option|` (computed-only) → **label-248** `option|` (computed-only) → **label-249** `option|` (computed-only) → **label-250** `option|` (computed-only) → **label-251** `option|` (computed-only) → **label-252** `option|` (computed-only) → **label-253** `option|` (computed-only) → **label-254** `option|` (computed-only) → **label-255** `option|` (computed-only) → **label-256** `span|caption_label` (computed-only) → **icon-4** `svg|chevron` (computed-only) → **part-0-1-0-0-1-1-0-0** `polygon|` (computed-only) → **label-257** `span||sr-only` (computed-only) → **label** `span|caption_label` (computed-only) → **month_grid** `table|month_grid` (computed-only) → **part-0-1-1-0** `thead|` (computed-only) → **weekdays** `tr|weekdays` (computed-only) → **label-2** `th|weekday` (computed-only) → **label-3** `th|weekday` (computed-only) → **label-4** `th|weekday` (computed-only) → **label-5** `th|weekday` (computed-only) → **label-6** `th|weekday` (computed-only) → **label-7** `th|weekday` (computed-only) → **label-8** `th|weekday` (computed-only) → **weeks** `tbody|weeks` (computed-only) → **week** `tr|week` (computed-only) → **day** `td|day.hidden.outside` (computed-only) → **day-2** `td|day.hidden.outside` (computed-only) → **day-3** `td|day.hidden.outside` (computed-only) → **day-4** `td|day.hidden.outside` (computed-only) → **day-5** `td|day` (computed-only) → **label-9** `button|day_button` (computed-only) → **day-6** `td|day` (computed-only) → **label-10** `button|day_button` (computed-only) → **day-7** `td|day` (computed-only) → **label-11** `button|day_button` (computed-only) → **week-2** `tr|week` (computed-only) → **day-8** `td|day` (computed-only) → **label-12** `button|day_button` (computed-only) → **day-9** `td|day` (computed-only) → **label-13** `button|day_button` (computed-only) → **day-10** `td|day` (computed-only) → **label-14** `button|day_button` (computed-only) → **day-36** `td|day.focused` (computed-only) → **label-258** `button|day_button` (computed-only) → **day-11** `td|day` (computed-only) → **label-15** `button|day_button` (computed-only) → **day-12** `td|day` (computed-only) → **label-16** `button|day_button` (computed-only) → **day-13** `td|day` (computed-only) → **label-17** `button|day_button` (computed-only) → **day-14** `td|day` (computed-only) → **label-18** `button|day_button` (computed-only) → **week-3** `tr|week` (computed-only) → **day-15** `td|day` (computed-only) → **label-19** `button|day_button` (computed-only) → **day-16** `td|day` (computed-only) → **label-20** `button|day_button` (computed-only) → **day-17** `td|day` (computed-only) → **label-21** `button|day_button` (computed-only) → **day-18** `td|day` (computed-only) → **label-22** `button|day_button` (computed-only) → **day-19** `td|day.today` (computed-only) → **label-23** `button|day_button` (computed-only) → **day-20** `td|day` (computed-only) → **label-24** `button|day_button` (computed-only) → **day-21** `td|day` (computed-only) → **label-25** `button|day_button` (computed-only) → **week-4** `tr|week` (computed-only) → **day-22** `td|day` (computed-only) → **label-26** `button|day_button` (computed-only) → **day-23** `td|day` (computed-only) → **label-27** `button|day_button` (computed-only) → **day-24** `td|day.selected` (computed-only) → **label-28** `button|day_button` (computed-only) → **day-25** `td|day` (computed-only) → **label-29** `button|day_button` (computed-only) → **day-26** `td|day` (computed-only) → **label-30** `button|day_button` (computed-only) → **day-27** `td|day` (computed-only) → **label-31** `button|day_button` (computed-only) → **day-28** `td|day` (computed-only) → **label-32** `button|day_button` (computed-only) → **week-5** `tr|week` (computed-only) → **day-29** `td|day` (computed-only) → **label-33** `button|day_button` (computed-only) → **day-30** `td|day` (computed-only) → **label-34** `button|day_button` (computed-only) → **day-31** `td|day` (computed-only) → **label-35** `button|day_button` (computed-only) → **day-32** `td|day` (computed-only) → **label-36** `button|day_button` (computed-only) → **day-33** `td|day` (computed-only) → **label-37** `button|day_button` (computed-only) → **day-34** `td|day` (computed-only) → **label-38** `button|day_button` (computed-only) → **day-35** `td|day` (computed-only) → **label-39** `button|day_button` (computed-only) → **month-2** `div|month` (computed-only) → **month_caption-2** `div|month_caption` (computed-only) → **dropdowns-2** `div|dropdowns` (computed-only) → **dropdown_root-3** `span|dropdown_root` (computed-only) → **dropdown-4** `select|dropdown.years_dropdown|sr-only` (computed-only) → **label-259** `option|` (computed-only) → **label-260** `option|` (computed-only) → **label-261** `option|` (computed-only) → **label-262** `option|` (computed-only) → **label-263** `option|` (computed-only) → **label-264** `option|` (computed-only) → **label-265** `option|` (computed-only) → **label-266** `option|` (computed-only) → **label-267** `option|` (computed-only) → **label-268** `option|` (computed-only) → **label-269** `option|` (computed-only) → **label-270** `option|` (computed-only) → **label-271** `option|` (computed-only) → **label-272** `option|` (computed-only) → **label-273** `option|` (computed-only) → **label-274** `option|` (computed-only) → **label-275** `option|` (computed-only) → **label-276** `option|` (computed-only) → **label-277** `option|` (computed-only) → **label-278** `option|` (computed-only) → **label-279** `option|` (computed-only) → **label-280** `option|` (computed-only) → **label-281** `option|` (computed-only) → **label-282** `option|` (computed-only) → **label-283** `option|` (computed-only) → **label-284** `option|` (computed-only) → **label-285** `option|` (computed-only) → **label-286** `option|` (computed-only) → **label-287** `option|` (computed-only) → **label-288** `option|` (computed-only) → **label-289** `option|` (computed-only) → **label-290** `option|` (computed-only) → **label-291** `option|` (computed-only) → **label-292** `option|` (computed-only) → **label-293** `option|` (computed-only) → **label-294** `option|` (computed-only) → **label-295** `option|` (computed-only) → **label-296** `option|` (computed-only) → **label-297** `option|` (computed-only) → **label-298** `option|` (computed-only) → **label-299** `option|` (computed-only) → **label-300** `option|` (computed-only) → **label-301** `option|` (computed-only) → **label-302** `option|` (computed-only) → **label-303** `option|` (computed-only) → **label-304** `option|` (computed-only) → **label-305** `option|` (computed-only) → **label-306** `option|` (computed-only) → **label-307** `option|` (computed-only) → **label-308** `option|` (computed-only) → **label-309** `option|` (computed-only) → **label-310** `option|` (computed-only) → **label-311** `option|` (computed-only) → **label-312** `option|` (computed-only) → **label-313** `option|` (computed-only) → **label-314** `option|` (computed-only) → **label-315** `option|` (computed-only) → **label-316** `option|` (computed-only) → **label-317** `option|` (computed-only) → **label-318** `option|` (computed-only) → **label-319** `option|` (computed-only) → **label-320** `option|` (computed-only) → **label-321** `option|` (computed-only) → **label-322** `option|` (computed-only) → **label-323** `option|` (computed-only) → **label-324** `option|` (computed-only) → **label-325** `option|` (computed-only) → **label-326** `option|` (computed-only) → **label-327** `option|` (computed-only) → **label-328** `option|` (computed-only) → **label-329** `option|` (computed-only) → **label-330** `option|` (computed-only) → **label-331** `option|` (computed-only) → **label-332** `option|` (computed-only) → **label-333** `option|` (computed-only) → **label-334** `option|` (computed-only) → **label-335** `option|` (computed-only) → **label-336** `option|` (computed-only) → **label-337** `option|` (computed-only) → **label-338** `option|` (computed-only) → **label-339** `option|` (computed-only) → **label-340** `option|` (computed-only) → **label-341** `option|` (computed-only) → **label-342** `option|` (computed-only) → **label-343** `option|` (computed-only) → **label-344** `option|` (computed-only) → **label-345** `option|` (computed-only) → **label-346** `option|` (computed-only) → **label-347** `option|` (computed-only) → **label-348** `option|` (computed-only) → **label-349** `option|` (computed-only) → **label-350** `option|` (computed-only) → **label-351** `option|` (computed-only) → **label-352** `option|` (computed-only) → **label-353** `option|` (computed-only) → **label-354** `option|` (computed-only) → **label-355** `option|` (computed-only) → **label-356** `option|` (computed-only) → **label-357** `option|` (computed-only) → **label-358** `option|` (computed-only) → **label-359** `option|` (computed-only) → **dropdown-5** `select|dropdown.months_dropdown|sr-only` (computed-only) → **label-360** `option|` (computed-only) → **label-361** `option|` (computed-only) → **label-362** `option|` (computed-only) → **label-363** `option|` (computed-only) → **label-364** `option|` (computed-only) → **label-365** `option|` (computed-only) → **label-366** `option|` (computed-only) → **label-367** `option|` (computed-only) → **label-368** `option|` (computed-only) → **label-369** `option|` (computed-only) → **label-370** `option|` (computed-only) → **label-371** `option|` (computed-only) → **label-372** `span|caption_label` (computed-only) → **icon-5** `svg|chevron` (computed-only) → **part-0-2-0-0-0-1-0-0** `polygon|` (computed-only) → **label-373** `span|` (computed-only) → **dropdown_root-4** `span|dropdown_root` (computed-only) → **dropdown-6** `select|dropdown.years_dropdown|sr-only` (computed-only) → **label-374** `option|` (computed-only) → **label-375** `option|` (computed-only) → **label-376** `option|` (computed-only) → **label-377** `option|` (computed-only) → **label-378** `option|` (computed-only) → **label-379** `option|` (computed-only) → **label-380** `option|` (computed-only) → **label-381** `option|` (computed-only) → **label-382** `option|` (computed-only) → **label-383** `option|` (computed-only) → **label-384** `option|` (computed-only) → **label-385** `option|` (computed-only) → **label-386** `option|` (computed-only) → **label-387** `option|` (computed-only) → **label-388** `option|` (computed-only) → **label-389** `option|` (computed-only) → **label-390** `option|` (computed-only) → **label-391** `option|` (computed-only) → **label-392** `option|` (computed-only) → **label-393** `option|` (computed-only) → **label-394** `option|` (computed-only) → **label-395** `option|` (computed-only) → **label-396** `option|` (computed-only) → **label-397** `option|` (computed-only) → **label-398** `option|` (computed-only) → **label-399** `option|` (computed-only) → **label-400** `option|` (computed-only) → **label-401** `option|` (computed-only) → **label-402** `option|` (computed-only) → **label-403** `option|` (computed-only) → **label-404** `option|` (computed-only) → **label-405** `option|` (computed-only) → **label-406** `option|` (computed-only) → **label-407** `option|` (computed-only) → **label-408** `option|` (computed-only) → **label-409** `option|` (computed-only) → **label-410** `option|` (computed-only) → **label-411** `option|` (computed-only) → **label-412** `option|` (computed-only) → **label-413** `option|` (computed-only) → **label-414** `option|` (computed-only) → **label-415** `option|` (computed-only) → **label-416** `option|` (computed-only) → **label-417** `option|` (computed-only) → **label-418** `option|` (computed-only) → **label-419** `option|` (computed-only) → **label-420** `option|` (computed-only) → **label-421** `option|` (computed-only) → **label-422** `option|` (computed-only) → **label-423** `option|` (computed-only) → **label-424** `option|` (computed-only) → **label-425** `option|` (computed-only) → **label-426** `option|` (computed-only) → **label-427** `option|` (computed-only) → **label-428** `option|` (computed-only) → **label-429** `option|` (computed-only) → **label-430** `option|` (computed-only) → **label-431** `option|` (computed-only) → **label-432** `option|` (computed-only) → **label-433** `option|` (computed-only) → **label-434** `option|` (computed-only) → **label-435** `option|` (computed-only) → **label-436** `option|` (computed-only) → **label-437** `option|` (computed-only) → **label-438** `option|` (computed-only) → **label-439** `option|` (computed-only) → **label-440** `option|` (computed-only) → **label-441** `option|` (computed-only) → **label-442** `option|` (computed-only) → **label-443** `option|` (computed-only) → **label-444** `option|` (computed-only) → **label-445** `option|` (computed-only) → **label-446** `option|` (computed-only) → **label-447** `option|` (computed-only) → **label-448** `option|` (computed-only) → **label-449** `option|` (computed-only) → **label-450** `option|` (computed-only) → **label-451** `option|` (computed-only) → **label-452** `option|` (computed-only) → **label-453** `option|` (computed-only) → **label-454** `option|` (computed-only) → **label-455** `option|` (computed-only) → **label-456** `option|` (computed-only) → **label-457** `option|` (computed-only) → **label-458** `option|` (computed-only) → **label-459** `option|` (computed-only) → **label-460** `option|` (computed-only) → **label-461** `option|` (computed-only) → **label-462** `option|` (computed-only) → **label-463** `option|` (computed-only) → **label-464** `option|` (computed-only) → **label-465** `option|` (computed-only) → **label-466** `option|` (computed-only) → **label-467** `option|` (computed-only) → **label-468** `option|` (computed-only) → **label-469** `option|` (computed-only) → **label-470** `option|` (computed-only) → **label-471** `option|` (computed-only) → **label-472** `option|` (computed-only) → **label-473** `option|` (computed-only) → **label-474** `option|` (computed-only) → **label-475** `span|caption_label` (computed-only) → **icon-6** `svg|chevron` (computed-only) → **part-0-2-0-0-1-1-0-0** `polygon|` (computed-only) → **label-476** `span||sr-only` (computed-only) → **label-477** `span|caption_label` (computed-only) → **month_grid-2** `table|month_grid` (computed-only) → **part-0-2-1-0** `thead|` (computed-only) → **weekdays-2** `tr|weekdays` (computed-only) → **label-478** `th|weekday` (computed-only) → **label-479** `th|weekday` (computed-only) → **label-480** `th|weekday` (computed-only) → **label-481** `th|weekday` (computed-only) → **label-482** `th|weekday` (computed-only) → **label-483** `th|weekday` (computed-only) → **label-484** `th|weekday` (computed-only) → **weeks-2** `tbody|weeks` (computed-only) → **week-6** `tr|week` (computed-only) → **day-37** `td|day` (computed-only) → **label-485** `button|day_button` (computed-only) → **day-38** `td|day` (computed-only) → **label-486** `button|day_button` (computed-only) → **day-39** `td|day` (computed-only) → **label-487** `button|day_button` (computed-only) → **day-40** `td|day` (computed-only) → **label-488** `button|day_button` (computed-only) → **day-41** `td|day` (computed-only) → **label-489** `button|day_button` (computed-only) → **day-42** `td|day` (computed-only) → **label-490** `button|day_button` (computed-only) → **day-43** `td|day` (computed-only) → **label-491** `button|day_button` (computed-only) → **week-7** `tr|week` (computed-only) → **day-44** `td|day` (computed-only) → **label-492** `button|day_button` (computed-only) → **day-45** `td|day` (computed-only) → **label-493** `button|day_button` (computed-only) → **day-46** `td|day` (computed-only) → **label-494** `button|day_button` (computed-only) → **day-47** `td|day` (computed-only) → **label-495** `button|day_button` (computed-only) → **day-48** `td|day` (computed-only) → **label-496** `button|day_button` (computed-only) → **day-49** `td|day` (computed-only) → **label-497** `button|day_button` (computed-only) → **day-50** `td|day` (computed-only) → **label-498** `button|day_button` (computed-only) → **week-8** `tr|week` (computed-only) → **day-51** `td|day` (computed-only) → **label-499** `button|day_button` (computed-only) → **day-52** `td|day` (computed-only) → **label-500** `button|day_button` (computed-only) → **day-53** `td|day` (computed-only) → **label-501** `button|day_button` (computed-only) → **day-54** `td|day` (computed-only) → **label-502** `button|day_button` (computed-only) → **day-55** `td|day` (computed-only) → **label-503** `button|day_button` (computed-only) → **day-56** `td|day` (computed-only) → **label-504** `button|day_button` (computed-only) → **day-57** `td|day` (computed-only) → **label-505** `button|day_button` (computed-only) → **week-9** `tr|week` (computed-only) → **day-58** `td|day` (computed-only) → **label-506** `button|day_button` (computed-only) → **day-59** `td|day` (computed-only) → **label-507** `button|day_button` (computed-only) → **day-60** `td|day` (computed-only) → **label-508** `button|day_button` (computed-only) → **day-61** `td|day` (computed-only) → **label-509** `button|day_button` (computed-only) → **day-62** `td|day` (computed-only) → **label-510** `button|day_button` (computed-only) → **day-63** `td|day` (computed-only) → **label-511** `button|day_button` (computed-only) → **day-64** `td|day` (computed-only) → **label-512** `button|day_button` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **192** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **5**
- svg-child-outside-grammar: Calendar.button_previous@label.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_previous@label.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_previous@dropdown.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_previous@dropdown.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_previous@dropdown-months.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_previous@dropdown-months.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_previous@dropdown-years.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_previous@dropdown-years.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_next@label.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_next@label.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_next@dropdown.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_next@dropdown.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_next@dropdown-months.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_next@dropdown-months.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_next@dropdown-years.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.button_next@dropdown-years.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-3@dropdown.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-3@dropdown.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-3@dropdown-months.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-3@dropdown-months.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-3@dropdown-years.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-3@dropdown-years.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-4@dropdown.1 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-4@dropdown.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-5@dropdown.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-5@dropdown-months.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-5@dropdown-years.2 — <polygon> (v1 carries path/g/circle); asset refused
- svg-child-outside-grammar: Calendar.icon-6@dropdown.2 — <polygon> (v1 carries path/g/circle); asset refused
- presence-carried: dropdowns (6/8 combos) →  hidden-when captionLayout=label
- presence-carried: dropdown_root (6/8 combos) →  hidden-when captionLayout=label
- sr-only-carried-as-hidden: dropdown is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- sr-only-carried-as-hidden: dropdown-2 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- literal-text-carried: label-153 = "January" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label-153 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- presence-carried: label-153 (6/8 combos) →  hidden-when captionLayout=label
- presence-carried: icon-3 (6/8 combos) →  hidden-when captionLayout=label
- presence-carried: part-0-1-0-0-0-1-0-0 (6/8 combos) →  hidden-when captionLayout=label
- literal-text-carried: label-154 = "2026" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-154 (4/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown
- presence-carried: dropdown_root-2 (2/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown-months, captionLayout=dropdown-years
- sr-only-carried-as-hidden: dropdown-3 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- literal-text-carried: label-256 = "2026" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label-256 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- presence-carried: label-256 (2/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown-months, captionLayout=dropdown-years
- presence-carried: icon-4 (2/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown-months, captionLayout=dropdown-years
- presence-carried: part-0-1-0-0-1-1-0-0 (2/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown-months, captionLayout=dropdown-years
- literal-text-carried: label-257 = "January 2026" (no text prop sample matches — the mounted value is carried verbatim)
- sr-only-carried-as-hidden: label-257 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- literal-text-carried: label = "January 2026" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label (2/8 combos) →  hidden-when captionLayout=dropdown, captionLayout=dropdown-months, captionLayout=dropdown-years
- table-lowering: month_grid table → flex column, children stretch (row groups stack and fill the table width); element <table> → <div> role="table" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: part-0-1-1-0 table-header-group → flex column, children stretch (row groups stack and fill the table width); element <thead> → <div> role="rowgroup" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: weekdays table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-2 = "Su" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-2 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-3 = "Mo" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-3 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-4 = "Tu" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-4 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-5 = "We" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-5 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-6 = "Th" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-6 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-7 = "Fr" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-7 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-8 = "Sa" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-8 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: weeks table-row-group → flex column, children stretch (row groups stack and fill the table width); element <tbody> → <div> role="rowgroup" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: week table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: day-5 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-9 = "1" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-6 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-10 = "2" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-7 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-11 = "3" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: week-2 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: day-8 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-12 = "4" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-9 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-13 = "5" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-10 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-14 = "6" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-11 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-15 = "7" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-12 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-16 = "8" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-13 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-17 = "9" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-14 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-18 = "10" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: week-3 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: day-15 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-19 = "11" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-16 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-20 = "12" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-17 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-21 = "13" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-18 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-22 = "14" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-19 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-23 = "15" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-20 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-24 = "16" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-21 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-25 = "17" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: week-4 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: day-22 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-26 = "18" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-23 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-27 = "19" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-24 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-28 = "20" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-25 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-29 = "21" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-26 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-30 = "22" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-27 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-31 = "23" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-28 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-32 = "24" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: week-5 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- table-lowering: day-29 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-33 = "25" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-30 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-34 = "26" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-31 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-35 = "27" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-32 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-36 = "28" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-33 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-37 = "29" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-34 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-38 = "30" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: day-35 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- literal-text-carried: label-39 = "31" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: month-2 (4/8 combos) →  hidden-when numberOfMonths=1
- presence-carried: month_caption-2 (4/8 combos) →  hidden-when numberOfMonths=1
- presence-carried: dropdowns-2 (3/8 combos) →  hidden-when captionLayout=label, numberOfMonths=1
- presence-carried: dropdown_root-3 (3/8 combos) →  hidden-when captionLayout=label, numberOfMonths=1
- sr-only-carried-as-hidden: dropdown-4 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- sr-only-carried-as-hidden: dropdown-5 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- literal-text-carried: label-372 = "February" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label-372 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- presence-carried: label-372 (3/8 combos) →  hidden-when captionLayout=label, numberOfMonths=1
- presence-carried: icon-5 (3/8 combos) →  hidden-when captionLayout=label, numberOfMonths=1
- presence-carried: part-0-2-0-0-0-1-0-0 (3/8 combos) →  hidden-when captionLayout=label, numberOfMonths=1
- literal-text-carried: label-373 = "2026" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-373 (2/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown, numberOfMonths=1
- presence-carried: dropdown_root-4 (1/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown-months, captionLayout=dropdown-years, numberOfMonths=1
- sr-only-carried-as-hidden: dropdown-6 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- literal-text-carried: label-475 = "2026" (no text prop sample matches — the mounted value is carried verbatim)
- full-width-carried: label-475 spans its row parent's content width in every enabled combo → layout.grow (geometry evidence)
- presence-carried: label-475 (1/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown-months, captionLayout=dropdown-years, numberOfMonths=1
- presence-carried: icon-6 (1/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown-months, captionLayout=dropdown-years, numberOfMonths=1
- presence-carried: part-0-2-0-0-1-1-0-0 (1/8 combos) →  hidden-when captionLayout=label, captionLayout=dropdown-months, captionLayout=dropdown-years, numberOfMonths=1
- literal-text-carried: label-476 = "February 2026" (no text prop sample matches — the mounted value is carried verbatim)
- sr-only-carried-as-hidden: label-476 is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- literal-text-carried: label-477 = "February 2026" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-477 (1/8 combos) →  hidden-when captionLayout=dropdown, captionLayout=dropdown-months, captionLayout=dropdown-years, numberOfMonths=1
- table-lowering: month_grid-2 table → flex column, children stretch (row groups stack and fill the table width); element <table> → <div> role="table" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: month_grid-2 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: part-0-2-1-0 table-header-group → flex column, children stretch (row groups stack and fill the table width); element <thead> → <div> role="rowgroup" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: part-0-2-1-0 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: weekdays-2 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: weekdays-2 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-478 = "Su" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-478 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: label-478 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-479 = "Mo" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-479 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: label-479 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-480 = "Tu" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-480 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: label-480 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-481 = "We" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-481 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: label-481 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-482 = "Th" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-482 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: label-482 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-483 = "Fr" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-483 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: label-483 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-484 = "Sa" (no text prop sample matches — the mounted value is carried verbatim)
- table-lowering: label-484 table-cell → flex row (counter axis center, main axis center from text-align:center); element <th> → <div> role="columnheader" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: label-484 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: weeks-2 table-row-group → flex column, children stretch (row groups stack and fill the table width); element <tbody> → <div> role="rowgroup" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: weeks-2 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: week-6 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: week-6 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-37 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-37 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-485 = "1" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-485 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-38 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-38 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-486 = "2" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-486 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-39 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-39 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-487 = "3" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-487 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-40 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-40 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-488 = "4" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-488 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-41 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-41 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-489 = "5" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-489 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-42 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-42 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-490 = "6" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-490 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-43 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-43 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-491 = "7" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-491 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: week-7 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: week-7 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-44 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-44 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-492 = "8" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-492 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-45 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-45 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-493 = "9" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-493 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-46 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-46 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-494 = "10" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-494 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-47 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-47 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-495 = "11" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-495 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-48 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-48 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-496 = "12" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-496 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-49 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-49 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-497 = "13" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-497 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-50 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-50 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-498 = "14" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-498 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: week-8 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: week-8 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-51 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-51 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-499 = "15" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-499 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-52 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-52 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-500 = "16" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-500 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-53 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-53 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-501 = "17" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-501 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-54 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-54 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-502 = "18" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-502 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-55 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-55 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-503 = "19" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-503 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-56 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-56 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-504 = "20" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-504 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-57 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-57 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-505 = "21" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-505 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: week-9 table-row → flex row, children stretch (every cell takes the row height — the table box model's own rule; the cell centers its own content); element <tr> → <div> role="row" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: week-9 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-58 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-58 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-506 = "22" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-506 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-59 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-59 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-507 = "23" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-507 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-60 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-60 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-508 = "24" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-508 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-61 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-61 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-509 = "25" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-509 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-62 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-62 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-510 = "26" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-510 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-63 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-63 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-511 = "27" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-511 (4/8 combos) →  hidden-when numberOfMonths=1
- table-lowering: day-64 table-cell → flex row (counter axis center, main axis center from text-align:center); element <td> → <div> role="cell" (a table element outside a <table> is dropped by the HTML parser — the lowering carries the semantics as ARIA)
- presence-carried: day-64 (4/8 combos) →  hidden-when numberOfMonths=1
- literal-text-carried: label-512 = "28" (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-512 (4/8 combos) →  hidden-when numberOfMonths=1
- **REFUSED**: non-painting-part: day <td> renders NO INK in any combo it appears in (visibility: hidden) and no descendant paints — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)
- **REFUSED**: non-painting-part: day-2 <td> renders NO INK in any combo it appears in (visibility: hidden) and no descendant paints — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)
- **REFUSED**: non-painting-part: day-3 <td> renders NO INK in any combo it appears in (visibility: hidden) and no descendant paints — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)
- **REFUSED**: non-painting-part: day-4 <td> renders NO INK in any combo it appears in (visibility: hidden) and no descendant paints — NOT promoted. A part that paints nowhere must carry the fact that hides it (declared display:none / an opacity token) or be refused by name; neither `visibility` nor `content-visibility` has a carried spelling, so promoting this box would put a VISIBLE frame on the canvas that the browser draws nowhere (the general form of the SVG <title> defect)
- **REFUSED**: part-presence-uncorrelated: day-36 present in 0/8 enabled combos and the presence set does not factor per-axis (nor does its ABSENCE — the round-5c complement spelling was tried) — part NOT promoted (named refusal; a phantom always-drawn part would be worse)

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 5 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-border-horizontal-spacing (day, day-10, day-11, day-12, day-13, day-14, day-15, day-16, day-17, day-18, day-19, day-2, day-20, day-21, day-22, day-23, day-24, day-25, day-26, day-27, day-28, day-29, day-3, day-30, day-31, day-32, day-33, day-34, day-35, day-36, day-37, day-38, day-39, day-4, day-40, day-41, day-42, day-43, day-44, day-45, day-46, day-47, day-48, day-49, day-5, day-50, day-51, day-52, day-53, day-54, day-55, day-56, day-57, day-58, day-59, day-6, day-60, day-61, day-62, day-63, day-64, day-7, day-8, day-9, label-10, label-11, label-12, label-13, label-14, label-15, label-16, label-17, label-18, label-19, label-2, label-20, label-21, label-22, label-23, label-24, label-25, label-258, label-26, label-27, label-28, label-29, label-3, label-30, label-31, label-32, label-33, label-34, label-35, label-36, label-37, label-38, label-39, label-4, label-478, label-479, label-480, label-481, label-482, label-483, label-484, label-485, label-486, label-487, label-488, label-489, label-490, label-491, label-492, label-493, label-494, label-495, label-496, label-497, label-498, label-499, label-5, label-500, label-501, label-502, label-503, label-504, label-505, label-506, label-507, label-508, label-509, label-510, label-511, label-512, label-6, label-7, label-8, label-9, month_grid, month_grid-2, part-0-1-1-0, part-0-2-1-0, week, week-2, week-3, week-4, week-5, week-6, week-7, week-8, week-9, weekdays, weekdays-2, weeks, weeks-2); -webkit-border-vertical-spacing (day, day-10, day-11, day-12, day-13, day-14, day-15, day-16, day-17, day-18, day-19, day-2, day-20, day-21, day-22, day-23, day-24, day-25, day-26, day-27, day-28, day-29, day-3, day-30, day-31, day-32, day-33, day-34, day-35, day-36, day-37, day-38, day-39, day-4, day-40, day-41, day-42, day-43, day-44, day-45, day-46, day-47, day-48, day-49, day-5, day-50, day-51, day-52, day-53, day-54, day-55, day-56, day-57, day-58, day-59, day-6, day-60, day-61, day-62, day-63, day-64, day-7, day-8, day-9, label-10, label-11, label-12, label-13, label-14, label-15, label-16, label-17, label-18, label-19, label-2, label-20, label-21, label-22, label-23, label-24, label-25, label-258, label-26, label-27, label-28, label-29, label-3, label-30, label-31, label-32, label-33, label-34, label-35, label-36, label-37, label-38, label-39, label-4, label-478, label-479, label-480, label-481, label-482, label-483, label-484, label-485, label-486, label-487, label-488, label-489, label-490, label-491, label-492, label-493, label-494, label-495, label-496, label-497, label-498, label-499, label-5, label-500, label-501, label-502, label-503, label-504, label-505, label-506, label-507, label-508, label-509, label-510, label-511, label-512, label-6, label-7, label-8, label-9, month_grid, month_grid-2, part-0-1-1-0, part-0-2-1-0, week, week-2, week-3, week-4, week-5, week-6, week-7, week-8, week-9, weekdays, weekdays-2, weeks, weeks-2); -webkit-locale (button_next, button_previous, day, day-10, day-11, day-12, day-13, day-14, day-15, day-16, day-17, day-18, day-19, day-2, day-20, day-21, day-22, day-23, day-24, day-25, day-26, day-27, day-28, day-29, day-3, day-30, day-31, day-32, day-33, day-34, day-35, day-36, day-37, day-38, day-39, day-4, day-40, day-41, day-42, day-43, day-44, day-45, day-46, day-47, day-48, day-49, day-5, day-50, day-51, day-52, day-53, day-54, day-55, day-56, day-57, day-58, day-59, day-6, day-60, day-61, day-62, day-63, day-64, day-7, day-8, day-9, dropdown, dropdown-2, dropdown-3, dropdown-4, dropdown-5, dropdown-6, dropdown_root, dropdown_root-2, dropdown_root-3, dropdown_root-4, dropdowns, dropdowns-2, icon, icon-2, icon-3, icon-4, icon-5, icon-6, label, label-10, label-100, label-101, label-102, label-103, label-104, label-105, label-106, label-107, label-108, label-109, label-11, label-110, label-111, label-112, label-113, label-114, label-115, label-116, label-117, label-118, label-119, label-12, label-120, label-121, label-122, label-123, label-124, label-125, label-126, label-127, label-128, label-129, label-13, label-130, label-131, label-132, label-133, label-134, label-135, label-136, label-137, label-138, label-139, label-14, label-140, label-141, label-142, label-143, label-144, label-145, label-146, label-147, label-148, label-149, label-15, label-150, label-151, label-152, label-153, label-154, label-155, label-156, label-157, label-158, label-159, label-16, label-160, label-161, label-162, label-163, label-164, label-165, label-166, label-167, label-168, label-169, label-17, label-170, label-171, label-172, label-173, label-174, label-175, label-176, label-177, label-178, label-179, label-18, label-180, label-181, label-182, label-183, label-184, label-185, label-186, label-187, label-188, label-189, label-19, label-190, label-191, label-192, label-193, label-194, label-195, label-196, label-197, label-198, label-199, label-2, label-20, label-200, label-201, label-202, label-203, label-204, label-205, label-206, label-207, label-208, label-209, label-21, label-210, label-211, label-212, label-213, label-214, label-215, label-216, label-217, label-218, label-219, label-22, label-220, label-221, label-222, label-223, label-224, label-225, label-226, label-227, label-228, label-229, label-23, label-230, label-231, label-232, label-233, label-234, label-235, label-236, label-237, label-238, label-239, label-24, label-240, label-241, label-242, label-243, label-244, label-245, label-246, label-247, label-248, label-249, label-25, label-250, label-251, label-252, label-253, label-254, label-255, label-256, label-257, label-258, label-259, label-26, label-260, label-261, label-262, label-263, label-264, label-265, label-266, label-267, label-268, label-269, label-27, label-270, label-271, label-272, label-273, label-274, label-275, label-276, label-277, label-278, label-279, label-28, label-280, label-281, label-282, label-283, label-284, label-285, label-286, label-287, label-288, label-289, label-29, label-290, label-291, label-292, label-293, label-294, label-295, label-296, label-297, label-298, label-299, label-3, label-30, label-300, label-301, label-302, label-303, label-304, label-305, label-306, label-307, label-308, label-309, label-31, label-310, label-311, label-312, label-313, label-314, label-315, label-316, label-317, label-318, label-319, label-32, label-320, label-321, label-322, label-323, label-324, label-325, label-326, label-327, label-328, label-329, label-33, label-330, label-331, label-332, label-333, label-334, label-335, label-336, label-337, label-338, label-339, label-34, label-340, label-341, label-342, label-343, label-344, label-345, label-346, label-347, label-348, label-349, label-35, label-350, label-351, label-352, label-353, label-354, label-355, label-356, label-357, label-358, label-359, label-36, label-360, label-361, label-362, label-363, label-364, label-365, label-366, label-367, label-368, label-369, label-37, label-370, label-371, label-372, label-373, label-374, label-375, label-376, label-377, label-378, label-379, label-38, label-380, label-381, label-382, label-383, label-384, label-385, label-386, label-387, label-388, label-389, label-39, label-390, label-391, label-392, label-393, label-394, label-395, label-396, label-397, label-398, label-399, label-4, label-40, label-400, label-401, label-402, label-403, label-404, label-405, label-406, label-407, label-408, label-409, label-41, label-410, label-411, label-412, label-413, label-414, label-415, label-416, label-417, label-418, label-419, label-42, label-420, label-421, label-422, label-423, label-424, label-425, label-426, label-427, label-428, label-429, label-43, label-430, label-431, label-432, label-433, label-434, label-435, label-436, label-437, label-438, label-439, label-44, label-440, label-441, label-442, label-443, label-444, label-445, label-446, label-447, label-448, label-449, label-45, label-450, label-451, label-452, label-453, label-454, label-455, label-456, label-457, label-458, label-459, label-46, label-460, label-461, label-462, label-463, label-464, label-465, label-466, label-467, label-468, label-469, label-47, label-470, label-471, label-472, label-473, label-474, label-475, label-476, label-477, label-478, label-479, label-48, label-480, label-481, label-482, label-483, label-484, label-485, label-486, label-487, label-488, label-489, label-49, label-490, label-491, label-492, label-493, label-494, label-495, label-496, label-497, label-498, label-499, label-5, label-50, label-500, label-501, label-502, label-503, label-504, label-505, label-506, label-507, label-508, label-509, label-51, label-510, label-511, label-512, label-52, label-53, label-54, label-55, label-56, label-57, label-58, label-59, label-6, label-60, label-61, label-62, label-63, label-64, label-65, label-66, label-67, label-68, label-69, label-7, label-70, label-71, label-72, label-73, label-74, label-75, label-76, label-77, label-78, label-79, label-8, label-80, label-81, label-82, label-83, label-84, label-85, label-86, label-87, label-88, label-89, label-9, label-90, label-91, label-92, label-93, label-94, label-95, label-96, label-97, label-98, label-99, month, month-2, month_caption, month_caption-2, month_grid, month_grid-2, months, nav, part-0-0-0-0-0, part-0-0-1-0-0, part-0-1-0-0-0-1-0-0, part-0-1-0-0-1-1-0-0, part-0-1-1-0, part-0-2-0-0-0-1-0-0, part-0-2-0-0-1-1-0-0, part-0-2-1-0, root, week, week-2, week-3, week-4, week-5, week-6, week-7, week-8, week-9, weekdays, weekdays-2, weeks, weeks-2); -webkit-text-fill-color (day, day-19, day-2, day-3, day-4, label-23); -webkit-text-stroke-color (day, day-19, day-2, day-3, day-4, label-23)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **65** → WITH folding: **66** (56 folded channels; every fold a named receipt).

- day.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-3.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-4.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- day-19.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-23.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-23.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-23.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-23.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-23.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-23.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **66** · bindings: 2081 base + 2 state
- shape: 2074 uniform · 9 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **673**
  - root.box-sizing = `border-box`
  - root.position = `relative`
  - months.box-sizing = `border-box`
  - months.max-width = `fit-content`
  - months.position = `relative`
  - nav.box-sizing = `border-box`
  - button_previous.appearance = `none`
  - button_previous.border-bottom-style = `none`
  - button_previous.border-left-style = `none`
  - button_previous.border-right-style = `none`
  - button_previous.border-top-style = `none`
  - button_previous.cursor = `pointer`
  - button_previous.font-family = `Times`
  - button_previous.position = `relative`
  - icon.box-sizing = `border-box`
  - icon.cursor = `pointer`
  - icon.overflow-x = `hidden`
  - icon.overflow-y = `hidden`
  - icon.text-align = `center`
  - part-0-0-0-0-0.box-sizing = `border-box`
  - part-0-0-0-0-0.cursor = `pointer`
  - part-0-0-0-0-0.text-align = `center`
  - button_next.appearance = `none`
  - button_next.border-bottom-style = `none`
  - button_next.border-left-style = `none`
  - button_next.border-right-style = `none`
  - button_next.border-top-style = `none`
  - button_next.cursor = `pointer`
  - button_next.font-family = `Times`
  - button_next.position = `relative`
  - icon-2.box-sizing = `border-box`
  - icon-2.cursor = `pointer`
  - icon-2.overflow-x = `hidden`
  - icon-2.overflow-y = `hidden`
  - icon-2.text-align = `center`
  - part-0-0-1-0-0.box-sizing = `border-box`
  - part-0-0-1-0-0.cursor = `pointer`
  - part-0-0-1-0-0.text-align = `center`
  - month.box-sizing = `border-box`
  - month_caption.box-sizing = `border-box`
  - dropdowns.box-sizing = `border-box`
  - dropdowns.position = `relative`
  - dropdown_root.box-sizing = `border-box`
  - dropdown_root.position = `relative`
  - dropdown.box-sizing = `border-box`
  - dropdown.font-family = `Arial`
  - dropdown.position = `absolute`
  - dropdown.text-wrap-mode = `nowrap`
  - dropdown-2.box-sizing = `border-box`
  - dropdown-2.font-family = `Arial`
  - dropdown-2.position = `absolute`
  - dropdown-2.text-wrap-mode = `nowrap`
  - label-153.box-sizing = `border-box`
  - label-153.font-family = `Times`
  - label-153.position = `relative`
  - label-153.text-wrap-mode = `nowrap`
  - icon-3.box-sizing = `border-box`
  - icon-3.overflow-x = `hidden`
  - icon-3.overflow-y = `hidden`
  - icon-3.text-wrap-mode = `nowrap`
  - part-0-1-0-0-0-1-0-0.box-sizing = `border-box`
  - part-0-1-0-0-0-1-0-0.text-wrap-mode = `nowrap`
  - label-154.box-sizing = `border-box`
  - label-154.font-family = `Times`
  - dropdown_root-2.box-sizing = `border-box`
  - dropdown_root-2.position = `relative`
  - dropdown-3.box-sizing = `border-box`
  - dropdown-3.font-family = `Arial`
  - dropdown-3.position = `absolute`
  - dropdown-3.text-wrap-mode = `nowrap`
  - label-256.box-sizing = `border-box`
  - label-256.font-family = `Times`
  - label-256.position = `relative`
  - label-256.text-wrap-mode = `nowrap`
  - icon-4.box-sizing = `border-box`
  - icon-4.overflow-x = `hidden`
  - icon-4.overflow-y = `hidden`
  - icon-4.text-wrap-mode = `nowrap`
  - part-0-1-0-0-1-1-0-0.box-sizing = `border-box`
  - part-0-1-0-0-1-1-0-0.text-wrap-mode = `nowrap`
  - label-257.box-sizing = `border-box`
  - label-257.font-family = `Times`
  - label-257.overflow-x = `hidden`
  - label-257.overflow-y = `hidden`
  - label-257.position = `absolute`
  - label-257.text-wrap-mode = `nowrap`
  - label.box-sizing = `border-box`
  - label.font-family = `Times`
  - label.position = `relative`
  - label.text-wrap-mode = `nowrap`
  - month_grid.box-sizing = `border-box`
  - part-0-1-1-0.box-sizing = `border-box`
  - weekdays.box-sizing = `border-box`
  - label-2.box-sizing = `border-box`
  - label-2.font-family = `Times`
  - label-2.text-align = `center`
  - label-3.box-sizing = `border-box`
  - label-3.font-family = `Times`
  - label-3.text-align = `center`
  - label-4.box-sizing = `border-box`
  - label-4.font-family = `Times`
  - label-4.text-align = `center`
  - label-5.box-sizing = `border-box`
  - label-5.font-family = `Times`
  - label-5.text-align = `center`
  - label-6.box-sizing = `border-box`
  - label-6.font-family = `Times`
  - label-6.text-align = `center`
  - label-7.box-sizing = `border-box`
  - label-7.font-family = `Times`
  - label-7.text-align = `center`
  - label-8.box-sizing = `border-box`
  - label-8.font-family = `Times`
  - label-8.text-align = `center`
  - weeks.box-sizing = `border-box`
  - week.box-sizing = `border-box`
  - day-5.box-sizing = `border-box`
  - day-5.text-align = `center`
  - label-9.border-bottom-style = `solid`
  - label-9.border-left-style = `solid`
  - label-9.border-right-style = `solid`
  - label-9.border-top-style = `solid`
  - label-9.cursor = `pointer`
  - label-9.font-family = `Times`
  - day-6.box-sizing = `border-box`
  - day-6.text-align = `center`
  - label-10.border-bottom-style = `solid`
  - label-10.border-left-style = `solid`
  - label-10.border-right-style = `solid`
  - label-10.border-top-style = `solid`
  - label-10.cursor = `pointer`
  - label-10.font-family = `Times`
  - day-7.box-sizing = `border-box`
  - day-7.text-align = `center`
  - label-11.border-bottom-style = `solid`
  - label-11.border-left-style = `solid`
  - label-11.border-right-style = `solid`
  - label-11.border-top-style = `solid`
  - label-11.cursor = `pointer`
  - label-11.font-family = `Times`
  - week-2.box-sizing = `border-box`
  - day-8.box-sizing = `border-box`
  - day-8.text-align = `center`
  - label-12.border-bottom-style = `solid`
  - label-12.border-left-style = `solid`
  - label-12.border-right-style = `solid`
  - label-12.border-top-style = `solid`
  - label-12.cursor = `pointer`
  - label-12.font-family = `Times`
  - day-9.box-sizing = `border-box`
  - day-9.text-align = `center`
  - label-13.border-bottom-style = `solid`
  - label-13.border-left-style = `solid`
  - label-13.border-right-style = `solid`
  - label-13.border-top-style = `solid`
  - label-13.cursor = `pointer`
  - label-13.font-family = `Times`
  - day-10.box-sizing = `border-box`
  - day-10.text-align = `center`
  - label-14.border-bottom-style = `solid`
  - label-14.border-left-style = `solid`
  - label-14.border-right-style = `solid`
  - label-14.border-top-style = `solid`
  - label-14.cursor = `pointer`
  - label-14.font-family = `Times`
  - day-11.box-sizing = `border-box`
  - day-11.text-align = `center`
  - label-15.border-bottom-style = `solid`
  - label-15.border-left-style = `solid`
  - label-15.border-right-style = `solid`
  - label-15.border-top-style = `solid`
  - label-15.cursor = `pointer`
  - label-15.font-family = `Times`
  - day-12.box-sizing = `border-box`
  - day-12.text-align = `center`
  - label-16.border-bottom-style = `solid`
  - label-16.border-left-style = `solid`
  - label-16.border-right-style = `solid`
  - label-16.border-top-style = `solid`
  - label-16.cursor = `pointer`
  - label-16.font-family = `Times`
  - day-13.box-sizing = `border-box`
  - day-13.text-align = `center`
  - label-17.border-bottom-style = `solid`
  - label-17.border-left-style = `solid`
  - label-17.border-right-style = `solid`
  - label-17.border-top-style = `solid`
  - label-17.cursor = `pointer`
  - label-17.font-family = `Times`
  - day-14.box-sizing = `border-box`
  - day-14.text-align = `center`
  - label-18.border-bottom-style = `solid`
  - label-18.border-left-style = `solid`
  - label-18.border-right-style = `solid`
  - label-18.border-top-style = `solid`
  - label-18.cursor = `pointer`
  - label-18.font-family = `Times`
  - week-3.box-sizing = `border-box`
  - day-15.box-sizing = `border-box`
  - day-15.text-align = `center`
  - label-19.border-bottom-style = `solid`
  - label-19.border-left-style = `solid`
  - label-19.border-right-style = `solid`
  - label-19.border-top-style = `solid`
  - label-19.cursor = `pointer`
  - label-19.font-family = `Times`
  - day-16.box-sizing = `border-box`
  - day-16.text-align = `center`
  - label-20.border-bottom-style = `solid`
  - label-20.border-left-style = `solid`
  - label-20.border-right-style = `solid`
  - label-20.border-top-style = `solid`
  - label-20.cursor = `pointer`
  - label-20.font-family = `Times`
  - day-17.box-sizing = `border-box`
  - day-17.text-align = `center`
  - label-21.border-bottom-style = `solid`
  - label-21.border-left-style = `solid`
  - label-21.border-right-style = `solid`
  - label-21.border-top-style = `solid`
  - label-21.cursor = `pointer`
  - label-21.font-family = `Times`
  - day-18.box-sizing = `border-box`
  - day-18.text-align = `center`
  - label-22.border-bottom-style = `solid`
  - label-22.border-left-style = `solid`
  - label-22.border-right-style = `solid`
  - label-22.border-top-style = `solid`
  - label-22.cursor = `pointer`
  - label-22.font-family = `Times`
  - day-19.box-sizing = `border-box`
  - day-19.text-align = `center`
  - label-23.border-bottom-style = `solid`
  - label-23.border-left-style = `solid`
  - label-23.border-right-style = `solid`
  - label-23.border-top-style = `solid`
  - label-23.cursor = `pointer`
  - label-23.font-family = `Times`
  - day-20.box-sizing = `border-box`
  - day-20.text-align = `center`
  - label-24.border-bottom-style = `solid`
  - label-24.border-left-style = `solid`
  - label-24.border-right-style = `solid`
  - label-24.border-top-style = `solid`
  - label-24.cursor = `pointer`
  - label-24.font-family = `Times`
  - day-21.box-sizing = `border-box`
  - day-21.text-align = `center`
  - label-25.border-bottom-style = `solid`
  - label-25.border-left-style = `solid`
  - label-25.border-right-style = `solid`
  - label-25.border-top-style = `solid`
  - label-25.cursor = `pointer`
  - label-25.font-family = `Times`
  - week-4.box-sizing = `border-box`
  - day-22.box-sizing = `border-box`
  - day-22.text-align = `center`
  - label-26.border-bottom-style = `solid`
  - label-26.border-left-style = `solid`
  - label-26.border-right-style = `solid`
  - label-26.border-top-style = `solid`
  - label-26.cursor = `pointer`
  - label-26.font-family = `Times`
  - day-23.box-sizing = `border-box`
  - day-23.text-align = `center`
  - label-27.border-bottom-style = `solid`
  - label-27.border-left-style = `solid`
  - label-27.border-right-style = `solid`
  - label-27.border-top-style = `solid`
  - label-27.cursor = `pointer`
  - label-27.font-family = `Times`
  - day-24.box-sizing = `border-box`
  - day-24.text-align = `center`
  - label-28.border-bottom-style = `solid`
  - label-28.border-left-style = `solid`
  - label-28.border-right-style = `solid`
  - label-28.border-top-style = `solid`
  - label-28.cursor = `pointer`
  - label-28.font-family = `Times`
  - day-25.box-sizing = `border-box`
  - day-25.text-align = `center`
  - label-29.border-bottom-style = `solid`
  - label-29.border-left-style = `solid`
  - label-29.border-right-style = `solid`
  - label-29.border-top-style = `solid`
  - label-29.cursor = `pointer`
  - label-29.font-family = `Times`
  - day-26.box-sizing = `border-box`
  - day-26.text-align = `center`
  - label-30.border-bottom-style = `solid`
  - label-30.border-left-style = `solid`
  - label-30.border-right-style = `solid`
  - label-30.border-top-style = `solid`
  - label-30.cursor = `pointer`
  - label-30.font-family = `Times`
  - day-27.box-sizing = `border-box`
  - day-27.text-align = `center`
  - label-31.border-bottom-style = `solid`
  - label-31.border-left-style = `solid`
  - label-31.border-right-style = `solid`
  - label-31.border-top-style = `solid`
  - label-31.cursor = `pointer`
  - label-31.font-family = `Times`
  - day-28.box-sizing = `border-box`
  - day-28.text-align = `center`
  - label-32.border-bottom-style = `solid`
  - label-32.border-left-style = `solid`
  - label-32.border-right-style = `solid`
  - label-32.border-top-style = `solid`
  - label-32.cursor = `pointer`
  - label-32.font-family = `Times`
  - week-5.box-sizing = `border-box`
  - day-29.box-sizing = `border-box`
  - day-29.text-align = `center`
  - label-33.border-bottom-style = `solid`
  - label-33.border-left-style = `solid`
  - label-33.border-right-style = `solid`
  - label-33.border-top-style = `solid`
  - label-33.cursor = `pointer`
  - label-33.font-family = `Times`
  - day-30.box-sizing = `border-box`
  - day-30.text-align = `center`
  - label-34.border-bottom-style = `solid`
  - label-34.border-left-style = `solid`
  - label-34.border-right-style = `solid`
  - label-34.border-top-style = `solid`
  - label-34.cursor = `pointer`
  - label-34.font-family = `Times`
  - day-31.box-sizing = `border-box`
  - day-31.text-align = `center`
  - label-35.border-bottom-style = `solid`
  - label-35.border-left-style = `solid`
  - label-35.border-right-style = `solid`
  - label-35.border-top-style = `solid`
  - label-35.cursor = `pointer`
  - label-35.font-family = `Times`
  - day-32.box-sizing = `border-box`
  - day-32.text-align = `center`
  - label-36.border-bottom-style = `solid`
  - label-36.border-left-style = `solid`
  - label-36.border-right-style = `solid`
  - label-36.border-top-style = `solid`
  - label-36.cursor = `pointer`
  - label-36.font-family = `Times`
  - day-33.box-sizing = `border-box`
  - day-33.text-align = `center`
  - label-37.border-bottom-style = `solid`
  - label-37.border-left-style = `solid`
  - label-37.border-right-style = `solid`
  - label-37.border-top-style = `solid`
  - label-37.cursor = `pointer`
  - label-37.font-family = `Times`
  - day-34.box-sizing = `border-box`
  - day-34.text-align = `center`
  - label-38.border-bottom-style = `solid`
  - label-38.border-left-style = `solid`
  - label-38.border-right-style = `solid`
  - label-38.border-top-style = `solid`
  - label-38.cursor = `pointer`
  - label-38.font-family = `Times`
  - day-35.box-sizing = `border-box`
  - day-35.text-align = `center`
  - label-39.border-bottom-style = `solid`
  - label-39.border-left-style = `solid`
  - label-39.border-right-style = `solid`
  - label-39.border-top-style = `solid`
  - label-39.cursor = `pointer`
  - label-39.font-family = `Times`
  - month-2.box-sizing = `border-box`
  - month_caption-2.box-sizing = `border-box`
  - dropdowns-2.box-sizing = `border-box`
  - dropdowns-2.position = `relative`
  - dropdown_root-3.box-sizing = `border-box`
  - dropdown_root-3.position = `relative`
  - dropdown-4.box-sizing = `border-box`
  - dropdown-4.font-family = `Arial`
  - dropdown-4.position = `absolute`
  - dropdown-4.text-wrap-mode = `nowrap`
  - dropdown-5.box-sizing = `border-box`
  - dropdown-5.font-family = `Arial`
  - dropdown-5.position = `absolute`
  - dropdown-5.text-wrap-mode = `nowrap`
  - label-372.box-sizing = `border-box`
  - label-372.font-family = `Times`
  - label-372.position = `relative`
  - label-372.text-wrap-mode = `nowrap`
  - icon-5.box-sizing = `border-box`
  - icon-5.overflow-x = `hidden`
  - icon-5.overflow-y = `hidden`
  - icon-5.text-wrap-mode = `nowrap`
  - part-0-2-0-0-0-1-0-0.box-sizing = `border-box`
  - part-0-2-0-0-0-1-0-0.text-wrap-mode = `nowrap`
  - label-373.box-sizing = `border-box`
  - label-373.font-family = `Times`
  - dropdown_root-4.box-sizing = `border-box`
  - dropdown_root-4.position = `relative`
  - dropdown-6.box-sizing = `border-box`
  - dropdown-6.font-family = `Arial`
  - dropdown-6.position = `absolute`
  - dropdown-6.text-wrap-mode = `nowrap`
  - label-475.box-sizing = `border-box`
  - label-475.font-family = `Times`
  - label-475.position = `relative`
  - label-475.text-wrap-mode = `nowrap`
  - icon-6.box-sizing = `border-box`
  - icon-6.overflow-x = `hidden`
  - icon-6.overflow-y = `hidden`
  - icon-6.text-wrap-mode = `nowrap`
  - part-0-2-0-0-1-1-0-0.box-sizing = `border-box`
  - part-0-2-0-0-1-1-0-0.text-wrap-mode = `nowrap`
  - label-476.box-sizing = `border-box`
  - label-476.font-family = `Times`
  - label-476.overflow-x = `hidden`
  - label-476.overflow-y = `hidden`
  - label-476.position = `absolute`
  - label-476.text-wrap-mode = `nowrap`
  - label-477.box-sizing = `border-box`
  - label-477.font-family = `Times`
  - label-477.position = `relative`
  - label-477.text-wrap-mode = `nowrap`
  - month_grid-2.box-sizing = `border-box`
  - part-0-2-1-0.box-sizing = `border-box`
  - weekdays-2.box-sizing = `border-box`
  - label-478.box-sizing = `border-box`
  - label-478.font-family = `Times`
  - label-478.text-align = `center`
  - label-479.box-sizing = `border-box`
  - label-479.font-family = `Times`
  - label-479.text-align = `center`
  - label-480.box-sizing = `border-box`
  - label-480.font-family = `Times`
  - label-480.text-align = `center`
  - label-481.box-sizing = `border-box`
  - label-481.font-family = `Times`
  - label-481.text-align = `center`
  - label-482.box-sizing = `border-box`
  - label-482.font-family = `Times`
  - label-482.text-align = `center`
  - label-483.box-sizing = `border-box`
  - label-483.font-family = `Times`
  - label-483.text-align = `center`
  - label-484.box-sizing = `border-box`
  - label-484.font-family = `Times`
  - label-484.text-align = `center`
  - weeks-2.box-sizing = `border-box`
  - week-6.box-sizing = `border-box`
  - day-37.box-sizing = `border-box`
  - day-37.text-align = `center`
  - label-485.border-bottom-style = `solid`
  - label-485.border-left-style = `solid`
  - label-485.border-right-style = `solid`
  - label-485.border-top-style = `solid`
  - label-485.cursor = `pointer`
  - label-485.font-family = `Times`
  - day-38.box-sizing = `border-box`
  - day-38.text-align = `center`
  - label-486.border-bottom-style = `solid`
  - label-486.border-left-style = `solid`
  - label-486.border-right-style = `solid`
  - label-486.border-top-style = `solid`
  - label-486.cursor = `pointer`
  - label-486.font-family = `Times`
  - day-39.box-sizing = `border-box`
  - day-39.text-align = `center`
  - label-487.border-bottom-style = `solid`
  - label-487.border-left-style = `solid`
  - label-487.border-right-style = `solid`
  - label-487.border-top-style = `solid`
  - label-487.cursor = `pointer`
  - label-487.font-family = `Times`
  - day-40.box-sizing = `border-box`
  - day-40.text-align = `center`
  - label-488.border-bottom-style = `solid`
  - label-488.border-left-style = `solid`
  - label-488.border-right-style = `solid`
  - label-488.border-top-style = `solid`
  - label-488.cursor = `pointer`
  - label-488.font-family = `Times`
  - day-41.box-sizing = `border-box`
  - day-41.text-align = `center`
  - label-489.border-bottom-style = `solid`
  - label-489.border-left-style = `solid`
  - label-489.border-right-style = `solid`
  - label-489.border-top-style = `solid`
  - label-489.cursor = `pointer`
  - label-489.font-family = `Times`
  - day-42.box-sizing = `border-box`
  - day-42.text-align = `center`
  - label-490.border-bottom-style = `solid`
  - label-490.border-left-style = `solid`
  - label-490.border-right-style = `solid`
  - label-490.border-top-style = `solid`
  - label-490.cursor = `pointer`
  - label-490.font-family = `Times`
  - day-43.box-sizing = `border-box`
  - day-43.text-align = `center`
  - label-491.border-bottom-style = `solid`
  - label-491.border-left-style = `solid`
  - label-491.border-right-style = `solid`
  - label-491.border-top-style = `solid`
  - label-491.cursor = `pointer`
  - label-491.font-family = `Times`
  - week-7.box-sizing = `border-box`
  - day-44.box-sizing = `border-box`
  - day-44.text-align = `center`
  - label-492.border-bottom-style = `solid`
  - label-492.border-left-style = `solid`
  - label-492.border-right-style = `solid`
  - label-492.border-top-style = `solid`
  - label-492.cursor = `pointer`
  - label-492.font-family = `Times`
  - day-45.box-sizing = `border-box`
  - day-45.text-align = `center`
  - label-493.border-bottom-style = `solid`
  - label-493.border-left-style = `solid`
  - label-493.border-right-style = `solid`
  - label-493.border-top-style = `solid`
  - label-493.cursor = `pointer`
  - label-493.font-family = `Times`
  - day-46.box-sizing = `border-box`
  - day-46.text-align = `center`
  - label-494.border-bottom-style = `solid`
  - label-494.border-left-style = `solid`
  - label-494.border-right-style = `solid`
  - label-494.border-top-style = `solid`
  - label-494.cursor = `pointer`
  - label-494.font-family = `Times`
  - day-47.box-sizing = `border-box`
  - day-47.text-align = `center`
  - label-495.border-bottom-style = `solid`
  - label-495.border-left-style = `solid`
  - label-495.border-right-style = `solid`
  - label-495.border-top-style = `solid`
  - label-495.cursor = `pointer`
  - label-495.font-family = `Times`
  - day-48.box-sizing = `border-box`
  - day-48.text-align = `center`
  - label-496.border-bottom-style = `solid`
  - label-496.border-left-style = `solid`
  - label-496.border-right-style = `solid`
  - label-496.border-top-style = `solid`
  - label-496.cursor = `pointer`
  - label-496.font-family = `Times`
  - day-49.box-sizing = `border-box`
  - day-49.text-align = `center`
  - label-497.border-bottom-style = `solid`
  - label-497.border-left-style = `solid`
  - label-497.border-right-style = `solid`
  - label-497.border-top-style = `solid`
  - label-497.cursor = `pointer`
  - label-497.font-family = `Times`
  - day-50.box-sizing = `border-box`
  - day-50.text-align = `center`
  - label-498.border-bottom-style = `solid`
  - label-498.border-left-style = `solid`
  - label-498.border-right-style = `solid`
  - label-498.border-top-style = `solid`
  - label-498.cursor = `pointer`
  - label-498.font-family = `Times`
  - week-8.box-sizing = `border-box`
  - day-51.box-sizing = `border-box`
  - day-51.text-align = `center`
  - label-499.border-bottom-style = `solid`
  - label-499.border-left-style = `solid`
  - label-499.border-right-style = `solid`
  - label-499.border-top-style = `solid`
  - label-499.cursor = `pointer`
  - label-499.font-family = `Times`
  - day-52.box-sizing = `border-box`
  - day-52.text-align = `center`
  - label-500.border-bottom-style = `solid`
  - label-500.border-left-style = `solid`
  - label-500.border-right-style = `solid`
  - label-500.border-top-style = `solid`
  - label-500.cursor = `pointer`
  - label-500.font-family = `Times`
  - day-53.box-sizing = `border-box`
  - day-53.text-align = `center`
  - label-501.border-bottom-style = `solid`
  - label-501.border-left-style = `solid`
  - label-501.border-right-style = `solid`
  - label-501.border-top-style = `solid`
  - label-501.cursor = `pointer`
  - label-501.font-family = `Times`
  - day-54.box-sizing = `border-box`
  - day-54.text-align = `center`
  - label-502.border-bottom-style = `solid`
  - label-502.border-left-style = `solid`
  - label-502.border-right-style = `solid`
  - label-502.border-top-style = `solid`
  - label-502.cursor = `pointer`
  - label-502.font-family = `Times`
  - day-55.box-sizing = `border-box`
  - day-55.text-align = `center`
  - label-503.border-bottom-style = `solid`
  - label-503.border-left-style = `solid`
  - label-503.border-right-style = `solid`
  - label-503.border-top-style = `solid`
  - label-503.cursor = `pointer`
  - label-503.font-family = `Times`
  - day-56.box-sizing = `border-box`
  - day-56.text-align = `center`
  - label-504.border-bottom-style = `solid`
  - label-504.border-left-style = `solid`
  - label-504.border-right-style = `solid`
  - label-504.border-top-style = `solid`
  - label-504.cursor = `pointer`
  - label-504.font-family = `Times`
  - day-57.box-sizing = `border-box`
  - day-57.text-align = `center`
  - label-505.border-bottom-style = `solid`
  - label-505.border-left-style = `solid`
  - label-505.border-right-style = `solid`
  - label-505.border-top-style = `solid`
  - label-505.cursor = `pointer`
  - label-505.font-family = `Times`
  - week-9.box-sizing = `border-box`
  - day-58.box-sizing = `border-box`
  - day-58.text-align = `center`
  - label-506.border-bottom-style = `solid`
  - label-506.border-left-style = `solid`
  - label-506.border-right-style = `solid`
  - label-506.border-top-style = `solid`
  - label-506.cursor = `pointer`
  - label-506.font-family = `Times`
  - day-59.box-sizing = `border-box`
  - day-59.text-align = `center`
  - label-507.border-bottom-style = `solid`
  - label-507.border-left-style = `solid`
  - label-507.border-right-style = `solid`
  - label-507.border-top-style = `solid`
  - label-507.cursor = `pointer`
  - label-507.font-family = `Times`
  - day-60.box-sizing = `border-box`
  - day-60.text-align = `center`
  - label-508.border-bottom-style = `solid`
  - label-508.border-left-style = `solid`
  - label-508.border-right-style = `solid`
  - label-508.border-top-style = `solid`
  - label-508.cursor = `pointer`
  - label-508.font-family = `Times`
  - day-61.box-sizing = `border-box`
  - day-61.text-align = `center`
  - label-509.border-bottom-style = `solid`
  - label-509.border-left-style = `solid`
  - label-509.border-right-style = `solid`
  - label-509.border-top-style = `solid`
  - label-509.cursor = `pointer`
  - label-509.font-family = `Times`
  - day-62.box-sizing = `border-box`
  - day-62.text-align = `center`
  - label-510.border-bottom-style = `solid`
  - label-510.border-left-style = `solid`
  - label-510.border-right-style = `solid`
  - label-510.border-top-style = `solid`
  - label-510.cursor = `pointer`
  - label-510.font-family = `Times`
  - day-63.box-sizing = `border-box`
  - day-63.text-align = `center`
  - label-511.border-bottom-style = `solid`
  - label-511.border-left-style = `solid`
  - label-511.border-right-style = `solid`
  - label-511.border-top-style = `solid`
  - label-511.cursor = `pointer`
  - label-511.font-family = `Times`
  - day-64.box-sizing = `border-box`
  - day-64.text-align = `center`
  - label-512.border-bottom-style = `solid`
  - label-512.border-left-style = `solid`
  - label-512.border-right-style = `solid`
  - label-512.border-top-style = `solid`
  - label-512.cursor = `pointer`
  - label-512.font-family = `Times`
- state declared facts: **1**
  - [focus-visible] button_previous.outline-style = `auto`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **535**
  - nav.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <nav>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <nav> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - icon.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `content-box`, 1 distinct value(s))
  - part-0-0-0-0-0.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - part-0-0-0-0-0.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - icon-2.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `content-box`, 1 distinct value(s))
  - part-0-0-1-0-0.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - part-0-0-1-0-0.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - month_caption.align-content — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `center`, 1 distinct value(s))
  - dropdown.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `center`, 1 distinct value(s))
  - dropdown.white-space-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `preserve`, 1 distinct value(s))
  - dropdown-2.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `center`, 1 distinct value(s))
  - dropdown-2.white-space-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `preserve`, 1 distinct value(s))
  - label-153.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - icon-3.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `content-box`, 1 distinct value(s))
  - part-0-1-0-0-0-1-0-0.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - part-0-1-0-0-0-1-0-0.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - label-154.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - dropdown-3.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `center`, 1 distinct value(s))
  - dropdown-3.white-space-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `preserve`, 1 distinct value(s))
  - label-256.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - icon-4.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `content-box`, 1 distinct value(s))
  - part-0-1-0-0-1-1-0-0.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - part-0-1-0-0-1-1-0-0.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - label-257.clip — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `rect(0px, 0px, 0px, 0px)`, 1 distinct value(s))
  - label-257.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - label.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - month_grid.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <table>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <table> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - month_grid.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table`, 1 distinct value(s))
  - month_grid.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <table>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <table> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - part-0-1-1-0.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <thead>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <thead> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - part-0-1-1-0.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-header-group`, 1 distinct value(s))
  - part-0-1-1-0.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <thead>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <thead> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - part-0-1-1-0.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <thead>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <thead> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - weekdays.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - weekdays.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - weekdays.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - weekdays.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-2.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-3.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-3.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-3.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-3.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-3.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-4.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-4.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-4.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-4.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-4.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-5.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-5.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-5.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-5.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-5.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-6.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-6.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-6.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-6.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-6.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-7.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-7.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-7.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-7.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-7.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-8.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-8.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-8.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-8.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-8.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - weeks.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tbody>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tbody> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - weeks.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row-group`, 1 distinct value(s))
  - weeks.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tbody>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tbody> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - weeks.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tbody>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tbody> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - week.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-5.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-5.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-5.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-5.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-9.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-9.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-6.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-6.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-6.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-6.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-10.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-10.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-7.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-7.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-7.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-7.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-11.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-11.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - week-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-8.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-8.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-8.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-8.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-12.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-12.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-9.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-9.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-9.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-9.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-13.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-13.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-10.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-10.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-10.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-10.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-14.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-14.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-11.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-11.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-11.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-11.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-15.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-15.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-12.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-12.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-12.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-12.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-16.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-16.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-13.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-13.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-13.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-13.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-17.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-17.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-14.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-14.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-14.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-14.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-18.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-18.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - week-3.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week-3.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week-3.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week-3.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-15.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-15.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-15.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-15.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-19.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-19.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-16.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-16.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-16.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-16.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-20.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-20.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-17.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-17.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-17.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-17.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-21.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-21.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-18.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-18.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-18.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-18.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-22.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-22.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-19.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-19.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-19.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-19.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-23.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-23.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-20.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-20.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-20.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-20.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-24.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-24.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-21.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-21.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-21.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-21.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-25.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-25.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - week-4.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week-4.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week-4.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week-4.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-22.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-22.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-22.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-22.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-26.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-26.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-23.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-23.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-23.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-23.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-27.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-27.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-24.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-24.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-24.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-24.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-28.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-28.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-25.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-25.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-25.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-25.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-29.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-29.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-26.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-26.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-26.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-26.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-30.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-30.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-27.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-27.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-27.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-27.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-31.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-31.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-28.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-28.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-28.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-28.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-32.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-32.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - week-5.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week-5.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week-5.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week-5.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-29.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-29.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-29.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-29.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-33.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-33.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-30.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-30.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-30.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-30.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-34.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-34.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-31.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-31.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-31.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-31.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-35.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-35.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-32.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-32.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-32.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-32.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-36.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-36.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-33.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-33.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-33.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-33.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-37.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-37.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-34.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-34.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-34.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-34.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-38.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-38.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-35.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-35.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-35.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-35.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-39.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-39.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - month_caption-2.align-content — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `center`, 1 distinct value(s))
  - dropdown-4.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `center`, 1 distinct value(s))
  - dropdown-4.white-space-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `preserve`, 1 distinct value(s))
  - dropdown-5.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `center`, 1 distinct value(s))
  - dropdown-5.white-space-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `preserve`, 1 distinct value(s))
  - label-372.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - icon-5.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `content-box`, 1 distinct value(s))
  - part-0-2-0-0-0-1-0-0.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - part-0-2-0-0-0-1-0-0.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - label-373.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - dropdown-6.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `center`, 1 distinct value(s))
  - dropdown-6.white-space-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <select>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <select> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `preserve`, 1 distinct value(s))
  - label-475.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - icon-6.overflow-clip-margin — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <svg>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <svg> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `content-box`, 1 distinct value(s))
  - part-0-2-0-0-1-1-0-0.height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - part-0-2-0-0-1-1-0-0.width — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <polygon>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <polygon> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `auto`, 1 distinct value(s))
  - label-476.clip — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `rect(0px, 0px, 0px, 0px)`, 1 distinct value(s))
  - label-476.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - label-477.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - month_grid-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <table>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <table> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - month_grid-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table`, 1 distinct value(s))
  - month_grid-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <table>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <table> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - part-0-2-1-0.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <thead>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <thead> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - part-0-2-1-0.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-header-group`, 1 distinct value(s))
  - part-0-2-1-0.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <thead>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <thead> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - part-0-2-1-0.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <thead>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <thead> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - weekdays-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - weekdays-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - weekdays-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - weekdays-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-478.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-478.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-478.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-478.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-478.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-479.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-479.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-479.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-479.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-479.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-480.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-480.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-480.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-480.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-480.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-481.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-481.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-481.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-481.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-481.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-482.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-482.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-482.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-482.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-482.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-483.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-483.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-483.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-483.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-483.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-484.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - label-484.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - label-484.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `normal`, 1 distinct value(s))
  - label-484.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - label-484.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <th>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <th> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - weeks-2.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tbody>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tbody> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - weeks-2.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row-group`, 1 distinct value(s))
  - weeks-2.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tbody>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tbody> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - weeks-2.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tbody>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tbody> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - week-6.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week-6.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week-6.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week-6.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-37.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-37.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-37.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-37.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-485.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-485.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-38.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-38.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-38.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-38.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-486.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-486.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-39.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-39.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-39.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-39.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-487.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-487.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-40.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-40.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-40.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-40.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-488.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-488.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-41.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-41.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-41.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-41.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-489.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-489.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-42.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-42.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-42.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-42.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-490.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-490.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-43.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-43.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-43.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-43.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-491.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-491.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - week-7.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week-7.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week-7.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week-7.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-44.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-44.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-44.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-44.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-492.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-492.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-45.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-45.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-45.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-45.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-493.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-493.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-46.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-46.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-46.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-46.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-494.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-494.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-47.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-47.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-47.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-47.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-495.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-495.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-48.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-48.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-48.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-48.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-496.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-496.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-49.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-49.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-49.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-49.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-497.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-497.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-50.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-50.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-50.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-50.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-498.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-498.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - week-8.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week-8.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week-8.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week-8.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-51.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-51.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-51.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-51.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-499.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-499.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-52.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-52.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-52.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-52.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-500.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-500.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-53.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-53.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-53.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-53.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-501.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-501.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-54.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-54.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-54.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-54.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-502.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-502.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-55.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-55.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-55.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-55.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-503.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-503.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-56.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-56.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-56.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-56.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-504.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-504.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-57.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-57.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-57.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-57.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-505.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-505.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - week-9.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - week-9.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-row`, 1 distinct value(s))
  - week-9.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - week-9.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <tr>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <tr> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - day-58.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-58.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-58.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-58.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-506.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-506.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-59.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-59.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-59.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-59.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-507.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-507.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-60.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-60.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-60.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-60.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-508.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-508.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-61.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-61.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-61.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-61.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-509.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-509.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-62.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-62.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-62.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-62.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-510.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-510.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-63.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-63.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-63.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-63.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-511.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-511.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
  - day-64.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `collapse`, 1 distinct value(s))
  - day-64.display — declared-channel value outside the bounded grammar — named residue (v15) (sample: `table-cell`, 1 distinct value(s))
  - day-64.unicode-bidi — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `isolate`, 1 distinct value(s))
  - day-64.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <td>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <td> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `middle`, 1 distinct value(s))
  - label-512.border-collapse — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `collapse`, 1 distinct value(s))
  - label-512.line-height — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **2**
  - {"part":"button_previous","channel":"outline-color","state":"focus-visible","ref":"{imported.calendar.button-previous.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"button_previous","channel":"outline-width","state":"focus-visible","ref":"{imported.calendar.button-previous.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **81.394%** (60284/74064 cells; 0/32 combo×state rows fully equal)
- pixel: 0/32 pairs perfect at threshold 0 · 0/32 at the AA point (mean AA 0.091%, max 0.221%; 32/32 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

