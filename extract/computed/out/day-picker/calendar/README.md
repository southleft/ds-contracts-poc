# react-day-picker Calendar — F1 capture measurement

First capture of the held-out config (`extract/computed/configs/day-picker.json`).
Double-run byte-identity is IDENTICAL. Config was not retuned.

- `captured-truth.json` — the ledger the fixture reader proposes from
- `LEDGER.md` / `numbers.json` / `source-bindings.json` — named receipts
- **No `scorecard.json` here.** Committing one would join the gated capture
  population (`docs:check`, `capability:fresh`) as a library this corpus
  does not register. Gate numbers are named on
  [`recipe/evidence/f1-held-out-v1/receipt.json`](../../../../recipe/evidence/f1-held-out-v1/receipt.json).

`f1Status` is `capture-only`. Not a pass. No live Figma.
