# Delta ledger — computed floor × static layer (Alert)

Generated by extract/computed/run.ts against antd@5.29.3 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Alert`.

- captures: **128** (32 combos, full-cartesian × 4 interactions), channels enumerated per element: **475** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|alert` (matched) → **alert-icon** `span|alert-icon.anticon` (computed-only) → **icon** `svg|` (computed-only) → **part-0-0-0** `path|` (computed-only) → **alert-content** `div|alert-content` (computed-only) → **label** `div|alert-message` (computed-only) → **label-2** `div|alert-description` (computed-only) → **alert-close-icon** `button|alert-close-icon` (computed-only) → **anticon** `span|anticon` (computed-only) → **icon-2** `svg|` (computed-only) → **part-1-0-0-0** `path|` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **7** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **5** · named refusals: **0**
- asset: `alert-alert-icon-error.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `alert-alert-icon-info.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `alert-alert-icon-success.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `alert-alert-icon-warning.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- asset: `alert-anticon.svg` (committed under out/<comp>/assets; promoted to examples/polaris/assets/icons at promotion)
- presence-prop-added: description (boolean, default false; library prop description)
- presence-prop-added: closable (boolean, default false; library prop closable)
- svg-viewbox-bumped: Alert.alert-icon@success.icon.off.off — computed size 14 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@success.icon.off.on — computed size 14 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@success.icon.on.off — computed size 24 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@success.icon.on.on — computed size 24 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@info.icon.off.off — computed size 14 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@info.icon.off.on — computed size 14 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@info.icon.on.off — computed size 24 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@info.icon.on.on — computed size 24 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@warning.icon.off.off — computed size 14 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@warning.icon.off.on — computed size 14 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@warning.icon.on.off — computed size 24 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@warning.icon.on.on — computed size 24 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@error.icon.off.off — computed size 14 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@error.icon.off.on — computed size 14 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@error.icon.on.off — computed size 24 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.alert-icon@error.icon.on.on — computed size 24 < path extent 960; viewBox reconstructed as 0 0 960 960 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@success.noIcon.off.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@success.noIcon.on.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@success.icon.off.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@success.icon.on.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@info.noIcon.off.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@info.noIcon.on.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@info.icon.off.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@info.icon.on.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@warning.noIcon.off.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@warning.noIcon.on.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@warning.icon.off.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@warning.icon.on.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@error.noIcon.off.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@error.noIcon.on.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@error.icon.off.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- svg-viewbox-bumped: Alert.anticon@error.icon.on.on — computed size 12 < path extent 857.69; viewBox reconstructed as 0 0 858 858 (named reconstruction)
- root-display-carried: flex
- aspect-carried: alert-icon computed width == height in every enabled combo (14/24px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- presence-carried: alert-icon (16/32 combos) →  hidden-when showIcon=noIcon
- literal-text-carried: label-2 = "Alert description copy." (no text prop sample matches — the mounted value is carried verbatim)
- presence-carried: label-2 (16/32 combos) → visibleWhen description
- presence-carried: alert-close-icon (16/32 combos) → visibleWhen closable
- presence-carried: anticon (16/32 combos) → visibleWhen closable

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on no part. Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 3 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-font-smoothing (alert-icon, anticon, icon, icon-2, part-0-0-0, part-1-0-0-0); -webkit-text-fill-color (alert-content, alert-icon, anticon, icon, icon-2, label, label-2, part-0-0-0, part-1-0-0-0, root); -webkit-text-stroke-color (alert-content, alert-icon, anticon, icon, icon-2, label, label-2, part-0-0-0, part-1-0-0-0, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **98** → WITH folding: **54** (100 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-icon.vertical-align → folds into `font-size` (em ratio -0.125) [em-tracking] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.line-height → folds into `font-size` (em ratio 1) [em-tracking] — equal in EVERY capture; its state deltas ride the source fact
- icon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.line-height → folds into `font-size` (em ratio 1) [em-tracking] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- alert-content.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.line-height → folds into `font-size` (em ratio 1.5714) [em-tracking] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- anticon.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- icon-2.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- part-1-0-0-0.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **54** · bindings: 66 base + 0 state
- shape: 52 uniform · 14 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **30**
  - root.border-bottom-style = `solid`
  - root.border-left-style = `solid`
  - root.border-right-style = `solid`
  - root.border-top-style = `solid`
  - root.box-sizing = `border-box`
  - root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - root.position = `relative`
  - alert-icon.box-sizing = `border-box`
  - alert-icon.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - alert-icon.text-align = `center`
  - alert-icon.text-rendering = `optimizelegibility`
  - alert-content.box-sizing = `border-box`
  - alert-content.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label.box-sizing = `border-box`
  - label.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - label-2.box-sizing = `border-box`
  - label-2.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - alert-close-icon.border-bottom-style = `none`
  - alert-close-icon.border-left-style = `none`
  - alert-close-icon.border-right-style = `none`
  - alert-close-icon.border-top-style = `none`
  - alert-close-icon.cursor = `pointer`
  - alert-close-icon.overflow-x = `hidden`
  - alert-close-icon.overflow-y = `hidden`
  - anticon.cursor = `pointer`
  - anticon.font-family = `Arial`
  - anticon.text-align = `center`
  - anticon.text-rendering = `optimizelegibility`
  - anticon.transition-duration = `0.2s`
  - anticon.transition-property = `color`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **16**
  - root.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `center`, 2 distinct value(s))
  - root.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - alert-icon.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - alert-icon.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - alert-content.flex-basis — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `0%`, 1 distinct value(s))
  - alert-content.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - alert-content.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - label.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - label.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - label-2.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - label-2.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - alert-close-icon.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - alert-close-icon.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
  - anticon.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - anticon.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **8**
  - {"part":"root","channel":"padding-bottom","ref":"{imported.alert.root.padding-bottom.{description}}","refusal":"presence-prop-driven styling (description) — boolean tokensByProp has no spelling (round 4 residue); the presence-OFF plane is carried as the base binding, the ON plane (description present) is not"}
  - {"part":"root","channel":"padding-left","ref":"{imported.alert.root.padding-left.{description}}","refusal":"presence-prop-driven styling (description) — boolean tokensByProp has no spelling (round 4 residue); the presence-OFF plane is carried as the base binding, the ON plane (description present) is not"}
  - {"part":"root","channel":"padding-right","ref":"{imported.alert.root.padding-right.{description}}","refusal":"presence-prop-driven styling (description) — boolean tokensByProp has no spelling (round 4 residue); the presence-OFF plane is carried as the base binding, the ON plane (description present) is not"}
  - {"part":"root","channel":"padding-top","ref":"{imported.alert.root.padding-top.{description}}","refusal":"presence-prop-driven styling (description) — boolean tokensByProp has no spelling (round 4 residue); the presence-OFF plane is carried as the base binding, the ON plane (description present) is not"}
  - {"part":"alert-icon","channel":"font-size","ref":"{imported.alert.alert-icon.font-size.{description}}","refusal":"presence-prop-driven styling (description) — boolean tokensByProp has no spelling (round 4 residue); the presence-OFF plane is carried as the base binding, the ON plane (description present) is not"}
  - {"part":"alert-icon","channel":"margin-right","ref":"{imported.alert.alert-icon.margin-right.{description}}","refusal":"presence-prop-driven styling (description) — boolean tokensByProp has no spelling (round 4 residue); the presence-OFF plane is carried as the base binding, the ON plane (description present) is not"}
  - {"part":"label","channel":"font-size","ref":"{imported.alert.label.font-size.{description}}","refusal":"presence-prop-driven styling (description) — boolean tokensByProp has no spelling (round 4 residue); the presence-OFF plane is carried as the base binding, the ON plane (description present) is not"}
  - {"part":"label","channel":"margin-bottom","ref":"{imported.alert.label.margin-bottom.{description}}","refusal":"presence-prop-driven styling (description) — boolean tokensByProp has no spelling (round 4 residue); the presence-OFF plane is carried as the base binding, the ON plane (description present) is not"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **86.638%** (12864/14848 cells; 0/128 combo×state rows fully equal)
- pixel: 16/128 pairs perfect at threshold 0 · 16/128 at the AA point (mean AA 3.035%, max 7.143%; 128/128 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

