# Delta ledger — computed floor × static layer (Input)

Generated by extract/computed/run.ts against antd@5.29.3 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Input`.

- captures: **288** (72 combos, full-cartesian × 4 interactions), channels enumerated per element: **475** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `input|input` (matched)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **1** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **0**

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::placeholder (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (root); -webkit-text-stroke-color (root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **560** → WITH folding: **416** (6 folded channels; every fold a named receipt).

- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [disabled] root.caret-color rides its source fact
- fold-carries-state-delta: [disabled] root.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] root.outline-color rides its source fact
- fold-carries-state-delta: [disabled] root.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] root.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] root.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **416** · bindings: 27 base + 25 state
- shape: 7 uniform · 15 per-axis · 30 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)
- border-style-by-axis-carried: root.border-bottom-style varies by "variant" (outlined=solid, borderless=none, filled=solid, underlined=solid) — carried as per-value stylesWhen rules (code: `.variant-<value> { border-bottom-style: … }`; canvas: a dashPattern on that variant's stroke)
- border-style-by-axis-carried: root.border-left-style varies by "variant" (outlined=solid, borderless=none, filled=solid, underlined=none) — carried as per-value stylesWhen rules (code: `.variant-<value> { border-left-style: … }`; canvas: a dashPattern on that variant's stroke)
- border-style-by-axis-carried: root.border-right-style varies by "variant" (outlined=solid, borderless=none, filled=solid, underlined=none) — carried as per-value stylesWhen rules (code: `.variant-<value> { border-right-style: … }`; canvas: a dashPattern on that variant's stroke)
- border-style-by-axis-carried: root.border-top-style varies by "variant" (outlined=solid, borderless=none, filled=solid, underlined=solid) — carried as per-value stylesWhen rules (code: `.variant-<value> { border-top-style: … }`; canvas: a dashPattern on that variant's stroke)

## Declared facts (v15 — carried, first-class)

- base declared facts: **9**
  - root.appearance = `auto`
  - root.box-sizing = `border-box`
  - root.cursor = `text`
  - root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - root.overflow-x = `clip`
  - root.overflow-y = `clip`
  - root.position = `relative`
  - root.text-overflow = `ellipsis`
  - root.transition-duration = `0.2s`
- state declared facts: **1**
  - [disabled] root.cursor = `not-allowed`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **1**
  - root.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <input>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <input> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `none`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **20**
  - {"part":"root","channel":"border-bottom-color","state":"active","ref":"{imported.input.root.border-bottom-color-state-active.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-left-color","state":"active","ref":"{imported.input.root.border-left-color-state-active.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-right-color","state":"active","ref":"{imported.input.root.border-right-color-state-active.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-top-color","state":"active","ref":"{imported.input.root.border-top-color-state-active.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"box-shadow","state":"active","ref":"{imported.input.root.box-shadow-state-active.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-bottom-color","state":"disabled","ref":"{imported.input.root.border-bottom-color-state-disabled.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-left-color","state":"disabled","ref":"{imported.input.root.border-left-color-state-disabled.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-right-color","state":"disabled","ref":"{imported.input.root.border-right-color-state-disabled.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-top-color","state":"disabled","ref":"{imported.input.root.border-top-color-state-disabled.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"color","state":"disabled","ref":"{imported.input.root.color-state-disabled.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-bottom-color","state":"focus-visible","ref":"{imported.input.root.border-bottom-color-state-focus-visible.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-left-color","state":"focus-visible","ref":"{imported.input.root.border-left-color-state-focus-visible.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-right-color","state":"focus-visible","ref":"{imported.input.root.border-right-color-state-focus-visible.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-top-color","state":"focus-visible","ref":"{imported.input.root.border-top-color-state-focus-visible.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"box-shadow","state":"focus-visible","ref":"{imported.input.root.box-shadow-state-focus-visible.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"background-color","state":"hover","ref":"{imported.input.root.background-color-state-hover.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-bottom-color","state":"hover","ref":"{imported.input.root.border-bottom-color-state-hover.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-left-color","state":"hover","ref":"{imported.input.root.border-left-color-state-hover.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-right-color","state":"hover","ref":"{imported.input.root.border-right-color-state-hover.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}
  - {"part":"root","channel":"border-top-color","state":"hover","ref":"{imported.input.root.border-top-color-state-hover.{status}.{variant}}","refusal":"state×status set-planes beyond the carried unset plane (S3 residue — leaves exist in the minted tree)"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **79.876%** (10812/13536 cells; 0/288 combo×state rows fully equal)
- pixel: 0/288 pairs perfect at threshold 0 · 0/288 at the AA point (mean AA 1.784%, max 6.576%; 288/288 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

