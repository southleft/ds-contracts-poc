# Delta ledger — computed floor × static layer (Tooltip)

Generated by extract/computed/run.ts against antd@5.29.3 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Tooltip`.

- captures: **1** (1 combos, full-cartesian × 4 interactions), channels enumerated per element: **475** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `div|tooltip.tooltip-placement-bottom` (matched) → **tooltip-arrow** `div|tooltip-arrow` (computed-only) → **tooltip-content** `div|tooltip-content` (computed-only) → **label** `div|tooltip-inner` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **4** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **1**
- pseudo-decor-carried: tooltip-arrow::before → UNCONDITIONAL shape part "tooltip-arrow-before" (rect 16×8 at 0,8, fill rgba(0, 0, 0, 0.85); drawn in 1/1 default-interaction combos; geometry uniform, size uniform, paint uniform, rotation 0 — pseudo-decor v2)
- literal-text-carried: label = "Tooltip text" (no text prop sample matches — the mounted value is carried verbatim)
- **REFUSED**: pseudo-decor-outside-grammar: tooltip-arrow::after is painted by a mechanism the grammar cannot read (box-shadow: rgba(0, 0, 0, 0.05) 2px 2px 5px 0px) — the decor grammar reads background-color alpha and border rings only; the box is NOT promoted

## Read-boundary frontier (conformance round — every line here used to be silence)

- read-boundary-not-read-on-portal-path: Tooltip sweeps through capturePortalRoots (two-phase baseline diff), which normalizes each root inside its own reader and never runs readBoundaryReceipts — so the text-fill fold and the closed-shadow-root signature are NOT MEASURED for this component. Zero lines above means UNREAD, not "none found"; a named residual of the molecule round, surfaced by the scope-independence fix (before it, this component displayed its SIBLINGS' folds and looked read).
- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::after (1 part(s)), ::before (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (label, root, tooltip-arrow, tooltip-content); -webkit-text-stroke-color (label, root, tooltip-arrow, tooltip-content)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **27** → WITH folding: **27** (40 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-arrow.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- tooltip-content.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **27** · bindings: 50 base + 0 state
- shape: 50 uniform · 0 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **12**
  - root.box-sizing = `border-box`
  - root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - root.position = `absolute`
  - tooltip-arrow.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - tooltip-arrow.overflow-x = `hidden`
  - tooltip-arrow.overflow-y = `hidden`
  - tooltip-arrow.pointer-events = `none`
  - tooltip-arrow.transform = `matrix(1, 0, 0, 1, -8, -16)`
  - tooltip-content.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - tooltip-content.position = `relative`
  - label.box-sizing = `border-box`
  - label.font-family = `Roboto, Helvetica, Arial, sans-serif`
- state declared facts: **0**

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **5**
  - root.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - tooltip-arrow.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - tooltip-content.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - label.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - label.overflow-wrap — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `break-word`, 1 distinct value(s))
- state channels outside mintable kinds: **0**
- refused/overflow bindings: **0**

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **95.000%** (95/100 cells; 0/4 combo×state rows fully equal)
- pixel: 0/1 pairs perfect at threshold 0 · 0/1 at the AA point (mean AA NOT MEASURED, max NOT MEASURED; 0/1 pairs MEASURED — 1 size-mismatched, 3 with no original screenshot, none averaged)

