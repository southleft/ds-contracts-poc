# Delta ledger — computed floor × static layer (Checkbox)

Generated by extract/computed/run.ts against antd@5.29.3 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Checkbox`.

- captures: **24** (6 combos, full-cartesian × 4 interactions), channels enumerated per element: **475** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `label|checkbox-wrapper` (matched) → **checkbox** `span|checkbox.wave-target` (computed-only) → **checkbox-input** `input|checkbox-input|sr-only` (computed-only) → **checkbox-inner** `span|checkbox-inner` (computed-only) → **label** `span|checkbox-label` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **5** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **3**
- root-display-carried: flex
- sr-only-carried-as-hidden: checkbox-input is visually hidden in the real component (clip-path/1px box) — promoted with declared display:none (visual parity exact; AT semantics ride the contract's own semantics — NAMED downgrade)
- pseudo-decor-carried: checkbox-inner::after → shape part "checkbox-inner-after" (rect 5.70312×9.14062 at 5.21534,2.7519400000000003, fill rgba(0, 0, 0, 0); drawn in 4/6 default-interaction combos, hidden-when checked=unchecked; size per checked; rotation per checked; translate folded into top/left — round 5c S5 + Wave B.2)
- **REFUSED**: pseudo-decor-state-paint-uncarried: checkbox-inner::after at checked.disabled paints {"bg":"rgba(0, 0, 0, 0)","borderW":{"top":0,"right":2,"bottom":2,"left":0},"borderColor":"rgba(0, 0, 0, 0.25)","radius":0} where the matching enabled combo paints {"bg":"rgba(0, 0, 0, 0)","borderW":{"top":0,"right":2,"bottom":2,"left":0},"borderColor":"rgba(255, 255, 255, 1)","radius":0} — the decor carries the ENABLED plane; a per-enum-value × per-state paint product has no contract spelling (states take token refs, decor paint is literal); named residue
- **REFUSED**: pseudo-decor-state-paint-uncarried: checkbox-inner::after at indeterminate.disabled paints {"bg":"rgba(0, 0, 0, 0.25)","borderW":{"top":0,"right":0,"bottom":0,"left":0},"borderColor":"","radius":0} where the matching enabled combo paints {"bg":"rgba(22, 119, 255, 1)","borderW":{"top":0,"right":0,"bottom":0,"left":0},"borderColor":"","radius":0} — the decor carries the ENABLED plane; a per-enum-value × per-state paint product has no contract spelling (states take token refs, decor paint is literal); named residue
- **REFUSED**: pseudo-content-not-canvas-ink: root::after carries content " " — the bounded decor grammar promotes EMPTY-content decor boxes only. A glyph drawn by an icon-font ligature (Bootstrap/FontAwesome/Material carets, chevrons, close ×s) is real ink that does NOT reach the canvas; named refusal

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::after (2 part(s)), ::placeholder (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (checkbox, checkbox-inner, label, root); -webkit-text-stroke-color (checkbox, checkbox-inner, label, root)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **67** → WITH folding: **69** (35 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox-inner.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox-inner.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox-inner.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox-inner.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- checkbox-inner.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- label.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

- fold-carries-state-delta: [disabled] label.border-bottom-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-left-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-right-color rides its source fact
- fold-carries-state-delta: [disabled] label.border-top-color rides its source fact
- fold-carries-state-delta: [disabled] label.caret-color rides its source fact
- fold-carries-state-delta: [disabled] label.column-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label.outline-color rides its source fact
- fold-carries-state-delta: [disabled] label.row-rule-color rides its source fact
- fold-carries-state-delta: [disabled] label.text-decoration-color rides its source fact
- fold-carries-state-delta: [disabled] label.text-emphasis-color rides its source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **69** · bindings: 56 base + 33 state
- shape: 74 uniform · 15 per-axis · 0 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **26**
  - root.box-sizing = `border-box`
  - root.cursor = `pointer`
  - root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - checkbox.box-sizing = `border-box`
  - checkbox.cursor = `pointer`
  - checkbox.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - checkbox.position = `relative`
  - checkbox.text-wrap-mode = `nowrap`
  - checkbox-input.appearance = `auto`
  - checkbox-input.box-sizing = `border-box`
  - checkbox-input.cursor = `pointer`
  - checkbox-input.font-family = `Arial`
  - checkbox-input.position = `absolute`
  - checkbox-input.text-wrap-mode = `nowrap`
  - checkbox-inner.border-bottom-style = `solid`
  - checkbox-inner.border-left-style = `solid`
  - checkbox-inner.border-right-style = `solid`
  - checkbox-inner.border-top-style = `solid`
  - checkbox-inner.box-sizing = `border-box`
  - checkbox-inner.cursor = `pointer`
  - checkbox-inner.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - checkbox-inner.text-wrap-mode = `nowrap`
  - checkbox-inner.transition-duration = `0.3s`
  - label.box-sizing = `border-box`
  - label.cursor = `pointer`
  - label.font-family = `Roboto, Helvetica, Arial, sans-serif`
- state declared facts: **14**
  - [disabled] checkbox-inner.cursor = `not-allowed`
  - [disabled] checkbox-inner.pointer-events = `none`
  - [disabled] checkbox-input.cursor = `not-allowed`
  - [disabled] checkbox-input.pointer-events = `none`
  - [disabled] checkbox.cursor = `not-allowed`
  - [disabled] checkbox.pointer-events = `none`
  - [disabled] label.cursor = `not-allowed`
  - [disabled] root.cursor = `not-allowed`
  - [focus-visible] checkbox-inner.outline-style = `solid`
  - [focus-visible] checkbox-inner.transition-delay = `0s, 0s`
  - [focus-visible] checkbox-inner.transition-duration = `0s, 0s`
  - [focus-visible] checkbox-inner.transition-property = `outline-offset, outline`
  - [focus-visible] checkbox-inner.transition-timing-function = `ease, ease`
  - [focus-visible] checkbox-input.outline-style = `auto`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **7**
  - root.align-items — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <label>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <label> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `baseline`, 1 distinct value(s))
  - root.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <label>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <label> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `none`, 1 distinct value(s))
  - checkbox.align-self — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `center`, 1 distinct value(s))
  - checkbox.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - checkbox-input.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today. NOTE — UNRELIABLE BASELINE: this part is a <input>, and the harness renders a control for button/span/a/div only, so it was measured against the <span> control (see the `control-fallback` receipt). The difference from that control may be the USER AGENT's default for <input> rather than a fact the library authored — this refusal loses nothing either way, but it must not be read as evidence the library declared it. (sample: `none`, 1 distinct value(s))
  - checkbox-inner.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - label.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
- state channels outside mintable kinds: **1**
  - [focus-visible] checkbox-inner.transition-behavior — value shape outside mintable kinds and outside the declared vocabulary (sample: `normal, normal`)
- refused/overflow bindings: **30**
  - {"part":"checkbox-inner","channel":"background-color","state":"active","ref":"{imported.checkbox.checkbox-inner.background-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-bottom-color","state":"active","ref":"{imported.checkbox.checkbox-inner.border-bottom-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-left-color","state":"active","ref":"{imported.checkbox.checkbox-inner.border-left-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-right-color","state":"active","ref":"{imported.checkbox.checkbox-inner.border-right-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-top-color","state":"active","ref":"{imported.checkbox.checkbox-inner.border-top-color-state-active.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-bottom-color","state":"disabled","ref":"{imported.shared.color-d9d9d9}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-left-color","state":"disabled","ref":"{imported.shared.color-d9d9d9}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-right-color","state":"disabled","ref":"{imported.shared.color-d9d9d9}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-top-color","state":"disabled","ref":"{imported.shared.color-d9d9d9}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"border-bottom-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"border-left-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"border-right-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"border-top-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"caret-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"column-rule-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"outline-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"row-rule-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"text-decoration-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"text-emphasis-color","state":"disabled","ref":"{imported.shared.color-545454}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"outline-color","state":"focus-visible","ref":"{imported.checkbox.checkbox-inner.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"outline-offset","state":"focus-visible","ref":"{imported.checkbox.checkbox-inner.outline-offset-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"outline-width","state":"focus-visible","ref":"{imported.checkbox.checkbox-inner.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"outline-color","state":"focus-visible","ref":"{imported.checkbox.checkbox-input.outline-color-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"outline-offset","state":"focus-visible","ref":"{imported.checkbox.checkbox-input.outline-offset-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-input","channel":"outline-width","state":"focus-visible","ref":"{imported.checkbox.checkbox-input.outline-width-state-focus-visible}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"background-color","state":"hover","ref":"{imported.checkbox.checkbox-inner.background-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-bottom-color","state":"hover","ref":"{imported.checkbox.checkbox-inner.border-bottom-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-left-color","state":"hover","ref":"{imported.checkbox.checkbox-inner.border-left-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-right-color","state":"hover","ref":"{imported.checkbox.checkbox-inner.border-right-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"checkbox-inner","channel":"border-top-color","state":"hover","ref":"{imported.checkbox.checkbox-inner.border-top-color-state-hover.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **85.043%** (2388/2808 cells; 0/24 combo×state rows fully equal)
- pixel: 0/24 pairs perfect at threshold 0 · 0/24 at the AA point (mean AA 0.664%, max 1.019%; 24/24 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

