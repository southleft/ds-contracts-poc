# Delta ledger — computed floor × static layer (Switch)

Generated by extract/computed/run.ts against antd@5.29.3 in Chromium 151.0.7922.34. Regenerate: `npm run extract:computed -- --harness <dir> --component Switch`.

- captures: **32** (8 combos, full-cartesian × 4 interactions), channels enumerated per element: **475** (the browser's full longhand set — no whitelist)
- determinism: byte-identical across two full sweeps in one session
- rendered anatomy: **root** `button|switch` (matched) → **switch-handle** `div|switch-handle` (computed-only) → **switch-inner** `span|switch-inner` (computed-only) → **switch-inner-checked** `span|switch-inner-checked` (computed-only) → **switch-inner-unchecked** `span|switch-inner-unchecked` (computed-only)
- axes held fixed at defaults (receipted, not enumerated): none

## DOM-anatomy promotion (Round 4 — computed-only elements carried as parts)

- parts carried: **5** (union anatomy incl. structure-creating-prop subtrees) · svg assets reconstructed: **0** · named refusals: **1**
- aspect-carried: switch-handle computed width == height in every enabled combo (12/18px) → declared aspect-ratio 1 / 1 (geometry evidence; the real square rides a pseudo-element padding hack)
- pseudo-decor-carried: switch-handle::before → UNCONDITIONAL shape part "switch-handle-before" (ellipse 18×18 at 0,0, fill rgba(255, 255, 255, 1); drawn in 8/8 default-interaction combos; geometry uniform, size per size, paint uniform, rotation 0 — pseudo-decor v2)
- **REFUSED**: pseudo-decor-shadow-uncarried: switch-handle::before paints box-shadow rgba(0, 35, 11, 0.2) 0px 2px 4px 0px in 8/8 drawn combos — the bounded decor grammar carries background alpha + border rings only; the box is promoted, its shadow is not

## Read-boundary frontier (conformance round — every line here used to be silence)

- closed-shadow-root-limit: 0 suspected closed shadow root(s) named above. A closed shadow root is UNREACHABLE from script (el.shadowRoot === null), so nothing about a closed host's interior is captured or carried; the reader names the two signatures of its absence it can see (a custom-element leaf that paints a box; a non-replaced display:inline leaf with a non-zero content box) and NAMES AS UNDETECTABLE the third — a closed root on a plain element blockified by a flex/grid parent, which no computed style distinguishes from an empty item. The decisive probe (attachShadow throwing NotSupportedError) mutates the page on its negative path and would break the double-run byte-identity self-check; it is refused, not forgotten.
- pseudo-elements-read: the reader looks at ::before, ::after, ::marker, ::placeholder — measured here on ::before (1 part(s)). Reading is not carrying: the bounded decor grammar promotes ::before/::after only, so every other pseudo the reader records is a MEASUREMENT with no carriage — named, never silently dropped.
- pseudo-element-not-read: ::selection — REFUSED BY NAME, not measured: transient user state — it paints only while a selection exists, and the capture drives prop combos and pseudo-class states, never a pointer drag; there is also no canvas spelling for a selection highlight
- pseudo-element-not-read: ::backdrop — REFUSED BY NAME, not measured: paints only for an element promoted to the TOP LAYER (dialog.showModal() / popover), which the sweep never calls — measured: getComputedStyle(dialog, "::backdrop") reads the authored scrim ONLY after showModal(). The canvas DOES have a spelling (a scrim rectangle), so this is a reader gap worth closing, not a canvas limit
- pseudo-elements-refused: 2 (::selection, ::backdrop)
- webkit-prefixed-channels-refused: 2 vendor-prefixed channel(s) are STYLED by this component and refused by the blanket `-webkit-` fusion exclusion (isFusable) — named here because the exclusion used to be silent: -webkit-text-fill-color (root, switch-handle, switch-inner, switch-inner-checked, switch-inner-unchecked); -webkit-text-stroke-color (root, switch-handle, switch-inner, switch-inner-checked, switch-inner-unchecked)
- webkit-refusal-argued: -webkit-text-fill-color — NOT a loss: this channel is FOLDED INTO `color` at the read boundary (lib.ts foldTextFillColor) because it IS the painted text ink whenever it differs from `color`. It stays in the capture as the evidence for that fold.

## Derived-channel folding (item 4 — before→after quoted)

Minted leaves WITHOUT folding: **105** → WITH folding: **106** (49 folded channels; every fold a named receipt).

- root.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- root.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-handle.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-checked.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.border-bottom-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.border-left-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.border-right-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.border-top-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.caret-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.column-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.outline-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.row-rule-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.text-decoration-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact
- switch-inner-unchecked.text-emphasis-color → folds into `color` [currentColor] — equal in EVERY capture; its state deltas ride the source fact

## Bound (static layer confirmed by computed truth)

0/0 carried-binding cells string-equal to the browser-probed token value (no tolerance).

Zero contradictions.

## Minted (no name recoverable — core/mint-tokens.ts, unchanged)

- leaves: **106** · bindings: 67 base + 10 state
- shape: 43 uniform · 22 per-axis · 12 per-axis-pair · 0 refused (uncorrelated — nothing minted, named)

## Declared facts (v15 — carried, first-class)

- base declared facts: **47**
  - root.border-bottom-style = `none`
  - root.border-left-style = `none`
  - root.border-right-style = `none`
  - root.border-top-style = `none`
  - root.cursor = `pointer`
  - root.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - root.position = `relative`
  - root.transition-duration = `0.2s`
  - root.user-select = `none`
  - switch-handle.box-sizing = `border-box`
  - switch-handle.cursor = `pointer`
  - switch-handle.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - switch-handle.text-align = `center`
  - switch-handle.transition-duration = `0.2s`
  - switch-handle.transition-timing-function = `ease-in-out`
  - switch-handle.user-select = `none`
  - switch-inner.box-sizing = `border-box`
  - switch-inner.cursor = `pointer`
  - switch-inner.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - switch-inner.overflow-x = `hidden`
  - switch-inner.overflow-y = `hidden`
  - switch-inner.text-align = `center`
  - switch-inner.transition-delay = `0s, 0s`
  - switch-inner.transition-duration = `0.2s, 0.2s`
  - switch-inner.transition-property = `padding-inline-start, padding-inline-end`
  - switch-inner.transition-timing-function = `ease-in-out, ease-in-out`
  - switch-inner.user-select = `none`
  - switch-inner-checked.box-sizing = `border-box`
  - switch-inner-checked.cursor = `pointer`
  - switch-inner-checked.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - switch-inner-checked.pointer-events = `none`
  - switch-inner-checked.text-align = `center`
  - switch-inner-checked.transition-delay = `0s, 0s`
  - switch-inner-checked.transition-duration = `0.2s, 0.2s`
  - switch-inner-checked.transition-property = `margin-inline-start, margin-inline-end`
  - switch-inner-checked.transition-timing-function = `ease-in-out, ease-in-out`
  - switch-inner-checked.user-select = `none`
  - switch-inner-unchecked.box-sizing = `border-box`
  - switch-inner-unchecked.cursor = `pointer`
  - switch-inner-unchecked.font-family = `Roboto, Helvetica, Arial, sans-serif`
  - switch-inner-unchecked.pointer-events = `none`
  - switch-inner-unchecked.text-align = `center`
  - switch-inner-unchecked.transition-delay = `0s, 0s`
  - switch-inner-unchecked.transition-duration = `0.2s, 0.2s`
  - switch-inner-unchecked.transition-property = `margin-inline-start, margin-inline-end`
  - switch-inner-unchecked.transition-timing-function = `ease-in-out, ease-in-out`
  - switch-inner-unchecked.user-select = `none`
- state declared facts: **10**
  - [disabled] root.cursor = `not-allowed`
  - [disabled] switch-handle.cursor = `not-allowed`
  - [disabled] switch-inner-checked.cursor = `not-allowed`
  - [disabled] switch-inner-unchecked.cursor = `not-allowed`
  - [disabled] switch-inner.cursor = `not-allowed`
  - [focus-visible] root.outline-style = `solid`
  - [focus-visible] root.transition-delay = `0s, 0s`
  - [focus-visible] root.transition-duration = `0s, 0s`
  - [focus-visible] root.transition-property = `outline-offset, outline`
  - [focus-visible] root.transition-timing-function = `ease, ease`

## Code-only / overflow (named, in the extension file)

- base channels outside mintable kinds: **9**
  - root.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - root.vertical-align — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `middle`, 1 distinct value(s))
  - switch-handle.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - switch-inner.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - switch-inner.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal, normal`, 1 distinct value(s))
  - switch-inner-checked.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - switch-inner-checked.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal, normal`, 1 distinct value(s))
  - switch-inner-unchecked.list-style-type — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `none`, 1 distinct value(s))
  - switch-inner-unchecked.transition-behavior — value shape outside mintable kinds (color/px/number/shadow/gradient) and outside the declared-channel registry — no schema channel today (sample: `normal, normal`, 1 distinct value(s))
- state channels outside mintable kinds: **1**
  - [focus-visible] root.transition-behavior — value shape outside mintable kinds and outside the declared vocabulary (sample: `normal, normal`)
- refused/overflow bindings: **4**
  - {"part":"switch-inner-checked","channel":"margin-left","state":"active","ref":"{imported.switch.switch-inner-checked.margin-left-state-active.{size}.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"switch-inner-checked","channel":"margin-right","state":"active","ref":"{imported.switch.switch-inner-checked.margin-right-state-active.{size}.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"switch-inner-unchecked","channel":"margin-left","state":"active","ref":"{imported.switch.switch-inner-unchecked.margin-left-state-active.{size}.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}
  - {"part":"switch-inner-unchecked","channel":"margin-right","state":"active","ref":"{imported.switch.switch-inner-unchecked.margin-right-state-active.{size}.{checked}}","refusal":"v13 Part.states carries plain color-kind refs only on non-root parts"}

## Fidelity gate (scorecard.json)

- computed-equality (styled channels, contract-mediated): **94.325%** (4920/5216 cells; 0/32 combo×state rows fully equal)
- pixel: 0/32 pairs perfect at threshold 0 · 28/32 at the AA point (mean AA 0.021%, max 0.225%; 32/32 pairs MEASURED — 0 size-mismatched, 0 with no original screenshot, none averaged)

