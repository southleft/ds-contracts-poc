# Desktop-MCP import round-trip receipt

<!-- GENERATED by extract/figma/mcp/receipt.ts (`npm run extract:figma:mcp:receipt`) — DO NOT EDIT. -->

Fixtures in `extract/figma/mcp/fixtures/` are RECORDED LIVE responses (Figma desktop Dev Mode MCP server + REST nodes endpoint, 2026-07-08) — `replayImport()` reruns the exact live code path over them, so this receipt is CI-safe and byte-reproducible. The Enterprise-only variables endpoint 403’d during both recordings: every variable name below was recovered through `get_variable_defs` value joins (extract/figma/mcp/import.ts), never guessed — ambiguous joins are receipts, and anything unnamed ships as a provisional `imported.*` mint at literal fidelity.

## Badge — contract-generated set vs its shipping contract

| Component | MATCHED | CANVAS-ABSENT | MISMATCH | Verdict |
|---|---|---|---|---|
| Badge | 11 | 4 | 0 | ✅ zero mismatch |

- ✅ Enterprise variables endpoint absent from the recording (it 403’d live) — every name below came from the MCP join
- ✅ every variable id name-resolved by the value join (unresolved: 0)
- ✅ per-node scoping disambiguated same-valued variables (4 id(s), e.g. danger vs error — both #fee2e2/#b91c1c at set scope)
- ✅ zero map degradations (got 0)
- ✅ zero unbound, zero minted — nothing was left for the fallback tiers (unbound 0, minted 0)
- ✅ comparator verdict vs contracts/badge.contract.json: ZERO MISMATCH (got 0) — name fidelity ≥ plugin-dump path

### Names recovered by the join (13 ids, 5 get_variable_defs calls)

- `VariableID:4:88` → `color/feedback/info/background` (set-scope, 1 occurrence(s))
- `VariableID:4:60` → `space/inset-x/sm` (set-scope, 10 occurrence(s))
- `VariableID:4:63` → `space/inset-y/sm` (set-scope, 10 occurrence(s))
- `VariableID:4:68` → `radius/badge` (set-scope, 20 occurrence(s))
- `VariableID:4:89` → `color/feedback/info/foreground` (set-scope, 1 occurrence(s))
- `VariableID:4:90` → `color/feedback/success/background` (set-scope, 1 occurrence(s))
- `VariableID:4:91` → `color/feedback/success/foreground` (set-scope, 1 occurrence(s))
- `VariableID:4:92` → `color/feedback/warning/background` (set-scope, 1 occurrence(s))
- `VariableID:4:93` → `color/feedback/warning/foreground` (set-scope, 1 occurrence(s))
- `VariableID:4:94` → `color/feedback/danger/background` (node-scope, 1 occurrence(s))
- `VariableID:4:95` → `color/feedback/danger/foreground` (node-scope, 1 occurrence(s))
- `VariableID:11:64` → `color/feedback/error/background` (node-scope, 1 occurrence(s))
- `VariableID:11:65` → `color/feedback/error/foreground` (node-scope, 1 occurrence(s))

### CANVAS-ABSENT (4) — declared fidelity limits (same list as the plugin-dump receipt)

- `semantics` — element/role are not drawn on the canvas ({"element":"span","role":"status"})
- `a11y` — accessibility requirements are not canvas-recoverable
- `text root font-weight (effective)` — {font.control.weight} resolves to the runtime text default (Medium) — canvas-indistinguishable from no weight token
- `text root font-family (effective)` — {font.control.family} — the canvas renders Inter regardless (documented fidelity scope)

## Eventz Alert — foreign, hand-built vocabulary

- ✅ foreign vocabulary recovered by name: "spacing/1"
- ✅ foreign vocabulary recovered by name: "spacing/2"
- ✅ foreign vocabulary recovered by name: "spacing/3"
- ✅ foreign vocabulary recovered by name: "spacing/4"
- ✅ foreign vocabulary recovered by name: "component/border/radius/rounded-md"
- ✅ foreign vocabulary recovered by name: "color/content/inverse"
- ✅ the U+2024 variable ("spacing/0․5", ONE DOT LEADER) is itself recovered by name by the join…
- ✅ …and then REFUSED by the token-ref grammar (binding not proposed, named in notes) — the refusal fires on live foreign data
- ✅ recovered names BIND in the proposal: root carries {spacing.4} / {spacing.3} / {component.border.radius.rounded-md} / {spacing.2} (got {"padding-inline":"{spacing.4}","padding-block":"{spacing.3}","border-radius":"{component.border.radius.rounded-md}","gap":"{spacing.2}"})
- ✅ everything the join could not name still ships at literal fidelity as imported.* mints (2 minted, 0 unbound)

### Names recovered by the join (7 ids, 9 get_variable_defs calls)

- `VariableID:2567:4338` → `spacing/2` (set-scope, 4 occurrence(s))
- `VariableID:2567:4342` → `spacing/4` (set-scope, 8 occurrence(s))
- `VariableID:2567:4349` → `spacing/3` (set-scope, 8 occurrence(s))
- `VariableID:2567:4438` → `component/border/radius/rounded-md` (set-scope, 16 occurrence(s))
- `VariableID:2567:4352` → `spacing/0․5` (set-scope, 4 occurrence(s))
- `VariableID:2373:135` → `color/content/inverse` (node-scope, 8 occurrence(s))
- `VariableID:2567:4350` → `spacing/1` (set-scope, 4 occurrence(s))

### Map degradations (named, never silent)

- `[paint-unsupported]` Alert:variant=success `fill` — first visible paint is GRADIENT_LINEAR, not SOLID — dump v1 carries solid paints only; paint omitted

### Proposal notes (review line items, 14)

- semantics.element defaulted to "div" — element/role/ARIA are not drawn on the canvas; set the real host element
- Alert:root/container: visibility bound to BOOLEAN "hasIcon" — proposed as prop `hasIcon` (default not recoverable from dump v1, review)
- Alert:root/container/Icon: nested instance of "check circle" has no known contract — component ref proposed as "ds.check-circle", review
- Alert:root/container/Icon: fixed prop values of the nested "check circle" instance are not captured in dump v1 — declared fidelity limit, author them if the instance is configured
- Alert:root/horizontal stack itemSpacing: variable name "spacing/0․5" contains characters outside the token-ref grammar ([a-z0-9.-]) — binding not proposed; rename the variable or map it manually
- Alert:root/horizontal stack/horizontal stack/Title: rides text style "body/md-bold" which is not a token-derived style — typography not proposed
- Alert:root/horizontal stack/horizontal stack/Description: rides text style "body/sm" which is not a token-derived style — typography not proposed
- Alert:root/horizontal stack/Text link: nested instance of "Text link" has no known contract — component ref proposed as "ds.text-link", review
- Alert:root/horizontal stack/Text link: fixed props of "Text link" canonicalized by spelling (dump v1.1) — verify against the child contract's bindings
- Alert:root/container 2: visibility bound to BOOLEAN "isDismissible" — proposed as prop `isDismissible` (default not recoverable from dump v1, review)
- Alert:root/container 2/Icon: nested instance of "close" has no known contract — component ref proposed as "ds.close", review
- Alert:root/container 2/Icon: fixed prop values of the nested "close" instance are not captured in dump v1 — declared fidelity limit, author them if the instance is configured
- MINTED {imported.alert.horizontal-stack-horizontal-stack-title.font-size} = 16px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Alert:root/horizontal stack/horizontal stack/Title font-size
- MINTED {imported.alert.horizontal-stack-horizontal-stack-description.font-size} = 14px — machine-named from a resolved value — rename against your real tokens (provisional); bound at: Alert:root/horizontal stack/horizontal stack/Description font-size

