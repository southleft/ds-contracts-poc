{
 "_provenance": {
  "fileKey": null,
  "dumpVersion": "1.17",
  "note": "canvas conformance fixture - MECHANICALLY EXPANDED from the CSS-suite design-side snippet conformance/cases/grid-absolute-overlay/dump.snippet.json (the hand-authored A1 artifact) into the full dump v1.17 grammar the shipping reader consumes. The expansion adds NO grid fact: `layout:\"grid\"` becomes {mode:'GRID', padding 0, sizing per axis (G8, 2026-08-08: a GRID frame's primary axis is HORIZONTAL, GP1b — an axis whose declared tracks carry no {fr} is AUTO/hug, which is what the canvas draws and what the reader carries as literals \"fit-content\"; an fr-bearing axis is FIXED, sized from outside), grid:<the snippet's own grid block verbatim>}, `layout:\"row\"` becomes the flex-era HORIZONTAL block, `cell` rides verbatim, `sizing.horizontal:FILL` becomes the dump's `fillWidth`, and `characters` becomes a TEXT node on the repo's `badge` derived style. Channels the dump grammar deliberately does NOT carry are named per case below rather than invented. EXPANSION NOTES: child \"b\" is layoutPositioning ABSOLUTE: the dump gates `cell` capture on it (P13 - absolute children still REPORT anchors 0,0), so no cell is written. The `abs` box's width/height/right/bottom are harness-supplied placeholders; the construct under test is the ANCHOR GATE, not the geometry."
 },
 "Case": {
  "setName": "CaseGridAbsoluteOverlay",
  "type": "COMPONENT",
  "variants": [
   {
    "name": "CaseGridAbsoluteOverlay",
    "type": "COMPONENT",
    "layout": {
     "mode": "GRID",
     "padding": [
      0,
      0,
      0,
      0
     ],
     "primarySizing": "FIXED",
     "counterSizing": "AUTO",
     "grid": {
      "rows": [
       {
        "px": 24
       },
       {
        "px": 24
       }
      ],
      "columns": [
       {
        "fr": 1
       },
       {
        "fr": 1
       }
      ],
      "rowGap": 0,
      "columnGap": 0
     }
    },
    "children": [
     {
      "name": "a",
      "type": "FRAME",
      "cell": {
       "row": 0,
       "column": 0
      }
     },
     {
      "name": "b",
      "type": "FRAME",
      "abs": {
       "x": 4,
       "y": 4,
       "right": 0,
       "bottom": 0,
       "width": 8,
       "height": 8
      }
     }
    ]
   }
  ]
 }
}
