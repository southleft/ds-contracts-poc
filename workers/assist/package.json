{
  "name": "ds-contracts-assist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: anonymous, hard-capped agentic assist for the ds-contracts playground. Every response is a proposal; the client-side contract schema is the referee.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p .",
    "test": "tsx --test test/handler.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "undici": "^7.28.0",
    "ws": "^8.21.0"
  }
}
